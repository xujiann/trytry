# ROADMAP

> 今天做什么，从这里挑。每日工作流见 `docs/日常开发工作流.md`。
> 排序：先安全止血（🔴），再让 CI 变真，再逐块治理/重构。**别自己另起题目——路线没有的，先加进来。**
> 来源与依据：`docs/TECH_DEBT.md`、`docs/模块分级_KEEP_IMPROVE_REFACTOR_REPLACE.md`、`docs/adr/`。

状态：☐ 待办 · ◐ 进行中 · ✅ 完成

---

## Now（本周期，优先）

### 🔴 安全止血（P0，独立于分级，尽快）
- ✅ **P0-19 住院医嘱 / 执行记录 / 病案首页三个读接口不收调用方身份**（2026-09-24 做 P2-38 住院页时发现，实测，已修）：
  乙院医生按住院号读甲院的医嘱、皮试结果、出院诊断与转归，三条全 200；不带住院号的医嘱清单吐全县。P0-10 收了
  `clinical_docs` 那一族，同一份病历的这三块在 `inpatient.py` 里没跟。照抄 P0-10 补可见性判定与留痕，回归 8 条。
  三道闸门都没报：分母只认直接带 `patient_id` 的表。按外键推一跳 / 两跳重扫，同形状还有 24 个（P1-69，待逐条判定）。
- ✅ **P0-23 慢专病在线咨询「结束」「转随访」不校验会话属于谁、清单不收口**（2026-09-24 查 P2-43 造数接口时发现，实测，已修）：
  乙院医生按会话号关掉甲院患者的咨询（200）、给甲院患者派随访任务（201），任一职员能列出全域会话（带姓名与病种）；
  同组回复与消息早就按患者可见性守着。照抄同组口径；回归 6 条。写侧棘轮只认带机构列的表——挂在患者上的写接口这一形状无人盯，另立 P1-71。
- ✅ **P0-43 慢专病手工建任务、按方案生成随访能派进别家机构的工作池**（2026-09-24 实测，已修）：两处 `org_id` 给了就照单全收，守着它的只有「患者看不看得见」——乙院医生接诊过的患者，建任务 201 且出现在甲院的任务列表里，生成随访 201 两条都落在甲院名下。P0-35 判据把患者可见性当成了机构守卫；两处补 `assert_org_writable`，判据补字段级一层（守卫得看着这个机构字段），量出 16 个字段：修 2、按设计是对方 13、待裁定 1（P1-56）。回归 5 条。
- ✅ **P0-42 接种登记能用别家机构的疫苗批次**（2026-09-24 实测，已修）：扣别家的库存、这一针挂到别人的批号上，按批号召回时找错机构。批次机构必须是接种机构，否则 422。回归 2 条。
- ✅ **P0-41 积分兑换清单把全县的核销码发给任一账号**（2026-09-24 实测，已修）：核销只认码不认人，拿到别人的码就能领走奖品；管理页本就不显示这一列。只给兑换人本人看，别人一律打码。回归 6 条。
- ✅ **P0-40 待办铃铛把全县的未确认危急值连同结论推给每个医生**（2026-09-24 实测，已修）：危急值只通知申请机构、别家也确认不了（P0-27），铃铛却按角色取全县。按申请机构收到本机构；「待诊断申请」随「谁算共享中心」一起等。回归 4 条。
- ✅ **P0-39 医废扫码追溯按码就能读别家的追溯链**（2026-09-24 按 P1-77 盲区实测，已修）：追溯码是「MW-日期-序号」，顺着就能逐包翻遍全县（类别、重量、点位、经手人）；清单与写接口早就按机构收口。按所属机构判可见；同文件的转运人员工作量不收任何范围，照 A 案收进统计范围；滞留预警（逐包明细）收到本机构。回归 18 条。
- ✅ **P0-38 按 id 读机构管理数据的明细不判归属**（2026-09-24 实测，已修；余 1 条待裁定）：清单只给看本机构，顺着 id 读明细什么都不看——新卫生院的经办 / 医生按 id 读到县医院的会计凭证、职工人事变动史、物资出入库流水，还有别家实训的报名名单（登录账号与姓名）和逐人考核成绩、别家审批流每一步的意见；「我的待办」还把别家机构办不了的单子算进来。照同文件写侧口径补收口，回归 37 条。余下 13 条逐条判过、立读侧棘轮：按设计 6、患者数据已在别的名单 4、待裁定 3（报告实例新写进待裁定清单）。
- ✅ **P1-77 按路径参数读、不 `db.get` 父对象的 GET，读侧棘轮都看不见**（2026-09-24 量并逐条判完）：27 条——实测修 1（医废追溯，P0-39）、患者数据已在别的名单 7、按设计 15、待裁定 4（医保基金池的账，写进待裁定清单）；这一层也钉成只减不增，新写的同形状读接口必须过一次人眼。
- ✅ **P1-50 死因报告卡导出被静默截断在 2000 条**（2026-09-24 已修）：法定上报口径，区间内死亡数过 2000 就少报一截且无从察觉。改成外连接患者一次取回，没有上限、接口契约不变，每张挂了患者的卡照旧留痕。回归 2 条。
- ✅ **P1-51 卫健端区域结构分析吃被截断的样本**（2026-09-24 已修）：纳管过两万人总数与人口结构就全错；改成 `count()` + 流式逐行数，体征计数按统计范围收口；同形状的团队工作台（`limit(5000)`，七个待办数算在前 5000 人上）改成子查询与 `NOT EXISTS` 计数。回归 6 条。
- ✅ **P1-81 体温单记满 500 次之后，新记的体征看不到**（2026-09-24 做 P2-8 第六批时发现，已修）：升序取前 500 条，截掉的恰好是最新的那一端——体温单停在第 500 次，移动端查房的「最近 8 次」其实是几周前的。改成从最近一次往前翻页、每页内仍升序，两个前端不用改；不超过一页时逐字节不变。回归 4 条。
- ✅ **P1-82 流程「我的待办」被别的角色的单子挤空**（2026-09-24 做 P2-8 第六批时发现，已修）：先取最新 200 条在办实例再按角色筛，最新 200 条都卡在别的角色的节点上时，医生的待办是空的、计数是 0。角色筛挪进查询、计数走 `count()`、列表切分页；判定一字不改。回归 4 条。
- ✅ **P1-83 慢专病四个清单的姓名 / 证件号搜索只在前 500 / 200 个匹配的患者里找**（2026-09-24 查 P2-8 同族形状时发现，已修）：候选人群、在管患者、呼叫任务、个案上报先取任意 N 个匹配的患者再筛，常见姓氏一搜名单少一截且无从察觉。改成子查询，匹配条件一字不改。回归 12 条。
- ✅ **P1-84 慢专病考核计分只算前 500 个考核对象**（2026-09-24 同族形状扫出，已修）：村医 / 医生过 500 人的县，多出来的没有分数、排名只在 500 人里排、重跑还可能换一批（同一周期两个第 1 名）。去掉上限、按 id 展开。回归 3 条。
- ✅ **P1-85 待办中心各节计数截在 100 条上**（2026-09-24 同族形状扫出，已修）：五节都是 `count = len(截断后的预览)`，积压越严重铃铛显示得越像「还好」。计数走 `count()`，预览保留 100 条；可见性口径不动。回归 6 条。
- ☐ **P1-86 按规则自动分组只看任意 1000 个在管患者**（2026-09-24 登记，待裁定）：不排序取 1000 人逐人算规则，其余人不参与也不提示；上限是性能闸，修法要选——见待裁定清单。
- ☐ **P2-45 随访质控抽样的样本框是「最近 2000 条」**（2026-09-24 登记，待裁定）：没有期间条件，按比例抽样抽的是这 2000 条的比例——样本框口径见待裁定清单。
- ✅ **P1-87 两处「到期不晚于」日期参数没校验、写法一变就静默筛错**（2026-09-24 发现，已修）：`2031/09/30` 按字符串比，筛出来的是全年。日期参数棘轮按名字认参数，`due_before` 漏在名单外；两处接 `require_date`，判据补 `_before` / `_after` 后缀。回归 10 条。
- ✅ **P2-46 手术排班的时刻只卡形状，全角数字让同一手术间能重复排**（2026-09-24 实测，已修）：「０９:３０」按字符串比排在一切半角时刻之后，冲突判定判不出重叠（实测 201）；`25:00` 也能排。新增 `datetypes.TimeStr`（半角 + 00:00–23:59），排班与撮合都换上。回归 16 条。
- ✅ **P1-88 7 个日期查询参数的校验器被框架丢掉**（2026-09-24 实测，已修）：`x: OptionalDateStr = Query(default="")` 这种写法下 FastAPI 不跑别名里的校验器，写坏的日期原样进筛选条件。改回 `str` + `require_date`，并立零基线闸门（含前提自证）。回归 21 条。
- ✅ **P2-47 预算年度认全角数字，执行对比的年度不校验**（2026-09-24 实测，已修）：`year=%` 把所有年份的收支加总报成「实际数」；「２０２６」的预算永远对不上执行对比。两处只认 `[0-9]{4}`，完整日期的形状也改 `[0-9]`；立零基线闸门：`pattern=` 里不许用 `\d`。回归 10 条。
- ✅ **P1-89 停用的病种照收新筛查、照跑自动识别**（2026-09-24 实测，已修）：建档拒停用病种、就诊自动识别也只看启用的，唯独手工筛查与批量自动识别照收——界面下拉里就选得到，一次最多 5000 人入池、全都建不了档。两处与建档同口径 404，三张开新业务的表单只列启用病种。回归 5 条。
- ✅ **P2-49 停用的医废点位照样收新医废、照样入暂存**（2026-09-24 实测，已修）：停用即科室撤并，收集与入暂存却不看停用（201 / 200）。两处补「已停用」422，与同文件点位校验同口径。回归 4 条。
- ✅ **P1-90 请求体外键 id 不查存在就写库**（2026-09-24 按形状扫出，已修）：18 个端点 / 21 个字段把请求体里的外键原样写库。SQLite 上存悬空 id；**真 PG 实测修前 22 个用例全错**——9 个被翻成「该患者已纳管此病种」「该中心编码已存在」这类 409 误报，13 个撞外键 500。写库前逐个查存在（不存在 404 点名是哪一项），回归逐端点 + 真 PG 再跑一遍，零基线闸门「请求体外键原样写库、一眼不看」。
- ✅ **P2-50 改档的风险分层什么都收**（2026-09-24 实测，已修）：建档只收四个值，改档裸 `str`——写进「HIGH」「高危」即从高危统计里消失。改档与建档同一个 pattern，回归 8 条。
- ✅ **P1-91 请求体字符串没有长度上限就写进定长列**（2026-09-24 扫出，同日三批修完）：SQLite 照存，**真 PG 实测超长即 500**（就诊摘要、转诊理由这类自由文本最容易撞，整条登记就没了）。量出 197 处，三批补 `max_length=列长` 清零转零基线；最后一处是角色变更留痕列只有 16、装不下 32 位的自定义角色键（PG 上改角色 500），迁移扩到 32。**第二层**：判据补上「请求体列表里的子项逐个写库」这一形状，又量出 4 处（处方明细的药品编码 / 名称、批量号源模板的资源名 / 时段，真 PG 实测超长即 500），同批补齐。**第三层**：再补「取出来的对象上显式赋值」这一形状，又量出 10 个字符串（检查报告修订、上门、医废交接、整改、远程咨询回复）+ 1 个金额（签合同），真 PG 实测超长 / 越界即 500，同批补齐。
- ✅ **P1-92 浮点入参收下 NaN / Infinity**（2026-09-24 扫出，已修）：**真 PG 实测**冷链录温 `NaN` → 201、记成「未超温」，质控测定 `NaN` → 201、判「在控」（NaN 的比较恒为假），金额 `Infinity` → 500；带约束的字段收到 `NaN` 记号连 422 都编不出来、回成 500（两个库皆然）。量出 66 处同批修完：接管 422（非有限值换成字符串，其余逐字节不变）、65 个字段换 `FiniteFloat`、阈值查询参数拒收、8 个继承的出参覆盖回 `float`（存量 NaN 读成 null，不让列表 500）；零基线闸门 + 真 PG 再跑一遍。
- ✅ **P1-93 整数 / 金额入参没有上限就写进定长数值列**（2026-09-24 扫出，已修）：SQLite 照存，**真 PG 实测越过容量即 500**（满意度评价对象号、收费单价、处方天数、号源容量）。量出 65 处，同批补 `le=列容量`（新增 `app/numtypes.py`），零基线闸门 + 真 PG 再跑一遍。
- ✅ **P1-94 慢专病配置改档端点用裸 dict 收请求体**（2026-09-24 扫出，已修）：20 个 `PATCH` 照单全收，建档的校验在改档上全部失效。**开发库实测**：问卷异常规则 / 报告模板段落 / 推送任务优先级写成字符串，坏值落库后**问卷、报告模板、推送任务三张列表对所有人 500**；随访方案时间点写成 99999999、管理目标随访周期写成 99999999，此后按方案生成随访、完成随访任务都 500。20 个端点换成 `*Patch` 模型（约束照抄建档、不传即不改、建档的业务校验改档同一句），零基线闸门；FHIR 入站 4 个按设计豁免。
- ✅ **P1-95 慢专病 6 个改档端点对不可空的列收得下 null**（2026-09-24 扫出，已修）：病种档案、随访记录、纳管档案、复诊、干预、路径实例的改档模型把不可空的列也声明成可空，显式传 null 即 `NOT NULL` 约束失败、**500**（开发库实测 30 个字段）。改用 `app/patchtypes.UNSET` 的写法，显式 null 是 422；判据并进裸 dict 闸门第二层，零基线。
- ✅ **P1-96 天数 / 分钟数入参没有上界就进日期运算**（2026-09-24 扫出，已修）：到期合同、未确认危急值、传染病多点预警、知识库到期、中药批次到期五个查询参数传个大数即 **500**；慢病病种随访周期写成 99999999 照存，此后该病种建档 500。补 `Query(le=3650)`、病种周期改业务上限 1~3650，零基线闸门。
- ✅ **P2-53 报告推送时点填「8:00」就永远不推**（2026-09-24 实测，已修）：调度按字符串比较时点，没补零的「8:00」和「24:00」比一天中任何时刻都大，任务每轮都被跳过、也不报错。建任务与改任务改收零补齐的 HH:MM。
- ✅ **入参写库判据补第四层**（2026-09-24，P1-91 / P1-93 同族）：共用写库形状原先看不见「按业务键查出来再改的对象」「字典字面量构造」「`body.x or 默认` 这类原样取值」，补上后又量出字符串 16 处（药品入库、绩效指标、手术记录与申请、住院文书记录人、儿童高危备注）、整数 3 处（调度周期、库存阈值、出血量），同批补齐，三族闸门仍为零。
- ✅ **P2-54 条数入参负数在生产库上 500**（2026-09-24 扫出，已修）：审计链校验与统一申请单的 `limit` 原样进 `LIMIT`，PG 上 `-1` 即 500、SQLite 上还回出「返回 -1 条」。补上下界（上界只到列容量），零基线闸门。
- ✅ **P2-40 出参继承的入参约束逐字段审完**（2026-09-24，修 2 处）：互认目录改名为空串照收，此后整张互认目录 500、页面打不开；存量导入收下单字机构名，机构清单对所有人 500。写入方收紧到与建档同口径，两个出参覆盖回 `str`；其余 69 个字段写入方都一样严。零基线闸门：写同一列的入口不得比出参约束松。
- ✅ **P1-93 判据盲区：可空的有限浮点不算数值**（2026-09-24，已修）：`FiniteFloat | None` 里的 Annotated 不被判据认作数值，收费项目改价漏在外面；调价经 helper 写库同样看不见。两处补 Money 上限，**真 PG 实测修前改价 1e13 即 500**。
- ✅ **入参写库判据补第五层：转一手再写**（2026-09-24，P1-91 / P1-93 同族）：`upsert_unique` 的字典、`add_amount` 累加、`update(...)` 字典与 `.values()`、取行 helper、局部变量与 `payload` 转手、推导式、传给 helper 再写，原先整个看不见。又清出字符串 37 处、金额 / 整数 23 处，**真 PG 实测修前 500**（超长用户名登录即 500，不用登录就能打出来）。
- ✅ **P1-98 平台侧改档比建档松**（2026-09-24 扫出，已修）：17 列的改档 / 入库收下建档不收的值——收费项目等 11 类配置能改成空名、ESB 流程能改成零步骤、库存入库传空药名把药名改空、随访记录渠道能改出枚举。改档补回建档的约束；零基线闸门比写同一列的入口，按设计的 6 列写明理由。
- ✅ **P1-97 数字判断放行全角 / 上标 / 圈码，下一步转换即炸**（2026-09-24 扫出，已修）：HL7 / FHIR 入站编号、身份证质控（整条规则 500、全角证件号反判合法）、模板版本号、导入天数；导入金额「NaN」「Infinity」让整批中断、剂量「nan」照存。改 `isascii() and isdigit()`、导入拒非有限值，零基线闸门。
- ☐ **P1-99 认证 / 验签 / 审计链比较碰到非 ASCII 即 500**（2026-09-24 登记，按 §8 待复核）：令牌签名段带非 ASCII 字节，未登录即可让任一端点 500；改法（失败关闭）见待裁定清单。
- ✅ **P2-55 两个「下次随访日」不走日期真源**（2026-09-24 登记，已修）：日期闸门只认名字带 `date` 的字段，两端随访表单是自由文本框，「10月1日」没到期就进超期名单、「2026/10/1」超期了却不在名单里。两个字段换 `OptionalDateStr`（出参覆盖回 `str`，存量坏值照常读出）、两端表单换日期控件、日期闸门认名补上 `due`。
- ✅ **P1-100 请求体时间戳是裸 str**（2026-09-24 扫出，已修）：13 个「某时某分」字段是自由文本，定时宣教按字符串比较到点（「2026-10-1 8:00」晚 9 天、「10月1日」当场就发、`T` 写法晚到次日零点）、体温单按字符串排序（8:00 排到 14:00 之后）、冷缺血解析不了即「未采集」（超 60 分钟的标本从质控里消失）。补时间戳真源（合法值原样落库，两个按字符串比较的消费方把 `T` 换成空格再比）、7 个输入框换日期时间控件、零基线闸门。
- ✅ **P2-56 区间入参不查起止顺序**（2026-09-24 扫出，已修）：报告推送任务有效期起晚于止照样 201，之后天天被调度跳过、一份报告也不生成；慢专病服务期、术中记录起止同样能倒置。三处补顺序判断（改档与存量合并后再比），派生闸门盯住全部区间入参。
- ✅ **P1-101 体征测量值收下 0 / 负数**（2026-09-24 扫出，已修）：慢病随访一次 0/0（血压计测量失败的常见回值）就把 3 级高危降成 1 级「控制良好」、上转建议消失；慢专病监测 0 按「只设上限」的目标判成正常。体征入参补下界（急诊可为 0）、慢专病按指标目录不收非正数，派生闸门盯住。
- ✅ **P1-102 离职员工能被派驻，派驻结束时被改回在岗**（2026-09-24 扫出，已修）：两条派驻入口对离职员工 409（补录已结束的历史派驻照收），离职的人不再借派驻结束回到在岗医师数里。
- ✅ **P1-103 写接口引用已停用的目录对象**（2026-09-24 扫出，已修）：报告推送任务建在停用模板上照样 201、之后一份报告也不出；停用的团队照样分发患者、挂纳管档案，停用的服务包照样绑包，停用的上报任务照样收上报。12 处取出来补看启用标志（404「不存在或已停用」），8 处按设计写明理由，派生零基线闸门盯住。
- ✅ **P1-104 父对象已经结束仍能挂上新的子行**（2026-09-24 扫出，已修）：离职医师照样放号、患者照样约上；已出报告的病理申请照样收标本（之后出不了报告）；县外就诊挂到已退回的转诊单算成有序转诊；死亡档案照样绑服务包。6 处补看父对象状态（含预约与批量放号），10 处按设计写明理由，派生零基线闸门盯住。
- ✅ **P1-106 写接口把新业务挂给已停用的账号**（2026-09-24 扫出，已修）：转派任务、随访执行人、主管医生、团队成员照样挂上登录不了的账号；新建任务 / 批量分配 / 分发目标患者四个字段连存在都不查、真 PG 上 500。统一经 `unusable_user`，停用的 404「X已停用」+ 派生零基线闸门。
- ✅ **P2-58 已结项的项目单独把进度改低照收**（2026-09-24 扫出，已修）：结项校验只在同一请求带 status=done 时才跑，结项后单独改进度到 60 得到「已完成但进度 60%」；改为与存量合并后再判。
- ✅ **P2-59 报告推送任务生成过报告还能删**（2026-09-24 扫出，已修）：真 PG 上撞外键 500，开发库留下悬空编号、新任务复用编号后把旧报告认成自己的；生成过报告的任务改为 409（请暂停），派生零基线闸门盯住「删被引用着的行」。
- ✅ **P2-60 抗菌药物使用强度算进了药师退回的处方**（2026-09-24 扫出，已修）：退回后重开一张，同一疗程算两遍，考核用的强度虚高；分子排除已退回的处方。
- ✅ **P2-61 同一份统计里一部分数字收口、一部分全县**（2026-09-24 扫出，已修）：乡镇中心端「待确认迁入」数的是全县的迁出，应急资源按类型计数不跟片区筛选，卫健工作台指定机构时服务人次仍是全县；三处套同一份范围 + 派生零基线闸门。
- ✅ **P2-62 附件文件名超长在真 PG 上 500**（2026-09-24 扫出，已修）：multipart 文件名不经请求模型，长度闸门看不见，257 个字写进 String(256) 即 500；存盘前按列长 422。
- ✅ **P2-63 服务端 CSV 导出不防公式注入**（2026-09-25 扫出，已修）：死因诊断等人填文本以 `=` 开头原样写进单元格，Excel 里即成外链 / DDE；与慢专病前端导出同一条规则（= + - @ 开头的非数字文本前置单引号）。
- ✅ **P2-64 居民端可约号源列出过去的号、照样约得上**（2026-09-25 扫出，已修）：列表只列今天及以后，预约对日期已过的号源 409（居民自助与窗口代约同一句）。
- ✅ **P2-65 金额入参收得下三位以上小数**（2026-09-25 扫出，已修）：开发库照存、生产库 Numeric(14,2) 悄悄四舍五入——单价录 0.004 存成 0.00（零价收费项目），录 12.345 计费两库算出不同的数；37 个金额字段换 `MoneyFloat`，多于两位小数 422、浮点噪声归整到分，派生零基线闸门盯住。
- ✅ **P2-66 关键词检索的大小写开发库不分、生产库区分**（2026-09-25 扫出，已修）：诊断字典搜 `i10` 生产库为空、开发库命中；26 处用户关键词检索改走 `deps.keyword_like`（两边转小写），派生零基线闸门盯住（按设计 5 处写明理由）。
- ✅ **P2-67 居民端干预方案看不出哪条已不作数、已移除的还能被标记完成**（2026-09-25 扫出，已修）：出参补后端状态文案、手机页显示状态；已移除的标记完成 409，标记已读照常。
- ✅ **P2-68 分组查询不排全序，开发库有序、生产库按哈希值**（2026-09-25 全量单元套件换到真 PG 上跑时扫出，已修）：DRG 统计分组行、缺药按状态计数、按计数排的「前 N 名」并列处，生产库与开发库顺序不同；82 处补 ORDER BY 分组键（开发库一字不变），派生零基线闸门盯住。
- ✅ **P2-69 截断取数不排序，生产库改过的行挪到最后**（2026-09-25 全量单元套件换到真 PG 上跑时扫出，已修）：资源总览、报告超期任务段、通知收件人、宣教定时派发 8 处补 ORDER BY，派生零基线闸门盯住（按设计 3 条）。
- ☐ **P2-70 金额出参形状随库不同（整数金额开发库 int、生产库 float）**（2026-09-25 登记，待裁定）：特征化网 40 条在真 PG 上红，钉的字节在生产库不成立；三个可选项见待裁定清单。
- ✅ **P2-71 开发库不开外键约束**（2026-09-25 登记，同日修完）：应用的 SQLite 引擎每条连接开 `PRAGMA foreign_keys=ON`（迁移引擎不挂）；开着外键量出 10 个文件 60 条红，全是夹具写占位 id（逐个改成先建父行），另测出一处接口缺陷——设备绑定不看患者编号（生产库 500，补 404）；方言护栏登记这条 PRAGMA、手写 SQL 上限 41 → 42；e2e / 探针在开着外键的开发库上照绿。
- ✅ **P2-72 工作人员端 15 处把英文状态码原样显示**（2026-09-25 扫出，已修）：制剂批次 `produced`、绿道 `en_route`、实训 `open`、特病申报 `applied`（且未转义）、随访失访 `unreachable`、健康日历三栏全不译、凭证核验「失效（void）」……出参补后端 `status_name`、页面改显示它（§13 第 7 项）；派生零基线闸门（按设计 3 条）+ 十张文案表逐一对照列注释。
- ✅ **P2-73 场景 / 异常分级码原样显示**（2026-09-25 扫出，已修）：居民自助随访后弹「系统判定为high异常」、没配处置措施时派出的任务标题「随访异常处置：high」、随访各表显示 `inpatient`、同意书页显示 `chronic_enroll` / `self`；出参补后端文案、页面改显示，闸门字段扩到场景 / 异常分级。
- ✅ **P2-74 状态文案取自后端的剩余两块**（2026-09-25 登记，同日修完，明细见下方同号条目）：47 条 409 报错嵌英文状态码（「当前状态 approved 不可审批」）→ 0；其余字段 75 个候选逐字段判完，平台 11 处 + 慢专病 14 处已修。
- ✅ **P1-109 必填文本字段收下纯空格**（2026-09-24 扫出，已修）：`min_length=1` 只挡空串，机构名、病种编码、用户名、员工姓名填一串空格（含全角空格）照样 201 落库，清单里多出看不见名字的行、查重拦不住第二串空格。268 个请求字段补 `NON_BLANK`（合法值一个字节不变），继承到它的 37 个出参字段按原约束覆盖（修前存进去的空白行照样列得出），前端报错换成「不能只填空格」；认证 / 核验入参 14 个按 §8 登记按设计；两道零基线闸门盯住请求与出参两侧。
- ✅ **P1-110 必填键收下空串**（2026-09-24 扫出，已修）：传染病报告没有病种、处方明细没有药品（审方自动通过）、检查申请没有项目、审方规则没有药品编码，照样 201。7 个字段补必填（与 P1-109 同口径），出参按原约束覆盖，前端留空报错换成「不能为空」；闸门第二层盯住「没有默认值却收空串」的请求字段（按设计 17 条逐条写明理由）。
- ✅ **P1-111 纳管档案生命周期没有终态**（2026-09-24 扫出，已修）：死亡档案「排除 → 恢复」回到在管、死亡后照样召回；迁出待确认期间离世，目标机构确认即给已故患者新建在管档案；恢复在管 / 召回成功撞另一份在管档案 500。死亡改为终态（409），确认迁入先看档案是否已死亡，待确认迁入不再数这种，恢复前先查另一份在管档案（409 说明在哪家）。
- ✅ **P1-113 传染病报告卡导出截在 2000 条**（2026-09-25 扫出，已修）：与死因报告卡（P1-50）同为法定上报口径，却还按编号正序截在 2000 条——截掉的是最新的卡，「只导迟报」在截断后才筛，新近迟报一张都导不出来；去掉上限。
- ✅ **P1-114 身份证号末位 X 大小写不同即建出第二份主索引档案**（2026-09-25 扫出，非认证部分已修）：建档查重、对接入站、患者与慢专病检索改成两种写法都认（真 PG 上 `LIKE` 区分大小写，修前按另一种写法连检索都查不到）；居民端实名绑定 / 家庭代管 / 免登录查档与凭证核验属认证链路，登记待复核。
- ✅ **P1-115 数据质控每条规则只扫前 5000 行、违规总数当全量报出**（2026-09-25 扫出，已修）：后面的违规永远查不出（5 万行的基准只报出十分之一）；按主键分批扫完整张表、只取要读的列，5 万 + 5 万行的汇总 2.4s。
- ✅ **P1-116 成本分摊比例合计超过 100%，来源科室总成本成负数；规则建错改不了删不了**（2026-09-25 扫出，已修）：后勤分给内外科各 60% 两条都 201，后勤总成本 −200；建规则按来源科室校验合计 ≤ 100（临界区防并发），补改比例、删除两个入口与页面按钮。
- ✅ **P1-117 手术间排班并发时重叠的手术全排进同一手术间**（2026-09-25 扫出，已修）：真 PG 八路同时排起点错开的重叠时段，八台全排上；判定与写入圈进手术间行的临界区，新增「先查别的行再写」防复发闸门（扫出的另 5 处：P1-118 下一提交修、1 处判为接受、3 处认证 / 账号链路登记 P1-119）。
- ✅ **P1-118 慢专病路径推进并发：同时推进八路八份任务、同节点最后两条同时办结路径停住**（2026-09-25 闸门扫出，已修）：真 PG 实测两种都现形；手工推进与办结自动推进都圈进路径实例行的临界区（办结一路在第一次写库前拿锁）。
- ☐ **P1-119 认证 / 账号链路三处先查后写的并发窗口**（2026-09-25 登记，待复核）：一次性验证码可用两次、家庭代管越过上限、最后两名管理员互相停用；改法只串行化、不改放行口径，选项见待裁定清单。
- ✅ **P1-120 慢专病写接口收下不存在的病种编码**（2026-09-25 扫出，已修）：24 个写接口里 22 个把填错的病种编码原样落库（规则试算自动开单一处 500），记录与配置挂到不存在的病种上、按病种的清单与统计从此不含它；考核重跑同一期填错病种，本期正式分数被改写成零（实测 100 → 0.0）。25 处写库前查病种（404「专病档案不存在」，居民端新纳入两处连停用病种也不收），1 处只作筛选条件按设计，派生零基线闸门。
- ✅ **P1-121 随访方案挂着填错的问卷编码，执行随访时异常分级整段跳过**（2026-09-25 扫出，已修）：疼痛 9 分本应高危，记成「无异常」、不派处置任务。方案建 / 改补查问卷编码（404 点名），管理目标的宣教素材、手工转诊的触发规则、团队 / 指标 / 考核方案的病种列表同批收口；改档与现值相同的编码不再查，存量悬空不挡改名停用；闸门改按字段表，新加的编码字段必须判过。
- ✅ **P1-122 随访问卷的异常分级从界面上一次都触发不了**（2026-09-25 读到，已修）：建问卷录不了题目、带规则的问卷存不进去（实测 422）、执行随访不交作答，居民自助随访只问「恢复情况」一题；接口侧规则引用不存在的题目照收、永远判不出。建 / 改问卷题目与规则合起来查，界面录题目、规则字段取自题目，医护执行与居民自助都按问卷逐题作答。
- ✅ **P1-123 分组自动规则不经校验、写坏即按规则批量入组 500**（2026-09-25 读到，已修）：界面上「介于」只填一个数即存进 `[60]`，按规则入组 IndexError 500，分组没有改档接口、永久用不了；建分组查规则结构（422），存量坏规则入组时 422 说清楚。
- ✅ **P1-124 报告推送任务的机构 / 订阅人编号填错一个，定时推送整轮失败、所有报告都不出**（2026-09-25 扫出，已修）：订阅人那一路失败在调度收尾提交、连失败都记不下；建 / 改任务查编号（404 点名），推送跳过悬空编号（全悬空不退回全域），调度在 try 里 flush。
- ✅ **P1-125 慢病分级规则写坏了照样存得进去，这个病种之后每次记随访都 500**（2026-09-25 扫出，已修）：管理端 JSON 文本框里阈值写成字符串即是；建 / 改病种查分级规则（422），存量坏规则记随访 422 说清楚、不悄悄跳过定级。
- ✅ **P1-126 居民端「上传凭证」只存附件、不记进任务**（2026-09-25 读到，已修）：要凭证的任务手机上提交恒 422、医护审核看不到凭证；居民上传即记进任务佐证清单。
- ✅ **P1-127 退回的慢专病任务被当成已结束**（2026-09-25 读到，已修）：同节点一条退回待重办、另一条办完，路径照样往下走；退回的任务从「我的待办」与各处待办数里消失、死亡结案不取消它、过期不超期、催办 409，居民在手机上重做不了。「未结束」七处各写一份、七份都漏了退回——改为一处定义，闸门不许查询里再手写状态清单。
- ✅ **P1-128 超期扫描之后只认 planned / running 的计数与收尾漏掉超期的与暂停的**（2026-09-25 扫出，已修）：管理端工作台超期随访提醒恒为 0、团队到期随访 / 复诊只剩今天的、居民首页待随访不含超期、死亡结案不收逾期复诊与暂停的路径、暂停着的路径能重复启动。「未完成 / 未结束」各定义一处，闸门不许查询再只认 planned / running。
- ✅ **P1-129 患者死亡 / 迁出 / 排除时结案不动随访记录**（2026-09-25 读到，已修）：死者名下的随访照旧到期、超期；结案一并移除本病种本机构未完成的随访（可手工恢复），留痕与别处的不动。待裁定清单 P1-108 一节里「结案置已移除」的事实描述已更正。
- ☐ **P1-130 呼叫中心回写结果按任务号即可写任一机构的通话记录**（2026-09-25 登记，待裁定）：建任务查患者可见性、回写不查；回写同时是网关回调入口、网关身份未建模，并入待裁定清单 P1-71 一节。
- ✅ **P1-131 管理端恢复因进入条件暂停的路径不判条件、不派任务，再点推进就跳过这个节点**（2026-09-25 读到，已修）：暂停的实例页面上只有「调整 → 恢复」，它只改状态；恢复改为与推进同一套（重判条件、派本节点任务，手工暂停的不重复派）。
- ✅ **P1-132 随访一超期，管理端看板就没有「执行」「转呼叫」**（2026-09-25 读到，已修）：超期扫描一过，过了日期没做的随访在页面上再也执行不了；按钮对待随访与已超期的都给。
- ☐ **P1-133 居民端能「填报并提交」医护的慢专病任务**（2026-09-25 登记，待裁定）：清单列的是患者名下全部任务，医护的「指标异常处置」居民一点就成了待审核；哪些任务归居民做是业务定义，选项见待裁定清单。
- ✅ **P1-134 「按患者特征自动匹配」出院场景不认回溯天数，同一次扫描同一患者排几份**（2026-09-25 读到，已修）：两年前出院的也被排上随访、计划日期早已过去，一排出来就超期；近几天两次就诊、一周内两次出院的各排两份。出院场景按出院时间取回溯窗口，本次扫描看过的「患者 + 方案」不再派。
- ☐ **P1-135 再次住院的患者，同一随访方案不再排出院随访**（2026-09-25 登记，待裁定）：查重键是「患者 + 方案」而不是「这一次出院」，第二次出院之后没有任何随访；旧计划里没做的点怎么办、要不要记来源住院号，选项见待裁定清单。
- ✅ **P1-136 量表作答从界面上录不准**（2026-09-25 读到，已修）：筛查登记选了量表却不作答（恒 0 分低危，量表这条通道从界面上判不出任何人）；评估与居民自查的单选默认选中第一项（没答当成答了「是」）；下拉列出同一量表的旧版本（按旧版作答、按新版评分）。改为逐题作答、默认未答、只列最新发布版。
- ✅ **P1-137 「逗号分隔」的清单只认半角逗号**（2026-09-25 读到，已修）：审方规则禁忌诊断写成「妊娠，哺乳期」整串成了一个词、这条禁忌从不触发（处方照样系统审通过）；DRG 关键词同理。全角逗号、顿号一并认。
- ✅ **P1-138 监测值没写病种一律判「正常」**（2026-09-25 读到，已修）：管理端录入的病种默认「全部病种」、居民端是「可留空」的编码框，没写就没有管理目标可比——收缩压 190 也不判异常、不派处置任务；改为按在管档案里配了这个指标目标的病种判级。
- ✅ **P1-139 发起转诊不写病种，转诊单不挂纳管档案**（2026-09-25 读到，已修）：有效上转的积分记给录单的人、下转承接随访任务不挂档案；留空时患者只在管一个病种的挂这份档案，表单病种改下拉；批量下发干预、任务中心手工派发、规则试算「命中即开上转单」同一族（干预先随模板的病种），一并修。
- ✅ **P1-140 孕产妇档案一位妇女一生只能建一本**（2026-09-26 读到，已修）：上一胎结案后再孕，建册拿回那本已结案的旧档案，这一胎的产检、分娩无处可记；改为只约束「在册」唯一（部分唯一索引 + 迁移，回退遇一人多本拒绝并指名），已分娩未结案又来建册 409 让人先结案，建册表单补孕次 / 产次。
- ✅ **P1-141 住院结算之后再记一笔费用，患者永远出不了院**（2026-09-26 读到，已修）：出院要求结清、再结算又撞「一次住院一张结算单」；已结算的住院计费改为 409。修复前已卡住的存量按运维手册第十八节人工处置。
- ✅ **P1-142 押金冲抵了个人自付，收银照默认额还能再收一遍**（2026-09-26 读到，已修）：收款默认额取冲抵后应补缴、全额冲抵的 422，超收判定与回调复核把冲抵算作已收。
- ✅ **P1-143 HL7 检验结果 PID 核验放行平台外证件号**（2026-09-26 读到，已修）：别人的结果与危急值写进申请单患者名下；改为核申请单患者本人的证件号，对不上即拒收。
- ☐ **P1-144 医保基金池预结不分险种**（2026-09-26 登记，待裁定）：居民池、职工池各记一整份；险种列从哪来，选项见待裁定清单。
- ☐ **P1-145 基金池清算缺月也当全年**（2026-09-26 登记，待裁定）：有几个月加几个月、不可撤销；池子是否总是 1–12 月，选项见待裁定清单。
- ✅ **P1-146 补一次货就把缺药预警阈值抹成 0**（2026-09-26 读到，已修）：阈值不传不改、页面留空不送；配好的缺药预警不再被补货静默关掉。
- ✅ **P1-147 召回了的「未标批号」兜底批次照样收入库**（2026-09-26 读到，已修）：直接入库 / 采购验收 / 盘盈一律 409，不再长出发不出去的幽灵库存；采购单停在已审批的残留已写明。
- ✅ **P1-148 待审处方与第 201 个起的药品批次页面上够不着**（2026-09-26 读到，已修）：待审单独取、排在队列最前；批次台账可按编码 / 批号查；召回回执不再永远「0 → 0」。
- ✅ **P1-149 薪酬「合计发放」只加最新 500 行**（2026-09-26 读到，已修）：合计按筛选条件在库里求和，清单上限不变、响应字节不变。
- ✅ **P1-150 修订过的检查报告打印仍署原医师、无修订标记**（2026-09-26 读到，已修）：打印版写明已修订几次、以本版为准，及最后一次修订的人、时间、原因。
- ✅ **P1-151 抗菌药物使用强度的收治人天少算**（2026-09-26 读到，已修）：与平均住院日同一个日期差口径；各机构强度会下降，需随版本告知。
- ✅ **P1-152 居民端账单「已支付」自造口径**（2026-09-26 读到，已修）：按个人自付是否结清判，与收费处同一套算术（押金冲抵算已付、医保渠道那一份不算居民已付）。
- ✅ **P1-153 网页建档的患者命中不了审方特殊人群**（2026-09-26 读到，已修）：建档表单加出生日期；性别未知的在册孕妇也算孕产妇。
- ✅ **P1-154 接种登记界面从不送疫苗批次**（2026-09-26 读到，已修）：表单加可用批次下拉与部位、接种人，批次三查与扣库存从界面上生效。
- ✅ **P1-155 试算平衡表不带机构号汇总全县、任一账号可读**（2026-09-26 读到，实测，已修）：照合并报表收进统计可见范围；两道闸门对带必填参数的 GET 是盲区，另行登记。
- ☐ **P1-156 驾驶舱指标下钻按全县吐患者明细行，任何登录账号都能翻；两道暴露闸门都数不到它**（2026-09-26 登记，待裁定）：选项见待裁定清单。
- ☐ **P1-157 合并报表的「资产负债表」只是一个月的发生额，不是期末余额**（2026-09-26 登记，待裁定）：选项见待裁定清单。
- ☐ **P1-158 药品召回只动本机构那一行：调拨出去的同批号副本照发，「发给了谁」也查不到**（2026-09-26 登记，待裁定）：选项见待裁定清单。
- ☐ **P1-159 盘点把「可发汇总」当账面数：召回封存还在库房里的药盘成盘盈，回到可发**（2026-09-26 登记，待裁定）：选项见待裁定清单。
- ☐ **P1-160 家庭代管授权：居民自签也算「窗口核验」、授权不绑定代管账户、撤回不收回既有代管；代管人替成年家属签同意记成「本人自签」**（2026-09-26 登记，待裁定）：选项见待裁定清单。
- ☐ **P1-161 慢病分级缺一个指标就整次不定级：已经确定的升级被一格空值取消**（2026-09-26 登记，待裁定）：选项见待裁定清单。
- ☐ **P1-162 档案调阅授权的「范围」只是个标签：授权「检查报告」，拿到的是全部档案**（2026-09-26 登记，待裁定）：选项见待裁定清单。
- ✅ **P2-74 状态文案取自后端的剩余两块**（2026-09-25）：①409 报错文案里的英文状态码 48 → 0 已修（「当前状态 approved 不可审批」→「当前状态 已审批 不可审批」，报错一侧派生零基线闸门）；②其余字段从模型列注释推导 75 处逐条判，平台侧真原样显示 11 处、慢专病侧 14 处已修（课程 / 科室 / 收费类别、公文类型、机构层级、更正申请类型；纳管网络 / 团队 / 考核方案层级、测量来源、宣教素材类型、筛查来源、分组范围、路径节点服务类型、就诊类型……），18 张新表进「文案表对照列注释」名单。
- ◐ **P2-75 终态对象再操作一次照样 200、终结时间被改写**（2026-09-25）：慢专病咨询「结束」、服务包「解绑」再点一次改 409、时间不动（已修）；人事变动不看员工现状并入 P1-56 待裁定。
- ✅ **P2-76 转诊规则试算勾「自动开单」推不出发起机构即 500**（2026-09-25 读到，已修）：不绑机构的账号代录、患者在所填病种下没有纳管档案，发起转诊是 422，规则试算自动开单却撞非空约束 500；判定挪进两条路径共用的建单帮手。
- ✅ **P2-77 按 make install 装出的开发环境 make typecheck 81 处红、CI 照绿**（2026-09-25，已修）：区间声明 `sqlalchemy>=2.0` 放进了 2.1（`Row` 泛型写法变了），本地门禁与 CI 对不上、锁再生成也会带红；加上界 `<2.1`，锁不动；防腐用例钉「锁内版本落在区间声明里」。
- ☐ **P2-78 事实规则引用字段表以外的字段照收、永远不命中**（2026-09-25 登记，待裁定）：接口调用方写 `agee` 也 201、按规则入组 0 人；界面只能选已知字段。转诊试算的 `extra` 事实是任意键，收紧方式待裁定。
- ✅ **P2-79 考核指标的评分规则写坏了，整张方案一计分就 500**（2026-09-25 扫出，已修）：满分写成文字、分档不是列表等五种写法建指标都 201，计分整张方案 500；建 / 改指标查评分规则（422），存量坏规则计分时逐指标记错、不 500。
- ✅ **P2-80 量表配置写坏了照样建得成、发得布，谁来作答都 500**（2026-09-25 扫出，已修）：选项写成字符串、分值写成文字等四种写法建量表 201、发布 200，作答 500（居民自查同一条路）；建 / 改 / 发布量表查配置（422），存量坏量表三个作答入口 422 说清楚。
- ✅ **P2-81 数据质控规则的配置写坏一条，整个质控看板打不开**（2026-09-25 扫出，已修）：六种坏配置建规则都 201，任一条都让汇总 / 运行检查整体 500 或 422；建 / 改规则按类型查配置（422），存量坏规则扫描时跳过并在出参 `skipped_rules` 点名。
- ✅ **P2-82 服务包的项目次数写成文字：建服务包 500、改服务包照收后每次绑定都 500**（2026-09-25 扫出，已修）：建 / 改同一句校验（原文案），存量坏项目绑定时 422 说清楚。同族派生零基线闸门：宽字典请求体字段逐个判过是配置还是数据，配置得在写库前查结构（修前点名 13 处）。
- ✅ **P2-83 批量「接收」的前置状态比单条宽**（2026-09-25 同批看到，已修）：自己提交待审核的任务批量一勾就被接收回「已接收」、拉出审核队列（审核人 409）；单条与批量改为同一前置状态，不满足的进 skipped 并给出原因。
- ✅ **P2-84 医生移动端要佐证的慢专病任务办结不了**（2026-09-25 读到，已修）：待办卡片没有上传佐证的入口（点办结恒 422），已接收的照摆「接收」（恒 409）、待审核的照摆「办结」（绕过审核）；按状态给按钮，要佐证的加「上传佐证」，与管理端同一走法。
- ✅ **P2-85 设备批量上传的监测值不查病种编码**（2026-09-25 读到，已修）：填错的编码照样落库，P1-120 的闸门看不见批量体条目里一层的编码；批量写库前逐个查（整批 404 一条不落），闸门补看一层子模型。
- ☐ **P2-86 路径节点的「超时处理」只有「升级上报」生效**（2026-09-25 登记，待裁定）：默认的「提醒」不提醒、「自动跳过」不跳过；提醒谁几次、跳过后任务记成什么、路径推不推进是业务口径，选项见待裁定清单。
- ✅ **P2-87 呼叫任务的结果回写不限次数**（2026-09-25 读到，已修）：已接通的通话能被改成已取消、录音地址被换掉；只有待呼叫的任务能回写，已有结果的 409。
- ✅ **P2-88 已取消的路径实例还能改档**（2026-09-25 读到，已修）：再取消一次改写结束时间、改回执行中「复活」任务全取消的路径；已取消与已完成同是终态，409。
- ✅ **P2-89 手工建任务挂的纳管档案不看患者与病种**（2026-09-25 读到，已修）：办完甲的随访任务，乙的下次随访被推后一个月；挂的档案须是这位患者、这个病种的（422）。
- ✅ **P2-90 随访问卷规则区：加载期间先填的题目不被认上**（2026-09-25 第四十轮端到端撞到，已修）：「添加异常规则」一直藏着；规则区建好先按题目框现有内容认一遍，用例扣住元数据请求把时序固定。
- ☐ **P2-91 随访方案的手术 / 医嘱关键词从不参与匹配，术后 / 体检场景扫的是门诊就诊**（2026-09-25 登记，待裁定）：基准日取就诊日，正是手工「生成随访计划」接口 docstring 警告的「整条计划错位」；按什么数据源命中、三类关键词取或还是且，见待裁定清单。
- ✅ **P2-92 随访方案面板没有新建入口、编辑不给诊断关键词**（2026-09-25 读到，已修）：预置方案都没配关键词，出院即派生随访与自动匹配从界面上一个人都匹配不到；无可用方案时弹「扫描 undefined 人」、平台管理员无机构可填恒扫 0 条。补新建表单、关键词列与编辑，原因显示在页面上，表单补机构一栏。
- ◐ **P2-93 动词级孤儿端点：路径有入口，「新建」「删除」没有**（2026-09-25 量出，闸门已立）：孤儿棘轮按路径算，清单有页面调就算接上；按动词再对一遍，21 条——配置项只能改不能建 14（评估量表、宣教素材、考核指标、数据源、报告模板、病种目录、质控规则、DRG 分组……）、窗口代录 2、手工建慢专病任务 1、只有接口的删除 3、按设计无界面 1。`KNOWN` 只减不增，按 `docs/模块完成度.md` 的「动词缺口」逐模块接；手工派发慢专病任务已接（20 → 19），新建宣教素材、接入数据源已接（19 → 17），删路径模板（补停用）、删推送任务已接（17 → 15），慢病病种目录新增已接（15 → 14），告知书模板新增已接（14 → 13），中医适宜技术入库已接（13 → 12），DRG 分组增补已接（12 → 11），会计科目增建已接（11 → 10），模拟诊疗病例新建已接（10 → 9），考核指标新建已接（9 → 8，口径与变量表收成一份、经元数据接口给界面），报告模板新建已接（8 → 7，段落勾选取自段落注册表），评估量表新建与改草稿已接（7 → 5，量表构建器：新建 / 编辑草稿 / 复制为新版本），数据质控规则新增已接（5 → 4）；余下 4 条都不在自动批次里接——质控规则删除（按设计给停用不给删，等裁定）、角色改名（授权相关）、窗口代录两条按 §8 待人复核再接。
- ✅ **P2-94 公卫随访采集器一次只看窗口里最早的 500 条**（2026-09-25 读到，已修）：窗口里多于 500 条随访，其余的体征永远同步不进慢专病，日志照写成功（实测连跑三轮 500 / 0 / 0，第 501 条始终不进）；按 id 翻页取完窗口，判重只查这一页。
- ✅ **P2-95 启动患者路径不看模板的病种**（2026-09-25 读到，已修）：高血压档案能跑上糖尿病的路径（201），任务内容、阶段、进入条件全对不上；启动时模板病种须是档案病种，不一致 422。
- ✅ **P2-96 新建路径模板的病种下拉拿「第几个 + 1」冒充病种号**（2026-09-25 读到，已修）：生产库序列一跳号，模板就挂到别的病种上（端到端实测选乙落到甲）；目录的病种带病种号、表单按号取，启动路径的模板下拉带病种名。
- ✅ **P2-97 有患者在走的路径改回草稿就能动节点**（2026-09-25 读到，已修）：删掉在跑实例停着的节点，办完这一步路径即「已完成」（实测 100%、后两步没走）；模板上有执行中 / 暂停的实例时节点不许增改删（409，复制新版本再改）。
- ✅ **P2-98 筛查 / 评估不看量表的病种**（2026-09-25 读到，已修）：糖尿病问卷答高了患者进高血压的疑似目标池、评估改写别的病种档案的风险；三个接口一处判定（不一致 422），筛查页的量表下拉随病种联动。
- ✅ **P2-99 干预模板、服务包不看病种**（2026-09-25 扫到，已修）：高血压患者下发到糖尿病的干预、档案绑上糖尿病的服务包（均 201）；不一致 422，干预模板下拉与绑包弹窗随病种联动。
- ✅ **P2-100 管理端上报异常不带病种**（2026-09-25 扫到，已修）：挂不上纳管档案，村医「异常上报」积分从界面上一分不入账、处置任务不挂档案；病种取上报任务的（写了的须一致），表单补病种下拉。
- ✅ **P2-101 医生移动端转诊卡片四个按钮一律给**（2026-09-25 读到，已修）：待审核的也有「登记到院」「承接随访」、点了 409；按状态给，与后端前置同一口径。
- ✅ **P2-102 FHIR 批量导出看不见修订过的检查报告**（2026-09-25 扫到，已修）：修订是原地改结论，主键水位只导新增，省平台一直是修订前的结论；按修订史的水位再导一次（status=amended）。
- ✅ **P2-103 报告的「考核排名」段落恒为空**（2026-09-25 读到，已修）：拿模板频率关键字去比分数的周期；频率关键字改取范围内最近一期，段落写明周期的按段落的。「服务工作量」不看周期等 P1-105，未改。
- ✅ **P2-104 考核指标改了目标值，计分照旧按原来的**（2026-09-25 读到，已修）：按比例计分先取评分规则里的目标，种子两处各一份，编辑弹窗只改得到指标上的——改 80 为 50，扣分理由仍是「未达目标值80.0」；目标改以指标的目标值为准，没设才看规则里的。
- ✅ **P2-105 考核方案里没写权重的指标一分不计**（2026-09-25 读到，已修）：「指标:权重」串把没写权重的送成 0（计分只在没有权重键时才用指标库的默认权重），编辑弹窗又回显成「:0」；改为不送、回显只写编码，权重写成文字的报人话。
- ✅ **P2-106 会诊计费的费用框留空照样提交、计 0 元**（2026-09-25 读到，已修）：数字框空值读成 0，这单标成「已计费」——标签自己写着「0 与未计费是两回事」；费用框改必填。
- ✅ **P2-107 慢专病目录只拉一次：路径页刚发布的模板，同一页的启动下拉里没有**（2026-09-25 读到，已修）：目录存在全局变量里、只有运行中枢强制重取，页面间跳转不重载浏览器；改为每次进页面都重取。
- ✅ **P2-108 考核方案条目的权重不经校验：写成文字的一跑分整张方案 500**（2026-09-25 读到，已修）：建 / 改方案时权重须不写或不小于 0 的数（422）；存量坏权重计分时逐指标记错、不 500。
- ✅ **P2-109 预约的状态转换不是原子的：同一预约并发取消两次，号源已约数被多扣一次**（2026-09-25 读到，已修）：取消 / 到诊核销 / 已取消的重约三条转换改为条件 UPDATE（影响 0 行即 409）；真 PG 八路并发修前八路都成功、已约数扣光，修后恰一路成功。
- ✅ **P2-110 用血申请连点两次「发血」库存扣两次；审批与驳回同时到两路都成功**（2026-09-25 读到，已修）：审批 / 发血改条件 UPDATE（影响 0 行即 409），发血先翻状态再扣库存；真 PG 八路并发修前八路都成功、扣了八袋，修后恰一路。
- ✅ **P2-111 实训连点两次「退报名」名额放两个、连点两次重报一人占几个名额**（2026-09-25 读到，已修）：退报名 / 重报名改条件 UPDATE（影响 0 行即回滚）；真 PG 八路并发修前七路退成、名额扣到 0，修后恰一路。
- ✅ **P2-112 慢专病服务包扣减登记是 JSON 读改写：并发扣减丢一笔，5 次的项目八笔并发全扣成**（2026-09-25 读到，已修）：圈进这条绑定的 `serialized_on`、锁里重读再判再写（状态也在锁里重判）；真 PG 修后恰扣 5 笔、已用与流水对得上。
- ✅ **P2-113 高值耗材同一条码并发登记给两位患者，都 200、追溯链记成后写的那位**（2026-09-25 读到，已修）：使用登记改条件 UPDATE（`WHERE status = 'in_stock'`）；真 PG 八路并发修前七路 200，修后恰一路。
- ✅ **P2-114 慢专病任务刚办结就被锁外旧状态改回未结束，再办一次随访分再记一笔**（2026-09-25 读到，已修）：任务状态一律条件翻转（`service.move_task`），审核只从「待审核」翻；结案收尾与超期扫描逐条条件翻转、按编号取；修前 SQLite 实测一条任务记 2 笔分，真 PG 扫描期间办结的成了「超期」。
- ✅ **P2-115 积分兑换同一核销码两个点位同时出示，几路都核销成功、奖品发几份**（2026-09-25 读到，已修）：核销改条件 UPDATE（`WHERE status = 'pending'`）；真 PG 八路并发修前六路 200，修后恰一路。
- ✅ **P2-116 CSV 导出防公式注入漏了制表符与回车开头**（2026-09-25 读到，已修）：服务端与前端两份前缀表都补上，并加用例对着比两边，一边改了另一边没改即红。
- ✅ **P2-117 中医体质辨识只认最高的一种偏颇体质：同样满 40 分的第二种在结果里哪儿都没有**（2026-09-25 读到，已修）：回执加兼夹体质（`also`），页面加一栏并在结果表标「是」。
- ☐ **P2-118 老年人 ADL 自动分级的阈值与常见两种 Barthel 口径都差一档**（2026-09-25 登记，待裁定）：95 判能力完好、60 判轻度、40 判中度——40 分的进不了重度失能预警；按哪版标准、挂不挂待遇资格要业务方定。
- ✅ **P2-119 预置的 2 型糖尿病分级规则漏了低血糖：空腹血糖 2.8 判「控制良好」、不建议转诊**（2026-09-26 读到，已修）：种子补一条「≤3.9 → 3 级」；已部署的库按运维手册第十六节在病种目录里手工补。
- ✅ **P2-120 审方特殊人群「孕产妇」只认孕期**（2026-09-26 读到，已修）：已分娩、产后访视还没结案的产妇开他汀、利伐沙班照样系统审通过（说明书哺乳期同样禁用）；改为按在册（未结案）的孕产档案认。
- ☐ **P2-121 传染病「迟报」从发病日起算**（2026-09-26 登记，待裁定）：法定时限从诊断起算，病例表没有诊断时间；发病几天后才就诊的病例几乎全被判迟报，迟报清单失去区分度。补诊断时间还是改名为监测指标，选项见待裁定清单。
- ☐ **P2-122 慢专病管理目标的上限按「含」判**（2026-09-26 登记，待裁定）：收缩压 140、空腹血糖 7.0 判达标，平台慢病分级判「需干预」、基本公卫口径判控制不满意；上限改不含还是改预置数字，选项见待裁定清单。
- ☐ **P2-123 抗菌药物使用强度的分子把门诊处方也算进去**（2026-09-26 登记，待裁定）：分母是住院收治人天，分子是全部处方；门诊量大、住院少的机构强度会被撑高，告警还归咎于 DDD 单位。按开方时是否在院推断还是给处方加类型，选项见待裁定清单。
- ✅ **P2-124 已结案的孕产妇档案照收访视**（2026-09-26 读到，已修）：分娩登记早拦了结案档案、访视没拦；一孕一册之后按旧档案号记的产检会挂到上一胎名下。结案的一律 409。
- ✅ **P2-125 老年健康「最新一次评估」按录入先后取**（2026-09-26 读到，已修）：补录一张更早的纸质评估表就成了「最新一次」，失能清单、重度失能专案、统计与复评提醒都按旧表算；改为按评估日期取最晚的，同日取后录的。
- ✅ **P2-126 慢病风险评分的趋势修正不看指标方向**（2026-09-26 读到，已修）：精神障碍的用药依从性（越低越危）好转加分、变差减分；改为按方向认变差 +15、好转 −10，页面说明同步。
- ✅ **P2-127 药品供应风险把已结案的缺药登记也数成缺药**（2026-09-26 读到，已修）：已取药 / 未取药 / 已取消的登记全算进「缺药」，药品因此挂高风险或凭空列成中风险；改为只数已登记 / 采购中。
- ✅ **P2-128 智能导诊的急诊提示只看排第一的科室**（2026-09-26 读到，已修）：「咳嗽、发热、胸痛」不提示急诊、急症科室排第四就看不见；改为命中任一急症症状即提示，急症科室排最前。
- ✅ **P2-129 报告修订为危急值只改状态不通知**（2026-09-26 读到，已修）：复位「已通知」却不发消息、不广播；改为按出具时同一套口径重新通知申请机构医师。
- ✅ **P2-130 慢专病团队工作台「召回中」恒为 0**（2026-09-26 读到，已修）：在管查询上再筛召回，两个条件同时成立不了；改为按「我的档案」范围数召回。
- ✅ **P2-131 居民自助随访派的处置任务不挂档案**（2026-09-26 读到，已修）：结案收尾取消不到、中度也次日到期；两条通道改为共用一个派单帮手。
- ✅ **P2-132 诊间提醒把已解除、已过期的疫苗禁忌也照样提示**（2026-09-26 读到，已修）：改按接种闸门同一个判定只提示生效中的。
- ✅ **P2-133 病理冷缺血超 60 分钟先截整分钟再判**（2026-09-26 读到，已修）：60 分 50 秒不算超时、均值偏低；改为按秒判、按秒平均。
- ✅ **P2-134 医废滞留预警早一天**（2026-09-26 读到，已修）：满 2 天当天就报、超期 0 天；预警、扫描任务、驾驶舱三处改为共用「超过 2 天」一条判定。
- ✅ **P2-135 占用床日在月初凭空多一天**（2026-09-26 读到，已修）：1 床日下限只给当日入出院；成本核算与床位使用率两处同改。
- ✅ **P2-136 应急队伍改档能填效期**（2026-09-26 读到，已修）：建档拦、改档不拦，队伍被判已过期进报废桶；改档同一条 422。
- ✅ **P2-137 押金不足预警先分页后筛选**（2026-09-26 读到，已修）：最缺钱的患者不在第一页就看不到；改为先筛、按缺口排好序再分页。
- ✅ **P2-138 专病 360 档案的「待办」少数**（2026-09-26 读到，已修）：漏待审核 / 退回、只看最近 20 条；改为与工作台、日报同一口径（`TASK_OPEN_STATUSES`）数全部任务。
- ✅ **P2-139 考核取数与变量字典对不上**（2026-09-26 读到，已修）：高危数进了去世 / 迁出的档案、评估完成率分子数人分母数档案；改为高危只数在管、评估分母按人去重。
- ✅ **P2-140 转诊「超时未推进」按建单时间判**（2026-09-26 读到，已修）：刚审核通过的单子照报超时、卡住的单子不按卡了多久排；转诊页预警与医生移动端工作台改为从最近一次推进（环节轨迹）起算。
- ✅ **P2-141 随访抽查计划抽不准、重跑换人**（2026-09-26 读到，已修）：按清单位置取模、步长取整；改为按（批次, 随访 id）散列，比例照给的抽、同批次重跑不换人。
- ☐ **P2-142 路径节点的「执行角色」不参与派单**（2026-09-26 登记，待裁定）：护士、村医节点的任务一律派给主管医生，村医手机上看不见；同角色多人时派给谁要先定，选项见待裁定清单。
- ✅ **P2-143 医废收集登记从不送产生点**（2026-09-26 读到，已修）：追溯时间线的「收集」一环没有地点；表单加必选产生点，机构随产生点带出。
- ✅ **P2-144 多重用药预警数历次处方的全部品种**（2026-09-26 读到，已修）：改按同时在用（处方服药期覆盖今天）的品种数判，画像每行标出在不在用。
- ✅ **P2-145 开单前互认预检与建单两套判定**（2026-09-26 读到，已修）：预检弹「可互认」、建单 422；预检改按建单口径逐份找可互认的报告，页面带上中心类型与申请机构。
- ☐ **P2-146 转诊「有效就诊」不看就诊记录**（2026-09-26 登记，待裁定）：页面恒送 true，到院即有效、即计分；判据与判定时机属考核口径，选项见待裁定清单。
- ✅ **P2-147 网关对账把现金单判成「本地有通道无」**（2026-09-26 读到，已修）：本地侧只取走这条通道收的支付单。
- ✅ **P2-148 疫苗批次统计的过期 / 临期连发完的批次一起数**（2026-09-26 读到，已修）：改为只数还有余量的，与临期清单同一口径。
- ✅ **P2-149 模拟诊疗最高分只在最近 500 条作答里算**（2026-09-26 读到，已修）：改按全部作答取最高分、作答序号补上窗口外次数。
- ✅ **P2-150 随访办结后下次随访拿别的阶段的周期排**（2026-09-26 读到，已修）：改为只看本阶段与不分阶段的管理目标，没配按 90 天。
- ✅ **P2-151 发布过的量表停用后能改题再发布回同一版本**（2026-09-26 读到，已修）：发布过的停用后照样不许改题目与评分。
- ☐ **P2-152 发药按毫克扣片数**（2026-09-26 登记，待裁定）：日剂量 × 天数直接当库存单位扣，缺规格换算；补处方发药数量还是药品规格，选项见待裁定清单。
- ✅ **P2-153 运营月报门急诊人次含住院就诊**（2026-09-26 读到，已修）：排除住院类就诊，与成本核算同一口径。
- ✅ **P2-154 住得久的在院患者从在院清单消失**（2026-09-26 读到，已修）：三处页面取「最新 200 条住院」再挑在院；改为按在院取数。
- ✅ **P2-155 手术排班表只给最早 300 条历史**（2026-09-26 读到，已修）：不指定日期时给今天及以后的排班。
- ✅ **P2-156 Westgard 压线值被浮点误差判成超线**（2026-09-26 读到，已修）：z 按录入的十进制数算。
- ✅ **P2-157 室内质控 L-J 图只给最早 500 个点**（2026-09-26 读到，已修）：改取最近 500 个点，刚失控的点能处理。
- ✅ **P2-158 体温单把未测的画成 0**（2026-09-26 读到，已修）：曲线组件认缺测、在那儿断开。
- ✅ **P2-159 传染病多点预警按手填名称拆组、窗口多一天**（2026-09-26 读到，已修）：按编码认同病种，窗口含今天共 N 天。
- ✅ **P2-160 HIS 的 A03 出院不派出院随访**（2026-09-26 读到，已修）：与平台出院共用派单；患者通知另定。
- ✅ **P2-161 门急诊告知书不挂就诊、完整度的待签署恒为 0**（2026-09-26 读到，已修）：页面载入就诊后默认挂上；接口查就诊存在且同一患者。
- ✅ **P2-162 统一申请单计数算在截断样本上、互认检查单状态是原码**（2026-09-26 读到，已修）：库里筛、全量计数；recognized 归已完成。
- ✅ **P2-163 / P2-164 号源撮合与资源视图的号源数在截断样本上、过期号源标可用**（2026-09-26 读到，已修）：按机构 / 按类别在库里全量聚合；号源只算今天及以后的。
- ✅ **P2-165 制剂批次缺省效期按 30 天一个月折算**（2026-09-26 读到，已修）：按日历月推到对应日的前一天（标签规定口径）。
- ✅ **P2-166 DRG 事中预警拿兜底组 QY 当同组**（2026-09-26 读到，已修）：基线与预警只认正式分组，在院 QY 计入未入组。
- ✅ **P2-167 基金池预结收年度外的月份**（2026-09-26 读到，已修）：预结月份须在池子年度内；存量年度外预结的查法与处置写在 TECH_DEBT。
- ✅ **P2-168 FHIR 批量导出把导出前就修订过的新报告再按修订导一次**（2026-09-26 读到，已修）：修订史与导出界在本批新增导出之前取。
- ✅ **P2-169 机构编号只过写权限守卫不查存在，错号翻成重复登记**（2026-09-26 读到，已修）：四处查存在；P1-90 闸门不再把写权限守卫当「看过」。
- ✅ **P2-170 消毒供应成本合计只算最近 200 批**（2026-09-26 读到，已修）：合计、件数与构成按全部批次在库里汇总。
- ✅ **P2-171 室内质控测定时间留空按 UTC 记**（2026-09-26 读到，已修）：缺省取本地时刻，与手填同一把尺子。
- ✅ **P2-172 手术申请表单没有「非计划重返手术室」可勾，指标恒 0**（2026-09-26 读到，已修）：表单补勾选框，清单出参带上标记。
- ✅ **P2-173 已有病种的纳入 / 排除规则页面上改不了**（2026-09-26 读到，已修）：病种行加「改规则」，保存即升一版并留快照。
- ✅ **P2-174 数据源停用后照旧显示正常、stopped 标成异常**（2026-09-26 读到，已修）：出参补状态文案，页面照它显示。
- ✅ **P2-175 结了项 / 中止的项目里程碑照样算逾期**（2026-09-26 读到，已修）：里程碑逾期同样排除已结束的项目。
- ✅ **P2-176 知识检索先截 200 条再滤过期**（2026-09-26 读到，已修）：过期的在库里就滤掉。
- ✅ **P2-177 慢专病团队清单先分页后按病种筛**（2026-09-26 读到，已修）：按病种筛在分页之前，总数跟着对。
- ✅ **P2-178 便捷寻医先截 200 位再排有号的**（2026-09-26 读到，已修）：有号的先排、再取前 200 位。
- ✅ **P2-179 移动端术中记录不录麻醉方式与切口等级**（2026-09-26 读到，已修）：两端都录、缺省带出申请时填的。
- ✅ **P2-180 ESB 手工消费不看出站接入方停用**（2026-09-26 读到，已修出站一侧）：停用的出站端点 409、消息留队；入站积压怎么处置待裁定。
- ✅ **P2-181 DRG 统计机构 CMI 与 MDC CMI 调权后对不上**（2026-09-26 读到，已修）：两者都用病例入组时的权重快照。
- ☐ **P2-182 病案首页手术栏只带一次、出院不查未完成手术**（2026-09-26 登记，文案已订正、行为待裁定）：首页能不能改、改后是否重新入组、出院拦不拦，选项见待裁定清单。
- ☐ **P2-183 告知书正文开具时冻结、文案却写签署时**（2026-09-26 登记，文案已订正、行为待裁定）：待签期间模板改版 / 停用还能不能签，选项见待裁定清单。
- ☐ **P2-184 监测指标 #11 / #12 数行不数人**（2026-09-26 登记，待裁定）：按人去重还是改名为档案 / 协议数。
- ☐ **P2-185 绩效慢病随访覆盖按档案、文案按人**（2026-09-26 登记，待裁定）：进基金分配，改分数须签字。
- ☐ **P2-186 基金分配名次同分按编号排**（2026-09-26 登记，待裁定）：并列名次还是平均名次。
- ☐ **P2-187 DRG 事中预警基线全县合并、文案写本院**（2026-09-26 登记，待裁定）：按本院建基线还是改文案。
- ☐ **P2-188 FHIR 入站观测算随访却不推到期日**（2026-09-26 登记，待裁定）：设备读数算不算一次随访。
- ☐ **P2-189 消毒供应响应不动批次、一批可响应多单**（2026-09-26 登记，待裁定）：与 P1-75 一起答。
- ☐ **P2-190 改专病路径回溯改写已出组病例完成度**（2026-09-26 登记，待裁定）：出组时冻结还是维持现算。
- ✅ **P2-191 基金分配负权重按 0 计却不说明**（2026-09-26 读到，已修）：快照参数里写明按 0 计了几家。
- ✅ **P2-192 待确认的整改任务一次登记进展就退回整改中**（2026-09-26 读到，已修）：待确认的登记进展 409，退回走确认接口。
- ✅ **P2-193 培训考核取最高分是读改写**（2026-09-26 清欠账，已修）：条件 UPDATE 原子取大，读改写欠账清单减一条。
- ✅ **P2-194 慢专病任务升级置紧急是读改写**（2026-09-26 清欠账，已修）：条件 UPDATE 只往上抬，读改写欠账清单再减一条。
- ✅ **P2-195 驾驶舱「近7日传染病报告」数 8 天**（2026-09-26 读到，已修）：含今天共 7 天、与多点预警同一窗口，发病日期在将来的不算。
- ✅ **P2-196 接种日期留空落库成空串**（2026-09-26 读到，已修）：有效日期算一次，查禁忌、查效期、落库同一个值。
- ✅ **P2-197 药品批号反查「仍在外面」只加最新 1000 行**（2026-09-26 读到，已修）：合计在库里全量求和，清单上限不变。
- ✅ **P2-198 审方规则「特殊人群」收任意文本**（2026-09-26 读到，已修）：闭集校验，建规则与导入都 422；存量排查 SQL 写在 TECH_DEBT。
- ✅ **P2-199 诊疗人次把住院类就诊也数进去**（2026-09-26 读到，已修）：驾驶舱基层占比、医师日均担负、公式变量与月报同一口径；监测指标会变动。
- ✅ **P2-200 有序转诊率照数挂上后被退回的转诊单**（2026-09-26 读到，已修）：现算时只数未被退回的关联。
- ✅ **P2-201 非计划重返率分子分母按不同时间归月**（2026-09-26 读到，已修）：分子在分母那批术中记录里数，同按做手术的月份归。
- ✅ **P2-202 入出院诊断符合率把没填入院诊断的算不符合**（2026-09-26 读到，已修）：缺一项计未采集、不进分母，与术前术后符合率同一条规矩。
- ✅ **P2-203 质控回执「参与规则 N 条」含跳过的规则**（2026-09-26 读到，已修）：只数真正参与评分的。
- ✅ **P2-204 出院小结住院天数多一天**（2026-09-26 读到，已修）：与居民端、DRG、成本核算同一口径（日期差，当日入出计 1 天）。
- ✅ **P2-205 体检报告打印「异常 ↑」方向瞎写、异常项提示自相矛盾**（2026-09-26 读到，已修）：只写「异常」；汇总没写时列出标了异常的分项。
- ✅ **P2-206 验真页把打印日期标成「签发日期」**（2026-09-26 读到，已修）：改标「打印日期」，与纸面页脚的打印时间对得上。
- ✅ **P2-207 合同到期提醒清不掉**（2026-09-26 读到，已修）：已续签、已离职的不再提醒；到期未续签的照旧提醒；接口与每日扫描一个口径。
- ✅ **P2-208 共享中心排班只给最早的 200 条历史**（2026-09-26 读到，已修）：不带日期时给今天及以后，与手术排班表同一处理。
- ✅ **P2-209 居民端点「押金余额」弹英文校验错误**（2026-09-26 读到，已修）：费用清单监听只挂在带住院号的按钮上。
- ✅ **P2-210 居民端 H5 的对照表与后端各说各的**（2026-09-26 读到，已修）：病种名、同意方式取自后端；费用类别、宣教推送、更正状态措辞对齐。
- ✅ **P2-211 孕产妇分娩当天能结案、结案档案照收产前筛查**（2026-09-26 读到，已修）：结案要有产后访视；结案档案不收筛查。
- ✅ **P2-212 老年复评提醒跳过没填日期的评估**（2026-09-26 读到，已修）：按录入那天算，与「最近一次评估」同一口径。
- ✅ **P2-213 慢专病个案上报办结后还能改、空备注抹掉旧备注**（2026-09-26 读到，已修）：办结 / 关闭的 409，空备注不覆盖。
- ✅ **P2-214 过期的调阅授权印成「有效」**（2026-09-26 读到，已修）：清单带「此刻算不算数」与状态名，取自调阅判定那唯一一份口径。
- ✅ **P2-215 慢专病宣教定时推送晚 8 小时、报告推送时点按 UTC 比**（2026-09-26 读到，已修）：人填的本地时间与本地时刻比。
- ☐ **P2-216 病历质控「危急值须有处置记录」按患者任何时候的危急值触发：后来出的危急值把以前的病历改扣 10 分**（2026-09-26 登记，待裁定）：选项见待裁定清单。
- ☐ **P2-217 医疗质量指标的「出院人次」数的是病案首页，在院就填了首页的也算**（2026-09-26 登记，待裁定）：选项见待裁定清单。
- ☐ **P2-218 自定义绩效公式的「越大越好」开关从来不起作用**（2026-09-26 登记，待裁定）：选项见待裁定清单。
- ☐ **P2-219 数据质控种子规则 QC004「就诊记录须填写诊断编码」（错误级）被平台自己的入院流程天然违反**（2026-09-26 登记，待裁定）：选项见待裁定清单。
- ☐ **P2-220 居民端代管家人：未满 14 周岁的孩子加不进来（表单没有监护人三要素），没留手机号的老人照手册办不成**（2026-09-26 登记，待裁定）：选项见待裁定清单。
- ☐ **P2-221 价格公示写「某日起执行」，调价却当场生效**（2026-09-26 登记，待裁定）：选项见待裁定清单。
- ☐ **P2-222 危急值报告不给居民发站内信，档案里却即时可见**（2026-09-26 登记，待裁定）：选项见待裁定清单。
- ☐ **P2-223 短信登录顺带自动绑定档案撞上唯一约束时，写好的兜底永远走不到；绑定手机号没有兜底**（2026-09-26 登记，待裁定）：选项见待裁定清单。
- ☐ **P2-224 居民端「我的档案」就诊 / 报告各截 50 条、站内信截 200 条，没有翻页也不给总数**（2026-09-26 登记，待裁定）：选项见待裁定清单。
- ☐ **P2-225 慢专病个案上报与它派生的处置任务两边状态互不知道**（2026-09-26 登记，待裁定）：选项见待裁定清单。
- ✅ **P2-226 已死亡 / 排除 / 迁出的慢专病档案照样派任务、改风险分层、记积分**（2026-09-26 读到，已修）：派生工作只挂在管档案，与改档 / 绑包 / 启动路径的 409 同一句。
- ☐ **P1-112 登记死亡只终止一个病种档案**（2026-09-24 登记，待裁定）：同一患者别的病种照常在管、照派任务，排好的宣教短信照发；要不要按人传递牵涉跨机构写权限与死亡的事实来源，选项见待裁定清单（建议与 P1-107 一并裁）。
- ☐ **P1-108 转诊闭环率 / 随访完成率三处三种算法**（2026-09-24 登记，待裁定）：同一份数据闭环率接口与工作台 100、考核计分 33.33（考核把撤回 / 退回算进分母，正是接口 docstring 明文否定的口径）；考核的「随访完成率」数的是全部任务；改口径即改考核分，选项见待裁定清单。
- ☐ **P1-107 档案注销之后照样发宣教短信、派随访、纳管建档**（2026-09-24 登记，待裁定）：注销只从检索与居民端绑定里消失，写接口与定时推送一处都不看 `deactivated_at`；注销该停到哪一步是个保法与业务口径，可选项与建议见待裁定清单。
- ☐ **P1-105 业务日期按本地时区、时间戳按 UTC，按日 / 按月统计的边界在非 UTC 部署下错位**（2026-09-24 登记，待裁定）：35 处把本地日期拼成边界去比 UTC 时间戳，东八区部署时每月 1 日 0–8 点的业务算进上个月；显示侧更直接：前端原样显示 UTC 时间戳，东八区用户看到的时间一律慢 8 小时；属日期策略顶层决策，方案与建议见待裁定清单（与 P2-35 一并裁）。
- ☐ **P2-57 居民端宣教清单列出未送达的推送**（2026-09-24 登记，待裁定）：读一条还没到点的定时推送，定时派发就此取消；读发送失败的计进已读率——居民端该看见什么见待裁定清单。
- ☐ **P2-52 全域角色填不存在的机构 / 患者号，生产库撞外键**（2026-09-24 登记，待裁定）：可见性守卫对 admin / director 直接放行、不查库，守卫之外不再看这个 id 的写端点有十几处——真 PG 实测 4 个 409 误报（「已纳管」「序列号已登记」「专家已存在」）、4 个 500。修法倾向在守卫的全域角色分支里查一次存在，动的是授权层，要 ADR + 复核，见待裁定清单。
- ✅ **两个跨层形状各扫一遍，都干净**（2026-09-24，负结果照样记账，见 TECH_DEBT 核查记录）：①比较的状态字面量从没被写入过（`monitor.py` 那一例的形状）——后端 13 处命中全是映射表 / 局部变量写入，前端 91 组对照 0 处；②前端发出去、请求模型里没有的键（pydantic 默认静默丢弃）——表单 142 个、字面量请求体 145 处、整个弹窗结果 27 处，0 处真缺陷，变异验证过扫描能点名。判据误报高，未落棘轮。
- ✅ **P2-48 系统参数页举例的键没有消费方**（2026-09-24，已修）：占位提示「如 portal.verify_lock_seconds」——后端不读这个键，锁定时长写死在代码里，照着配了什么也不变。示例改成真在用的公众号模板键，回归钉住「示例键得有消费方」。
- ✅ **孤儿端点闸门的反方向：前端写出来的 `/api` 地址都得有路由、动词都得对**（2026-09-24 立，零基线）：三端 977 处地址全部对得上运行期路由表，972 处推得出动词、全部对得上——现状干净，闸门防以后（拼错一个字母、PATCH 写成 PUT，`make verify` 原本全绿、上线才 404 / 405）。变异验证过：真实文件里改一处动词、拼错一处居民端地址，当场点名。`tests/test_frontend_api_calls_resolve.py`，闸门现状加一行。
- ✅ **P2-44 四个慢专病工作台的路径统计把范围内的纳管整表读进内存**（2026-09-24 同族形状扫出，已修）：全域视角每刷一次读整张纳管表、拼一条十几万个参数的 IN（P1-51 同形状）。改子查询，统计数字不变；回归拦截语句参数个数。
- ✅ **P1-80 写侧判据第五个盲区：请求体捎带的第二个对象不看是谁家的**（2026-09-24 量并逐条判完）：9 条——修 1（接种批次，P0-42）、同患者一致性已校验 4、按设计 3、待裁定 1（病理送检）；立棘轮只减不增。
- ☐ **P1-79 适宜技术实训只能报本机构的计划**（2026-09-24 实测）：县医院办的推广实训，乡镇医生看得见、点「报名」吃 403（提示还是「无权以该机构名义写入数据」）。推广实训的本意是县级办、基层学——放不放开要人定，写进待裁定清单。
- ☐ **P1-78 村医扫码绑定是半截**（2026-09-24）：码印出去了、扫开什么也不发生；令牌明文在全县可见的村医清单回执里——接上绑定之前先把它收掉。
- ✅ **P0-37 统计 / 报表类接口只拿 `resolve_org_scope` 当范围**（2026-09-24 实测，已修）：它只解析范围不授权，新卫生院的医生 / 经办读到县医院的合并报表、运行效率、药占比、床位统计。照第九轮 A 案新增 `scope_stats_orgs`（本机构 + 同医共体；全域角色看全县）接上四个接口，连带修好绩效公式变量取运行效率的内部调用；回归 16 条。余下 16 条立读侧棘轮：按设计 12 条写理由，待裁定 4 条（质量指标、项目台账、资源清单、派驻台账）。
- ✅ **P0-36 按病历号读就诊病历不收调用方身份**（2026-09-24 逐条判 P1-69 名单时实测，已修）：乙院医生按病历号读到甲院患者的主诉、现病史、治疗方案原文（200 且不留痕）；同文件清单按机构收口、复评按机构判归属，唯独详情不看。照 P0-19 / P0-20 按患者判可见性并留痕；回归 4 条。P1-69 隔跳名单 20 → 19。
- ✅ **P0-35 请求自己声明「新记录归哪家机构」的写端点，没有任何棘轮在看**（2026-09-24 量出）：`db.get(Organization, body.org_id)` 只查存在不查归属，三条写侧棘轮的分母都数不到 `Organization`。94 个写端点在请求里收机构号，27 个无守卫：admin-only 10（自动豁免）、按设计 4（写理由）、待裁定 1（家医签约，早已是 P1-45「故意未修」）、**候选 12**（建就诊、开处方、检查申请、代煎单、转诊、会诊申请、传染病报告、慢病建档、消毒供应、智能随访）。已钉成只减不增的棘轮；照 `assert_org_writable` 既定口径分批修，每批带实测回归。✅ 第一批诊疗行为 4 条（建就诊 / 开处方 / 检查申请 / 代煎单，实测全 201）已修，连带修好 FHIR 入站；候选 12 → 8。✅ 第二～四批 8 条（转诊 / 会诊申请的发起方、传染病报告、慢病建档、消毒供应批次、智能随访）已修；**候选清零，棘轮即禁令**，只剩待裁定的家医签约（P1-45）。
- ✅ **P1-74 按 id 写的棘轮只认名叫 `org_id` 的列**（2026-09-24 顺带量出）：归属在 `center_org_id` / `from_org_id` 上、又不挂患者的表按 id 写没人盯；粗扫 22 处逐条判过，真候选只有消毒供应两处（已随 P0-35 修）。已另起棘轮盯住这一族（按设计 5 条写理由，自证拿掉消毒供应的判定当场点名）。
- ☐ **P1-75 消毒供应「基层申领、中心响应」对非全域角色是反的**（2026-09-24 实测）：中心看不到基层的申领、响应 403，申领方自己却能拿中心的批次「响应」（200）。申领单不写发给哪家中心——与共享中心同一个待裁定问题，答之前不改守卫。
- ✅ **P1-76 按请求体里的患者号新建挂在患者上的行，11 个创建端点逐条判过**（2026-09-24）：修 1（P0-34 宣教推送）、按设计 5、随读侧裁定 3、待裁定 1（接种禁忌登记：拦还是只留痕）。已落进棘轮（按设计 6 / 待裁定 4 两张名单只减不增，满意度代录因不是可见性问题归入按设计并另记）。
- ✅ **P0-34 慢专病宣教推送不判患者，任一机构能让平台给全县任意居民发短信**（2026-09-24 实测，已修）：docstring 写着「向纳管患者推送」，实际只看角色——乙院医生填甲院患者号 201、短信照发。照同文件批量干预逐个判可见性并留痕，且**先判完再发**（边判边发时第一条短信在 403 之前就已送达）。回归 4 条。
- ✅ **P0-33 统一申请单中心不带患者号时吐全域五类在办单据**（2026-09-24 动态探针发现，已修）：自带注释说它是「最省事的越权入口」，
  守卫却只包在 `if patient_id` 里。不带患者号时按可见患者过滤五个子查询；本机构经手的单据照常可见。回归 2 条。
- ✅ **P0-32 慢专病评估与干预清单不收口**（2026-09-24 动态探针发现，已修）：不带患者号就吐全域评估结果与干预内容（带姓名）——
  与 P0-23 / P0-24 同文件同形状，守卫被包在 `if patient_id` 里，静态闸门看不出。照同口径 `scope_patient_list`；回归 4 条。
- ◐ **P0-31 远程会诊五个流转端点只看角色**（2026-09-24 实测，第三方这一层已修）：第三家医师能受理、写会诊意见、评价，第三家经办能计费（各 200）。
  `_get` 统一判患者可见性并留痕，申请方与受邀方照常；「哪一步由哪一方做」（申请方今天仍能写「上级」意见）写进待裁定。回归 8 条。
- ✅ **P0-30 受理居民服务申请就能改派别家目标池的候选**（2026-09-24 实测，已修）：乙院受理一条申请，甲院认领的人挪进了乙院名下（200），
  而走「认领」是 403。照认领口径只修这一支；池子里没有这位居民时的受理口径仍在 P1-48。回归 3 条。
- ✅ **P0-29 转诊证明任一机构都能签；特病 / 双通道审核同 P0-28 的洞**（2026-09-24 实测，已修）：第三家经办给别家的转诊签证明（200）；
  复制 director 权限的自定义账号看不到患者也批得了申报。三处按患者可见性判定并留痕；转诊证明只挡第三方，哪一方签留待裁定。回归 7 条。
- ✅ **P0-28 知情同意按编号就能撤回；更正 / 注销审核被整包授权的自定义角色够得着**（2026-09-24 实测，已修；同日先误判成「按设计」，
  取证代理复核后更正）：乙院撤回甲院患者的同意（200）；复制 director 权限的自定义账号不是全域角色、看不到患者，却批准更正改了主索引姓名（200）。
  两处按记录所属患者判可见性并留痕、先判归属再判状态；全域角色审核照旧只多一条留痕。回归 6 条。
- ✅ **P0-27 危急值「确认接收」「处置反馈」按报告号就能替别家做**（2026-09-24 逐条判 P1-71 时发现，实测，已修）：乙院把甲院的危急值
  确认接收再反馈一句，危急值就此闭环——而它连处置轨迹都读不到。报告表离患者隔一跳，两条写侧棘轮都看不到它；照读侧
  `_report_visible_or_404` 判定并留痕，先判归属再判状态。回归 5 条；既有闭环用例改由申请机构的医师确认与反馈。
- ✅ **P0-26 慢病随访按档案号就能记，顺带改掉分级与下次随访日**（2026-09-24 按 P1-71 名单实测，已修）：乙院给甲院管着的患者
  记随访（201）。同文件两个读接口早就按患者可见性守着；照同一口径，管档案的机构照常能记。回归 3 条。名单 24 → 23。
  慢病档案清单本身不收调用方（第一层无身份读名单），界面上仍列得出别家档案、点进去记会 403——随 P1-49 一起定。
- ✅ **P0-25 接种禁忌按禁忌号就能解除**（2026-09-24 按 P1-71 名单实测，已修）：乙院医生解除甲院患者的接种禁忌（200），
  接种前评估从此不再拦这一条——而乙院连这条禁忌都看不到。照同文件口径判可见性并留痕，先判归属再判状态；回归 4 条。名单 25 → 24。
- ✅ **P0-24 慢专病复诊计划按计划号就能改、复诊看板不收口**（2026-09-24 按 P1-71 名单实测，已修）：乙院医生按计划号把甲院患者的
  复诊计划改期、办结、移除（200）；看板不带患者号时列出全域计划（带姓名）。同组建计划与按患者查早就守着，与 P0-23 同文件同形状；
  照抄同组口径，回归 5 条。P1-71 名单 26 → 25。
- ◐ **P1-73 清单接口「有条件的守卫」静态看不出，改用动态探针量**（2026-09-24）：守卫被包在 `if patient_id` 里的清单，
  静态闸门一概失灵。`scripts/probe_list_exposure.py` 让一家毫无关系的新机构调全部清单、看拿回了谁的患者；首跑查出 P0-32 / P0-33（已修），
  现在暴露 10 = 已登记待裁定 9 + 按设计 1，未登记 0。已接进 CI（e2e job 末尾一步，阻断）与 `make probe-lists`。
  **机构维度同日补上**（`9043e35` 之上量）：再问「拿回了哪些别家机构的行」，首跑 23 条——医废滞留预警是真漏（P0-39 续，已修），余 22 = 按设计 10 + 已登记待裁定 12，未登记 0；消毒供应批次清单新登记，随共享中心那一问。
  **按 id 维度也补上**（`f572c38` 之上量）：整数路径参数填 1/2/3 再调一遍，拿实际响应对静态按 id 读棘轮——首跑 8 条全在静态名单里，未登记 0；判定包在条件里的那种形状从此也有东西盯着。
- ✅ **P1-72 调阅授权的发放、撤销不留痕**（2026-09-24 逐条判 P1-71 时发现，已修）：授权管理按设计只留痕不阻断，同文件两个读接口早就
  留痕，偏偏改变「谁能调阅这个人」的两个写动作一条痕都不留。照同文件口径补上；回归 3 条。发给本机构要不要设限写进待裁定。
- ◐ **P1-71 按 id 写「挂在患者上的表」的接口，写侧棘轮看不见**（2026-09-24 修 P0-23 时量出）：分母从「带 `org_id` 列的表」
  换成「有 `patient_id`、没有 `org_id` 的表」重扫，另有 26 个写接口按 id 取这类记录而无任何归属守卫，已钉成只减不增的名单
  （自证：拿掉 P0-23 的守卫行当场点名 `close_consult`）。**逐条判定待办**：远程会诊受邀方、共享诊断中心、转诊接收方、
  急救调度、医保审核这类按设计跨机构的，写明理由移进豁免；其余先实测取证，越权的照 P0-10 / P0-19 补守卫并带回归。
  逐条实测（2026-09-24）：20 条照样 200 / 201。已修三条（P0-24 复诊计划、P0-25 接种禁忌解除、P0-26 慢病随访）；判据误报一条
  （平台转诊早有接收方守卫，登记进领域守卫）；按设计不阻断四条写明理由（窗口撤回同意、三条只收全域角色的审核，前提钉住）。26 → 18。
  分母往外推一跳（P0-27 藏在这一跳里：危急值报告挂在申请单上）：隔一跳的 10 张表重扫又出病理标本三条（待裁）与
  站内消息标已读一条（只认收件人，按设计）。18 → 21，按设计 4 → 5；自证拿掉 P0-27 的守卫当场点名。
  取证代理复核后又修 P0-28～P0-31 与 P1-72：21 → 13。剩下 13 条（急救四、诊断中心三、病理三、中药房一、妇幼二）都要人定
  「这一步由哪家做」，连同会诊 / 转诊证明的分工与「为调阅而建单」的口径写进待裁定清单 P1-71 一节。
- ✅ **P0-22 门急诊按就诊号读处置与护理记录不收调用方身份**（2026-09-24 逐条判 P1-69 名单时实测，已修）：乙院按就诊号
  读甲院的处置（部位、剂量、不良反应）与护理记录，200 且不留痕；同文件按患者查处置史与完整性自查早就按患者可见性守着。
  照抄同文件口径，就诊号不存在照旧回空清单；回归 6 条。第一层无身份名单 36 → 35，隔跳名单 21 → 20。
- ✅ **P0-21 检验检查报告的修订史与危急值处置轨迹不收调用方身份**（2026-09-24 逐条判 P1-69 名单时实测，已修）：
  乙院按报告号读甲院报告的历次前结论与危急值轨迹（轨迹里带结论原文），200 且不留痕；同文件的修订接口早就按
  `exam_requests.patient_id` 判可见性，读侧没跟。照同一口径补上，回归 6 条；隔跳名单 23 → 21。
- ✅ **P0-20 术中记录读接口不收调用方身份**（2026-09-24 逐条判 P1-69 名单时实测，已修）：乙院医生按申请号读甲院的术式、
  术中所见、转归与术前术后诊断，200 且不留痕；同文件申请清单与写接口都收了口，唯独这条没有。照 P0-19 补可见性判定与留痕，
  回归 3 条；隔跳名单 24 → 23。
- ◐ **P1-69 无调用方身份的读接口，分母再往外推一跳**（2026-09-24 修 P0-19 时量出）：原判据只认直接带 `patient_id` 的表，
  隔一跳 / 两跳挂在患者上的病历表整族在分母之外。按外键推导重扫又出 24 个，已钉成只减不增的名单（自证：退回 P0-19
  修复前当场点名那三个端点）。**逐条判定待办**：统计类、按设计跨机构的留名单写理由；按 id 读病历的
  （分娩记录、病理标本）倾向照 P0-10 / P0-19 收口，先实测取证再动（`docs/待裁定事项清单.md`）。
  术中记录（P0-20）、报告修订史与危急值轨迹（P0-21）、门诊护理（P0-22）、就诊病历详情（P0-36）已按此修掉，24 → 19；分娩记录挂在妇幼模块上，那个模块的清单口径本身在 P1-49 待裁，一并等。
- ✅ **P0-18 经办填写的耗材条码能在管理员浏览器里执行脚本**（2026-09-24 做 P2-38 物资页时发现，e2e 实测，已修）：
  「使用登记」按钮把条码裸插进 `data-use` 属性，条码带一个双引号就越出属性往页面里塞标签，打开这一页的院长、管理员执行它。
  按形状全仓扫又出公式编码、规则编码两处（仅 admin 可写）；五处一并 `esc()`，四处 URL 路径补 `encodeURIComponent`
  （`#` 条码此前按半截查成 404）。属性值位置汇点确定，立了零豁免守卫；2026-09-07 "§8 守住了"的核查结论已更正。
  ⚠️ 条码含 `/` 的仍用不了（服务端把 `%2F` 解回 `/`），属后端路径设计，未改。
- ✅ **P1-59 / P1-60 写侧越权判据的第三个盲区：id 从 body 收的写端点，候选 16 → 1**（2026-09-18，实测取证，已修）。
  起因是写 ADR-0024 时实测到乙院 `operator` 能给甲院医师建派驻、签劳动合同（201，台账里显示"甲院 → 乙院"）。
  `test_stage15_horizontal.py` 的写侧判据第一步只看**路径带 `{}`** 的端点，
  而把 `employee_id` / `admission_id` 放在 body 里的端点从来没进过分母——**不是"查过没问题"，是"没被数到"**。
  该文件的注释早就列了两个同形状的历史案例（P1-42、P1-47），两次都是手工发现、各补一条用例，判据本身没动；这是第三次。
  另立棘轮 `tests/test_body_id_org_write_guard.py`（不放宽原判据，免得"已判定完、零欠账"与"一批未判定候选"混在一条里；
  候选只许变少，数字登记进 `docs/闸门现状.md`），然后逐族实测、逐族修：
  HR 3 条（mgmt 派驻建档 / 劳动合同 + staffing 派驻建档）、
  **钱 5 条**（billing：乙院 operator 能把甲院一次住院的计费、押金、退押金、出院结算、收款**整条资金链走完**；退费与结算两句守在临界区之前）、
  临床 4 条（入院占床 / 长期医嘱 / 手术申请 / 交接班）、
  医废收集（追溯码落在甲院头上）与慢专病路径启动（同文件六个流转端点早有守卫，唯独"启动"没有）。
  📌 **判定为不该修的同样钉住**：4 条角色门只放 `GLOBAL_ROLES` 的（`create_slot` / `create_bed` / 两条 cost）
  单列 `GLOBAL_ROLE_ONLY`，角色门哪天放宽就红；支付回调按设计没有调用方身份（HMAC 验签承担）进豁免。
  **余下 1 条 `appointments.book` 倾向按设计**（乡镇替患者约县院专家号是正路），属产品口径，留在候选里等确认。
  📌 **三处自己说错的已更正**：用 `director` 取证得出的"越权"（全域角色跨机构是设计，改用 `operator` 重跑才是真证据）；
  `create_payroll` 称可越权（角色门只放全域角色，本批那句校验是纵深防御）；
  正则取角色名看不见 `require_roles(*SERVICE_ROLES)`，把 `start_path_instance` 说成"连角色门都没有"——
  同一个毛病让一条判定用例**为错误的理由通过**（空集合恒满足"只有全域角色"），已改成解析模块级常量、解析不出来就红。
  回归 5 个文件 34 条，每批撤掉守卫后越权反例各自转红、正路用例不动。
- ✅ **写侧闸门不再只看 HTTP 方法：「会写库的 GET」纳入判据**（2026-09-11，ADR-0022）。
  起因是 `quality:rescore_medical_record`——它是个**会写库的 GET**，于是写侧几道闸门
  （只扫 post/put/patch/delete）整族看不见它，那个跨机构越权最后是靠读侧闸门才现形的，
  **而它本质上是一次写**。两道判据各自的盲区正好在那里重叠。
  📌 量清了范围：这类端点全仓 **6 个**，5 个是进页面顺手跑 `sweep_overdue`——
  **有意为之且写了理由**（「只靠定时任务，演示环境没开调度就永远看不到超期；
  只靠进页面刷新，没人进页面的机构就永远不超期」），两头都说到了，不是随手写的。
  只有 `rescore_medical_record` 不幂等于时间（规则库一改，同一个 GET 得到不同分数并回写）。
  📌 **改方法属破坏性变更，没有自己动**：写成 ADR-0022（Proposed）交产品/前端/运维裁定，
  并单独建议先改那个不幂等的。落地的只有零破坏的那一半（闸门判据）。
  📌 **放宽当天 0 条新增**——补的不是今天的洞，是明天的。两臂实测：造一个会写库、
  无守卫的 GET 探针，新判据抓得到、旧判据漏得掉。
- ✅ **ADR 索引补齐并加新鲜度闸门**：索引停在 0018，而 0019/0020/0021 **已经在盘上躺着**
  ——三份都是 §8 要求「请人复核」的越权类决策，按索引找"有哪些决策待裁"的人会漏掉它们。
  这是同一天第五次撞见「写下来就没人回头核」（前四次：`batch_tasks` 注释、`clock.py`
  docstring、欠账清单结论、ROADMAP 过期数字）。已补齐，并加
  `test_adr_index_freshness.py`：漏列、死链、扫描面空转三条各自转红。
- ✅ **P0-17 病历复评可被别家机构触发并改写质控等级（实测取证，已修）**：
  `GET /api/quality/records/{id}/qc` 乙院 doctor 与 operator 都 **200**，
  甲院病历的 `qc_score` 从 **100 改成 43**、`qc_grade` 从**甲改成丙**——质控等级是考核依据。
  📌 **第八次"同一个文件、两套口径"，而且这次风险早就写下来了**：同文件
  `upsert_medical_record` 走**同一条 `_apply_qc` 路径**，它的注释白纸黑字写着
  「这条路径会同步跑 `_apply_qc` 算缺陷扣分，写进去的内容直接进乙院的质控成绩」，
  并因此加了 `assert_org_writable`；而复评这个入口一直什么都不校验。
  **风险被写下来了，只写在了其中一条路径上。**
  📌 **它是被 P2-36 的修复炸出来的**：这是个**会写库的 GET**，写侧闸门只扫
  post/put/patch/delete 所以看不见；读侧闸门此前只跟进一层，而取到患者数据在第三跳
  （`_apply_qc → evaluate_record → _record_context → db.get(Encounter, …)`）。
  回归 `tests/test_quality_rescore_org_guard.py` 四条，含一条"被拒之后快照没有被改写"
  （会写库的 GET 若把判定排在 `_apply_qc` 之后，会返回 403 而分数已经改掉了）。
  「会写库的 GET」本身登记 P2-37，改 POST 属破坏性变更，未做。
- ✅ **P2-36 已解决：闸门从"跟一层"改成"跟到底"（传递闭包）**（2026-09-11）。
  当初只跟一层的理由是"跟太深会把 `db`/`paginate` 这类通用工具卷进来"——
  **那个理由不成立**：跟的是本模块定义的函数，`db.get(...)` 是属性调用、
  `paginate` 是 import 进来的名字，都不在候选集里，跟多深都收不到。
  改前逐项量过：分母 133 → 135（多出的正是 projects 的里程碑两条）、无守卫 8 → 8、
  **假阴性 0 条**，所以敢改。这一改同时解决了"'修好了'与'闸门看不见了'长得一模一样"，
  并当场炸出上面那个真洞。
- ✅ **P0-16 凭据处置与专病路径推进可被别家机构操作（实测取证，已修）；两张越权欠账清单同时清零**：
  乙院对甲院——回收甲院发的卡（200）、作废它（200）、给甲院的病例记路径节点（201）、
  替甲院的病例出组并下疗效结论（200）。
  📌 **互认是"认别家的卡"，不是"处置别家的卡"**：`credentials.py` 的读侧
  （`/lookup`、`/resolve`）按设计跨机构，那是互认；而作废之后该凭据立即不可用于核验，
  被别家作废掉等于让别人吊销了你发出去的证件。`issue_credential` 一直按 `user.org_id`
  发卡，处置这一半却谁都能做。**读侧本轮没动，并有一条用例钉住"互认没被关掉"。**
  📌 专病是第七次"同一个文件、两套口径"：`enroll` 一直 `assert_org_writable`，
  记节点与出组不校验。出组要写疗效评价（治愈/好转/死亡），那是本院随访的结论。
  📌 **`update_program` 判为豁免而不是补守卫，理由值得记**：它挂 `require_admin`，
  而 `admin` 属 `GLOBAL_ROLES` → `assert_org_writable` 恒放行。给它加守卫是一句
  **永远不会触发**的代码：闸门变绿、行为一点没变。**一句永不触发的守卫比没有守卫更糟**——
  它让闸门不再问这个端点，而问题（如果有）原封不动。已登记进 `BYID_CROSS_ORG_OK`
  并写明理由，另有一条用例钉住**豁免的前提**（角色仅限全域）：角色一放宽就红。
  回归 `tests/test_credentials_disease_org_guard.py` 七条；五处变异各自转红（含前提那条）。
  `NEWLY_VISIBLE_UNGUARDED_WRITES` 19 → 17 → 11 → 6 → 5 → **0**，
  与 `ONEHOP_UNGUARDED_WRITES` 一样转为**零基线**。
- ✅ **P0-15 归属隔一跳的最后五个写端点（实测取证，已修），一跳欠账清零**：
  乙院对甲院——把团队成员改名停用（200）、直接删掉（204）、给甲院的质控样本写"不合格"（200）、
  取消甲院号源上的预约（200）、替甲院核销到诊（200）。五个端点**都没有 `user` 形参**。
  📌 **三处理由各不相同，所以逐条判、逐条写**，没有一把守卫扫过去：
  · **团队成员** —— 同文件 `update_team`/`add_team_member` 一直校验（`assert_org_writable` 7 处），
    只有成员改/删没跟上。第六次"同一个文件、两套口径"。
  · **预约** —— 守卫写在端点里，**刻意不下沉**进 `release_appointment`：那个函数与居民端共用
    （`portal.py` 也调），居民取消自己的预约走门户令牌那套口径，不该被员工的机构规则判。
    这条决定单独有用例钉住（把守卫下沉就转红）。
  · **质控抽查** —— **不是给质控加新口径**，是把同一个功能的两半对齐：`plan_qc` 一直按
    `visible_org_ids` 收口（抽得到谁本来就只有可见范围内那些），而判结论那一半什么都不校验。
    合格率是考核依据，能被无关机构写进去，这个数就不能用了。
  回归 `tests/test_onehop_org_guard_final.py` 九条，两个方向都钉；五处变异各自转红。
  `ONEHOP_UNGUARDED_WRITES` 从 14（其中 5 条误报）→ 9 → 5 → **0，转为零基线**，
  比只减不增强一档：新出现一条就红。
- ✅ **P0-14 归属隔一跳的四个慢专病写端点可被别家机构操作（实测取证，已修）**：
  乙院 doctor 对甲院名下的对象——办结干预任务并替它填反馈（200）、扣甲院服务包的次数（201）、
  把服务包解绑（200）、取消甲院的临床路径实例（200）。四个里有三个**连 `user` 形参都没有**。
  📌 **这正是上一轮我写错的那条**：`adjust_path_instance` 曾被记成"修不了，`SpdPathInstance`
  没有机构列"。没有机构列是真的，"所以校验不了"是错的——`enrollment_id` 指向
  `spd_enrollments`，那张表有 `org_id`。**一张表没有机构列，不等于这个对象没有归属。**
  📌 又是"同一个文件、两套口径"（第五次）：三个文件里 `assert_org_writable`
  分别已用了 2 / 15 / 5 处，只有这四个端点没跟上。本轮只是把既有口径补齐，没换机制。
  归属判定一律排在状态机之前（先 403，免得用"该服务包已解绑"探别家档案的状态）；
  `enrollment_id` 可空时不拦，沿用 `assert_org_writable` 的既定语义并单独钉一条用例。
  回归 `tests/test_spd_enrollment_org_guard.py` 八条，两个方向都钉；四处变异各自转红。
  一跳欠账 9 → **5**，按 id 欠账 6 → **5**。
  📌 夹具又被业务不变式咬了一次：`spd_enrollments` 上 `(patient_id, program_code)` 唯一，
  每个用例各造一份档案就撞约束。**没去绕开约束，改成每次换一个病种码**——
  同一位患者同一病种只能在管一次，那是业务规则不是夹具的麻烦。
  剩下 5 条：appointments 两条、teams 两条、`record_qc_result` 一条
  （质控抽样按设计跨机构，大概率是写明理由的豁免，待逐条裁定）。
- ✅ **P0-13 项目 / 里程碑 / 通用资源的七个写端点可被别家机构操作（实测取证，已修）**：
  乙院 `operator` 对甲院的对象——把项目改成"已中止"、进度改 3（200）；往甲院项目里
  插一条里程碑（201）；把里程碑标完成、再撤销完成（各 200）；把甲院的超声机改名改位置、
  发布、再用自己的理由撤回（各 200）。七个端点**原先连 `user` 形参都没有**。
  📌 又一次"同一张表上两套口径"：两个文件里**建**（`create_project` / `register_resource`）
  一直 `assert_org_writable`，**改/发布/撤回**却什么都不校验。这是第四族
  （P0-10 住院文书 / P0-11 知情同意书 / P0-12 慢专病任务 / 本条）。
  📌 **里程碑那两条闸门从来看不见**：`project_milestones` 没有 `org_id`，归属要经
  `project_id` 回到 `admin_projects.org_id`。顺线量出**同形状 14 个**并钉成只减不增
  （P1-54）；判据刻意排除指向 `users` 的外键——不排除是 50 个，而 `created_by.org_id`
  说的是"谁建的"不是"归谁所有"，人一调动就全错。
  📌 **更正上一轮写错的一条**：`adjust_path_instance` 曾被我写成"修不了，`SpdPathInstance`
  没有机构列"。前半句对、后半句错——它有 `enrollment_id → SpdEnrollment.org_id`。
  **"这张表没有机构列"不等于"这个对象没有归属"**。P1-48 的前提已更正。
  📌 **自审时被自己的重构咬了一次**：第一版把取行放进 `_project_readonly` 再加守卫，
  取行退到第二跳、端点整个掉出闸门分母——**把守卫整条删掉，闸门照样报绿**。
  "修好了"与"闸门看不见了"在棘轮上长得一模一样（登记 P2-36）。改成本跳自取后变异才转红。
  回归 `tests/test_projects_resources_org_guard.py` 十一条，两个方向都钉（含
  "读侧本轮未改是有意的"一条，把范围决定显式化）；六处变异各自转红。欠账 11 → **6**。
- ✅ **立表当天就把自己的新闸门修了一次：14 条里 5 条是误报**（2026-09-11，紧接上条）。
  `spd/referral.py` 五个端点**早就有守卫**，只是不叫 `assert_org_writable`——
  `_assert_holds_case` 按"这张单现在谁拿着"（`current_org_id`）、`withdraw_referral`
  按"谁发起的"（`initiator_id`，个体级，比机构级更严）。而且它们**比通用守卫更对**：
  转诊天然跨机构，按 `enrollment.org_id` 判会把正常的逐级下转全挡掉。
  📌 **最该记的一点**：其中三条早就写在同一个文件的**上一张表**里
  （`BYID_CROSS_ORG_OK`，配 ADR-0005 的理由）。答案一直在那儿，我的新判据只是没去问它。
  一份三成是误报的欠账清单，下一个人只会学会忽略它。
  改为**登记制** `DOMAIN_ORG_GUARDS`：形状（"看着 user 抛 403"）只用来发现候选，
  算不算归属判定由人逐条读过、把理由写在值里——纯按形状认会把
  `if user.role != "admin"` 这种角色判定也当成归属判定。实数 14 → **9**。
  📌 变异又抓到两处自己的疏漏：①只认名字的白名单会**替一个已被删掉的守卫继续报绿**
  （已改成"登记过 **且** 形状今天仍然成立"）；②`_sites_raising_403_on_user` 没缓存时
  每个端点各重解析一遍全部路由文件，把这一档从 27 秒拖到分钟级——
  **闸门自己太慢，下一个人就会开始用 `-k` 跳过它**。
  另有一行变异证不出来（`- BYID_CROSS_ORG_OK` 今天是冗余的），
  没有假装它被验证过，而是给它单独写了一条直接钉不变式的用例并说明理由。
- ✅ **P0-12 慢专病任务流转可被别家机构操作（实测取证，已修）**：乙院 `doctor` 对甲院的任务
  `claim`/`urge`/`escalate`/`submit`/`complete` **全部 200**。
  📌 **最该记的是注释**：同文件 `batch_tasks` 早就修过这个形状（P1-47），它的注释写着
  「同文件的单条接口**一直是校验的**（见 `claim_task`）」——**而 `claim_task` 恰恰是没校验的那个**。
  批量版照着单条版修，可单条版本身就没有；注释这么写了之后就再没人回头核过。
  同一个对象、同一个文件、九个写端点两套口径。
  修法：`_load_task` 改成「取行 + 机构校验」一体。**特意留了两个出口并各有用例钉住**：
  `director` 是全域角色 → 县级中心跨机构督办照常；`org_id` 为 None 的任务不拦。
  回归十三条两个方向都钉，三处变异转红。欠账 17 → **11**。
  余下 `adjust_path_instance` **不是不想修是修不了**：`SpdPathInstance` 没有机构列，
  正是 P1-48 那条「归属未定义」，要先裁定。
  📌 写用例时又被自己的夹具咬了一次：`org_id=None` 被当成「没传」，
  那条用例实际建在甲院名下。**None 是有意义的取值时，就不能拿它当「没传」**——已改哨兵。
- ✅ **P0-11 知情同意书可被别家机构签掉 / 替记拒签（实测取证，已修）**：
  `POST /consents/{id}/sign` 与 `/refuse` 原先**连 `user` 形参都没有**。
  乙院医师（与该患者及该机构均无关）实测：签署 **200**、替甲院记拒签 **200**。
  ⚠️ 知情同意书是**证据性法律文书**，机构靠它证明「告知过」——被别家签掉等于伪造证据。
  📌 最刺眼的是：同一份文书上，**开具**一直 `assert_org_writable`，**签署/拒签**却什么都不校验。
  修法照抄本文件既有口径，且**归属判定排在状态机之前**（先 403，免得用 409 的措辞
  把别家单据状态探出去）。回归四条两个方向都钉，三处变异转红。
  由 P0-10 放宽闸门后现形，欠账清单 19 → **17**。
  📌 减的时候又发现自己少写了一条：写侧只查豁免清单的腐烂、没查欠账清单的——
  变异实测把已修好的条目塞回去，闸门照样绿。**一份不会腐烂的欠账清单，
  修完了也不会变短，那它就不再表示「还欠多少」了。**已补。
- ✅ **P0-10 住院临床文书跨机构越权（实测取证，已修）**：`/api/inpatient/admissions/{id}/…`
  七个端点一律**按 id 直取、不校验归属、不留痕**。乙院医生（与该患者毫无业务关系）实测：
  读体温单 **200**（体温 38.5 / 脉搏 90）、病程记录 200、护理记录 200、文书完整性 200；
  **写**病程记录 **201**——写进了别家的法定病历，写体温 201。全程无 `AccessLog`。
  正是 §8 原文禁止的那一种。**闸门一直没报**，因为取行在本模块的 `_admission_or_404` 里，
  而闸门只看端点自身函数体——「判据只认一种写法」这条线上的第三例。
  修法：helper 改成「取行 + 校验 + 留痕」一体，四个读端点补 `user`；
  回归八条**两个方向都钉**（无关机构 403 ×7、本院照常 200/201、放行留痕、被拒不留痕）。
  闸门同步跟进一层本模块 helper，分母 89 → **101**，覆盖率 95.5% → 96.0%；
  放宽后新看见的 19 个已登记为欠账（不是豁免），下一批先看
  `outpatient_docs:sign_consent`/`refuse_consent`——**连 `user` 形参都没有**，
  任一医师可为任意患者签署知情同意书。
  📌 补上校验后 `test_doctor_mobile::test_round_tab_backend_flow` 当场变红，查下来
  **不是修错了，是夹具不真实**：它把病区挂在另建的「查房演示院」下，于是那位医师
  是在给别家机构的患者查房——它当时能过，只因这族端点根本不校验归属。
  已改挂到该医师自己的机构（查房本就是在本院病区查自己的病人）。
  **不是为了让 CI 变绿改测试，是改完这条用例才真的在测查房流程。**
- ✅ 生产凭据守卫改为**强度校验**（长度 + 不同字符数 + 字符类别 + 占位符词表），部署文件弱默认值清零。原守卫只比对「是否等于代码默认值」，实测两处漏网：`docker-compose.yml` 注入的 `change-me-in-production` **不等于**代码默认的 `dev-secret-change-in-production`，而该文件里写着 `MEDPLAT_ENV: prod`——`docker compose up` 不设任何变量也能起来，起来的是一个用**仓库里人人可见的字符串签 JWT** 的生产实例；`MEDPLAT_SECRET=x` 一字符同样放行。compose 三个凭据改 `${VAR:?}` 形态（没设就报错退出），`render.yaml` 用 `generateValue: true` 让平台生成随机值。回归 `test_prod_credential_guard.py` 十八条，两处变异验证。
- ✅ `routers/portal.py:168` `debug_code` 回显收紧为显式开关（新增 `sms_debug_echo` 默认关 + 生产双重门；回归测试 `test_portal_auth.py` 两条）。
- ✅ 打印 4 端点 + `attachments` 下载/列举/上传补归属校验与 `AccessLog`。口径已定：附件按 owner_type 声明 `scope`（exam_report/spd_task=患者 · adverse_event=机构 · course_material=**全员**）；`scope` 无默认值，漏声明在装载期 TypeError 炸掉。顺带补 `exam_requests.claimed_org_id`（迁移 a4c8e2f60b19，带回填）——共享诊断中心与患者的服务关系此前在模型里没有落点，中心医师写完报告打不开自己写的那份（/review 提出并已复现）。回归 `test_print_attachment_visibility.py` 十条，两处变异验证。
- ✅ `spd/routers/referral.py` 转诊审核补机构层级校验（口径 1：按机构树 `parent_id`，见 ADR-0004）：`review` 仅本单当前机构的**直接上级**可推进，全域角色放行；回归 `test_referral_review_requires_parent_org`。**后续（另案）**：① 机构树 `parent_id` 缺陷可见 —— ✅ `GET /api/organizations/tree-health` 体检接口（列 orphans + broken_chains + `referral_ready`，`test_org_tree_health.py`）；☐ 运行期"补齐"parent_id（需真实机构关系，属数据/运维口径，不自动种子）；② ✅ `_NEXT` 已收敛为村→乡→县三级（ADR-0005，存量 station_reviewed 兼容续走）；③ ✅ 机构校验已推广到 `arrive`/`down`/`receive-followup`（`_assert_holds_case`：本单当前持有机构才能操作，全域放行；回归 `test_referral_arrive_down_receive_require_current_org`）。
- ✅ `static/pages-mgmt.js` 会计科目等 `<option>` XSS 转义（含就近同类 4 处：会计科目 code/name、监测域 domain×2、流程定义 key）。

### 📐 功能完善（规则已立，按台账逐模块推进）
- ✅ **功能完善开发规则**（2026-09-15，用户要求「为本平台的继续开发制定开发规则，完善各模块功能」）：
  `docs/功能完善开发规则.md`——模块「功能完整」的七项定义、开工前四问、跨模块排序、明确不做清单、
  本月量出来的反模式；CLAUDE.md 新增 §13 摘要。规则同时做成可检，不然就是又一条没人回头核的声明：
  - `tests/test_orphan_endpoints.py`：care 那条「每个端点有前端入口」扩到全平台，判据同源、右边界收紧
    （只调过 `/tasks/${id}/assign` 不算调过列表 `/tasks`）。实测 730 条路径里 **209 条没有任何前端调用形态**，
    扣掉 11 条按设计无界面（HL7/FHIR 9、支付回调、设备批量回传）仍有 **198 条欠账、分布在 50 个模块**，
    只有 36/86 个模块无入口为 0——三张对照表里的「已实现」有相当一部分只是「有端点」。
    显式名单只减不增、两个方向都钉（新孤儿红；接通未划掉也红），两臂变异各自转红后才提交。
  - `docs/模块完成度.md`（生成器 `scripts/dump_module_completeness.py` + 新鲜度用例）：87 个模块逐行列
    端点/路径/无入口/契约/分页/越权豁免/待裁读接口/读改写，附无入口路径明细——「完善各模块功能」从这张表挑批次。
  - 判据先自证再用：字符串拼接 URL 三端 0 处，所以没有为它放宽正则；匹配器三种写法与右边界有专门用例钉住。
- ✅ **孤儿端点 198 条逐模块清完（P1-55，2026-09-15～18）：198 → 4，无入口为 0 的模块 36/86 → 84/86。**
  48 个批次、一个模块一个提交，全程**没有动过一行后端代码**（唯一例外是 admin_mgmt 那处
  `assignment_type` 数据正确性缺陷）。**剩下的 4 条不是"还没做"，是"不该由自动批次做"**：
  auth 的 3 条 TOTP 须人工复核（§8，已写成 ADR-0023——顺带量出缺口比这三条大：
  `totp_code` 在三端前端出现 0 次，登录表单根本没有验证码框，**只接这三条会把人锁在门外**）；
  admin_mgmt 的 1 条等 P1-56 架构裁定（已写成 ADR-0024）。
  清账路上还捞出一批「有端点不等于能用」的实例：指标目录 13 项可下钻只绑了 9 项、
  tcm_heritage 页那句"作答在医师端 H5 完成"是一份没兑现的计划、约 8 处陈旧夹具
  （主调是**后端根本不会产生的值**）、两处渲染出字面星号的中文文案。
  清账时的建议顺序（按规则 §3：一线操作先于报表、写入口先于读入口），实际即按此执行：
  `spd/tasks`（9，任务分配/升级/提交/复核/批量）→ `spd/population`（13，目标池认领/召回/服务申请）
  → `portal` + `spd/portal`（18，居民端绑定手机/微信、知情同意、纠错、押金、随访/复诊/宣教）
  → `printing`（8，八种打印件模板都在只差按钮）→ `spd/config`（29，配置对象的改/删，多数与 P2-2 的 `prompt()` 编辑器是同一件事）
  → 其余 44 个模块。每模块一个提交：界面 + 从 `KNOWN_ORPHANS` 划掉 + `render_diff` 夹具 + 重生成台账。
  ✅ 用户裁定（2026-09-15）：按此顺序、一个模块一个提交往下清。
  - ✅ **第一批 `spd/tasks`：9 条全部接通，欠账 198 → 188**（2026-09-15）。「标准路径与任务中心」页新增：
    路径实例的**明细**（节点清单 + 各节点任务状态，并按模板详情对出 node_id 挂上**进入条件校验**）与
    **调整**（暂停/恢复/取消、改负责人，PATCH）；任务行按状态给操作——待接收可接收/分派，办理中可
    **提交**（提交审核或存草稿）/**转派**/**上传佐证**（附件 owner_type=spd_task，草稿提交把 id 写进 evidence，
    后端提交时再核）/催办/**升级**，待审核只剩**审核**（通过即完成并推进路径，退回回到办理中），已结束只剩**详情**
    （表单、办理结果、佐证走鉴权下载、审核意见、来源）；勾选列 + **批量**表单（接收/催办/升级/分配/取消，跳过的连原因一起显示，
    不走整页重画）；**导出 CSV**（前端拼 CSV：BOM、引号翻倍、`=+-@` 开头的文本防公式注入）。
    顺带接通 `spd/config` 的 `GET /path-templates/{id}`（明细页对 node_id 用到它），该模块 29 → 28。
    取证：`render_diff --dump spdpath` 五条任务的按钮分支逐行核对（pending/doing+佐证/done/overdue+已升级/submitted）、
    夹具里 3 处 XSS 载荷全部转义、差异恰好从新增的 `#spd-inst-detail` 容器起始；301 条前端守卫与闸门用例全绿。
    医生移动端「办结」的 `prompt()` 换成卡片内文本域（P2-38：134 → 133）。
  - ✅ **第二批 `spd/population`：13 条全部接通，欠账 188 → 174**（2026-09-15）。落在两页：
    「全程管理中心端」目标池行加**认领 / 改状态 / 画像**，新增**居民服务申请**（受理即进目标池，拒绝须写原因）与
    **召回跟进**（逐次留痕，登记为已召回时档案自动恢复在管）两个面板；「筛查建档与纳管」档案行加**明细**
    （服务包绑定：绑定 / 记用量 / 用量明细 / 解绑，路径实例）、**调整**（阶段 / 风险 / 团队 / 医生 / 个案管理师 / 下次随访，PATCH）、
    **画像**（专病 360：纳管、路径、服务包、任务、监测、评估、转诊一屏），分组行加**成员**（列表 / 批量加入或按规则吸入 / 移出）。
    绑定服务包时按需取 `GET /api/spd/service-packages` 填下拉，顺带接通 `spd/config` 这条（28 → 27）。
    取证：两页 `render_diff --dump` 逐行核对按钮分支（已纳管候选无认领、已拒绝申请显示原因、已召回无按钮、非在管档案无调整），
    夹具 14 处 XSS 载荷全部转义；`MIGRATED_PAGES` 的 `panel()` 计数 5→7、4→5（新增面板，非手写外壳）；301 条守卫全绿。
  - ✅ **第三批（上）`portal` 居民端：9 条清零，欠账 174 → 165**（2026-09-15）。6 条接进 `/m`：「我的档案」账户栏加
    **补绑手机号**（purpose=bind 验证码）与**绑定微信**（mock 通道就地完成；真实微信留回跳标记，回跳带 code 走 bind-wechat 而不是登录）；
    档案页新增**疾病管理档案·慢病 + 慢专病**（ADR-0003 聚合 `/me/enrollments/all`，每条标来源）、**知情同意**（记录 + 本人自签，
    未成年人须填监护人）、**信息更正 / 注销申请**（个保法更正权/删除权，ADR-0010；可改字段只有姓名/性别/出生日期/电话，身份证号提示走窗口）；
    「在线服务·住院」加**押金余额**（仅本人）。3 条**按设计不接**并登记豁免：`my-archive`（GET/POST）与 `surveys` 是已标 deprecated 的
    无账户过渡通道（身份证号入 query），`me/referrals` 已被 `/me/referrals/all` 取代——顺手给它标上 `deprecated=True`（只改 OpenAPI，不改行为）。
    家庭成员验证码那处 `prompt()` 换成表单输入（P2-38：133 → 132）。居民端没有渲染夹具，取证靠三端静态守卫 + 手工核读。
  - ✅ **第三批（下）`spd/portal` 居民端慢专病页：9 条清零，欠账 165 → 156**（2026-09-15）。新增五个分段——
    **复诊**（计划/科室/项目/状态）、**宣教**（内容/资料链接/标记已读）、**评估**（量表/得分/风险/建议）、
    **历程**（每个病种的路径进度、任务、转诊）、**档案**（基本资料、各病种习惯/危险因素/并发症、就诊时间线）；
    任务卡片加**上传凭证**（multipart 单独 fetch，鉴权与 authApi 同口径）。两条按设计不接：`/referrals` 已被
    `/me/referrals/all?source=spd` 取代（标 deprecated）；`/referrals/{case_id}` **是活的**——聚合列表的 `detail_path`
    指向它、前端按该字段请求，闸门按字面匹配看不见变量地址，登记为豁免的第三类并写明字段来源与调用点。
    居民端三处 `prompt()`（任务填报/随访自答/干预反馈）换成卡片内 `inlineInput()`，`m/m.js` 弹窗归零（P2-38：132 → 129）。
  - ✅ **第四批 `printing`：8 种打印件全部有按钮，欠账 156 → 148**（2026-09-15）。模板与防伪验真早就在（ADR-0015），只差入口：
    住院记录行加**费用清单 / 病案首页 / 出院小结**（出院小结只给已出院的摆——后端未出院 409）、结算单行加**打印结算单**、
    转诊行加**打印转诊单**、体检记录行加**打印报告**、知情同意台账加**打印**、接种史加**打印接种证明**。全部走既有的
    `openPrintPage()`（带令牌取回再唤起打印），后端每种打印件都按患者可见性再判一次。`render_diff --dump` 四页核到按钮，0 处裸载荷。
  - ✅ **第五批 `spd/config`：27 条全部接通，欠账 148 → 121**（2026-09-15）。全是配置对象的改/删/明细/发布类端点，
    后端早就有、界面一个入口都没有（与 P2-2「规则编辑器实际没做」是同一件事），按页落、不新建页面：
    「运行中枢」病种表改从 `/programs` 取（带 id），加**编辑 / 版本历史 / 管理目标**（目标可增可改）；新增**评估量表**
    （发布 / 停用 / 二维码）、**服务包**（新建含 `编码:名称:次数;…` 项目解析、编辑）、**宣教素材**（编辑）、**标签字典**（新建）、
    **设备台账**（登记、绑定 / 解绑——`patient_id` 留空即解绑）、**机构树**（递归渲染，带团队数与在管数）六个面板；
    数据源表加**编辑 / 同步日志 / 记一次同步**并接监控总览。「基层执行」新增**团队维护**（新建、编辑、成员面板：加入 /
    改角色与权限位 / 移出）与**村医档案**（建档、按行批量导入、编辑、绑定二维码）。「临床指导中枢」中心行加**编辑**。
    「路径与任务」模板行加**节点**面板（已发布只读——后端 409，草稿可改可删）。
    鉴权 SVG（量表 / 村医二维码）不能裸 `window.open`：在点击手势里先开窗、再带令牌取回 blob 写入，被拦截时明说
    （与 `openPrintPage` 同一口径）。编辑一律走 `spdModal()`，本批新增 0 处 `prompt()`。
    取证：`render_diff --dump` 四页核到按钮与面板，夹具里的 XSS 载荷全部转义；夹具行补齐后端本就返回的 `id`——
    漏了会渲染出 `undefined`，比对器当场抓到；请求体与后端 PATCH 的 allowed 键、批量导入的返回形状逐一对过。
  - ✅ **第六批 `spd/assess`：10 条全部接通，欠账 121 → 111**（2026-09-15）。积分体系后端是完整的（规则 / 账户 / 流水 /
    商品 / 兑换 / 核销），可村医既拿不到积分（没有签到入口）也花不掉（没有兑换入口）。八条落在「考核与积分」页：指标行加
    **编辑**（目标值走文本框——0 是合法目标，`Number("")` 不能把留空变成 0）与**使用情况**（被哪些方案引用、各占多少权重）；
    方案行加**编辑**（指标串解析与新建共用一份）与**得分分析**（分数段分布、高频扣分项、排名，无结果提示先跑分）；新增
    **工作量统计**（按人员 / 机构 + 周期 + 机构 / 病种筛选，首屏按默认周期先出一张）、**村医积分规则**（新建 / 编辑）与
    **兑换记录**（只看状态，核销码不回显——它是村医手里的凭证，而列表对任何登录用户可见）三个面板；商品行加**编辑**（含下架）。
    两条落在医生移动端「积分绩效」：**每日签到**（重复签到后端 409 照实报）与商品卡片**兑换**（积分够且有库存才摆按钮，
    成功后核销码直接显示在提示里），另列**我的兑换**。取证：`render_diff --dump spdassess` 核到八个面板与全部按钮，夹具 XSS
    载荷全部转义、两个核销码都没出现在管理端 HTML 里；医生端没有渲染夹具，模板手工核读（`kv()` 只转义键，值一律 `esc()`）。
  - ✅ **第七批 `spd/followup`：9 条全部接通，欠账 111 → 102**（2026-09-15）。八条落在「智能随访」页：方案行与问卷行加
    **编辑**（方案可改时间点 / 问卷 / 执行角色 / 启停，问卷只改名称 / 跟踪科室 / 处置角色 / 启停——题目与异常规则走新建）；
    随访看板每行加**前置资料**（基本资料 + 近期就诊 + 住院 + 历史随访 + 本次问卷一屏，随访前先看）与**调整**（改期 / 改执行人 /
    改渠道 / 移除或恢复，留空不改，已完成的后端 409）；新增**患者健康日历**面板（某天的随访 / 复诊 / 任务）；随访质量面板
    下加**抽查样本表**（批次 / 被抽随访 / 结论，未判定的可**判定**：合格 / 提醒 / 不合格 + 核查方式）；呼叫任务待呼叫的行加
    **回写结果**（接通 / 未接通 / 取消 + 时长 + 录音地址 + 沟通结果，接通结果后端同步写回随访记录）。一条落在「报告推送」页：
    模板行加**编辑**（名称 / 周期 / 层级 / 启停）。顺手修一处就近的老问题：呼叫任务 `cancelled` 一直显示成「待呼叫」。
    取证：`render_diff --dump spdfollowup / spdreport` 核到面板与按钮，夹具补问卷 `id`、随访记录 `patient_id`
    （旧夹具里「转呼叫」按钮的 `data-pid` 一直渲染成 `undefined`，这次一并补上）与三条抽查样本（含 `record` 为 null 的防御分支）。
  - ✅ **第八批 `users`：7 条全部接通，欠账 102 → 95**（2026-09-15）。等保 E1 要求的运维动作后端都做了、界面一个都没有：
    账号停不掉（离职就只能改密码）、口令重置不了（忘密只能进库改）、动态口令换手机就锁死、登录留痕（含失败尝试）查不到、
    审计哈希链没人校验、审计归档导不出。账号页加**停用 / 启用**（停用二次确认，既有登录立即失效）、**重置口令**
    （临时口令只能用一次，本人首次登录须改密）、**重置动态口令**（二次确认），角色字典改从 `/users/roles` 取——
    自定义角色此前在表格与下拉里都只显示原始 key；审计页加**校验哈希链**（结论与后端自述的能力边界一起显示，
    不把"审计不可篡改"说过头）、**导出归档**（NDJSON，走既有 `downloadCsv`）与**登录留痕**面板（登录名 / IP / 通道 /
    成功失败 / 失败原因，可按三项筛）。后端鉴权一个字没动。顺手把账号页「调角色」的 `prompt()` 输序号换成下拉
    （P2-38：129 → 128），`spdModal` 补 password 输入类型。
  - ✅ **第九批 `medwaste`：6 条全部接通，欠账 95 → 89**（2026-09-16）。医废是有法定追溯要求的链条，
    而链上三个环节没有入口：点位（产生点 / 暂存间）建不了也停不了、`stored` 这个状态没有接口能走到
    （收集之后只能直接交接，暂存整段是空的）、追溯码印在包上却没地方扫。新增**点位台账**面板
    （新建 / 停用 / 启用——停用不删行，历史医废的来源必须永远查得到）、**扫码追溯**查询框（时间线：收集 → 暂存 → 交接）、
    **转运人员工作量**面板（未挂员工档案的交接单列，不硬凑到某个人头上）；清单加**追溯码**列，`collected` 的行加
    **入暂存**（暂存间下拉只列本机构在用的，跨机构后端 422），交接改走 spdModal 收员工 ID 或姓名（P2-38：128 → 127）。
    取证：`render_diff --dump medwaste` 三条废物的操作列按状态正确分支（已收集 = 入暂存 + 交接 / 已暂存 = 交接 / 已交接 = —），
    夹具 4 处载荷全部转义。**顺带记一个自己踩的坑**：`reactivate` 起初写成 `${id}${on ? "/reactivate" : ""}` 拼后缀，
    孤儿闸门按字面匹配当场看不见这条路径（6 条只销了 5 条）——闸门的判据是"全仓库字符串拼接 URL 为 0 处"才收紧的，
    改成两条显式调用，别从这里开口子。
  - ✅ **第十批 `billing`：5 条全部接通，欠账 89 → 84**（2026-09-16）。住院押金整套后端都在（预交 / 退费 /
    余额 / 催缴预警），界面上一处入口都没有——押金只能在居民端看，经办收不了也退不了，「押金不够抵未结费用」
    的预警更是从来没人看过。新增**住院押金**面板：预交（仅在院，后端 409）、退费（不得超余额，后端原子判定）、
    按住院单查余额与流水（余额 = 预交 − 退费 − 结算冲抵，现算）、**催缴预警**（缺口 = 余额 − 未结费用，
    可调阈值提前预警，按缺口升序——最缺钱的排最前）。收费项目行加**维护**（名称 / 类别 / 启停）：
    改价仍走「调价」，因为价格要对外公示、依据与生效日期必须留痕。
    顺带（童子军）把本页三处弹窗录入换成 `spdModal`：调价的三连问、支付退款的金额（P2-38：127 → 123）。
    **另修一处夹具缺陷**：对账差异那一行的键名四个全错（`type`/`payment_id`/`channel_amount`/`note`，
    而后端是 `diff_type`/`order_id`/`remote_amount`/`detail`），那张表一直渲染出空标签与 `undefined`——
    也就是说它从来没被比对器真正覆盖过。改成后端真实形状，并补一行「通道有本地无」（`order_id` 为 null）
    与未知 `diff_type` 走回落的分支。
  - ✅ **第十一批 `education`：5 条全部接通，欠账 84 → 79**（2026-09-16）。课程列表行加**培训统计**
    （参训人次 / 合格 / 合格率）；已结束的直播行加**上传回放**（后端只允许已结束的挂回放——排期中的挂上去，
    学员点进去是空的）、**评价**（一人一场一条，再评即覆盖）与**看评价**（条数、均分、逐条）；
    直播表新增「回放」列，链接经 `esc()` 且 `rel=noopener`。新增**健康宣教文章**面板（新建 + 发布，
    发布后进居民端）。顺带（童子军）把本页三处弹窗录入换成 `spdModal`（P2-38：123 → 120）。
    **登记一处后端欠账**：`HealthArticle` 只有 POST 新建与 POST 发布，管理端**没有列表接口**
    （居民端 `/api/portal/health-articles` 只读已发布的），所以发布只能按 ID 走——界面把这句话明说，
    不假装有列表；补列表接口是另一个批次的事，不在本批夹带。
  - ✅ **第十二批 `appointments`：4 条全部接通，欠账 79 → 75**（2026-09-16）。新增**便捷寻医**（指引⑨：
    按姓名 / 科室 / 职称找医师并带出近期可约号源；没号的医师也在列并标注「暂无号」——只给有号的，
    居民会以为这位医师不存在）、**服务黑名单**（⑫ 爽约 / 缺药不取两个业务域，增与删；
    域中文名用服务端给的 `domain_name`，不在前端维护第二份映射）两个面板，
    发布号源面板加**批量排班**（模板 × 日期区间，可跳过指定日期与周末；幂等——已有号源的日期跳过，
    所以**跳过数要报出来**，否则「补生成生效了」和「什么都没生成」看起来一模一样）。
    移出黑名单时 `domain` 必须带上：后端按 (domain, patient_id) 定位，不带会误伤另一个业务域那条。
  - ✅ **第十三批 `exams`：4 条全部接通，欠账 75 → 71**（2026-09-16）。新增**报告模板**（管理员维护，
    出报告时照着写）与**报告修订与修订史**（按报告 ID 找任意一份；危急值预警表的行上直接给「修订」「修订史」）
    两个面板；申请单表新增「样本」列，检验类未出报告的行按流程给一个按钮：登记采样 → 发起转运 → 中心核收
    （非检验类后端 422、已核收 409，所以按状态摆而不是摆了等报错）。修订会把改前的结论 / 所见 / 危急标记
    连同修订人与理由写进历史表，仍为危急值的会把闭环状态复位——回执里这两件事都报给人看。
    顺带（童子军）把「出报告」的 `prompt` + `confirm` 两问合成一个 `spdModal`（P2-38：120 → 119），
    并把后端一直收、界面一直没给的**所见**字段补上——原先只能填结论，`finding` 永远是空的。
  - ✅ **第十四批 `quality`：4 条全部接通，欠账 71 → 67**（2026-09-16）。新增**环节质控规则台账**
    （规则是评分的唯一依据，管理员可改扣分与启停；界面写明停用不回溯重算、要新结论走「复评」）；
    病历行加**详情**（落库时的缺陷快照 + 六个环节的正文，与「复评」的现算结果明确区分）；
    院感面板标题接上**院感统计**（已确认 / 待核实例数 + 按部位分布）。
    顺带（童子军）把不良事件审核意见与整改措施两处弹窗换成 `spdModal`（P2-38：119 → 117）。
    **闸门第二次抓到同一个形状**：改写审核/整改那段时图省事把动作名拼进了地址
    （`` `/adverse-events/${id}/${rectify ? "rectify" : "review"}` ``），孤儿闸门当场把这两条
    **本来已接通**的端点判回孤儿——它按字面匹配，拼出来的地址看不见。这次是回归方向命中的
    （欠账不降反升），比第一次（medwaste 只销了 5 条）更明确。两处都改成显式调用并写明理由。
  - ✅ **第十五批 `spd/referral`：4 条全部接通，欠账 67 → 63**（2026-09-16）。转诊行加**全轨迹**
    （每一步的环节 / 动作 / 经办人 / 机构 / 意见 / 时间，患者端 #16 要的「触发依据与处理意见」就是这条）
    与**撤回**（只在 `submitted` / `station_reviewed` 两个状态摆——后端只允许发起人本人在进上级审核前撤，
    且撤回单不计入闭环率分母，确认框里把这句说出来）；规则行加**编辑**；新增**按规则试算**面板
    （拿某位患者的当前事实过一遍启用中的规则，看命中哪几条、哪个条件命中的；
    **默认不开单**——命中即自动开单，一次批量随访录入能开出几十张单子）。
    **又修一处陈旧夹具**：闭环统计夹具还是旧形状，缺 `denominator`/`effective_visits`/`effective_rate`，
    并把 `pending_by_level` 写成 `open_by_level`——于是那排卡片一直显示 0 与 `undefined%`、在途分级图恒空。
    转诊单的 `status` 写的是 `pending`，而这条状态机根本没有这个取值。两处都照后端契约改正。
  - 🔧 **修 CI e2e 红（run 581）并把这类盲区收口**（2026-09-16）。第十三批把「出报告」的
    `prompt` + `confirm` 换成 `spdModal` 之后，端到端档那条用例红了——它用 `page.on("dialog")`
    应答原生对话框，模态框既不触发 dialog 事件、全屏遮罩又拦住后续点击，
    于是**失败现场是「点不到左侧导航」而不是「这个按钮没反应」**。
    修法：端到端档新增 `_spd_modal()` 驱动模态框（按字段名填值 → 确定 → **等遮罩消失**），
    出报告那步改用它并顺带填上新接通的「所见」字段；`_answers` 留给仍在用原生对话框的
    排班 / 术中记录 / 处置反馈 / 随访办结四处，两者互斥、文件头注明。
    **盲区本身**：`make verify` 用 `-m "not e2e"` 排除端到端档，所以这类改动本地一条都不会红，
    要等推上去半小时后 CI 才说话。新增 `make test-e2e`（镜像预装的 chromium 与 pip 装的 playwright
    版本目录对不上，目标里直接指内核路径，不必再下一份），CLAUDE.md §7 写明
    「改了交互形态必须跑」。取证：修前在本地复现出与 CI 一模一样的超时，修后 11 条全绿。
  - ✅ **第十六批 `admin_mgmt`：3 条里接通 2 条，欠账 63 → 61；第 3 条判为重复实现，登记 P1-56 待裁**（2026-09-16）。
    人财物页物资行加**调拨**（弹框选调入机构，不把现属机构列进去——后端不拦"调给自己"，摆出来就是一次白点）
    与**报废**（确认框写明后端没有反向端点）；机构列随之由裸 ID 改印机构名（不然调完只看到数字变了），
    状态列的 `in_use` 也不再直接把英文印给人看。
    **第 3 条 `/api/mgmt/secondments/{id}/end` 没接也没豁免**：它与 `/api/staffing/secondments/{id}/end`
    写同一张 `secondments` 表，而后者更全（不送日期默认今天、校验结束日不早于开始日、返回 18 键台账行）
    且早有界面（人员下沉调度页的「结束派驻」）。再接一个入口等于让同一个动作有两条校验不同的路；
    豁免的含义是"按设计不需要界面"，这条也不是。怎么收口是架构裁定，已写成 P1-56 进待裁定清单
    （四个选项各自代价都列了，其中"并到 staffing"卡在孤儿闸门豁免名单的 7 条上限上——那是个前置决定）。
  - 🔧 **顺带修掉这两套之间最贵的一处差异：人财物页派出去的巡诊被算进国家监测指标**（2026-09-16）。
    `POST /api/mgmt/secondments` 不收 `assignment_type`，于是该端点建出来的派驻**全部**按列默认
    落成 `long_term`，而 `/api/staffing/dispatch-stats` 的「长期派驻满半年」只统计 `long_term`——
    从人财物页派出去的巡诊与短期支援，一律被算成长期下沉。补上同一套取值域与默认值
    （默认仍是 `long_term`，老调用方行为不变），表单加派驻类型下拉；
    回归用例**用 staffing 的台账读回来**验证（读写两头看到同一个值才算真接上），
    并做了变异验证：去掉那一行赋值，断言当场红成 `('long_term', '长期派驻') != ('rounds', '巡诊')`。
  - ✅ **第十七批 `pharmacy`：3 条全部接通，欠账 61 → 58**（2026-09-16）。中心药房页新增两个面板：
    **批次台账**（总量/已用、可用、不可发余量、状态与召回原因）——正常批次给「召回」，
    所有批次给「发给了谁」；**采购建议**（近 30 天处方用量 − 当前全网库存，只列差值为正的品种）。
    召回这条尤其要紧：召回端点自己的 docstring 把反查称作「召回时唯一有用的那个查询」，
    而这两条此前**一条都没入口**——发现批号有问题，界面上既召不了回、也查不出发给了谁。
    召回回执报出「退出可用汇总多少、不可发余量多少」而不是一句"操作成功"：
    召回最要紧的后果是账面上少了多少，不是状态翻了。
    ⚠️ 反查那条（`batch_dispense_trace`）是 **P1-49 登记在册的 11 个无机构收口端点之一**，
    载荷含患者姓名。界面只从已收口的 `list_batches` 取批次，所以页面本身不越界；
    但它此前"反正没人调"的那层事实缓解没有了，已在 P1-49 条目里就地记明。
    **没有顺手给它加守卫**——那属于越权改动，按 CLAUDE.md §8 要走 ADR 并请人复核。
    **顺手改正近效期夹具**（本轮第四处同类）：后端 `ExpiringBatchOut` 继承 `BatchOut` 共 15 键，
    夹具只写了 8 键。那张表恰好没印缺的那几列所以一直没暴露，可新面板要用同一批字段，
    照旧形状造会连「哪条能召回」都判错。
  - ✅ **第十八批 `prescriptions`：3 条全部接通，欠账 58 → 55**（2026-09-16）。合理用药页的规则库
    此前是**只读**的：录错一条只能靠同 `drug_code` 覆盖，停不掉也删不掉（后端早有停用/启用，
    `deactivate_rule` 的 docstring 还专门写了"通用规则引擎一直是有停用的"）。本批接上
    **停用 / 启用 / 批量导入**三条，并把取规则改成 `include_inactive=true`——
    不带这个参数，停用的规则**整行看不见**，「这条规则怎么不生效了」在界面上无从查起，
    重新启用更无从谈起。新增**状态**列与**抗菌/DDD**列（DDD 留 0 是"未维护"，
    使用强度统计会把它计入未覆盖，所以单独标橙而不是印 0）。
    停用的确认框写明后果：**停用期间该药按「规则未维护」处理**——不是按上限 0 拦截，
    是根本不参与审方（后端 `_active_rule` 把停用与不存在同路处理）。
    批量导入用文本域收一个 JSON 数组、前端先 `JSON.parse` 报错再提交，
    回执报出「新建几条、覆盖更新几条」；说明里写明**同 drug_code 是整条覆盖不是合并字段**。
    三个写入口都只对 admin 摆（后端 `require_admin`），不是 admin 摆出来只会点出 403。
    **顺手改正规则夹具**（本轮第五处同类）：后端 `DrugRuleOut` 13 键，夹具只写了 7 键，
    缺 `id`/`active`/`antibiotic`/`ddd` 等——停用那一支与 DDD 那一列都要靠这些字段才渲染得出来。
  - ✅ **第十九批 `consultations`：3 条全部接通，欠账 55 → 52**（2026-09-16）。远程会诊页新增
    **运行统计**（申请量 / 完成率 / 评分均值含已评未评 / 已计费金额含已计未计，附后端给的口径说明）
    与**会诊专家库**（建档限 admin，列出专业方向与排班状态）；已完成的会诊行加**计费**。
    专家库那条最值得说：受理会诊此前是 `prompt("受理专家姓名")` **手打**，
    打进来的名字对不上任何一条专家记录——于是"谁接得多""某专家评分如何"永远是笔糊涂账。
    现在受理从**可排班**专家里选；专家库为空时才退回手工输入，不至于卡住受理。
    计费的弹框写明「0 与未计费是两回事」（后端用 `fee_settled` 区分而不是拿 0 当哨兵），
    面板里也写明**行上看不到这单计没计费**——会诊列表的出参不含 fee 字段，
    只能看统计里的已计/未计件数。这是如实说出来的局限，不是把它藏起来。
    童子军：本页三处弹窗换成 `spdModal`（P2-38：117 → 114，口径是含 `prompt(` 的行数）；
    **顺手改正 `rating` 夹具**（本轮第六处同类）：后端 `ConsultationOut.rating` 是 int、空值为 0，
    夹具五条全写成 null——渲染上看不出差别（都走「—」），但形状是错的。
  - ✅ **第二十批 `credentials`：3 条全部接通，欠账 52 → 49**（2026-09-16）。就诊凭据页新增
    **一码通（动态码）**出码与核验、**多卡（码）协同**认人两个面板。
    一码通是指引⑧点名的能力，后端连签名子密钥与轮换宽限期都做了，
    界面上却**一次都没出过码**；出码结果印在可整段选中的 `<code>` 里（要能复制给人），
    面板说明写明它**不落库**、过期即失效、换平台密钥即全部作废，
    以及核验时**过期与签名错误分开报**（前者重新出码，后者是伪造，处置完全不同）。
    多卡协同把「窗口人员手上可能是实体卡号 / 健康卡号 / 身份证号」这件事收进一个入口，
    回执注明命中的是哪一类，并在说明里写清它与旁边「凭据核验」的分工
    （那条只认凭据号、回这张卡的台账行；这条认三类标识、回的是人）。
    过程中避掉一个会当场抛异常的写法：初版用了 `kv()` 排版卡片，
    而 `kv` **不是管理端的全局**——它只存在于 `pages-spd.js` 某个函数内部与两个 H5 文件里，
    `index.html` 根本不加载后者。改成照搬同一页 `#cred-result` 的卡片写法。
  - ✅ **第二十一批 `checkups`：2 条全部接通，欠账 49 → 47**（2026-09-16）。体检记录行加
    **分项结果**（项目编码 / 项目 / 结果 / 单位 / 参考范围 / 正常异常判定）与**总检**
    （结论 + 总检医师，留空则署当前登录医师；限医师，公卫岗只录入）。
    总检提交后**不走 `route()`**：体检列表的出参里没有总检字段，整页重画什么都不会变，
    回执连同分项一起摆进详情容器，人才看得见自己刚写的结论落成了什么。
    写面板说明时抓到自己一处不实：初稿写「点分项结果会把当前总检结论一并取回来」，
    但 `/items` 只返回分项、后端也**没有单条详情端点**，那句话是假的。
    查了打印端点确认 `final_conclusion` 与总检医师**印在打印件里**，
    于是改成如实写：结论写完在此处回显一次，之后查结论走同一行的「打印报告」。
  - ⏸ **`auth` 的 3 条（TOTP 开通 / 验证一次启用 / 关闭）按 CLAUDE.md §8 暂缓**（2026-09-16）。
    后端已经完整且设计稳妥（setup 落 pending 密钥、activate 验一次才启用、
    disable **必须验当前验证码**——拿到会话令牌不等于拿到手机），缺的只是账号页上的入口。
    但 §8 写着「涉及认证、越权、加密、审计链的改动，走 §9 的 ADR 并请人复核」，
    而接这个入口意味着把 TOTP 密钥与 otpauth URI 印到页面上——**这一步该由人确认，
    不该由一次自动批次顺手做掉**。理由已写在 `KNOWN_ORPHANS` 名单旁边（清到这里请先找人复核），
    不是漏做，也没有假装它不存在。
  - ✅ **第二十二批 `cssd`：2 条全部接通，欠账 47 → 45**（2026-09-16）。消毒供应页此前只有
    中心侧的批次流转（灭菌中 → 已灭菌 → 已发放 → 已回收），**基层申领这一头整条链路没有入口**：
    申领提不了、中心也响应不了。新增「基层物品申领与中心响应」面板——申领表单 + 申领台账，
    `requested` 的行给「以批次响应」，弹框里**只列已完成灭菌的批次**
    （后端对灭菌中的批次直接 409，摆出来就是让人点一次看一句错），说明行还报出当前有几个可用。
    申领机构列印机构名而不是裸 ID（夹具里特意放了一条不在名单里的，走回落）。
    童子军：批次「发放」那处的 `prompt("接收机构ID")` 换成机构下拉——
    这一步是"发给谁"，选机构比默写数字靠谱（P2-38：114 → 113）。
  - ✅ **第二十三批 `infectious`：2 条全部接通，欠账 45 → 43**（2026-09-16）。这两条是
    **法定上报的出口**：平台不直连国家传染病网络直报系统，报卡要靠导出后手工网报
    （或交给县疾控前置机），而导出与单张报卡此前一个入口都没有。
    病例行加**报告卡**（机构 / 病种 / 分类 / 发病日 / 报告时刻 / 法定时限 / 及时性），
    新增**批量导出 CSV** 面板（可按病种筛、可只导迟报清单）。两者都限管理层（后端 `require_roles("director")`）。
    及时性按**三态**渲染而不是两态：后端 `late` 是 `bool | None`，
    目录外病种或发病日期非法时给 null——**那不是"及时"**，单独印「无法定时限」。
    报告卡说明里照抄后端的口径：这是平台留存的法定字段集，病例登记本身不含患者个体标识，
    卡片不虚构未存储的字段；导出说明里写明它不是网络直报、单次上限 2000 条。
  - ✅ **第二十四批 `certs`：2 条全部接通，欠账 43 → 41**（2026-09-16）。与上一批同一形状的另一半：
    人口死亡登记也是"导出后手工网报"，而**死因报告卡与批量导出同样一个入口都没有**。
    死亡医学证明的行加**死因报告卡**（证明编号 / 姓名 / 性别 / 身份证号 / 电话 / 出生 / 死亡 /
    死因 / 签发机构 / 签发人 / 签发时间），新增**按死亡日期筛的批量导出 CSV**；
    两者都限管理层（后端 `require_roles("director")`——与体检总检的 `doctor` **不是同一把钥匙**，
    所以页面上是两个独立的角色判据，没有合并成一个）。
    身份证号与电话**由后端按角色脱敏**（非 admin 掩码），前端只转义、**不再叠一层掩码**——
    叠了之后看的人会以为那是两段不同的号。面板说明照抄后端口径：不直连人口死亡信息登记
    管理系统、每张卡的档案调阅都落 AccessLog、单次上限 2000 条。
    夹具角色从 doctor 改成 **admin**（admin 同时满足总检与死因报告卡两条判据，一次把两支都渲染到），
    并补一条 `cert_type=death` 的证明——原来两条是 birth 与一个目录外类型，
    「死因报告卡」那一支**根本渲染不到**。
  - ✅ **第二十五批 `emergency`：1 条接通，欠账 41 → 40**（2026-09-16）。判定抢救转归
    是**抢救成功率的唯一数据来源**（后端 docstring 的原话），而界面上判不了。
    急救事件表新增**抢救转归**列与**判定转归**按钮（限医师，后端 `require_roles("doctor")`）。
    两处按后端口径照做：**只对已到院/已收治的病例摆**（车还在路上就写"抢救成功"，
    这个指标就没有可信度，后端对未到院的直接 409）；转归按**三态**渲染——
    `success` / `failed` / 空串**未判定**，而空串与 `failed` 是两回事，
    误把"还没下结论"填成"抢救无效"会把抢救成功率算低，弹框与说明行都写了这句。
    童子军：「回传体征」的 `prompt(心率)` + `prompt(备注)` 两连问合成一个 `spdModal`
    （P2-38：113 → 111），心率留空即"未测"而不是 0。
    **夹具第七处同类**：后端 `CaseOut.rescue_outcome` 一直有（默认空串），夹具四条一条都没写；
    补齐并加到五条，把未到院 / 已到院未判定 / success / failed 四种渲染全走到。
  - ✅ **第二十六批 `drgs`：2 条全部接通，欠账 40 → 38**（2026-09-16）。DRG 页此前只有**事后**
    统计（机构 CMI / MDC 汇总 / 组均费用）——事前提示与事中预警两条都没入口，
    于是这套东西只能用来算账，**管不了还在院的病人**。
    新增**事中预警**面板（住院日超同组均值的在院病例，倍数可调 1.0～5.0）：
    把后端刻意分开的三类原样分开呈现——预警行、**基线不足未预警**（单列报出，
    "3 个病例算出来的均值预警的是噪声"，但不提的话看的人会以为这些病例没问题）、
    以及**未入组的在院病例数**（没有 DRG 就没有同组均值可比）。
    新增**事前提示**面板：按拟诊断/拟手术预判，给**候选组而不是结论**，
    未命中时明说"不落兜底组"并解释为什么（兜底组是出院入组时保证每个病例都有归属用的，
    事前拿它当预测结果毫无信息量）。两处的 `caliber` 都原样印在页面上。
    童子军：「调权」的 `prompt` 换成 `spdModal`（P2-38：111 → 110）；
    这一页本来没有 `await draw()` 形态，本批加了之后**顺手登记进原生提交守卫**
    （`test_native_submit_guard.py`，只进不退）。
  - ✅ **第二十七批 `medication`：2 条全部接通，欠账 38 → 36**（2026-09-16）。缺药登记此前
    **只能往前流转、结不了案**——「登记 → 采购 → 配送」走完就停在那里，
    "取没取到药"这件事永远没有结论，于是履约率的分子分母都是空的。
    缺药行加**结案**：结论按状态给选项——**已取药 / 未取药只能在「已配送」之后判定**
    （药还没到就说"未取药"是冤枉人，后端对未配送的直接 409），
    **已取消**任何阶段都可以（患者转院、药源已解决）；未配送的行弹框里就只有"已取消"一项。
    新增**缺药登记统计**面板（在途 / 已取药 / 未取药 / 履约率 + 按状态件数 + 后端口径），
    履约率分母只含已判定的，**在途与已取消不计**——"药还没到就算进分母，
    等于拿采购周期长短去背履约率的锅"。
    同批**把状态字典补齐三个结案取值**（`collected` / `no_show` / `cancelled`）：
    这不是锦上添花——本批把结案接上之后它们会真的出现在列表里，
    而原字典只有流转中的三个，结案行会把英文键直接印给人看。
  - ✅ **第二十八批 `consents`：2 条全部接通，欠账 36 → 34**（2026-09-16）。两条都关系到
    个人信息保护法的落地：**撤回同意**后端有、界面上做不到；同意记录里的「文本版本」
    指向的**文本版本库**也没有入口，于是"我当时签的到底是哪一版、写了什么"查不到。
    同意台账行加**撤回**（只对未撤回的摆；确认框写明记录会保留并标记撤回时刻、
    但**没有反向端点**，撤了就恢复不了），已撤回的行顺带把撤回时刻印出来。
    新增**同意文本版本库**面板（可按场景筛、可勾选含已停用版本），
    说明里写清一件容易误解的事：**停用旧版不会改动已登记的同意**——
    那条记录仍指着它签署当时的版本，所以历史举证不受影响。
    `drawTexts()` 与页面原有两个 draw 一起放在渲染末尾，并同步更新了
    原生提交守卫的锚点（新表单 + 新的尾部调用）。
  - ✅ **第二十九批 `chronic`：2 条全部接通，欠账 34 → 32**（2026-09-16）。病种目录是
    **分级规则与随访周期的唯一数据源**，而它建了就改不了；风险评分同样没入口。
    病种目录表改成带 ID / 状态 / 操作的正式表格，admin 可**编辑**
    （名称 / 随访周期 / 启停 / 指导要点 / 分级规则 JSON）。
    留空的字段**一律不放进 body**——后端是 `exclude_unset` + `if value is not None`，
    送空串会把人家的值清掉；说明行写明「停用是不再新增，不是作废存量」
    （已建档案不受影响、仍按原规则随访）。
    在管名单行加**风险评分**：趋势指标 / 最近三次值 / 趋势 / 评分 / 风险档，
    trend 与 risk_level 两个英文枚举都做了中文映射，并把算法写在说明里
    （分级基础分 + 趋势修正，**不足两次记「数据不足」不作修正**——一次随访推不出趋势）。
    取目录改成**不带 `active` 取全部**（停用的要能看到并重新启用，在管名单里挂着停用病种的
    档案病种名也才查得到），**建档下拉仍只列启用中的**（后端对停用病种直接 422）。
    ⚠️ **顺手修一处转义漏洞**：那个下拉原先是 `<option value="${v}">${t}</option>`，
    `v`/`t` 直接插值未 `esc()`——夹具里 `diabetes<b>` 这种编码会把 value 属性截断。
    改成 `esc()` 之后属性里是 `diabetes&lt;b&gt;`，浏览器读回来仍是原值（§8 前端转义红线）。
    **夹具第八处同类**：目录夹具缺 `id`/`active`/`guidance`，新列只能印 undefined。
  - ✅ **第三十批 `outpatient_docs`：2 条全部接通，欠账 32 → 30**（2026-09-16）。告知书模板是
    **签署时冻结正文的来源**，而它建了就改不了（措辞写错一次，此后每一份都照着错的签）；
    处置记录也只能按单次就诊看，而换药、雾化这类**连续处置要跨就诊看才有意义**。
    新增**告知书模板**面板（类型 / 标题 / 版本 / 状态 / 正文，admin 可编辑标题/版本/正文/启停），
    说明里写清那句最容易被误解的话：**改模板是"从此往后"，不是"追溯修订"**——
    停用的模板不能再用来开具（后端 409「请选用现行版本」），但已签的一个字都不会变。
    新增**按患者查处置史**面板（跨就诊，按患者维度落调阅留痕）。
    取模板改成**不带 `active` 取全部**（停错了要能改回来），**开具下拉仍只列现行版**
    （后端对停用模板 409）。留空字段一律不进 PATCH body（后端 `exclude_unset`）。
    **夹具第九处同类**：模板夹具只有 `id`/`title`/`version` 三键，缺 `consent_type`/
    `consent_type_name`/`body`/`active`——新表六列有四列印不出来，状态列还会全判成停用。
  - ✅ **第三十一批 `projects`：2 条全部接通，欠账 30 → 28**（2026-09-16）。里程碑**能加不能销**：
    加完就永远挂在那里，于是项目行里的「已完成 / 总数」分子恒为 0，逾期数只增不减。
    新增**里程碑（全部项目）**面板——项目 / 里程碑 / 到期日 / 状态 / 完成日 + 操作，
    未完成的给「完成」（可填完成日，留空按业务日期记），已完成的给「撤销完成」。
    状态按**三态**渲染：已完成 / 逾期未完成 / 进行中——逾期是**现算**的，
    已完成的不算逾期，没填到期日的也不算，说明行里写清楚了。
    撤销的确认框写明**会把完成日一并清掉**（后端 reopen 的 docstring：
    "误点了要能改回来——凡是拦得住的都要放得开"）。
    童子军：「报进度」的两连问与「加里程碑」的两连问各合成一个 `spdModal`
    （P2-38：110 → 106），状态从"手打 planning/ongoing/done/suspended"变成四个中文选项。
    ⚠️ **本批写坏过一次语法**：说明文字里写了 `` `reopen` `` ——反引号在模板字面量里
    直接把字符串截断了，`new Function` 当场报 missing )。以后中文说明里别用反引号引代码名。
    **夹具第十处同类，而且这次会整页抛错**：项目行缺 `milestones` 数组（与 `status`），
    而新面板正是 `p.milestones.map(...)`——undefined 上调 map 是 TypeError，不是少渲染一列。
  - ✅ **第三十二批 `fund`：2 条全部接通，欠账 28 → 26**（2026-09-16）。基金池**建错就改不了**
    （筹资总额、预付比例、备注、启停都改不了）；分配结果也只看得到"当前打开的那一池"。
    池子行按状态二选一摆：未清算的给**编辑**，已清算的给**分配结果**。
    这不是省事——**已清算的池子后端直接 409**（"清算是一次性动作，改了筹资总额
    就等于改了已经分下去的结果"），摆出来只会让人点一次看一句错。
    「分配结果」用的正是 `GET /pools/{id}/distributions`：当前打开那一池的分配
    本来就随 `settlement` 一起返回、页面早就在渲染，**这条端点的价值在于看别的池**——
    不用切换当前选择就能对比另一池分给了谁多少。说明行把这层分工写出来了。
    留空字段不进 PATCH body；状态下拉只给 `active`/`closed`（`settled` 由清算置，改不了）。
    童子军：池子状态列此前直接印 `active`/`settled` 的英文键，换成 `statusTag` + 中文字典。
    夹具同批改正：`status` 原先写的是 `open<b>`——**这个取值后端根本没有**
    （真值见 `PoolUpdate` 的 pattern 与清算逻辑），并补上 `prepay_ratio_pct`/`planned_prepay`/`note`，
    三条池子覆盖 在用 / 已清算 / 已关闭 三支。
  - ✅ **第三十三批 `esb`：2 条全部接通，欠账 26 → 24**（2026-09-16）。编排流程**建了就改不了**
    （名称、步骤、启停都改不了），更要紧的是**跑过没有、跑挂在第几步，界面上一条都看不见**——
    `esb_flow_runs` 把每一步的 `{step,type,status,detail}` 逐条落成了快照，
    而这张表此前没有任何入口。于是"那天检验结果为什么没同步过去"只能去翻库。
    流程行加「编辑」（名称 / 启停 / 步骤 JSON，留空的键不进 PATCH body）与「执行记录」
    （行下容器列最近 50 条：记录 / 流程 / 消息 / 状态 / 步骤结果 / 错误 / 时间）。
    说明行把**步骤结果是执行当时的快照**写出来：流程后来改了步骤，旧记录不会跟着变——
    排查"那天为什么失败"靠的正是这一点，这也是「编辑只改此后的执行」这句话的依据。
    执行记录只有 `succeeded`/`failed` 两种终态（`esb.py` 的 `status="failed" if error else "succeeded"`），
    与消息状态那五个取值不是一套，因此单列了 `ESB_RUN_STATUS` 而不是复用 `ESB_MSG_STATUS`。
    童子军：停用的流程不再摆「对消息执行」——后端按 code 找得到它、但会回 409「流程已停用」，
    摆出来只会让人点一次看一句错；要再用先在这里「编辑」改回启用。
    夹具同批改正 `/api/esb/stats`：`by_endpoint` 的键名写的是 `code`/`name`，
    后端 `EsbStatsEndpointOut` 是 `endpoint_code`/`endpoint_name`，还缺 `queued`/`backlog`/`failure_rate_pct`，
    于是「失败率」一列一直印着 `undefined%`、接入方一列印的是空标签。
    **本轮第七处陈旧夹具，也是第一处键名写错而不只是缺键的**——比对器每次都照渲染，
    没人回头看那一列对不对。顺带补上第二条（停用）流程，把按状态收按钮的那一支渲染出来。
  - ✅ **第三十四批 `rbac`：2 条全部接通，欠账 24 → 22**（2026-09-16）。授权此前**只增不减**：
    「按模块授权」一次授进去几十个权限点，授错了界面上撤不掉，只能把整个自定义角色删掉重建。
    而权限点清单本身也看不到——模块表只给得出「medwaste 有 6 个」，具体是哪六个接口查不到。
    - 新增**权限点清单**面板（按模块 / 路径关键词筛），每行可直接「授给角色」（走 `permission_ids`）。
      说明写明两件容易误会的事：**只登记写接口**（POST/PUT/PATCH/DELETE，读接口不进这套授权表），
      以及「代码声明的角色」那一列就是授权时「复制内置角色」所依据的东西。
      后端这条是 `limit(1000)` 而不是 `paginate`，所以到顶时**把截断说出来**，不让人以为"就这些"。
    - **角色权限明细**每行加「撤销」，撤完就地重画明细与角色表（授权点数是角色表的一列，
      不跟着刷新就会当场对不上）。
    - 「查看」对**内置角色也摆**：后端 `RolePermissionsOut.note` 是个条件键，
      专为解释"内置角色这里为什么是空的"而设（它的权限来自代码声明，不走授权表），
      而此前界面只给自定义角色摆查看，这句话根本到不了人眼前。
    童子军：「授权」那处的 `prompt("按模块授权，逗号分隔（如 medwaste,medication）")`
    换成模块下拉 + 内置角色下拉——**模块名就在同一页的表里，手打只会打错**（P2-38：106 → 105）。
    两项都留空时当场说"等于没指定权限点"，不送到后端换一句 422。
    渲染器顺带按 P2-31 样板收口（监听与首屏 innerHTML 同一同步块、取数放最后），
    并登记进 `test_native_submit_guard.py` 的 `ROOT_FIXED_RENDERERS`。
  - ✅ **第三十五批 `disease_programs`：2 条全部接通，欠账 22 → 20**（2026-09-16）。
    专病目录是路径节点的唯一来源，**建了就改不了**；而每个节点的执行记录
    （`disease_path_records`：谁、什么时候、做了哪个节点、结果如何）界面上**一条都读不到**
    ——列表行只印一个完成度分数，"这个病例当时走到哪一步、卡在谁手里"只能去翻库。
    - 目录行加「编辑」（名称 / 说明 / 启停 / 路径节点 JSON，留空的键不进 PATCH body）。
      说明行照抄后端 docstring 的口径：**改路径只影响此后的判定**，已记下的节点一条不删，
      但在管病例的「待办节点」会跟着新路径变——把必需节点改成选做，完成度当场就上去。
    - 入组行加「轨迹」：节点 / 完成时间 / 经办人 / 结果 / 备注，外加出组日、退出原因、
      疗效备注这几个列表行从不显示的字段。节点名按**当前目录**翻译，目录里已删的节点
      印原始 key——记录不会因为改目录而消失，这正是上面那句话的另一面。
    - ⚠️ **轨迹只按行上的 id 取，不提供「输入任意入组ID」的入口**。这条读接口没有归属校验
      也不留痕（已登记 `test_stage15_horizontal.py::NEWLY_VISIBLE_UNGUARDED_READS`），
      而行里的 id 来自它旁边那条按机构收口过的列表。给它配一个自由输入框，
      等于把一条已登记的无守卫读接口变成界面上的枚举入口——**接线不该顺手扩大暴露面**。
    童子军：本页仅剩的 5 处 `prompt(` 全换成 `spdModal`（P2-38：105 → 100）。
    这不只是换壳：节点键原先是**照着提示字符串手打**，打错了后端 422，而更糟的是
    打对了却打成别的节点——完成度就永远算不对；疗效原先手打 `cured/improved/...` 五选一。
    顺带把后端早有、界面从没给过入口的三个字段补上：完成日期（留空按业务日期记）、
    经办人、备注。
    夹具同批改正：`status_name` 写的是「在组 / 已出组」、`by_outcome` 的键写的是
    `effective` / `invalid`——**这些取值后端一个都不产生**（真值见 `ENROLL_STATUS`
    与 `OUTCOMES`：在管 / 完成出组 / 中途退出；治愈 / 好转 / 稳定 / 加重 / 死亡 / 未评价）。
    一并补上 `DiseaseEnrollmentOut` 缺的 9 个键与目录的 `description`（编辑弹框要预填它）。
  - ✅ **第三十六批 `access_logs`：2 条全部接通，欠账 20 → 18**（2026-09-16）。
    这一批的两条**分属两个端**，是本轮第一次一个批次同时改管理端与居民端：
    - `GET /api/access-logs/mine` 用 `current_resident_patient`，是《个人信息保护法》
      **第 44 条知情权**在本平台的落点（"个人有权知悉其个人信息的处理情况"——端点 docstring 原话）。
      居民端 H5 的个人信息权利那一段里，知情同意（第 14 条）、更正与注销（第 46/47 条）
      早就摆着了，**唯独"谁看过我的档案"这条没有入口**：平台老老实实留了痕，居民却看不到。
      现在按 时间 / 调阅人 / 所属机构 / 看了哪份 / 依据 列最近 50 条。
      这一段要额外说清一句：它按**登录账户本人**绑定的档案取（接口不收 patient_id），
      **代管家人时看到的仍是本人的记录**——不写这句，切到家人视角的人会以为在看家人的。
    - `GET /api/access-logs/stats` 是 director 专用的按依据汇总，接在管理端调阅留痕页，
      新增「调阅构成」面板（总次数 + 前四类卡片 + 全量条形图），与上面的列表同一次查询刷新。
      两处口径写在页面上：**聚焦到某个人本身也会留痕**（后端 `_log_view`，与页面原有那句
      "查谁看过某人本身也是在看这个人的隐私"同一条）；以及**起止日期与调阅人两个筛选
      对这条统计不生效**——它只收 `patient_id`，docstring 里"一段时间内"那句话
      在实现里没有对应的过滤，界面不能顺着它说成"本次筛选的构成"。
    夹具同批改正：`at` 原先写的是 `null`，而 `AccessLogOut` 的 docstring 明写
    「isoformat **或空串**，不是 null」——照契约改成空串，并补齐 11 键里缺的
    `id` / `viewer_org_id` / `patient_id` / `resource` / `basis`。
  - ✅ **第三十七批 `tcm`：2 条全部接通，欠账 18 → 16**（2026-09-16）。
    体质辨识是国家基本公共卫生服务「中医药健康管理」的核心动作。后端把简表计分、
    转化分公式、两条判定阈值、九种体质各自的调养建议与参考方剂**全做了**，
    界面上却**一个入口都没有**——中医药页只有智能辨证、共享中药房、适宜技术库三块。
    - 新增「体质辨识（标准化简表）」面板：`/constitution/spec` 的口径**原样印在页面上**
      （计分方式、原始分与转化分公式、≥40 判定为该体质 / 30-39 倾向 / 均 <40 平和质），
      八个偏颇维度各一个 0-100 输入框。**平和质不收分**：它是"八种偏颇都不够格"的结论
      而不是一个维度（后端判定时 `k != "balanced"`）。
    - 判定结果一律取后端的 `constitution` 与 `tendencies`，**前端不把 40/30 两条线再算一遍**。
      阈值日后要调，改一处页面就跟着变；算两遍的代价是两处各错一半，而且很难发现。
    - 留空的维度不参与判定，这句话写在界面上：只填两三项就下结论本身就不可靠，
      但平台不替人拦——把哪几项当依据是辨识者的判断，界面只保证不替人编。
    童子军：「智能辨证」的结果原先是整块 `<pre class="json">` 甩在页面上——
    命中了哪几个症状、推荐什么方，得让人自己从括号里读；而 `note` 那一句干脆没印。
    换成表格（证型 / 命中症状 / 命中数 / 推荐方剂 / 适宜技术）+ note。
    **甩一段 JSON 给人看，是"有端点不等于能用"的另一种形态**，与 `prompt()` 录入同一族。
    顺带把 `renderTcm` 登记进 `test_native_submit_guard.py` 的 `ROOT_FIXED_RENDERERS`：
    它本来就是"监听与首屏 innerHTML 同一同步块、取数放最后"的形状，只是一直没登记。
  - ✅ **第三十八批 `vaccine_supply`：2 条全部接通，欠账 16 → 14**（2026-09-16）。
    - 顶部卡片一直印着「30天内到期 N 支」，**却看不到是哪几个批号**——
      看得到数、看不到条目，这是本轮反复出现的一种形态。新增「临期与过期批次」面板
      （天数可调 1-365），把后端两条口径原样印出来：**已过期的一并列出并标注**
      （不是催人用掉，是提示尽快按报废流程处理，别让它躺在冰箱里被误用），
      以及**只列还有余量的批次**（发完的批次不删行、只累加已用量，列出来没有意义）。
      `today` / `within_days` / `generated_at` 三项也印出来——这张表的"临期"
      是相对**业务日期**算的，不是浏览器当下。
    - AEFI 行加「转归」。上报当时多半只能填「未知」，好转、痊愈还是留有后遗症，
      是后续随访才知道的（后端 docstring 原话），而界面上改不了——
      于是每一份报告的转归都永远停在上报那天，而**严重反应的转归构成**
      正是疑似预防接种异常反应监测最要紧的那张表。
      说明写明「没有改完就锁这一说」：随访结论变了就再改一次。
    童子军：本页两处 `prompt(`（封存原因、超温处置说明）换成 `spdModal`（P2-38：100 → 98）；
    `renderVaccineSupply` 登记进 `ROOT_FIXED_RENDERERS`（本批新增 `#vx-form`，
    监听与首屏同一同步块、取数放最后）。
    夹具同批改正：AEFI 缺 `id`（转归按钮要它）、`outcome`、`record_id`、`org_id`；
    `outcome_name` 写的是「好转」而后端 `AEFI_OUTCOMES` 里是「**好转中**」；
    批次的 `manufacturer` 写了 `null` 而契约是 `str`。又两处"后端不产生的取值"。
  - ✅ **第三十九批 `dictionaries`：1 条接通，欠账 14 → 13**（2026-09-16）。
    「四统一」编码字典此前只能**一条一条敲**——国标 ICD-10 上万条，界面上没有任何批量入口。
    加「批量导入」：JSON 数组，每条至少 code 与 name，另可带 spec / dosage_form /
    manufacturer / unit / insurance_code / national_code / extra 七个可选键（入参模型有它们，
    单条新增表单里一个都没有）。回执按后端原样印：新增 N 条、跳过（编码已存在）M 条。
    **写在页面上的那句最要紧**：已存在的编码会被**跳过，而不是更新**。
    入参模型叫 `CodeEntryUpsert`，但实现是 `if entry.code in existing: continue`，从不更新——
    想靠重新导入去改一条已有条目的名称是不管用的，而这与"导入"两个字给人的印象正好相反。
    不写这句，第一个拿它改名的人会以为改成功了。
    另一句是并发口径：整批一次提交，撞车的那一条由 `insert_if_absent` 自己跳过，
    不会把整批带回滚（后端注释里写得很清楚，值得让用的人也知道）。
  - ✅ **第四十批 `eldercare`：1 条接通，欠账 13 → 12**（2026-09-16）。
    指引㉓点名的「统计分析」。后端把两条**最容易做错**的口径都做对了，而界面上读不到：
    - **按每位老人最近一次评估算，不是评估条数**。同一人评三次不该在失能构成里占三个坑，
      否则复评勤快的机构看起来失能率特别高——这是典型的"统计口径惩罚做得好的人"。
    - **认知筛查与体质辨识"未做"单列，不按 0 分并入**。这两项本就不是每次评估必做，
      按 0 分并进平均值会把"没做"读成"分数是 0"，筛查做得少的机构反而显得认知水平差。
    两条都随后端的 `caliber` 字段**原样印在面板上**，另加一句把"未做 ≠ 0 分"讲白。
    失能构成走条形图；失能率无评估时印「无评估」、认知平均分无筛查时印「无筛查」，
    都不印 0——后端这两个字段是 `float | None`，null 与 0 在这里是两件事。
  - ✅ **第四十一批 `inpatient`：1 条接通，欠账 12 → 11**（2026-09-16）。
    这一条路径上有两个动词（POST 登记执行 / GET 执行记录），**两个都没有入口**——
    医嘱单此前只有「开立」与「停止」，中间那一段"到底执行了没有、谁执行的、皮试什么结果"
    整条链路在界面上不存在。医嘱开了、停了，执行环节是一片空白。
    - 医嘱行加「执行记录」（记录 / 执行人 / 执行时间 / 皮试 / 说明）与「登记执行」
      （说明 + 皮试结果，只对执行中的医嘱摆——后端对已停止的医嘱回 409）。
    - **皮试那一格是给药安全里最不能丢的**。后端专门留了 `skin_test_result`
      （`negative`/`positive`，可空 = 不需要皮试），而界面上既录不进也看不到。
      弹框里空值那一项写的是「不适用」而不是留白，并且**空就不送这个键**——
      后端的 pattern 只认两个值，送空串会被 422 拦下。
      表格里的「—」也在说明行里点明是**不需要皮试**，不是"没填"。
    - `nursing_record_count` 的含义写在面板上：护理记录挂在**医嘱**上而不是单次执行上，
      所以同一响应内各条相同——不写这句会被读成"这一次执行有 N 条护理记录"。
    与前几批同一条口径：**只按行上的 id 取，不做"输入任意医嘱ID"的入口**，
    行本身来自按住院单查出来的医嘱列表。
  - ✅ **第四十二批 `metrics`：1 条接通，欠账 11 → 10**（2026-09-16）。
    这条端点的 docstring 写的就是「供前端指标卡绑定下钻入口」——
    而前端一直是**把下钻 key 写死在卡片数组的第 4 项里**，从没调过这份目录。
    两边对一下就看出问题（本批实测）：
    - 后端 `METRIC_QUERIES` 有 **13** 项可下钻，驾驶舱卡片只绑了 **9** 项。
    - 其中「慢病随访超期 / 医废滞留 / 近7日传染病报告」三项只经**预警横幅**出现，
      而横幅是 `alerts.total ? … : ""`——**没有预警时它整块不渲染**，那三项也就没了入口。
    - 剩下的 `referrals_completed`（转诊结案）**卡片没有、横幅没有、哪儿都没有**：
      后端能下钻，界面上一个入口都点不到。
    新增「可下钻指标目录」面板：按目录列全（指标 / 名称 / 当前计数 / 业务页 / 卡片入口），
    每行可直接下钻，没有卡片入口的行标橙色「无」。
    面板里那三个数——卡片绑了几项、横幅此刻覆盖几项、剩几项没入口——**是现算的**
    （从 `cards` 数组的第 4 项与 `alerts.items` 里数）。写死一个数字会随卡片增删悄悄过期，
    而这一批要修的恰恰就是"写死的那份清单与真源对不上"。
  - ✅ **第四十三批 `organizations`：1 条接通，欠账 10 → 9**（2026-09-16）。
    这条端点存在的理由写在它自己的 docstring 里：ADR-0004 起，转诊分级审核按 `parent_id`
    逐级上收——**非顶层机构缺上级，它经手的转诊单就只有全域角色推得动，其余账号一律 403**。
    这正是「正式上线前」那一节反复说的形状：平时完全看不出来，偏偏在最需要它的时刻失效。
    端点做出来是"供运维在越权校验咬人之前先把树建好"，而界面上没有入口，
    于是它只能等基层报障之后有人想起来去 curl 一下。
    机构页新增「机构树体检」面板：五张卡片（总数 / 合法树根 / 最深链路 / 缺上级 / 层级错位）
    + 两张明细表。标题直接说结论：`referral_ready` 为真写「分级转诊口径已满足」，
    否则写「有缺陷，会卡住分级审核」。
    - 它是 `require_admin`：非管理员拿到 403，**照驾驶舱绩效那处的先例不阻塞本页**
      （`try/catch` 后 `healthHtml` 留空，其余部分照常渲染）。
    - 「最深链路」标明是纯信息项、不参与判定；
      「层级错位」表下写清**判据是层级相邻而不是链路长度**——
      市→县→乡→村四层合法，而县→村室→村室只有三层却已经错位：
      那张单子的「卫生院审核」会由一家村卫生室完成，环节名与实际处理机构对不上，
      闭环统计跟着失真。这句不写，看到"三层比四层短"的人会以为是误报。
  - ✅ **第四十四批 `org_groups`：1 条接通，欠账 9 → 8**（2026-09-16）。
    分组页此前只有「分组 → 成员」一个方向。而这一页的页头写的就是
    "一家机构可以既在某片区，又在某专科联盟"——**反过来那个问题
    （这家机构到底在哪些分组里）却一直查不了**。
    覆盖情况那一块一次只看一种分组类型，所以"某机构有没有入组"在那里也答不准。
    新增「按机构查归属分组」面板：选机构 → 列出它归属的全部分组（可跨类型），
    一个都没有时明说后果——"转诊、调拨与按分组的统计口径都不会把它算进去"。
    说明里还照后端 docstring 写清一句：分组归属是组织架构拓扑、不含经营或诊疗数据，
    所以这条**按设计不做横向隔离**，查别家机构的分组归属是允许的。
  - 🧹 **顺手修两处会把字面星号印给用户的文案**（同一提交，两行）：
    `core.js` 便捷寻医的「没号的医师也在列」、`pages-clinical.js` 病历缺陷快照的「落库时」，
    中文说明里写成了 Markdown 的 `**粗体**`——**而这些字符串是直接进 innerHTML 的**，
    浏览器不认 Markdown，星号原样显示。改成 `<b>`。
    这条教训本批自己也差点再犯一次（写到一半的说明里又打了一对星号，渲染比对时当场看见）。
    与第三十一批记下的「中文说明里别用反引号引代码名」是同一族：
    **前端说明文字全在模板字面量里，它既不是 Markdown 也不是代码注释。**
  - ✅ **第四十五批 `patients`：1 条接通，欠账 8 → 7**（2026-09-16）。
    患者页此前只有一个**模糊**搜索框（姓名 / 身份证 / 卡号一起往里匹）。
    而窗口拿到一张实体卡时要的恰恰是**精确命中这一张**：
    卡号少打一位就该查不到，而不是模糊匹出一串同前缀的人来让人挑——
    **挑错一个，就是给另一个人的档案记了一次调阅。**
    新增「按电子健康卡号精确查」面板。查不到时把后端那句「患者不存在」**原样给出来**：
    这里最怕的是把"查不到"说成别的意思，窗口会以为系统坏了，而不是卡号打错了。
    说明里点明两处都按角色脱敏（走 privacy 的 desensitize），没有全科室可见的明文。
    ⚠️ 本批写坏过一次语法：说明里又用反引号引了代码名，模板字面量当场截断——
    与第三十一批同一处教训，`new Function` 语法自检再次当场抓到。
  - ✅ **第四十六批 `resources`：1 条接通，欠账 7 → 6**（2026-09-16）。
    通用资源此前**登记完就定死**：位置换了、联系人换了、容量当初报错了，
    一概只能重登一条新的——**而旧的那条还留在统一资源视图里继续算可用量**。
    资源行加「编辑」：名称 / 容量 / 位置 / 联系方式 / 备注，留空的键不进 PATCH body
    （只想改位置的那一次，不该把备注清掉）。
    说明里写明**编码与资源类型建后不可改**：入参模型里就没有这两个键，
    它们是这条资源的身份，改了等于换了一个东西。
    童子军：「撤回理由」那处 `prompt(` 换成 `spdModal`（P2-38：98 → 97）——
    这段理由会印在资源状态列上给别人看，弹框里该说清楚。
    夹具同批改正：`location` 写的是 `null` 而 `ResourceOut.location` 是 `str`；
    并补上缺的 `org_id` / `resource_type` / `contact` / `note`——编辑弹框要预填后两个。
    ⚠️ 本批又在说明里用反引号引了代码名，模板字面量当场截断——**同一处教训，一天之内第三次**。
  - ✅ **第四十七批 `surveillance`：1 条接通，欠账 6 → 5**（2026-09-16）。
    应急物资此前**登记完就改不了**：补了货改不了数量、储备下限当初报错了改不了、
    换批次改不了效期——**而「低于下限 / 已过期」两个预警恰恰全靠这三个字段算**。
    页面上原先只有按机构汇总的保障情况（哪家缺、缺什么），缺的正是"逐条改"的那张台账。
    新增「资源台账」：可按类型筛、可只看缺口与过期，行内「编辑」改数量 / 下限 / 效期 /
    联系方式 / 位置，留空的键不进 PATCH body。说明里点明**效期一列的「—」
    对应急队伍是正常的**（队伍本就没有效期），对物资才是没填。
    ⚠️ **这一批踩到 `spdModal` 的一个坑，如实绕开并记在这里**：
    它的 `number` 字段把空串折成 `Number("") === 0`，于是"留空不改"与"改成 0"
    变成同一个值——而**储备下限的 0 是合法且有意义的取值**
    （= 不设下限，后端 `bool(r.min_quantity)` 据此判 `below_min`）。
    数量与下限因此改用 `text` + `^\d+$` 校验，空串才算没填。
    这是 `spdModal` 这个组件的既有形状，本批没有改组件（改它要过一遍全部调用点）。
    守卫同步：`renderSurveillance` 进 `ROOT_FIXED_RENDERERS`，尾部取数锚点带
    换行+两空格前缀——两个 handler 里各有一句同文的 `await drawResources();`，
    不加前缀锚点歧义，守卫会直接红（本批实测红了一次，按它的 docstring 修好）。
  - ✅ **第四十八批 `tcm_heritage`：1 条接通，欠账 5 → 4**（2026-09-16）。
    模拟病例后端把决策点、计分、及格线、**逐题解析**全做了，而界面上**做不了题**。
    管理端那句 hint 写的是「作答与评分在医师端 H5 完成；此处仅维护病例」——
    **可医师端 H5 里并没有这个东西**（它只有待办 / 危急值 / 检查 / 查房 / 手术 / 随访 /
    慢专病 / 速查八个页签，一行 `tcm-heritage` 都没有）。那句话是一份没兑现的计划，
    而它读起来像是"已经在别处做好了"——这比没有说明更坏。
    本批把作答接在传承页上：点「作答」展开题干与选项，交卷后给得分 / 及格线 / 通过与否
    + 逐题对错 + 正确答案 + 解析。hint 改成实情（保留"列表刻意不含正确答案"那一句，
    因为它仍然为真：答案只在交卷回执里给）。
    真要挪去医师端是**加一个新页签**（HTML 段 + 页签按钮 + 载入器），
    超出一个接线批次的范围，不在本批夹带——这一点写在提交信息里。
    两处口径印在页面上：**只有答错的才给解析**（后端原话：答对的人不需要，
    堆一屏解析反而没人看），所以解析列的「—」意味着答对了、不是没写解析；
    每次交卷都留一条尝试记录、分数不覆盖，练几次、进步多少都查得到。
    停用的病例不摆「作答」——后端直接回 409。
  - 🎯 **至此「可清的孤儿端点」全部清零**：欠账 198 → 4，无入口为 0 的模块 36 → 84 / 86。
    **剩下的 4 条不是"还没做"，是"不该由自动批次做"**——
    auth 的 3 条 TOTP 属 §8 认证改动须人工复核，admin_mgmt 的 1 条等 P1-56 架构裁定。
    全程没有动过一行后端代码（唯一例外是第十六批那处 `assignment_type` 数据正确性缺陷）。
  **顺序供裁定，不是决定。**
- ✅ **ADR-0024 第一步（2026-09-18）：补上日期治理缺的「查询参数」那一半。**
  D-3 把 22 处日期字段收敛到了 `DateStr`，但改的是 **body 字段**；查询参数不在射程内，
  而 `datetypes.py` 开头举的唯一反面例子恰恰就是派驻。于是两条「结束派驻」端点的
  `end_date` 一直是裸 `str`：`?end_date=完全不是日期` 200 落库，那条派驻被
  `dispatch-stats` 整条跳过——**一名真派满半年的中级医师从国家监测指标里消失**（实测复现）。
  `staffing` 那条看着有校验，其实只有一句字典序比较，`2026-13-99`/`abc`/`2026-02-31` 照样落库。
  已补 `datetypes.check_date` + `deps.require_date`（与 `require_month` 对称），两条端点都接上；
  同批修掉 `mgmt:end` 无条件把离职员工改回在岗（污染 analytics 在岗医师数）、
  补上"结束日不早于开始日"。回归 18 条、四处变异各自转红；棘轮把余下 25 处钉住（P1-58）。
- ✅ **P1-58 日期查询参数逐模块接上 `require_date`：27 → 0**（2026-09-24；ADR-0024 第一步留下的棘轮，
  现在 `tests/test_date_query_params.py::KNOWN_BARE_DATE_PARAMS`）。开工前逐条取证了三件事：
  后端怎么用这个值、三端前端实际发什么、非法输入现在是什么结果——**SQLite 与真 PG 各跑一遍**。
  取证结论（2026-09-24）：
  - **对账是写入口，且界面可达**：`POST /billing/reconciliation/run?date=2026-9-1` 201，
    与 `2026-09-01` 并排出现**同一天的第二个对账批次**（`strptime` 不要求补零，唯一约束按字符串判重）；
    对账页那个输入框是自由文本，手敲 `2026-9-1` 再自然不过。
  - **三个 spd 端点 6 个参数在真 PG 上是 500**（`case-reports` / `call-tasks` / `referrals-stats/closure`
    把参数拼成 `f"{x} 00:00:00"` 去比 DateTime 列，`abc` / `2026-02-31` → `InvalidDatetimeFormat` /
    `DatetimeFieldOverflow`）；**SQLite 上同样的请求是 200 空集**——正是"别把 SQLite 绿了当成 PG 也对"。
  - 其余多是只读筛选，非法值 200 返回空集或错集；三端前端只有对账日期、死亡卡导出起止、寻医起始日三处会发，
    且都只在非空时发——**留空语义一律保持**。
  - 顺带量出**第三套日期校验** `resolve_business_date`（41 个参数经它），其 `fromisoformat` 随 Python 3.11
    放宽，`?start=20260901` 让县域就诊率的县外一侧整段落空（实测）——✅ 已收口到 `check_date`（f702eaf）。
  - 棘轮判据补全：承认 `resolve_business_date` 是守卫（剔 2 条误报）、数进不带 `date` 字样的
    `start`/`end`/`since`/`day`（补 4 条漏数），**25 → 27**。
  按模块一批一个提交：接 `require_date` + 回归（非法 422、合法与留空行为不变）+ 从棘轮划掉 + 重生成 `docs/闸门现状.md`。
  清账 12 批、一个模块一个提交：billing → access_logs → spd/care → spd/followup → spd/referral → certs → medwaste
  → appointments → portal → admin_mgmt → clinical_docs → surgery。每批撤掉修复后对应用例各自转红；
  真 PG 上的那一族另有 `test_date_filter_pg_dialect.py`，并由 `test_postgres_real.py` 整份换到 PG 上再跑。
  清单清成空集合后照样是棘轮。**body 侧另有一批 D-3 没数到的裸 `str` 日期字段，见下一条 P1-61。**
- ✅ **P1-61 请求体里 22 个裸 `str` 日期字段逐模块换成 `DateStr`**（2026-09-24 清零；HL7/FHIR 入站出生日期待裁定）（2026-09-24 清 P1-58 时量出，棘轮已立：
  `tests/test_datestr_single_source.py::KNOWN_BARE_BODY_DATE_FIELDS`）。D-3 收敛的是写了日期正则的 22 处，
  没写过正则的这 22 处从来不在分母里。最要紧的一处实测：**对接方不带 `period` 建凭证时，
  `voucher_date="2026/09/24"` 201，`period` 推成 `"2026/09"`，过账后不在 2026-09 的试算平衡里**
  （凭证页会带上当前期间，走页面不落假期间，但日期串照样原样入库）。月度同形的 `VoucherIn.period` /
  `CostIn.period` 两处随各自模块一起收（该用 `PeriodStr`）。
  **修之前逐个查写入方**（三端前端 / HL7·FHIR 适配器 / 导入脚本 / 种子），别把对接方的现有写法直接变成 422；
  一个模块一个提交，凭证先行。
  - ✅ 第一批 accounting（22 → 21）：`voucher_date` → `DateStr`，`period` → 新增的 `OptionalPeriodStr`（留空仍按日期推）。
  - ✅ 第二组 analytics / followups / clinical_docs / surgery（21 → 16），逐端点回归统一在 `tests/test_body_date_fields.py`。
  - ✅ admin_mgmt（16 → 14）：质控登记日期、人员变动生效日 → `OptionalDateStr`；继承来的 `QcOut` 同批覆盖回 `str`（P1-63）。
  - ✅ eldercare（14 → 13）：评估日期写成 `2025/01/15` 的老人永远不进年度复评提醒（实测）→ `OptionalDateStr`，`AssessmentOut` 同批覆盖。
  - ✅ homevisits（13 → 12）：上门服务期望日期 → `OptionalDateStr`。
  - ✅ knowledge（12 → 10）：有效期写成 `2026/01/01` 的药品政策过期九个月仍标"有效"（实测）→ 新建与续期都走 `OptionalDateStr`。
  - ✅ materials（10 → 9）：高值耗材入库效期 → `OptionalDateStr`（顺带发现使用登记不看效期，另立 P1-64）。
  - ✅ maternal（9 → 5）：产检访视、儿童随访、新生儿筛查、妇女保健四个日期 → `OptionalDateStr`，`WomenHealthOut` 同批覆盖。
  - ✅ publichealth（5 → 4）：卫生监测上报日期 → `OptionalDateStr`，`MonitorOut` 同批覆盖。
  - ✅ quality（4 → 3）：院感上报日期 → `OptionalDateStr`。
  - ✅ tcm（3 → 2）：效期写成 `2026/01/01` 的过期中药制剂批次照样能发放（实测）→ `OptionalDateStr`。
  - ✅ contracts（2 → 1）：家医签约日期 → `OptionalDateStr`，`ContractOut` 同批覆盖。
  - ✅ patients（1 → 0，**清零**）：`2016/03/05` 建档的 10 岁孩子登记知情同意不要监护人（实测）→ 建档与档案更正都走
    `check_date`（更正的待审存量在审核时 409）；HL7 PID-7 不查日历、FHIR 部分日期原样收，是对接口径，**交待裁定**。
  - ✅ 导入脚本：`_valid_date` 改走 `check_date`——`20260603` 这类写法的就诊此前每重导一次多一份（实测 2 → 3 → 4）。
  - ✅ cost：成本归集期间 → `PeriodStr`（`2026/09` 归集"成功"却不进任何报表）；成本页补上与会计页一样的坏期间兜底。
- ✅ **P1-62 月度期间查询参数：P1-58 的月度版**（2026-09-24 量出并收口，余 `region_stats` 1 条待裁定）。
  真 `YYYY-MM` 且未校验 5 处（accounting 三个报表口径，凭证页"切换期间"是自由文本，`2026-9` → 试算平衡**空表**；
  admin_mgmt 财务汇总与薪资列表）；**`spd/assess.workload` 在 SQLite 上就会 500**（手写的 `_period_range`，
  `2026/09`、`abc`、`2026-Qx` 都炸）；`quality.clinical_indicators` 是第四种写法（`strptime`，`2026-9` 照过）；
  `spd/workbench.region_stats` 的 `period` 声明了却从不读。其余 4 处不是 `YYYY-MM`（间接校验 / 考核期标签 / 枚举）。
  **workload 的 500 先修**；棘轮随第一批一起立，判据要跟进一层 helper。
  - ✅ workload 的 500 已修，并顺藤摸到同一个解析函数在**跑分写路径**上：`2026-8` 之类 200 且写进一套垃圾期的考核结果。
    三种写法（年/季/月）收成一个 `check_assess_period`，月度交给 `datetypes.check_month`；先写用例在未修代码上跑出 11 条红。
  - ✅ accounting 三个报表口径接 `require_month`，**前端同批加兜底**：存下的坏期间会让凭证页整页画不出来、
    连切换框都没有——只对 422 回落本月；切换时先让后端判再存。只撤前端兜底，e2e 以"切换框找不到"转红。
  - ✅ admin_mgmt 财务汇总与薪资列表的 `period` 接 `require_month`（纯对接方入口）。
  - ✅ quality 那处"第四种写法"其实是 `month_bounds` 的第五份副本，另有第六份在基金周期预结——都抄的是修之前那一版，
    `9999-12` 两处 500。收回 `month_bounds`，加 AST 守卫防第七份。
  - ✅ 棘轮已立（跟进一层 helper，否则间接校验的会误报）：未经校验的 `YYYY-MM` 清零；4 处按设计不是 `YYYY-MM` 的写理由豁免，
    其中 `region_stats` 那条（声明了却不读）交待裁定。修复前可能已写入垃圾期考核结果，需人工按期间查出处置。
- ✅ **P1-63 7 个响应模型继承了入参的严格日期类型：库里一条存量坏日期，整个列表 500**（2026-09-24 做 P1-61 时发现，已修）。
  `class XxxOut(XxxCreate)` 连日历校验一起继承，FastAPI 又拿 `response_model` 校验出参——D-3 / P1-61 之前入的库、
  导入脚本或直改库写进的坏日期一读就炸：一条 `duty_date="2026-02-31"`，**整张排班表 500**（实测）；体检总检结论已 commit、
  响应却是 500。排班 / 体检 / 儿童建档 / 接种史 / 传染病个案 / 号源 6 个读接口 + 总检 + 财务建档回执，共 8 个字段。
  出参覆盖回 `str`（字段顺序、默认值逐模型比对不变）；守卫 `tests/test_response_date_types.py` 无豁免名单——
  **P1-61 剩下的字段里还有 6 个出参是继承来的**（含 `PatientOut.birth_date`），换的那一批守卫就红。
- ✅ **P1-70 界面记录的门诊告知书拒签，拒签人一律记成"本人"**（2026-09-24 做 P2-38 门急诊文书页时发现，已修）：
  请求体写死 `signer_relation: "self"`；拒签是"已告知、对方拒绝"的证据，家属或委托人代为拒签的被记成本人。关系改成五选一；
  **修复前的存量拒签关系不可信**，告知书有结论后不能再改，需按纸质签字页人工复核。
- ✅ **P1-68 界面填的病案首页，转归一律是"好转"**（2026-09-24 做 P2-38 住院页时发现，已修）：与 P1-66 手术转归同一个形状——
  请求体从不送 `outcome`，后端默认"好转"，质量指标的住院治愈好转率恒 100%、死亡病例进不了死亡率。表单里转归可选、备注可填；
  **修复前的存量首页转归不可信**，首页填了就不能改（409），需按病历人工复核。
- ✅ **P1-67 页内表单的数字框收不了小数**（2026-09-24 做 P2-38 物资页时发现，Chromium 实测，已修）：`spdModal` 的数字框不带 `step`，
  浏览器按步长 1 拦下 1.25、12.80、6.1——DRG 基准权重、收费调价、会诊计费、基金池、慢专病管理目标、服务包价格、考核指标权重、
  课程考核得分十个字段从界面只能填整数（其中三处是 P2-38 从 `prompt()` 换壳时换出来的回归）。
  一律带 `step="any"`，小数交后端判；e2e 以调权写 1.25 钉住。
  **修复前经这些表单保存的值可能被取整过**，需按需复核。
- ✅ **P1-66 界面登记的术中记录，转归一律是"好转"**（2026-09-24 做 P2-38 手术页时发现，已修）：管理端与医生移动端都把
  `outcome` 写死，质量指标的手术治愈率与死亡数取的正是它；术前/术后诊断录不进去。两端换成表单后可选可填；
  **修复前的存量记录转归不可信**，需按病历人工复核。
- ✅ **P1-65 一条对接消息让患者主索引列表对所有人 500**（2026-09-24 量 P1-63 同族时实测，已修）：`PatientOut` 继承了证件号
  15–18 位约束，HL7/FHIR/ESB 入站只查"至少 15 位"；20 位证件号的患者建进库、入站却回 422，此后 `GET /api/patients` 500。
  出参覆盖回 `str`；证件号长度口径并进待裁定的入站条目。同族其余 90 个出参约束登记为 P2-40（未逐个审计；2026-09-24 已逐字段审完，修 2 处，见 P2-40）。
- ✅ **P1-64 高值耗材使用登记不看效期**（2026-09-24 做 P1-61 materials 批次时发现，实测，已修）：效期 `2020-01-31` 的耗材
  登记使用 200，追溯链上就这么挂在患者和手术下面。现在效期早于业务日期 → 409 且不改状态；效期当天、留空、
  存量认不出的照旧放行（后者没有更正入口，拦下就永远用不了，需人工核对）。
- ✅ **P2-42 调阅留痕的「看了哪份」「凭什么」对居民显示英文码**（2026-09-24 发现，已修）：居民端「谁看过我的档案」与监管页的
  可读名词表只有 14 个词，源码实际写入 81 个，家庭代管的依据 `delegate` 也没有名字——居民读到的是 `inpatient_order`。
  词表补齐；守卫从源码推导全部写入点（汇点签名现读、包装求不动点、推不出的写法一律红）。遗留：居民主体的调阅人仍显示账号号。
- ✅ **P2-43 点一下就生效的不可逆操作没有确认**（2026-09-24，P2-38 逐页替换时接连撞见后按形状全仓库扫出十处，已修）：
  替居民取消预约、撤销调阅授权、结束问诊 / 咨询、孕产妇与公卫事件结案、结束派驻（顺带给出结束日期）、停用公式与规则、删路径节点——
  各补一个写明后果的确认表单。守卫盯住破坏性调用须在同一分支里先确认，可逆的进豁免并写明怎么恢复。
  e2e 十处全覆盖：各先取消、按接口核对没动，再确认并读回；把前端退回改前版本，这十条逐条变红。
- ✅ **P2-38 前端 `prompt()` 录入逐页换成页内表单**（此前随孤儿批次顺带清，134 → 97；2026-09-24 起单独成批，同日清零）。
  **棘轮已立**：`tests/test_frontend_prompt_guard.py` 盯含 `prompt(` 的行数，只减不增、降了不改基线也红，
  现值登记进 `docs/闸门现状.md`——此前这个数字只写在文档里，规则文档里的 134 早已过期却没有任何东西发现。
  **清零后即为禁令**（出现一行即红），并加了一条：`confirm()` 不得当二选一用——文案里出现「确定=」「取消=」即红；
  这一路撞见的四处（处方点评、移动端出报告、开单前互认、住院开医嘱）都是这个形状：想放弃的人点取消，得到的是另一个选项。
  - ✅ 收尾七处（10 → 0）：流程画布加节点（角色改从字典里选——后端不校验角色名，打错就是一个谁也推不动的节点）、定时任务间隔、
    凭据作废（写明不能恢复）、按月导出（月份写错时导出失败带上后端原因，原先只报状态码）、知识库续期与停用（停用补确认）、
    指标权重、集成平台对消息执行编排；`spdModal` 注释里那一行换了说法。
  - ✅ 接种禁忌解除（11 → 10）：换成表单并写明后果（接种前评估不再拦截这一条）。
  - ✅ 人员下沉职称等级（12 → 11）：原先手打英文代码；换成下拉，选项与后端同一张表（有用例钉住）。
  - ✅ 随访中心（13 → 12）：完成随访的结果改用多行表单；同块补上「取消」任务的确认（原先点一下就从待随访清单里消失）。
  - ✅ 适宜技术实训考核（16 → 13）：学员原先要手打用户 ID、评语框点取消照样提交；换成表单，学员从报名名单里选。
  - ✅ 上门服务（18 → 16）：派单 / 完成换成表单（服务记录多行）；同块补上「取消」工单的确认——原先点一下就作废居民的申请。
  - ✅ 急救绿道录节点（20 → 18）：节点原先要按「1=发病，2=呼救…」输序号、再手打时刻；换成表单（节点下拉）。
    时刻刻意不预填「现在」：绿道时效取的正是它，补录时顺手确定会记错。
  - ✅ 家医签约（22 → 20）：履约类型原先要手打英文代码、备注框点取消照样记；换成表单（类型下拉）。
    同页补上「解约」的确认——原先点一下就解约，页面上没有恢复入口。
  - ✅ 室内质控失控处理（24 → 22）：两连问的第二问点取消交上空措施、被后端拒回；合成一个表单，两项必填。
  - ✅ 公卫事件处置记录（26 → 24）：两连问的「执行人」点取消照样提交；合成一个表单，取消就是不记。
  - ✅ 双通道申报审核（28 → 26）：批准 / 驳回的意见框点取消照样审了；换成表单，取消就是不审。
  - ✅ 更正 / 注销申请审核（29 → 28）：「通过」的意见框**点取消照样通过——改写患者主索引，注销申请则直接注销档案**。
    换成表单，通过前写明这一下会改什么，取消就是不审（e2e 按接口核对申请状态与档案姓名）。
  - ✅ 流程引擎推进 / 终止（31 → 29）：**「推进」点意见框的取消照样推到下一节点**，「终止」的原因框点取消照样终止；
    换成表单，取消就是不动（e2e 按接口核对节点与状态）。
  - ✅ 开单前互认（32 → 31）：命中可互认时 confirm「确定=互认」——**取消就是不互认**，理由框再点取消照样开单，
    想放弃开单的人连点两次取消反而开出一张重复检查。换成一个表单，互认与否显式选，取消就是不开单。
  - ✅ 危急值处置反馈 + 医生移动端（37 → 32，移动端清零）：**危急值「处置反馈」点取消照样提交，危急值就此「闭环」、处置说明为空**
    （管理端、移动端同一个坑）；移动端出报告的「确定=危急值」确认框，想放弃时点取消，报告照样出、还记成非危急值；转诊审核点取消照样通过 / 退回。
    管理端换 `spdModal`，移动端加卡片内表单 `cardForm()`；e2e 各先点取消、按接口核对状态没动，再走完主链路。
  - ✅ 病理标本（40 → 37）：核收 / 拒收 / 推进换成表单。**拒收原因后端只收五个标准项，弹窗却让人手打**——现在从后端给的选项里选；
    推进按环节只问该环节的数（原先一律问"蜡块数或切片数"、点取消照样推进）。
  - ✅ 互联网+诊疗（43 → 40）：回复三连问换成表单。医师姓名原先留空就记成"医师"、处方号写错悄悄变成"不关联"；
    现在医师必填、处方号原样交后端判。署名本身由客户端自报（后端不取登录账号），另立 P2-41。
  - ✅ 集中审方（46 → 43）：审方意见与处方点评换成表单。点评原先是 alert 看要点 → confirm「确定=合理，取消=不合理」→ 两连问，
    **想放弃时点取消记下的却是"不合理"**；审方意见点取消照样提交。点评要点改放在表单上方（`spdModal` 加了可选的只读说明 `opts.intro`）。
  - ✅ 绩效改进任务（50 → 46）：登记进展 / 提交完成 / 确认关闭 / 退回四处换成表单。**原先在弹窗上点"取消"照样提交**——
    "确认关闭"点了取消任务照样关，"退回"点了取消照样退回且没有理由；表单里取消就是放弃（e2e 先点取消再核状态）。
  - ✅ 门急诊文书页（54 → 50）：告知书签署两连问（关系要手打 `self/spouse/…` 代码）、拒签两连问换成表单，关系五选一——
    **拒签原先把关系写死成"本人"，家属代为拒签的在证据上记成了患者本人**（P1-70）。
  - ✅ 住院页（59 → 54）：转床两连问（手输病区与床位 ID）改从本机构空闲床位里选；开医嘱去掉「确定=长期，取消=临时」
    确认框（想放弃时点取消，反而开出一条临时医嘱）；病案首页四连问换成表单并补上转归与备注——**转归原先从不送、
    后端默认"好转"，住院治愈好转率与死亡率取的正是它**（P1-68）。做这一页撞见同文件三个读接口不收调用方身份（P0-19）。
  - ✅ 物资页（66 → 59）：签合同三连问（手输供应商 ID）、验收两连问、使用登记两连问换成 `spdModal`；供应商改下拉
    （只列在用的——后端只认在用的），验收数量默认按采购量，使用登记的标题里带上效期。做这一页撞出两条缺陷：
    **合同金额这种带分的小数，`spdModal` 的数字框根本提交不了**（P1-67，已修，影响全站十个字段）；
    **条码裸插进按钮属性，经办写的条码能在管理员浏览器里执行脚本**（P0-18，已修）。e2e 一条走完审批 → 签合同 → 验收
    （先超量被拒）→ 使用登记，并读回合同金额 12345.67。
  - ✅ 孕产妇页（97 → 87）：访视三连问、分娩四连问、新筛输序号加确认框、标记高危原因，五组换成 `spdModal`；
    顺带补上后端早就收、页面一直没入口的孕周 / 新生儿数 / 儿童身长体重 / 各处备注，分娩机构改下拉。
    e2e 一条走完建册 → 产检标高危 → 分娩 → 儿童访视（体重写错报人话）→ 新筛异常进高危儿清单。
  - ✅ 手术页 + 医生移动端手术页签（77 → 66）：排班四连问、术中记录四连问换成表单；手术间改下拉；
    **术中记录的转归原先写死成"好转"（P1-66，质量指标的治愈率/死亡数取的正是它）**，现在可选，术前/术后诊断也能填。
    `spdModal` 顺带限高可滚动（字段多的表单会比视口高，fixed 遮罩下"确定"够不着）。
  - ✅ 人财物页（87 → 77）：挂科室（手输科室 ID）、登记变动（输序号再三连问）、签合同（三连问）、物资出入库（输序号再两连问）
    四组换成 `spdModal`；科室只列员工所在机构的（后端本就只收同机构科室），调入机构改下拉；调动没选机构、合同止期写错
    都由后端报人话（前端不另抄规则）。e2e 一条走完科室 → 员工 → 挂科室 → 变动 → 合同进到期提醒 → 物资领用记录。

### 🚀 正式上线前（收口中）

> 这一节记录「割接前必须解决」的口子。共同点是**平时完全看不出来，偏偏在最需要它们
> 的时刻失效**——所以只能靠主动找，等它自己暴露就是在生产上暴露。
> **凡是本容器内做不完的，写清卡在哪、需要谁**，别用"已完成"糊过去。

**已做（本轮）**
- ✅ 割接前配置体检脚本 `server/scripts/preflight_check.py`：把十几项「缺失时不拒启」
  的配置一次列全（退出码 0/1/2 + `MEDPLAT_PREFLIGHT_ACK` 认账机制）。
- ✅ 容器生命周期：`start.sh` 生产路径改 `exec "$@"`（此前 SIGTERM 到不了 uvicorn，
  滚动更新每次都是 SIGKILL 硬杀）；补两份 `.dockerignore`。
- ✅ 三个跨机构写接口补归属校验：停医嘱 / 修订报告 / 支付退款。
  ⚠️ **同时暴露了守卫的分母问题**——既有横向越权门禁诚实地报 95.5%，但它的分母
  **结构性地排除了「归属要经一跳外键才看得到」的对象**，这三个洞正因此从未被报出来。
  扩大分母是独立任务，未做。
- ✅ 生产渠道硬门：微信 `official` 缺 appid 不再静默退回 Mock（那是认证绕过）；
  生产不受理无真实网关的线上支付（否则会把单据标成已支付而实际未收到款）；
  配置枚举值拼错一律拒启，不再静默走默认分支。
- ✅ 备份/恢复主线三脚本补自动化检查（此前连语法检查都没有）。
- ✅ **可观测性与出网韧性四条**（本次提交）：监控计数的超时/复用/熔断；未捕获异常
  计入错误率并带 X-Request-ID；`MEDPLAT_LOG_FILE` 覆盖全部 18 个 logger；
  `/api/health` 库不通回 503。详见提交说明与 `docs/运维手册.md`。
- ✅ **spd/care 专业侧界面补齐（2026-08-27，用户裁定"根据建议完成开发"）**：
  此前 22 条路径 / 31 个端点全部无前端调用，而需求对照表把它们逐条写成了
  成员端/个案管理师端/中心端承诺条目的实现依据——后端交付了、界面缺失。
  现补齐：管理端新增「服务团队成员端」（监测录入与趋势/量表逐题评估与统计/
  干预模板与批量干预/宣教推送与成效/异常上报与处置）与「个案管理师端」
  （在线咨询应答·转随访/复诊计划看板·邀约留痕/健康处方）两页；中心端补
  上报任务配置面板；居民端新增「咨询」页签（发起/续聊/查看对话）——**咨询两端
  一次接通**，医生工作台那个恒为 0 的"待回复咨询"计数从此有门可进。
  `measurements/batch` 一条按需求语义豁免（设备/HIS 接入路径，不需要界面）。
  取证：`render_diff.js` 实渲三页（新页非空无假绿、中心页差异恰好只从新面板
  起始字符开始）、`--dump` 证 24 处 XSS 载荷全部转义；棘轮
  `tests/test_spd_care_frontend_coverage.py` 把"每个 care 端点必须有前端调用点
  或书面豁免"钉死（分母从路由对象现算，变异验证：删一个调用点即红）。

**本容器内做不完（需要外部资源，不是代码问题）**
- ☐ **RPO≤15min 未达标**：需要一套开了 WAL 归档的真 PostgreSQL 才能实测归档延迟。
  `tests/test_ops_wal_pitr_scripts.py` 只验脚本形状，验不了指标。
  文档已按「这是路线不是能力」标注，别当成已具备。
- ☐ **四个外部通道未做过真连通**：短信网关 / 微信公众号 / 支付网关 / 电子证照。
  各自都有 Mock 与生产硬门，但**没有一次真实握手记录**。需要四套厂商测试账号。
  **准备工作已就绪**：`scripts/channel_smoke.py`——拿到账号当天在目标环境跑一遍
  即出可入档的验收记录（微信=拉 access_token 验凭据+IP 白名单；支付=签名拉当日
  流水，只读不动钱；短信须显式 `--sms-phone` 才真发，拒绝擅发；电子证照如实报
  「平台侧尚无对接代码」）。无凭据必 SKIP 绝不假 PASS，形状由
  `tests/test_channel_smoke.py` 六条锁住。
- ☐ **信创栈未验**：达梦/金仓/麒麟上一次都没跑过。需要对应环境。

**本轮量到但没修（需 ADR，不适合夹带）**
- ✅ **审计落库移出事件循环**（P2-30 → **ADR-0016**）：`audit_middleware` 两条
  路径改 `await run_in_threadpool(_write_audit, …)`。事件循环不再陪等（登记时
  实测单次中位 2.49ms × 每个写请求）；**本请求仍等落库完成才返回**——"响应
  返回时审计已尝试落库"与吞异常两条既有保证一字不变，串行化锁（PG advisory /
  SQLite 进程锁）原样复用。fire-and-forget 与队列批量写被否：省 2.5ms 的代价
  是审计链在崩溃窗口静默丢失。AST 钉（不得直调 + 必须 await）进
  `test_audit_middleware_hardening.py`，两处变异各自转红；既有 6 条硬化用例
  （并发链完整、故障注入不拖业务）原样通过。
- ✅ **依赖锁定补决策记录与防腐化守卫**（P2-29 → **ADR-0017**）：lock 本体与
  四处安装点在 A7 批次已落地，本轮补上登记时要求的 ADR，以及守卫
  `test_dependency_lock.py` 三条：直接依赖必须全部入锁 / 锁内必须全钉版 /
  安装点必须走锁不得旁路（第三条最阴——旁路后本地与 CI 照绿，只有生产漂移）。
  三处变异验证。无 `--hash` 供应链校验是记档接受的缺口，日后可在 lock 之上
  换 pip-compile 增量补。

**卡在业务裁定（不是技术问题，不应由实现方替客户决定）**
- ☐ **ADR-0014 病种目录收敛**：慢病口径随访 90 天、慢专病治疗期 30 天，同一个高血压
  患者两个数字都进各自完成率的分母。合表当天必须二选一，**选谁都会让某一批机构的
  完成率当场变化**。需要卫健口径裁定，代码侧不动。
- ☐ **居民端是否提供平台侧随访视图**：属于加功能，不是补缺口，未擅自做。
- ✅ **已按默认路径回退（2026-08-27，用户批准"根据建议完成开发"）**：共享诊断中心
  计分曾同时挂着「需卫健口径支持」与「已实施（2026-08-22）」两句互斥记录，且查不到
  批复留痕。按治理默认（影响资金分配的口径不由实现方拍板）回退到"只按申请方计分"：
  `performance.py` 计分行退回单侧，`remote_exams_provided` **保留展示**（正是"中心
  工作量被看见但没算分"的哨兵，恢复了 2026-08-22 之前的语义），响应字段不增不减、
  非中心机构字节不变。两条哨兵用例（`test_中心出报告暂不改变得分_待卫健批复` /
  `test_互认计入申请方_中心出报告量可见但不入计分`）钉住回退态。
  **待卫健批复后恢复**：`performance.py` 端点 docstring 写明恢复点（一行 + 两条用例翻转）。

### 一行/小修（童子军级，碰到即修）
- ✅ **P2-39 三端前端把请求体校验失败显示成 `[object Object]`（2026-09-24 已修）**：422 的 `detail` 是数组，
  6 处请求帮手都 `new Error(data.detail)`，`datetypes` 写好的人话一个字都到不了用户眼前——所有表单都是。
  `shared.js` 加 `errorText`、6 处改走它；后端 422 形状没动（标准契约，改了是破坏性变更）。
  静态守卫 + 真浏览器 e2e 各一道，撤掉修复 e2e 以 `[object Object]` 转红。
- ✅ `routers/triage.py` 的 `triage_suggest` 注入了 `db: Session = Depends(get_db)`
  却**一次都没用**——每次调用白白从连接池借还一次连接。看着像是「知识库将来落表」
  的占位（模块 docstring 明写硬编码 KB 是现状不是设计），但占位不该真开会话。
  **已修（陈账，2026-09-02 核实销账）**：签名里早已没有 `db`，函数 docstring 记着
  "没有 db 形参是有意的"及原因；路线上这条一直没勾。
- ✅ 打印件验真二维码落地（**ADR-0015**）：占位框替换为真码。设计正是此前登记的顾虑
  的答案——「按单据号开放查询」违反 §8，故走**签名不透明令牌**（HMAC 域分隔派生密钥，
  sm 套件下 HMAC-SM3；不可枚举、不可伪造），公开核验端点 `GET /api/print/verify`
  只回显纸面已印字段 + 现势状态（同意已撤回会报告），限速 30 次/分/IP；密钥轮换走
  `verification_keys` previous 宽限（纸比密钥长寿）。二维码生成上移平台侧 `qrsvg.py`，
  spd 经 `platform.py` 委托回来（单向依赖方向修正）。扫码落 `/verify` 静态页，令牌在
  `#` 片段不进访问日志。回归 `test_print_verify.py` 15 条，五处变异各自转红。
  遗留（接受，ADR 记档）：无打印留痕/单张吊销——要补在方案 C 之上加打印流水表即可。
- ✅ `routers/monitor.py:79` `"success"` → `"succeeded"`（带回归测试 `test_monitor_overview.py`）。
- ✅ `spd/reporting._score` 补上机构与周期过滤。原先渲染器签名收了 `org_id` 与 `period` 却**两个都没用上**——任何机构、任何周期的报告，这一段都是同一份「最近 20 条」：甲机构的报告里印着乙机构的排名，一季度的报告里印着二季度的分数。此前登记为「需业务决策」是过度谨慎了：`period` 过滤毫无歧义；`org_id` 按 `object_type` 分派（机构/团队/村医/医师四类各自查归属）也是唯一自洽的读法，口径取「恰好属于该机构」——与本模块其余段落一致（`_screening` 等都是 `X.org_id == org_id`），报表段落之间口径不一样比少一段更难查。机构名下没有考核对象时返回空表，**不退回全域数据**（那是别家的数字）。回归四条，变异验证：退回原实现四条全红。
- ✅ `scheduler.py` `_release_lock` 校验持有者，防误删他实例锁（token 所有权 + Lua 比对删，回归测试 `test_scheduler_lock.py`）。
- ✅ 调度双跑收窄：① 锁续期心跳（每 TTL/4 秒「仍持有才续期」，`_RENEW_LUA` 原子比对+expire；实例崩溃心跳停、锁 ≤TTL 自愈；易主即停并告警；续期失败 5s 快速重试）——超 TTL 长任务不再丢锁；② `tick()` 拿锁后**重新确认到期**——锁只保证不重叠、保证不了不重复，陈旧到期快照会让已被他实例跑过的任务再跑一遍。回归 `test_scheduler_lock.py` 四条。
- ✅ 手工触发 `routers/jobs.py` 的 `run_job` 改走执行锁（口径：**加锁校验**）。抽 `scheduler.job_lock` 给调度循环与手工触发共用，占用中返回 409。**两层锁**：Redis 锁只挡跨实例，同进程内调度线程与请求线程照样重叠，且不配 Redis 是默认形态（那时锁等于不存在）——故补进程内 `threading.Lock`。回归四条，两处变异验证。
- ✅ CI 加了 Redis service，新增 `test_scheduler_lock_real_redis.py` 七条在**真 Redis** 上验：`SET NX` 互斥、TTL 确实设上、两段 Lua（比对后删 / 比对后续期）的原子语义、锁到期可被接管、`job_lock` 完整路径（拿锁→心跳顶 TTL→退出释放）。假 redis 的 `eval` 是自己写的 if，证明不了脚本本身对——两处变异（Lua 退化成无条件 del / 无条件 expire）各自转红。
- ✅ 迁移-模型**列级** parity 门（`test_迁移与模型的列集合零漂移`）：真 PG 上跑完 `upgrade heads` 后逐表比对 inspector 与 `Base.metadata` 的列集合，两个方向都查（模型有迁移没建 → 生产 UndefinedColumn；迁移建了模型没有 → 多半是删列忘写迁移）。ADR-0002 停用 `create_all` 后这是最后一处「开发 SQLite 看着正常、上线才炸」的口子。已变异验证：给模型加一列不写迁移，当场转红。**补充（2026-08-22）**：那条只跑在 integration（本地默认 skip），等于把「改模型」这个纯本地动作的反馈推到 CI，与 CLAUDE.md §7「别把 CI 当第一道防线」相悖。已补 `test_migration_model_parity.py`：同一份比对逻辑（抽到 `tests/schema_parity.py`，不是第二套实现）跑在一次性 SQLite 空库上，约 7 秒，进 test-unit。两条各守一半——SQLite 那条证明「列写全了」，PG 那条证明「PG 认这个类型」。
- ✅ 机构树体检增报**层级错位**：`tree-health` 新增 `broken_chains`（转诊阶梯上父子层级不相邻的机构，带期望层级与自根到叶链路）与 `max_depth`（信息项）；`referral_ready` 收紧为"无孤儿且无错位"。判据是层级相邻而非链路长度：市级四层合法不误报、county/city 之上的挂载（县疾控、市协作院）不参与判定，而 county→村室→村室 三层照报。回归 `test_org_tree_health.py` 七条，改函数级 client 消除顺序耦合。
- ✅ 加固 `test_monitor_overview.py`：每条用例先清空 `job_runs` 再播种，seeded 行不会被别的失败记录挤出 limit-5 窗口（断言改精确相等）；另补一条钉住"只返回最近 5 条、按 id 倒序"的窗口语义。已验证单跑/乱序均绿，且把端点 limit 改小会让新用例转红。

### 工具链
- ✅ 清掉 8 处存量 lint（5 未用 import + 2 无占位 f-string + 1 未用变量改显式 assert），`make lint` 归零；`make verify` 的 typecheck 步改为渐进式 warning 不阻断——verify 现可用。
- ✅ mypy 存量 **187 → 0**（CI 口径，全程零行为改动）。分三步：①抽 `deps.row_dict()` 收掉最大一族（`dict(query.all())` 重复 35 处，Row 与 dict 的类型对不上）；②抽 `concurrency.ensure_present()` 收掉「insert_if_absent 后重查」那族（8 处，正常路径必不为 None，但那是**推理**不是保证——并发删除会变成 500，现在给 409 与人话）；③其余逐个处理：分支复用变量改名（workflows/followup/assess/drgs）、可空外键当字典键改宽键声明、混值字典就地标注、`Result.rowcount` 用 `cast(CursorResult, ...)`。
- ✅ **lint 转阻断**（CI 实测 ruff 0 项）；`requirements-dev.txt` 给 ruff/mypy 钉上界——本仓库无 lockfile，门一旦阻断就必须可复现（实测 mypy 2.3 报 139 处、1.19 在同一份代码上报 187 处，版本飘一下结论就变）。
- ✅ **mypy 环境探针 `scripts/check_mypy_env.py`（阻断）**：`ignore_missing_imports=true` 的代价是——mypy 解析不到的库会被**静默当成 Any**，依赖它的代码全部「通过」。本轮真踩到：开发机的 `mypy` 是 `uv tool install` 的隔离环境（没有 SQLAlchemy），同一份代码本地报 **41** 处、CI 报 **187** 处，差的 146 处全是 ORM 相关。探针先 `reveal_type` 探 sqlalchemy/pydantic，是 Any 就直接失败并给修复指引，杜绝再拿假绿下结论。
- ✅ **typecheck 已转阻断**：`make verify` 去掉 `-` 前缀、CI 去掉 `|| echo ::warning`。六项能力（build/lint/typecheck/unit/integration/smoke）自此全部阻断。
- ✅ **CI 解释器与生产对齐**：两个 job 改用 `PYTHON_VERSION: "3.12"`（与两个 Dockerfile、ruff `target-version`、mypy `python_version` 同版）。切换前在 3.12 上实测过全套：compileall / alembic upgrade heads（247 表）/ ruff 0 项 / mypy 139（与 3.11 逐条相同）/ 单元 1465 passed / smoke 2 passed / app 起得来。新增 `test_python_version_alignment.py` 两条把四处钉在一起（版本一致 + 不许写死版本号，后者扫全部 workflow 且带不带引号都认）——以后升级要么四处一起改、要么用例变红（此前它们没有任何互相约束，正是漂开的原因）。

### 让 CI 变真（关联 ADR-0002）
- ✅ **冻结用起来了：四个日期敏感的契约档不再依赖「别跨午夜」**（2026-09-11，紧接 P1-53）。
  📌 关键的一行是 `conftest.business_today()` 改走 `app.clock.today`——判据与被判对象
  从此**同源**。若判据还自己问 `date.today()`，冻住服务端只会让两边**对不上**，比不冻更糟。
  改完之后那七个文件的断言**一行没动**就获得了可冻性。
  📌 冻结当场炸出一个我上一轮留下的不一致：这些档在**用例体内**还留着裸
  `date.today()`（当时判断「已经是现取、无需改」）。它确实是现取的，但问的是
  **另一把尺子**——服务端从冻住的日期推算，用例从真实时钟推算，五条当场红：
  `{'period_end': '2026-07-15'} != {'period_end': '2026-10-11'}`。
  📌 于是规则立成**组合禁令**而不是风格禁令：不冻结的档用 `date.today()` 没问题
  （全仓还有 25 个文件、73 处），**冻了就不许再绕过入口**。配一条分母自证
  （一个在用冻结的档都没有时，那条规则恒为真、绿得毫无意义）。
  📌 每个冻结档各加一条**正面断言**「本档的今天确实被冻住了」：冻结若失效，
  其余用例**也照样绿**（两边都走真实时钟、彼此一致），只是悄悄退回「别跨午夜」那个前提。
  三处变异各自转红。
- ✅ **P1-53 已清零：`app/` 里 69 处 `date.today()` 全部收敛到 `clock.today()`，
  跨午夜那条窗口就此归零**（2026-09-11）。
  📌 收敛的动机不是整齐，是**能冻得住**。这条线走了三步：
  ① 测试里的模块顶层快照改成现取 → 窗口从一小时缩到毫秒（当时就写明**没有归零**）；
  ② 服务端仍在请求发生时取日期 → 要归零就得让时间可冻，而可冻的前提是只有一个取日期的地方；
  ③ 本轮把 69 处收敛成一个入口，`freeze_business_date()` 换掉 `app.clock.today`
  一个属性即覆盖全平台。日期敏感用例不必再依赖「发请求与断言之间不跨午夜」。
  📌 **连 `from …clock import today` 一并禁掉**：它在 import 那一刻就把函数绑成本模块的
  名字，换 `app.clock.today` 对它无效。收敛前 `surgery.py` 正是这么写的——
  一个模块就足以让「冻住业务日期」变成「冻住了大部分」，而**冻住了大部分比没冻更难排查**：
  你会以为时间是固定的，于是把偶发的差一天当成 flake 去重跑。
  📌 **只冻日期不冻时间戳**（`models.utcnow()` 照旧走真实时钟）。把时间戳一起冻掉，
  会让「对账日切在 UTC」那类问题（P2-35）在测试里消失而线上还在。
  回归 `tests/test_business_date_freeze.py` 六条；四处变异各自转红
  （重新出现 `date.today()` / 退回直接 import / `today_str` 自己问时钟 / 冻结不还原）。
- ✅ **CI 跨午夜必红：九个测试文件在模块顶层快照时间**（2026-09-11，run 552 实证）。
  那一轮单元档跑了 **62 分钟**、23:42 开跑，**跨过午夜**，12 条整齐地差一天：
  `{'at': '2026-09-10'}` vs `'2026-09-11'`、`{'today': …}`、`{'period_end': …}`。
  **不是 flake 是真缺陷**：判据（`TODAY = date.today().isoformat()`，import 那一刻取）
  与被判对象（服务端在请求时才算）取自**两个时刻**，中间隔着整轮测试的时长；
  重跑一次就绿，正是它被当成 flake 的原因。CI 什么时候跑不由人定，深夜那班必然踩上。
  改成用例里现取（`conftest.business_today` / `utc_today_str`），窗口从一小时缩到毫秒。
  📌 **两个"今天"必须分清**：`clock.today()` 是本地业务日期，`models.utcnow()` 切出来的是
  UTC 日（对账走这条）。挑错了 CI 也照样绿——runner 时区是 UTC，只有东八区的开发机会红。
  📌 **`app/clock.py` 的 docstring 声称 `test_clock.py` 有一条扫描用例——那个文件从来不存在。**
  今天第二次被「一句声称守卫存在的注释」咬（上次是 `batch_tasks` 说 `claim_task` 有校验）。
  已补 `tests/test_clock.py` 18 条：裸 `datetime.now/utcnow` 零基线、`date.today()` 只减不增
  基线 69、`app/` 与 `tests/` 不得在**import 时**取时间（判据含函数默认值与装饰器参数——
  `def f(d=date.today())` 是同一个缺陷，只按"顶层赋值"判会整族漏掉）。
  📌 自己的判据又窄了一次（今天第五次）：不解 import 别名时，`from datetime import date as _d`
  能让两道闸门都报绿；解了别名反而多数到一处真实存在的（`spd/jobs.py`），68 是漏数。
  七处变异各自转红（含别名与非别名两种写法）；`date.today()` 绕过入口登记为 P1-53，
  对账日切在 UTC 登记为 P2-35（未改，业务口径待裁定）。
- ✅ 覆盖率门禁转阻断（落地实测 87%，门槛 70%）；集成/迁移门在真 PG 上转阻断。
- ✅ 生产禁用 `create_all`（lifespan 环境分支 + 守卫测试 `test_adr0002_create_all_guard.py`）、`start.sh` 启动前 `alembic upgrade heads`、README 修正复数（ADR-0002 已 Accepted）。

## Next（治理逐块推进，只进不退）

- ✅ **「只认一种写法」第二问：闸门只认 `db.get(M, id)`；顺着这条线量出 36 个「连谁在问都没有」的患者读接口**（2026-09-10）。
  - **机制先补上**：读侧越权闸门的分母是 `db.get(带 patient_id 的模型, …)`，
    而 `db.query(M).filter(M.id == 路径参数).first()` **语义完全相同**。
    带对照组实测：拿掉守卫、保持 `db.get` → 红；拿掉守卫、换等价写法 → **绿**。
    已放宽——**放宽后仍是 24 个，今天没有任何端点靠等价写法脱账**，补的不是今天的洞是明天的；
    两 arm 变异各自转红。
  - ⚠️ **顺着这条线摸出一个更大的形状**：闸门问的是「按 id 直取」，
    而**列表族整个不在它的分母里**。换一个**结构性**判据重量——
    「这个 GET 端点有没有任何形参绑到调用方身份」，没有就**在结构上不可能**按归属收口。
    实测 **46 个**命中，其中 **36 个响应带个体身份字段**、10 个只吐聚合（信息项）。
    风险最集中的三个吐的是**联系方式与证件号**而不只是 `patient_id`：
    `spd/followup.py:list_call_tasks`（phone）、
    `spd/population.py:list_group_members` 与 `:list_service_applies`（phone + ehc_no + birth_date + name）。
  - **一条都没擅改。**「这份名单该给谁看」是业务问题——质控抽样、结果互认、集中审方
    按设计就是跨机构的（横向越权闸门里已有带书面理由的同类豁免），
    而群组成员、服务申请、外呼任务显然不该全域可见。与 P1-49 同一性质，
    故新增 `tests/test_unscopable_patient_reads.py` 把 36 个钉成**只减不增**并打印覆盖面自证，
    同时登记进 `docs/待裁定事项清单.md`（**并入 P1-35/48/49 那条，不另起编号**——
    同一个决定不该拆成两处）。缺口从「看不见」变成「数得出、不许变大」。
  - 📌 **同一天第三次被「判据只认一种写法」咬，而且都是咬我自己写的判据**：
    ① 这条新闸门第一版按形参**名字**判有没有 `user`，被 `access_logs:my_access_logs` 打脸——
    它写的是 `patient: Patient = Depends(current_resident_patient)`，确确实实收了口，
    改成按**绑没绑身份依赖**判；
    ② 分母第一版只认列名**恰好等于** `patient_id`，漏掉 `child_records`（列叫 `guardian_patient_id`），
    而挂在它上面的 `maternal:list_high_risk_children` **正是待裁定清单里被单独点名
    「这一族里风险最集中」的那一个**——放宽到「以 `patient_id` 结尾」后 33 → 36。
    **仍看不见**完全不带这类外键的人物表，这一格已写进用例，归 P1-35 逐张裁。

- ✅ **把上一轮的观察反过来用，第一枪就中：一句 docstring 能冒充守卫，四道闸门中招**（2026-09-10）。
  上一轮的结论是「判据越贴近**一种写法**，越容易在改动之后悄悄失效」。
  那就先查最普遍的那一种：**匹配用的源码里含不含 docstring**。
  `ast.unparse(fn)` 与 `ast.dump(fn)` **都含**，而闸门恰恰是在函数源码里找守卫名。
  - **中招的那句话，正是知道有竞态但还没处理的人最会写的**：
    `并发下同一患者重复拉黑会撞唯一约束抛 IntegrityError，留待后续处理。`
    闸门把它读成「有守卫」。
  - **带对照组实测**（对照必须先红，否则实验无效——第一版实验就是因为对照也绿而作废的）：
    `appointments:add_blacklist` 真拿掉 `except IntegrityError` → **红**；
    同样拿掉、docstring 里加那一句 → **绿**。
    `chronic:list_followups` 拿掉 `assert_patient_visible` 同理，**而这条是 §8 红线**。
  - **中招四处**：并发冲突闸门 1、横向越权闸门 3（写侧 / 批量 / 读侧）、加密列检索点闸门 1。
  - 📌 **最值得记的一点**：分页棘轮的 `_code()` **早就修过同一个坑**，
    而且它的 docstring 白纸黑字写着——
    > 这个坑两个方向都会错：docstring 里提一句 `paginate(` 会让一个真有缺陷的端点被静默排除
    > ——那是假阴性，**比多算一条严重得多**。

    **预言应验了，只是应验在另外四道闸门上。教训被写下来了，却没有外推。**
    所以这次不是再各自修一遍，而是抽 `tests/astcode.py` 共享，五个模块统一改用；
    下一道闸门写出来时，用的就是已经剥好的源码。
  - **四道闸门的基线逐个复核不变**：分页 102、唯一表 0、越权覆盖率 95.5%、加密列 3。
    两组「对照 / 实验」重跑，修复后**两arm 都红**。
  - ⚠️ **新规则自己的边界，写在用例里**：它按变量名（`fn`/`func`）认「这次 unparse 的是不是函数节点」
    ——**它自己就是一条「只认一种写法」的判据**。第一版还带 `node`/`f`，实测**四处误报**
    （那几处 unparse 的是语句节点，与 docstring 无关），已收窄；另有 1 处**明知故犯**的裸调用
    带书面理由（分页棘轮的前提自证：先证明 docstring 里那句 `.limit(5000)` 还在，
    再证明 `_code` 把它剥掉了——正好是本规则要守的那件事的活样本）。
    对**新写**的闸门这条规则管不着，只能靠 `astcode.py` 的模块 docstring 提醒下一个人。
  - 顺带把自己那条 `"import astcode" in src` 的子串判据改成 AST 判据——
    `import astcode as x` 会让子串判据通过（自审时试出来的）。**要求别人别只认一种写法，自己先别犯。**

- ✅ **换了一个轴：不问「分母够不够宽」，问「它今天还会不会红」——18 道闸门，3 道不会**（2026-09-10）。
  「写的时候变异验证过」是**历史声明，不会重跑**。判据可能已经不再匹配（正则、AST 形状、扫描面变了），
  而闸门照样报绿。这一轮把那句话做成了可执行的：脚本逐条改坏源码 → 只跑对应闸门 →
  要求它变红 → 还原（每轮结束核 `git status` 为空）。
  - **15 道当场变红**：出网超时、角色名拼写、查表兜底、默认页、加密列检索、生产凭据、
    子系统边界、created_at 棘轮、核心数据不可变定义、契约棘轮、迁移数据安全、
    PII 出口、横向越权、并发冲突、ADR-0002。
  - **① 核心表冻结闸门没红**：`test_核心表结构已冻结` 比的是 `sorted(table.c.keys())`——**只有列名**。
    把 `patients.gender` 从 `String(8)` 改成 `String(16)`，它照样绿；而 `schema_parity.diff_schema`
    比的同样是列名集合。于是**冻结核心表改列类型，全仓没有任何一道闸门会红**，
    可 CLAUDE.md §4 明写这类改动要先写 ADR。
    补了两样：`FROZEN_CORE_COLUMN_TYPES`（五张表逐列的类型 + 非空，配一条"两份快照必须同步"），
    以及更要紧的 `tests/test_schema_snapshot_freshness.py`——`docs/schema/SCHEMA.md` 必须与模型
    **逐字节一致**。那份文件带着列类型 / NOT NULL / 索引 / 外键 / 唯一约束，覆盖全部 **259 张表**，
    恰恰是另两道闸门都不看的东西；而它此前的现势性**全靠人记得重跑脚本**。
    生成器抽出 `render()` 以便不落盘比对，输出逐字节不变。
    ⚠️ **说清它不保证什么**：它比的是「模型 ↔ 文档」，不是「模型 ↔ 数据库」。
    「改了模型、也重跑了脚本、但没写迁移」仍然不红——那格归 parity 管，而 parity 目前只比列名。
    把 parity 扩到类型要先做跨方言的类型归一层，**已登记，未做**。
  - **② 列表分页棘轮没红**：把 `labqc:list_lots` 的 `paginate(...)` 换成 `.all()`，
    欠账数**纹丝不动**（102 → 102）。它的判据是「硬编码 `.limit(数字)` 且无翻页参数」，
    而退回 `.all()` 既没有 `.limit(N)`、也不叫截断——它是**无上界**返回，比静默截断更糟，
    却正好落在判据之外。于是 P2-8 五个批次的成果没有任何东西守着：
    欠账可以一边变小、已切好的一边被改回去。
    补了**反方向棘轮** `PAGINATED_ENDPOINTS`（135 个端点，只许多不许少，配防腐烂用例），
    与契约闸门的 `FULLY_GOVERNED` 同一纪律。
  - **③ 前端转义闸门没红**：把 `esc(MAP[x] || x)` 退成 `(MAP[x] || x)`，闸门照样绿——
    `BARE_MAP_FALLBACK` 的正则不允许外层括号。顺手实测还漏 `??`（空值合并同样是兜底）。
    两处都属于**该闸门 docstring 自己声明过**的「与缺陷无关的拼写差异不该成为后门」。
    已放宽（真实代码上仍是 **0 处**，没白送任何一行），并补正反两组 parametrize
    （四种该抓、三种不该误伤——放宽判据必须同时钉住不误伤，否则它会先被加豁免、再被删掉）。
  - 📌 **一个跨闸门的观察**：没红的这三道，判据都是**只认一种写法**的
    （一种比较方式、一种截断方式、一种拼写）；红了的那些，判据要么**从元数据推导**
    （列类型、模型注册表），要么认的是**语义**（有没有守卫调用）。
    **判据越贴近「一种写法」，越容易在改动之后悄悄失效。**

- ✅ **第十二个形状：换个文件也能绕过闸门——顺带把 WS 定向广播的过滤依据改成现查值**（2026-09-10）。
  上一条记的是「换个**关键字**（`async def`）绕过检查」，这条是同一族的第三副面孔：**换个文件**。
  闸门只走 `app/routers` 与 `app/spd/routers`，于是 `main.py` 的 `/api/health` 与四个静态页入口、
  `ws.py` 的 `/ws/notifications` **在任何一份自证输出里都看不到**——分页闸门报「覆盖面自证」、
  契约闸门报「覆盖率 100.0%」，说的都只是它们各自扫得到的那 946 个。
  - ⚠️ **`ws.py` 那条不是无所谓的一条**：它推的是危急值
    `{patient_id, item_name, conclusion}`（由 `routers/exams.py` 发出）。
  - **逐条查了这条通道**：4 个 `broadcast()` 调用点——缺药与危急值两处都带了 `target_org_id`（定向）；
    `jobs.py:_alert` 与 `spd/platform.py:broadcast` 两处不带（全量广播），
    但载荷只有 `{type, title, count}` 聚合计数、不含任何个体身份，判为正当。
  - **真正要紧的是 `_send_all`**：它每次投递前都调 `_authorize(..., cached=True)`
    **现查**出 `(ok, org_id, role)`，**却只取 `[0]`**——定向过滤读的仍是 `connect()`
    登记的握手旧值。**今天这个洞走不通，靠的是两件巧合**：改角色会顺带吊销令牌
    （`users.change_user_role` 推 `token_valid_from`），而 `User.org_id` 全仓没有任何更新路径
    （AST 查过：三处 `.org_id =` 分别落在 `Asset`/`Employee`/`SpdCandidate` 上，
    人事调动改的是 `Employee` 不是 `User`）。
    **「今天恰好不会漂」不该是一条推患者数据的通道的安全依据**——哪天补一个「调动用户机构」的接口，
    长连接就会继续收原机构的危急值，而且没有任何用例会因此变红。
    已改用现查值并回写 `meta`（现查值本就在手边，零额外查询；今天两者恒等，故行为不变），
    并补回归用例：直接改库造出「机构变了、令牌还有效」这个状态，撤掉修复即转红。
    ⚠️ **这条动的是鉴权路径（§8）**，改动本身是「把已经算出来的值用上」而非换机制，
    但仍请复核。
  - **另把「扫描面之外的 6 个端点」做成逐条写明理由的清单** `OUTSIDE_ROUTER_SCAN`：
    新增未登记即红、登记项失效也红（两个方向各变异验证过）。
    判据说的是「我查什么」，这份清单说的是「我不查什么」——
    **后者才是下一个人判断「这盏绿灯值多少」的依据。**

- ✅ **第十一个形状：AST 闸门看不见 `async def` 端点——扩完分母第一次跑就点出一处真缺陷**（2026-09-10）。
  `ast.walk()` 走出来的 `async def` 是 `ast.AsyncFunctionDef`，**不是** `ast.FunctionDef` 的子类。
  而四个扫路由的闸门（stage14 并发 / stage15 横向越权 / 列表分页棘轮 / 分页排序稳定性）
  共 **17 处**枚举点只写了 `isinstance(n, ast.FunctionDef)`——每一个 async 端点因此
  **整个不在分母里**：规则没被删、豁免清单没变短也没变长、闸门照样报绿。
  与「路由拆进子包，一层扫漏掉整包」是同一种失效的另一副面孔：那次换的是**目录**，这次换的是**关键字**。
  - ⚠️ **不是纸面风险**。全仓 946 个端点里 3 个是 async，扩完分母跑第一遍，
    读-改-写规则当场点名 `billing.py:payment_callback → order.trade_no = trade_no or order.trade_no`。
  - **缺陷本身**：它在 `serialized_on` 临界区里做读-改-写，**却没有先 `db.refresh(order)`**——
    而 `serialized_on` 自己的 docstring 正写着「进临界区之前拿到的 ORM 对象是锁外读的旧值，
    锁到手不会自动刷新」。后果不是 500，是更难发现的一种：两路回调**同时**到达时，
    两路的锁外幂等检查各自在旧快照上通过；赢家先把单置 paid、写下自己的网关流水号；
    输家进临界区后再置一次 paid，用**自己的** trade_no 覆盖掉赢家那个、把 paid_at 也改成自己的时间，
    然后回 200 `idempotent=false`。对账时这张单与网关那边对不上，而平台侧看不出任何异常。
    同一批判据在**顺序**重放下回的是 409「回调与已有流水不符」——
    **同一件事，并发下和顺序下答案不一样**，这本身就是缺陷。
  - **修法**：锁内先 `refresh` 再复检状态；锁外与锁内两处幂等判定抽成同一个
    `_settled_callback_result`（写两遍就会漂）。回归用例是**确定性**的——
    把赢家那一笔精确塞在「锁外检查」与「锁内」之间，不靠调度运气；
    撤掉修复即转红（200 且赢家流水号被覆盖）。
  - **另外 2 个 async 端点逐条核过是干净的**（附件上传有 `assert_owner_visible(..., write=True)`；
    spd 佐证上传在居民端体系内），所以这次扩分母对四个闸门是**零基线变化**，只多出这一处真命中。
  - **新增 `tests/test_async_endpoint_scan_coverage.py` 两条钉住它**：一条**防空转**
    （全仓一个 async 端点都没有就报错，并把清单打印出来——省得规则哪天变成空转还没人发现）、
    一条**零基线棘轮**（扫路由的测试模块里 `isinstance(..., ast.FunctionDef)` 必须同时认 async）。
    两条各做了变异验证（撤回一处枚举点 / 把扫描面缩到没有 async 的子包，分别转红）。

- ✅ **两个形状各扫一遍，两遍都干净——负结果照样记账**（2026-09-10）。
  - **除零**：151 处 `/`、`//`、`%`，剔掉 pathlib 的 `Path / "x"`（同一个运算符、完全不同的语义）
    与字面量分母后剩 37 个候选，逐条追去向，**0 处可触发**——`sd`/`weight`/`volume_cap`/`ratio`
    各自有 `Field(gt=0)`、`ge=1`、422 或提前返回挡着。
    📌 **我的判据认不出「提前返回式」的守卫**（`if not xs: return` / `raise` / `continue`），
    37 个里绝大多数是这个原因误报——所以它的产物是**候选**不是结论，必须逐条读。
  - **用户可控字符串被直接解析（失败即 500 而不是 422）**：分母 946 个端点函数
    （与路由对象数逐个对上，说明没缩水），命中 8 个，**0 处真缺陷**——四处入参本就是
    `DateStr`/`OptionalDateStr`、`emergency` 的 `occurred_at` 有 `field_validator`、
    `users:audit/export` 在**开流之前**先 `resolve_business_date(until)`
    （这处尤其要紧：它是 NDJSON 流式响应，`ValueError` 真漏进生成器就不是 500 而是 **200 + 半截文件**）、
    `1/body.ratio` 有 `Field(gt=0)`，还有一处把 Session 参数 `db` 当成用户输入的假阳性。
    已知的 P1-52（`maternal.py` 的 `float(bp.split("/")[0])`）**没出现在命中里是对的**——
    它包在 `try/except` 里，属于「静默不标高危」那条已登记的欠账，不是 500。
  - 两次都**没有落棘轮**：判据误报率过高（提前返回认不出、pathlib 同符号不同义），
    落下去只会被豁免名单填死。

- ✅ **CI 抓到的真竞态：对账重跑在 `commit()` 与 `refresh()` 之间被另一路删掉 → 500**（2026-09-10）。
  run 544 的 integration 档红在 `test_八路同日对账在真PG上恰得一张单`，报
  `InvalidRequestError: Could not refresh instance '<ReconciliationBatch ...>'`。
  **没有当成 flake**——`billing.run_reconciliation` 的端点契约就是「同日重跑覆盖上一批次」，
  所以**另一路调用同一个端点的人就是删除者**，这条路径天然会撞。
  - **原代码已经想清楚了两处并发**：删旧走批量 DELETE（避开 ORM 逐对象删的 SAWarning）、
    INSERT 撞唯一约束回 409。漏的是**第三处**：我们自己 `commit()` **之后**、`db.refresh(batch)`
    之前，对方的批量 DELETE 正好落进这个窗口，把我们刚提交的批次连同差异明细一起删掉，
    refresh 找不到行 → 未捕获即 **500**。而这一路其实**已经提交成功**，只是成果被覆盖了。
  - ⚠️ **是真 PG 才现形的窗口**：本机原样跑 8 遍 **0 次**复现。临时在 commit→refresh 之间
    塞 50ms 探针把窗口撑开 → **必现**，报错与 CI 逐字一致；修完带着探针再跑绿，
    再把探针撤掉确认无残留。**"复现不了"不等于"不存在"，等于"窗口比我的调度粒度窄"。**
  - **修法与既有 409 同口径**：那张批次已不在库里、差异明细也被一并删掉，
    返回它等于给出一份查不到的对账单——让调用方去看赢家那张。
    回归用例做成**确定性**的（把 `Session.refresh` 打桩成必抛，断言 409 而非异常），
    撤掉修复即转红。
  - **按形状全仓扫过，确认不必立棘轮**：「commit 后 refresh」有 148 处，但绝大多数不是缺陷——
    只有端点自身语义是「删旧建新」时，并发调用同一端点的人才会删掉你已提交的行。
    按「同一函数既批量删该模型、又新建同一模型」精确筛，全仓**恰好 2 个函数**命中：
    本例，与 `fund.py:distribute`（删 `FundDistribution` 再整套重插）。
    **另一个逐条核过是安全的**：它 commit 之后不 refresh，而是 `_settlement_out()` **重查**一遍——
    被删的是明细行、`FundSettlement` 那行没人删；并发插入撞唯一索引已经回 409
    （连同自己的 DELETE 一起回滚）。所以真正的判据不是「删旧建新」，
    而是「删旧建新 **且** commit 后还拿着旧对象不重查」——全仓 1 处。
  - 📌 **一条自检值得固定下来**：第一版筛选脚本报 **0 处**——它只认 `db.add(Model(...))` 内联写法，
    而这里是 `batch = ReconciliationBatch(...)` 再 `db.add(batch)`。
    **一个找不到我已知那一例的扫描，本身就是坏的**；写完扫描先拿已知案例验一遍，再信它的 0。

- ✅ **第十个形状：又问了三道闸门「分母够不够宽」——这次一个盲区都没有，如实记下来**（2026-09-10）。
  这一问前两次连中（并发闸门漏事件订阅者、PII 出口闸门漏非 JSON 导出），这次三道全部干净。
  **负结果照样记账**：省下一个人再问一遍，也说明这条线开始收敛了。
  - **`test_pii_query_point_guard.py`（加密列检索点）比我准备补的还严**：分母从 `Base.registry`
    按列类型 `EncryptedPII` 推导；扫描根是 `app` + `scripts`——**本来就不只扫路由**；
    而且**把自己的盲区逐类数出来打印**（手写 SQL 0 处、动态取列 0 处、动态 `filter_by` 1 处，
    那 1 处是 `concurrency.upsert_unique` 的实现，调用侧另有检查）。
    我原本要查的「原生 SQL 里的等值比较它看不看得见」，**它自己已经声明并且在监视**。
  - **`test_api_contract_governance.py` 早就单独处理了非 JSON 端点**：`_declares_non_json_media()`
    要求在 `responses` 里写明媒体类型才算达标，并**明确否决了「设了 `response_class` 就算」**
    这条更弱的判据（理由写得很清楚：StreamingResponse 根本不写媒体类型）。
    ——有意思的是，我上一轮在 PII 出口闸门那边用的正是 `response_class`，
    而那里只是用它**圈分母**（不是判达标），两处目的不同，不冲突。
  - **`test_stage15_horizontal.py` 确实只扫路由，但它的规则语义不适用于后台代码**：
    横向越权问的是「这个**用户**能不能看这个患者」，而定时任务/订阅者根本没有用户，
    `assert_patient_visible(db, user, …)` 无从谈起。**照搬上一轮的结论就会写出一条假规则。**
    改问一个语义对得上的问题——「后台代码会不会把患者数据投递给错的人」——再实测：
    11 个非路由模块查带 `patient_id`/`org_id` 的模型，其中唯一**决定收件人**的是 `notify.py`。
  - **顺着 `notify.py` 追到底**：收件人 = 该患者本人的 active 账户 ∪ 代管该档案的家属账户。
    注意到一处不对称——账户查询过滤了 `status == "active"`，家属关系查询**没有任何状态过滤**。
    查下来是**非问题**：`resident_family_members` 根本没有状态列（只有 id/account_id/patient_id/
    relation/created_at），解除代管走硬删（`remove_family_member` 的 `db.delete(member)`，
    且按 `account_id == account.id` 收口，只能解自己的）。没有「已撤销的监护人还在收消息」这回事。
  - 📌 **一个跨闸门的观察**：这五道闸门里，凡是**把自己的盲区写出来、并且数出来**的
    （PII 检索点、契约治理），都没有意外；有盲区的那两道（并发、PII 出口）恰恰是
    **只写了判据、没写「我看不见什么」**的。判据说的是「我查什么」，盲区声明说的是「我不查什么」，
    后者才是下一个人判断「这条绿灯值多少」的依据。

- ✅ **第九个形状：把「闸门的分母够不够宽」这一问推到 PII 出口闸门，找出第二个结构性盲区并补上**（2026-09-10）。
  上一轮在并发闸门上问这一问，问出了「闸门盯着路由，而写库的不止路由」。**同一问对每道闸门都成立**，
  这次挑 PII 出口闸门，因为 §8 是安全红线。
  - **盲区**：`test_privacy_egress_guard.py` 的分母是各 APIRoute 的 `response_model` 递归展开的 PII 字段名，
    `_classify()` 对「既没有 PII 字段、又不是裸 dict/Any」的端点直接 `continue`。而 **CSV / NDJSON /
    HTML / SVG / 文件流导出**的 `response_model` 要么是 `str`、要么根本没有——正好落进那条 `continue`。
  - **落在盲区里的恰恰是最该看的两类**：`certs.py:export_death_report_cards_csv` 的列头就写着
    「身份证号」（还是法定上报口径，见 P1-50）；`printing.py` 那 12 个打印件渲染患者身份证号与电话。
    **两处本来就写对了**（分别走 `_death_card` 与 `mask_id_card`/`mask_phone`）——但那是**靠人记得**，
    它们哪天停止脱敏，原有三条用例一条都不会红。这正是这道闸门当初被写出来要解决的问题
    （它的开篇写着「红线靠人记得」），只是红线的另一半没被盖住。
  - **实测 20 个端点：13 已脱敏 + 7 确实无个体身份 + 0 未处置。** 那 7 个逐条核对后写进
    `EXPORT_NO_PII_REVIEWED`（附件是上传字节流、两份报表是聚合、两个二维码是 URL、
    传染病报告卡列头无姓名证件、审计 NDJSON 只含 path）。
  - **审计那条顺带查了一条链**：NDJSON 导出里有 `path`，而全仓有三个端点把 `ehc_no` 放进路径模板
    （`/api/archive/{ehc_no}`、`/api/patients/{ehc_no}`、FHIR `Patient/{ehc_no}`）。查下来是安全的：
    `request.url.path` **不带 query**，且审计只记 `_AUDITED_METHODS`（POST/PATCH/PUT/DELETE），那三个都是 GET。
    结论写进了清单条目，免得下一个人再查一遍。
  - ⚠️ **判据必须两条并用，这是实测出来的**：只看 `response_class` 会漏掉 certs/infectious 两份 CSV
    （没声明 response_class，只是 `return _csv_response(...)`）；只看函数源码会漏掉 printing 那 12 个
    （HTML 由共用帮手 `_page()` 组装）。**单独任一条只认得 8 个，并起来才 20 个。**
    ——**第十二次栽在「判据比它想描述的那件事窄」上，而这次是在写「防判据太窄」的闸门时栽的。**
    幸而先做了一次干跑才发现，没直接写进用例。
  - 三处变异各自转红：新增一个吐 id_card 的 CSV 导出 / 死因卡停止脱敏 / 清单里塞一条已不成立的条目。
  - 顺带更正原 docstring 里「裸 dict/Any 契约是这道守卫**唯一**的结构性盲区」那句——已被证伪。
  - **这轮没有修到缺陷，堵的是「哪天出缺陷没人知道」。** 与前几轮「查过是干净的」不同：
    那些是确认代码对，这次是**把对的状态钉住**。

- ✅ **第八个形状问「闸门够不够宽」，结果闸门自己漏了一类模块——修一处真竞态、扩一格扫描面**（2026-09-07）。
  挑这个靶子是因为「判据够不够宽」是本轮最有产出的一问。并发闸门
  （`test_stage14_concurrency.py`）本身写得极好：唯一表清单**从 `Base.metadata` 推导**
  （UniqueConstraint / column.unique / index.unique 三路），不是硬编码；还有逻辑唯一清单、
  逐表书面理由、递归扫子包。它的 docstring 甚至写着「路由拆成子包是这类扫描最常见的失效方式」。
  - **但它只遍历 `app/routers` 与 `app/spd/routers`。** 实测非路由模块里还有 **22 处**
    往带唯一约束的表 `add()`。**同一个失效方式再往外一层**：变的不是目录层级，是模块类别
    ——闸门盯着"路由"，而写库的不止路由。
  - **其中一处是真竞态**：`app/spd/subscribers.py:on_encounter_created` 对
    `spd_candidates`（`UniqueConstraint(patient_id, program_code)`）做 check-then-act，
    而它是**事件订阅者**，在正常业务流程中同步运行。
  - **而且它的后果比普通竞态更重，因为事件总线的两条契约在这里互相拆台**：
    契约 2 说「订阅者不得连累业务，本模块统一兜住」，`publish()` 确实包了 try/except；
    但契约 1 说「同事务、只 add 不 commit」——**这条 INSERT 推迟到发布方 commit 才执行**，
    那时早已离开契约 2 的罩子。**实测复现**：

        publish() 正常返回，投递 1 个订阅者 —— 没有任何异常被兜住
        业务侧 commit：✗ IntegrityError: UNIQUE constraint failed: spd_candidates...

    也就是说并发下**登记就诊会 500 并回滚**，起因是一个可装卸子系统的订阅者。
  - **修法照抄仓库已有的助手**：`concurrency.insert_if_absent`（SAVEPOINT 圈单行，
    撞了只退这一行），它的 docstring 描述的正是同一个形状。`concurrency` 在
    spd 的平台白名单里，spd 内另有两处已在用——不是新约定。
    如实记下残留：极少数竞态下上面那条 `screening` 已 flush、不随之退掉，统计口径上多算一次筛查；
    要连它一起退需手写 SAVEPOINT 包两行，那是复制助手逻辑，按 §6 优先复用。
  - **扫描面只扩一格**：把事件订阅者纳入闸门（零基线，变异验证——撤掉修复当场点名
    `spd/subscribers.py:on_encounter_created → SpdCandidate(spd_candidates)`）。
    **种子与调度器那 21 处没有一起扩**：它们是启动期一次性的幂等预置，
    「要不要也强制处理 IntegrityError」是另一个判断，已登记 **P2-33** 待裁定；
    同时登记的还有「要不要在 `publish()` 里给每个订阅者包 SAVEPOINT」——那是**改事件总线契约**，
    影响所有订阅者，属架构级变更（§9 要 ADR）。本轮只做单点收口，没动总线。

- ✅ **第六个形状：`except: pass` 吞异常——195 个处理器里只有 5 个，4 个正当，第 5 个是临床缺口（登记 P1-52，未修）**（2026-09-07）。
  - **AST 扫全部 `app/**.py`：195 个 except 处理器，体内只有 `pass` 的 5 个。** 逐条读：
    `monitor.py:298`（Redis 心跳，`# pragma` 注明「Redis 抖动不该影响业务请求」）、
    `inpatient.py:462`（`except ImportError`，DRG 模块 M12 上线前的可选依赖）、
    `state_store.py:91`（关一条作废的 Redis 客户端，`# noqa` 注明「关不掉就算了」）、
    `ws.py:144`（关一条已断开的 WebSocket，`# noqa` 注明「无需再关」）——**四条都正当且都写了理由**。
  - **第五条是真的**：`maternal.py:147` 用 `float(body.bp.split("/")[0])` 取收缩压、`≥140` 自动追加
    风险因素「妊娠期高血压可能」，解析失败 `except ValueError: pass`。而 `VisitCreate.bp` 是
    **`bp: str = ""`，完全没有格式校验**。实测：

        '140/90' → 标   '140 / 90' → 标   '  145/95  ' → 标
        '140-90' → 静默不标      '140／90'（全角）→ 静默不标
        'BP140/90' → 静默不标    '高血压140/90' → 静默不标

    **全角斜杠尤其可能**——中文输入法默认就打 `／`。血压值照常入库、医生看得见，
    但自动风险标记不触发，且**不报错、不留日志、界面上与「血压正常」一模一样**。
  - **又是同一族**：判据比它想描述的那件事窄，缺口不报错，只是给出一个确信的错结论
    ——这次的错结论是「这个孕产妇不是高危」。
  - **没修，登记 P1-52 待裁定。** 三条路各有代价：收紧 schema 加 `pattern` 会 422 掉现在收得下的数据
    （破坏性变更，§7）；放宽解析会让**更多**孕产妇被自动标高危，**那是临床判断不是代码清理**；
    只加告警日志能把「静默」变成「可观测」，但临床上什么也没改善。
    **哪条都不该我替人定**——与 P2-8 停在 P1-49、P2-21 停在 URL 形状是同一个判断。
    定级我按 P1 记（参照 P1-50「法定报表被静默截断」同属「静默的数据/判定缺口」），但定级本身也请一并裁定。
  - 顺带一提：这 5 个里 **4 个都写明了吞异常的理由**，唯一没写理由的那个正好就是唯一有问题的那个。
    「吞异常必须写清为什么」这条纪律，在这个仓库里是**真的有判别力**的。

- ✅ **第五个形状：`Number(表单值)` 把留空的可选数字字段送成 0——修 2 处，而这次扫描差点让我改坏 3 处**（2026-09-07）。
  - **先把前提改对**：检查点里我自己写的是「`Number(空串)` → NaN → JSON `null`」。
    **实测 `Number("") === 0`**（`Number(null)`、`Number(" ")` 也都是 0；只有 `Number("abc")` 才 NaN）。
    方向反了而且更糟：NaN 会被 `JSON.stringify` 变成 `null`（看得出"没填"），
    **0 是个长得很正常的值**，后端分不出"用户填了 0"和"用户没填"。
  - **`formJson()` 本来就是对的**：`if (v === "") continue` —— 空字段整个不进 body。
    137 处调用点全部安全。风险只在**绕过它、自己 `new FormData` + `Number(...)`** 的 57 处。
  - **逐表单核对**（按 `$("#form-id").onsubmit` 反查同 id 的 `<form>` 里那个输入框有没有 `required`），
    57 → 6 处字段可为空。然后逐条读代码：
    - **3 处其实早就写对了**：`#org-form` 的 `parent_id` 与 `#user-form` 的 `org_id` 是
      `f.get(x) ? Number(f.get(x)) : null`，`#esb-ep-form` 的 `rate_limit_per_min` 是 `… || 60`。
      **我的正则只匹配 `Number(f.get("x"))` 本身，看不见外面那一层三元/`||`**
      ——**判据比它想描述的那件事窄，这次的后果不是漏报而是「差点去改本来是对的代码」。**
      本轮同一形状的第十一次，也是第一次差点造成实际损害。
    - **1 处是真误报**：`threshold` 的后端默认值本来就是 `Field(default=0, ge=0)`，送 0 与默认一致。
    - **2 处是真的**：`#slot-form` 的 `capacity`、`#rx-form` 的 `days`。两者后端都是
      `Field(default=1, ge=1)`，送 0 直接违反 `ge=1` → **用户拿到一句 422，而不是默认值**。
  - **严重度如实说：低。** 这两个输入框都带 `value="5"` / `value="7"` 预填，
    要**手动清空**才会触发；后果是一句看不懂的报错，不是脏数据。仍然修，因为修法小、安全、
    且仓库里已有两处同款写法可抄（`#og-form` 的「空字符串要去掉，否则后端按『指定了牵头机构 0』处理」、
    `#pay-form` 的 `if (f.get("amount"))`）——**这条形状被人撞见过一次、修过一处，剩下的没扫**。
  - 取证：`{a:1, capacity:Number("")}` → `{"a":1,"capacity":0}`（修前）；
    改成展开式条件键后 → `{"a":1}`（修后），有值时仍是 `{"a":1,"capacity":5}`。
    92 个夹具页 render_diff **零差异**（改的是提交处理器，不碰渲染）。
  - **没有加守卫**（本轮第三次不加，理由各不相同）：这次是**判据自己不够准**——
    最后一轮 6 个候选里 3 个是假的，正是因为静态正则看不到外层兜底。
    50% 误报的守卫会被豁免名单填满然后失效。前两次分别是「既有守卫已经抓得住」（P2-3）
    与「汇点静态判不出来」（§8）。**三次都是先证明、再决定写不写。**

- ✅ **账本扫完之后转去查代码：验 §8 转义红线，26 处候选全是误报，并据此决定「不补这条守卫」**（2026-09-07）。
  账本被验干净不等于代码干净——本轮最大的几笔（分页截断 9 处、查表没兜底 14 处、存量选择 6 处）
  都不在账上。所以扫完账目就换回按形状查代码。
  - **一共查了四个形状，全部干净**（每个都先剥注释保行号、先自证数了全部、再逐条追去向）：
    1. **219 个 submit 处理器漏 `e.preventDefault()`** → **0 处**。（漏了会整页刷新、
       SPA 状态全丢，看起来像"莫名其妙掉线"。）
    2. **`e.target.dataset` 取不到**：`<button data-x><b>字</b></button>` 点到子元素时
       `e.target.dataset.x` 是 undefined、按钮静默失效。全仓 **232 个** 带 `data-*` 的元素里
       只有 **3 个内含子元素**（`core.js:362` / `m/m.js:340` / `m/m.js:835`），
       而这 3 个**全都处理对了**：两个走 `e.target.closest(...)`，一个是逐元素绑定
       （`chip.dataset` 与 `e.target` 无关）。
    3. **后端 `x = ….first()` 之后不判 None 就取属性** → AST 扫 **151 处**，
       **0 处**未判空。
    4. **§8 转义红线**（下面详述）→ **0 处**违反。
  - **再查红线**：发现现有转义守卫只盯 `MAP[x] || 兜底` 那一族，**没有任何守卫拦得住新写一处
    裸 `${服务端字段}`**。按「自由文本字段名且未包 esc()」扫七个脚本，命中 26 处——
    **逐条追去向，26 处全部安全**：`setMsg()` 走的是 `textContent`；`alert`/`confirm`/`prompt`
    是纯文本对话框；几处是 API URL 的路径段；其余落在 `panel()`/`table()`/`barChart()`/`spdModal()`
    里而这四个组件自己会转义（`spdModal` 连 title、label、name、value、placeholder 全转）。
  - **两条结论**。①**§8 真的守住了**，这是验出来的不是假设的。
    ②**这条红线不该用静态守卫补**：判据已经算精确，误报率仍是 **26/26 = 100%**——
    因为决定安全与否的是**汇点**（innerHTML 还是 textContent/alert/URL），而汇点静态判不出来。
    硬上这么一条，只会被豁免名单填满然后失效，**比没有更糟**。
  - 这与 P2-3 那条删掉的守卫是同一个判断，只是理由不同：那次是「既有守卫已经抓得住」，
    这次是「判据抓不准」。**两次都是先证明，再决定写不写**——而不是默认「多一条守卫总没坏处」。
  - 真正在起作用的是 ADR-0009 那条路：**把转义收进组件**。继续沿它走，不另起炉灶。

- ✅ **把「开放条目逐条实测复核」走完：P2-11 销账，P2-1/6/7 按实测改写，顺手修两处失效文档**（2026-09-07）。
  这条线本轮已经四次证明账目会失真，而复核的产出一直高于「照着账本做事」。
  - **P2-11 早就修好了**（第四条这样的）。全仓已无 `collect_internal`；它现在叫
    `collect_encounter_probe`，docstring 开头就写「**探针**（不是采集器）……一行都不落库」，
    并说明**不再注册给 HIS/EMR**。实测 `COLLECTORS` 只有 `{'publichealth'}`，
    `unregistered_types()` 会把 HIS/EMR 列进实施待办并告警。那段 docstring 里的话值得抄下来：
    **「看起来是好的比『明摆着没接』更危险：没接会有人去接，看起来好的没人会去查。」**
  - **顺手修两处失效文档**（童子军法则，只改事实不改政策）：
    ① `spd/collectors.py` 模块 docstring 的小节标题还写着「内置采集器：`internal`」、
    描述它从**就诊与住院**同步——而实际内置的是 `publichealth`、读的是**公卫随访**；
    照着找 `COLLECTORS["internal"]` 会扑空，更糟的是会以为就诊/住院正在被同步。
    ② `CLAUDE.md` 三处指向已不存在的 `models.py`（ADR-0008 拆包后是 `app/models/` 17 个文件），
    其中 §4 的「`Money`（`models.py:44`）」实际在 `models/_base.py:29`——照着行号去看是空的。
    **只改路径，政策措辞一字未动。**
  - **P2-1 数对得上但分类不对**：259 张表里无前缀 184、`spd_` 60、`tcm_` 5、`esb_` 4、`ph_` 2、`cssd_` 2。
    但 **`spd_` 那 60 张不是债**——它是 CLAUDE.md §4 要求且被 `test_子系统的表一律带前缀` 强制的，
    拿掉才会红。真正「各自发明」的只有四套共 **13 张**。改写之后这条的规模才看得清：13 张，不是 75 张。
  - **P2-6 的「三种来源」准确，但三处都有正当理由**：`dataquality.py:361`（明细是在 Python 里
    跨规则算出来的列表，`paginate` 吃不了）、`metrics.py:549`（返回信封不是裸列表）、
    `portal.py:1330`（空名单早退，P2-8 第二批我自己加的，docstring 已写明理由）。
    **症结不是「三种来源」而是「总数有两种承载方式」**：走 `paginate` 的总数在响应头，
    自带信封的总数在 body。要统一得先定信封标准，而定了就是**改响应字节**，
    与 §11 冲突——需要裁定。
  - **P2-7 一字不差**：`get` 456 / `post` 415 / `patch` 52 / `delete` 15 / **`put` 1**
    （唯一那处 `printing.py:759`）。账本上偶尔也有完全准确的条目，验一遍才知道是哪些。
  - 📌 **本轮同一个错误又犯了一次，而且就发生在复核「别人数错了」的那一条时**：
    P2-6 第一遍没剥 docstring，数出 17 处（真值 3）。**知道这个坑、正在因为这个坑复核别人，
    仍然会踩** ——所以判据里必须写进「先剥注释」和「先自证我数了全部」，靠记是记不住的。

- ✅ **复核 P2-2 与 P2-3：一条数错了并上棘轮，一条实测后销账**（2026-09-07）。
  开放条目里凡是我没亲手验过的，先验再谈做不做——本轮这已经是第三次证明账目会失真。
  - **P2-2 的「3 组近碰撞」是错的，真值 10 组**，而且错因就是本轮反复栽的那一个：
    数它的正则只认老模板 `revision = "..."`，而 94 个迁移里有 54 个是新模板
    `revision: str = "..."`——**只看见了 40 个文件**。判据比它想描述的那件事窄，
    就会给出一个看着很确定的错答案，而这个数本来是要拿去排优先级的。
  - **危害不是审美**：`down_revision` 写错一位会落到**另一条真实存在的迁移**上——
    alembic 不报「找不到该 revision」（那个 id 真的在）、迁移图仍连通、
    `make build` 的双 head 校验也过，链条却被悄悄改了顺序。而
    `test_migration_model_parity.py` 是**从空库 upgrade heads 再比模型**，
    它发现得了「最终结构不对」，发现不了「最终结构对、可升级路径顺序不对」。
  - **存量 10 对改不掉**（revision id 已写进每个已部署库的 `alembic_version`，
    改名等于让在跑的库找不到自己的当前版本），故只上棘轮
    `tests/test_migration_revision_id_hygiene.py`：十对逐条列出可审、不许再新增，
    外加一条**覆盖面自证**（每个文件都真解析到、无重复 id）——
    **那条自证正是为了拦住我这次犯的这个错**。三处变异各自转红
    （新增近似 id / 正则退回只认老写法 / 两个文件同一个 id）。
  - **P2-3 两条实测后都不成立为「待修的债」，销账**：
    ①「`spd/platform.py` 遮蔽标准库」**是错的**——Python 3 没有隐式相对导入，
    包内模块不会遮蔽顶层标准库模块；实测 `platform is app.spd.platform` 为 False，
    两者 `__file__` 各指各的。
    ②「两个 `RuleError` 不同类」**是对的**（互不继承，`except RuleError` 只抓得住导进来的那个），
    四个导入点目前全部绑对。
  - **为②写过一条专门守卫，实测之后删掉了**：两个方向的误导入各做一次变异，
    `test_子系统只依赖白名单内的平台能力` 与 `test_平台不得依赖慢专病子系统`
    **各自当场转红**（`rules` 不在 `PLATFORM_ALLOWLIST` 里），而且它们报的是
    「越了边界」，比「绑错了类」更准。**冗余守卫只会稀释信号**——
    先证明既有守卫抓不住，再写新的；这次是抓得住。

- ✅ **复核 P2-19 并登记 P2-32：一条前提过期、一条从没记过**（2026-09-07）。
  按检查点「先读 ADR-0009 再决定要不要做 P2-19」查下来，两件事都不是原以为的样子。
  - **ADR-0009 已经全部兑现完了**：Recommendation 批的是「先 D 后 B」，方案 D（`$`/`esc`/`api` 合一）
    2026-08-22 落地、方案 B（`panel()`/`table()`）十七批收官。**`crudPage()` 从来没被裁定过**
    ——它只出现在方案 B 描述里的「等组件」举例中，而同一份 ADR 的 Disadvantages 就写着
    「组件抽象一旦定错，改回来更贵」。所以自行发明一个 `crudPage()` 正是 ADR 自己警告的那件事。
  - **P2-19 的前提过期了，按现码重测改写**（与本轮早先「8 条开放 P2 里 6 条描述的是已修好的代码」
    同一种账目失真）。原文列的四样里三样已不成立：91 个 render 里 `panel()` 318 / `table()` 260 /
    `statusTag()` 33 / `postAction()` 218 / `formJson()` 137 处调用，手写外壳只剩 17 处
    （7 处代码 + 10 处 `class="panel hidden"` 点击容器，各有理由且被棘轮钉住）；
    加载态也早就集中在 `route()` / `loadService` / `loadArchive` / `loadSpd` 里。
    **真正还剩的重复是 208 处手写 `.onsubmit = (e) => { e.preventDefault(); postAction(...) }`。**
    顺带一提：我这次数出手写外壳 17 处，与收官小节记的「7 处代码 + 10 处 hidden 容器」对得上
    ——**独立重数一遍能对上，是那份账可信的证据**，不是多余功夫。
  - **P2-32（新登记）：后端发了分页总数，三套前端一个都读不到。** 实测 `deps.paginate` 在
    **135 个调用点、57 个路由文件**上设 `X-Total-Count` 并接受 `offset`/`limit`（P2-8 本轮整改的产物），
    而三套前端的 `api()` **全都只 `return data`**，把响应对象连同响应头一起丢掉
    （`core.js:38`、`m/m.js:47`、`m/doctor.js:32`）；全仓读 `X-Total-Count` 的地方 **0 处**，
    发 `offset=` 的调用点 **1 处**（`core.js:301` 指标下钻，自带一套专用翻页）。
    **这不是本轮引入的回归**——本轮迁的那些端点，`limit` 默认值一律取它原来的硬编码上限、
    首屏字节没变（更早就在用 `paginate` 的那批没有逐个回溯，不替它们打包票）；
    但两件事同时成立：后端算出来的总数**写完就扔**，而列表截断对用户**依然不可见**
    ——看到 100 行的人无从判断究竟只有 100 行还是 5000 行。**那正是 P2-8 从未覆盖到的前端那一半。**
  - **两条都只登记、不动手**，理由是同一类：P2-19 要先补 ADR 定清楚抽什么；
    P2-32 要裁定的是**交互形状**（翻页控件 / 加载更多 / 只显示「共 M 条」），
    而且改 `api()` 的返回形状是三套前端所有调用点的共享契约，属破坏性变更（CLAUDE.md §7）。
    与 P2-8 停在 P1-49、P2-21 停在 URL 形状是同一个判断。

- ✅ **P2-21 修掉正确性那一半：存量选择（localStorage 里的 id）没跟列表对过账**（2026-09-07）。
  - **先评估再动手，评估结果推翻了原条目的判断**。原条目写的是"不可分享 / 不支持前进后退 /
    跨标签污染"——听起来是体验问题。全仓扫 15 个键逐个分类之后，真正的代价是**6 处会出错的代码**：
    这些 id 跨会话、跨标签页持久，而它指向的对象会消失（住院记录会出院，基金池/协作分组/专病
    会被删或变得不可见）。两种坏法都不报错。
  - **① 静默指错人（最重的一条，涉及临床写入）**。住院临床文书的 `<select>` 只列在院记录，
    存量 id 出院之后**没有一个 option 带 `selected`**，浏览器于是显示第一条——而下面四个面板
    与三个**写入**表单仍然指向那条旧记录。这一条读代码看不出来，它依赖浏览器对"没有 selected"
    的默认行为，所以补了取证夹具 `clinicaldocs_stale`（存量选 #78、#78 已出院、#77 在院）
    真渲染一遍：

        修之前  <option value="77" >…</option>   面板 = "这是已出院的78号的病程" / 钱医生
        修之后  <option value="77" selected>…    面板 = #77 的记录 / 张医生

    屏幕上写着 `#77 患者31`，`#note-form` 却 POST 到 `/admissions/78/progress-notes`
    ——**病程记录写进了另一个人的档案**。出院是必然发生的，不是边角情况。
  - **② 永久卡死（5 处）**。子资源取数排在 `#page-body` 赋值之前（实测行号 orggroups 1239<1240、
    fund 1342<1349、diseaseprograms 1558<1563、outpatientdocs 1111<1116），而后端对不存在的
    对象一律 404（`_pool`/`_get`/`_program`/`encounter_completeness`）、对看不到的患者 403
    （`assert_patient_visible`）。一抛就被 `route()` 的 catch 把整个 `#page-body` 换成一行错误
    ——连"换一个"的那张列表、那个输入框都渲染不出来；而 id 在 localStorage 里不会自己消失，
    于是这一页对这个用户**每次进来都是同一行错误，连清空筛选都做不到**。统一申请单最容易撞上：
    手输一个本机构没关系的患者 ID 就是 403。
  - **修法照抄仓库里已经写对的地方，不发明新约定**：有权威列表的 4 处走新的 `pickedId()`
    ——专病页本来就写对了一半（`programs.find((p) => p.id === picked)`），只是那份校验**只兜住了
    渲染、没兜住排在它前面的取数**；手输 id 的 2 处没有列表可校验，改成取数失败退化为"那一段
    报错"而不是掀掉整页，`renderCost` 的 `.catch(() => null)` 本来就是这么写的。
  - 守卫 `tests/test_frontend_picked_id_guard.py` 四条，五处变异各自转红：**注册表覆盖**
    （新增一个 localStorage 键必须表态"它是不是一个指向某条记录的 id"——分类本身就是那次思考）、
    列表型必须过 `pickedId`、**`pickedId` 真做了成员校验**（否则上一条只是在数括号）、
    手输型的取数必须容错。第一条按**区间**跳过 `pickedId` 自己那行 `getItem(key)` 而不是按名字跳过
    ——按名字跳的话，任何一处写成 `getItem(key)` 的裸读都能从底下溜过去（已用反证变异验过）。
  - 91 个夹具页 render_diff 逐字符一致（成功路径没改一个字节），唯一的差异是新加的取证夹具。
  - **顺手把同一形状推到另外两套前端查了一遍，结论是它们本来就写对了**（读过、不是假设）：
    居民端 `m/m.js:334` 的家庭成员切换（"代管成员被解除后回落到本人"）与医师端
    `m/doctor.js:599` 的查房住院记录（`admissions.some(...)` 后退到第一条）**各自就是
    完整的那条规矩**，而且两处的选中项都只留在内存里、根本不进存储；两套的取数也都
    包在写子容器的 try/catch 里（`loadService` / `loadArchive` / `loadSpd`），
    抛了只报那一段、外壳不掉。**管理端是三套里唯一没写的那个**——这与 P2-22 同形只是
    方向相反（那次落后的是居民端）。据此做了两件事：`pickedId` 的 docstring 改成引这两处
    真正的样板（原先只引了专病页那个"写对一半"的）；守卫的注册表扫描扩到
    `m/m.js` 与 `m/doctor.js`、并把 `sessionStorage` 一并纳入（医师端用的是它，
    同样跨刷新存活）——不是因为那两套现在有问题，而是让"往里塞一个持久化的选中项"
    也得先表态。变异验证：给居民端的 `viewingPatientId` 加一句 localStorage 读取，
    守卫当场点名 `m/m.js:320`。

  - **剩下的一半刻意没做**：把状态搬进 URL 才能解决"不可分享 / 前进后退不响应"，但
    `location.hash` 现在**整体就是 page id**（`route()`/`nav()`/`pageAllowed()`/导航栏 `data-page`
    全按全等匹配），加 `#page?k=v` 要改路由核心；而且每次改筛选都会压一条 history，
    现有的"写完 localStorage 直接调 `route()`"必须同时拆掉，否则 `hashchange` 会双渲染。
    这是**改整个管理端 URL 形状**的交互契约变更（书签、文档、e2e 都吃它），按 CLAUDE.md §9
    该先有 ADR——已在 `docs/TECH_DEBT.md` 写清事实待裁定，与 P2-8 停在 P1-49 是同一个判断。

- ✅ **P2-20 修掉：默认页靠 `PAGES[1]` 硬编码下标取**（2026-09-07）。
  - **它不是「不优雅」，是一插就崩**：`PAGES[0]` 是 `{ group: "总览" }`——分组标题不是页面
    （没有 id/title/render），所以 `PAGES[1]` 指到驾驶舱纯属排列的巧合。
    实测往头部插一个分组（加一组菜单而已，多正常的改动）：`PAGES[1]` 拿到
    `title=undefined render=undefined`，`page.render()` 直接抛 TypeError
    ——**整个管理端路由不起来**，而插分组的人完全想不到会碰这里。
  - **同一个意图上一行本来就是按 id 写的**（`location.hash… || "dashboard"`），
    只有兜底那两处退回了下标。改成 `homePage()` 按 `HOME_PAGE_ID` 取，
    并留第二重兜底（首页被改名/删掉时退到第一个**真能渲染**的页面）——
    那种情况退化成「进了别的页」而不是「拿到分组项然后白屏」，**前者看得见**。
  - 守卫三条，各自变异转红：形状（不许再按下标取）、闭环（首页 id 真在表里）、
    以及一条**前提自证**——「首项确实是分组」。第三条不是为了拦谁，是**把「为什么
    下标不能用」钉在代码上**：哪天首项变成真页面，它会红，提示重新读一遍路由再决定，
    而不是想当然以为下标又安全了。
  - 91 个夹具页 render_diff 逐字符一致（改的是路由兜底，不碰任何页面渲染）。

- ✅ **P2-25 修掉：裸 `${MAP[key]}` 查不到时页面显示字面量 `undefined`**（2026-09-07）。
  - **判据这次是「太宽」而不是「太窄」**：按 `${MAP[任意表达式]}` 扫出 27 处，
    超过一半是误报。逐类排掉三种「查不到不可能发生」的形状——数组下标
    （`colors[si % colors.length]`）、键来自 `Object.keys(自己)` 的循环（`ROLE_NAMES[k]`）、
    被三元守着（`NOTIFY_LINK_PAGE[x] ? … : ""`）——只剩 **14 处**真的。
    **判据比缺陷宽会淹在误报里**，与越权那边是同一条教训、方向相反。
  - **兜底必须经 `esc()`，这不是风格是必须**：`MAP[x] || x` 的 `x` 是服务端数据，
    不转义正好变成 P2-23 那个洞。实测过：`<img src=x onerror=alert(1)>` 会原样进 DOM。
    安全写法只有 `${esc(MAP[x] || x)}` 一种——而这不是新发明，
    `pages-mgmt.js:884` 本来就这么写，本轮是把其余 14 处拉齐。
  - 取证两头都有：91 个夹具页 `render_diff` **逐字符一致**（映射命中的路径一个字节没改），
    外加直接跑未命中路径的前后对比（`undefined` → `chemical`）。
  - 守卫**与 P2-23/24 分成两条**：那两条管未转义（XSS），这条管没兜底（显示缺陷）。
    **严重度不同的东西挤进一条守卫，迟早因为噪声被加豁免，那条守卫就废了。**
  - 📌 **第七次栽在「注释不是代码」上**，而且这次还多栽一层：剥块注释时直接删掉，
    报出来的行号与原文件差了几十行。**注释不是代码，但注释占的行数是**——
    块注释要换成等量换行。已写进守卫的 `_strip_comments` 与自证用例。

- ✅ **P2-22 修掉：居民端靠中文文案判断掉线**（2026-09-07）。P2-8 等裁定期间挑的第一条。
  - **根因比条目写的更靠前**：不是「正则写得脆」，而是 `api()` 里
    `throw new Error(data.detail || …)` **把状态码就地丢了**，上层只剩文案可用。
    而且那条正则里的 `401` 分支**形同虚设**——它只在后端不给 `detail` 时才命中
    （那时消息才是 `请求失败(401)`）；只要给了 detail，状态码早没了。
    也就是说「按状态码兜底」这层保险，实际上从来没生效过。
  - **失效方式是静默的**：后端把 `detail` 改一个字，居民端就不清本地登录态、
    不回登录页，只弹一句看不懂的错误——不抛异常、不变红、不写日志。
  - 改为 `api()` 把 `status` 挂在错误上、`authApi` 只认 `err.status === 401`。
    **不是发明新约定，是把异类拉齐**：管理端 `core.js:35` 与医生端 `doctor.js:30`
    本来就先判 `resp.status === 401`，`shared.js` 的注释还早写着三套 401 处理各不相同。
    改完后端新增任何一种 401 都自动覆盖，改文案也不再影响。
  - **跨层两头都钉**（4 条守卫，三处变异各自转红）：前端不得再按文案判 +
    `current_resident` 的**每条**失败路径必须是 401。只钉一头的话，
    另一头改了照样静默失效——那正是原缺陷的成因。
  - 📌 **写守卫时又栽了同一个坑（第六次）**：我在 `authApi` 的注释里原样引了旧正则
    来说明改了什么，守卫于是在**注释里**看见那几个中文片段，当场判定「又按文案判了」。
    仓库里 `test_frontend_panel_component.py:336` 早有同名同实现的 `_strip_comments`，
    也是被同一个坑逼出来的。**注释不是代码**这件事，靠记是记不住的，得写进判据。

- ✅ **复核 TECH_DEBT 里 8 条开放的 P2 条目：6 条描述的是已经修好的代码**（2026-09-07）。
  这不是清扫，是**让账重新可信**——账是每一轮排优先级的输入，
  里面躺着已修条目，下一个人就会照着它白花一轮。逐条给了证据：
  - ~~P2-9~~ `PLATFORM_MODELS` 全仓已无此符号；~~P2-14~~ `monitor.py:125` 拼写早已改对
    （**它正是 CLAUDE.md §12 童子军法则举的那个例子**，修完条目没销）；
    ~~P2-15~~ 已有 `_SCORE_OBJECT_ORG` 按 object_type 解析归属；
    ~~P2-16~~ 两个入口已共用 `is_suspect_risk`；~~P2-17~~ ADR-0005 收敛三级后「值域不一致」
    成了刻意的；~~P2-13~~ 两处死 import 现带 `# noqa` 并注明理由。
  - **P2-10 改判**：`register_collector`/`set_call_provider` 不是死代码，是注释里写明的
    **待接入扩展点**——原条目按「从未调用」记会误导下一个人去删。
  - **P2-12 改判并升级**：`region_stats` 声明了 `period` 却完全忽略，**这不是「未用参数」
    是给调用方一个错答案**——卫健端按月查区域分析，拿到的是全时段数据且无从察觉。
    与本轮那一族同形：**声明了却不兑现，比没有这个参数更糟**（同样的话我早写进
    分页棘轮的盲区说明里，那边说的是 `offset`）。修它要产品先定义 period 语义
    （「截至该月仍在管」还是「该月新纳管」），在那之前既不该乱实现也不该悄悄删参数。
  - **顺带发现第九例同病并登记 P1-51**：`region_stats` 的 `total` 与年龄/性别分布
    全算在 `.limit(20000)` 的样本上；同函数还有一处 `db.query(SpdMeasurement).count()`
    **不带机构过滤**，在已收口的端点里返回全域计数。
- ⚠️ **P2-8 的 A 类到此清空，剩余 96 个全部等裁定**（B 34 / C 8 / D 49 + 5 个已各自定案）。
  **分页整改这条线在没有人裁定之前做不下去了**——继续做只会是把暴露面悄悄放大。
  - 分类脚本这次又被自己咬了一口（**第五次同一形状**）：`spd/workbench.py:region_stats`
    明明有收口，但它走的是模块内的 `_scope()` / `_apply_scope()`，名字不在 `GUARDS` 里，
    于是被判成「没有收口」。**B 类的数因此报大了，而那个数是给人拿去裁定的。**
    已改成顺着模块内辅助函数**递归两层**找收口，A 从 2 涨到 5、B 从 36 降到 34、D 从 50 降到 49。

- ✅ **P2-8 第六批（2026-09-24）：A 类（已有收口）8 个逐个判完，A 类清零，基线 102 → 92**。
  P0-19 / P0-22 / P0-38 补上收口之后，报表把 8 个端点归进 A 类（「可直接切」）。逐个读前端怎么用之后，只有 5 个是纯分页：
  住院医嘱清单 / 执行记录、门急诊处置 / 护理记录、慢专病转诊超时预警，照五批的六项检查切 `paginate`（转诊预警排序补 id 尾键）。
  另三个不是：**体温单**升序截断砍错了端（P1-81，新记的看不到）、**流程待办**按角色筛在截断之后（P1-82，待办被挤空）——
  这两个是缺陷，各自单独修、单独提交；**卫健工作台**的 `.limit(20)` 是「考核结果排名」前 20 名的展示上限，登记为误报。
  住院医嘱两条原本在 P1-49 待裁定名单里（理由是没有收口），P0-19 按患者可见性收了口，前提不再成立，名单 11 → 9。
  报表剩 85 = A 0 / B 33 / C 8 / D 44，全部卡在 P1-49 的「谁该看见全部」上；另有 3 条是同日 P1-50 / P1-51 去掉上限时没随手降的基线，一并补记。
- ◐ **P2-8 第五批 5b（2026-09-07）：15 个纯分页整改，基线 117 → 102**。
  与 5a（修两处预警截断）分开提交。两处不是照抄就能过：
  - **`appointments:list_slots` 补 `id` 尾键**：按 `(slot_date, slot_time)` 排，
    而号源表的唯一索引是六元组，同一个「日期+时段」上**按设计**并排着各机构各资源的号源。
    居民端同一张表的 `/me/slots` 在第二批已经补过——**同一张表的两个入口，缺陷也是成对的**。
    变异验证：撤掉尾键，零豁免闸门当场点名 `appointments.py:list_slots`。
  - **`surveillance:list_pathogens` 显式传 `max_limit=1000`**：它的原上限 1000
    **高于** `paginate` 默认的 500，照默认切会让不带参数的第一页从 1000 条缩到 500 条
    ——那是**减少**返回内容，不是兼容增强。本轮 100+ 个端点里只此一处，
    但它正是第三批对抗复核提醒过要盯的那个形状。
  - 覆盖面：路由文件 96 个，用 `paginate` 的 46 → **57**；排序全序闸门 135/135 零豁免。

- ◐ **P2-8 第五批（2026-09-07）：A 类不等于"干净"——21 个里只有 15 个是纯分页**。
  基线 118 → 117。分类脚本的 A 类只说明「**有**收口」，另外五项检查照样要逐个过，
  这一批就此又捞出两个真缺陷（同一个病的第七、第八例）与一个不敢顺手修的。
  - **`drgs:in_stay_alerts`**：`.limit(500)` 截断的是**预警的输入**——按 `id DESC` 排，
    砍掉的是**较早入院**的病例，而住院越久越该预警，**砍错了端**。
    超过 500 张在院床位之后，第 501 个病例即使住院日超均值三倍也不会被报出来。
    行数由在院床位数封顶、不随历史增长（同函数的历史基线查询本来也没上限），直接去掉。
    变异验证：520 张在院床位，原实现只看见 500。
  - **`spd:referral_alerts`**：`count: len(rows)` 被 200 截断——积压 500 单时预警页显示
    「超时 200 单」，而这正是拿来判断「积压有多严重」的那个数。改成 SQL 总数。
    **列表上限保留**：它按 `created_at` **升序**排，留下的正是最久未推进的那些——
    **砍的是对的那一端**。这与临期预警那两处（升序 + 只有上界 → 砍掉最该看的）
    正好相反，说明同样是「取完再截断」，**升降序决定了它砍掉的是最该看的还是最不该看的**，
    不能一律照搬同一种修法。
  - ⚠️ **`certs:export_death_report_cards_csv` 已登记 P1-50，故意没修**：死因报告卡导出
    被静默截断在 2000 条，而 docstring 自己写着这份 CSV 供**手工网报人口死亡登记系统**
    ——法定报表少报比列表少几行严重得多。但最小正确改法需要设计：直接去上限会撑爆随后的
    `Patient.id.in_([...])`（参数个数上限），得分批或流式；保留上限而超量报错则是新增
    4xx 路径、属契约变更。**两条都不该在分页整改里顺手定**，且放开上限会同比放大
    每张卡一条的 `AccessLog` 量。
  - 另三处判为**业务规则上限、不是缺陷**，已并入棘轮的误报清单并写明理由：
    `chronic:risk_score`（`.limit(3)` = 「最近 3 次随访」的评分口径，docstring 写着）、
    `spd:followup_context`、`spd:patient_profile`（各维度各附最近 N 条）。

- ☐ **P1-49 已拆成四类形状，等人裁 D 类那 50 条**（`server/scripts/pagination_debt_report.py`，
  只读、随时可重跑，不是一次性快照）。P2-8 剩余 115 个端点：
  **A 21 个已有收口**（可直接切，纯分页整改）；**B 36 个表里有机构列却没收口**
  （补一行守卫即可，但「该不该收」要人定）；**C 8 个归属要经一跳外键**
  （`PayrollRecord.employee_id`、`MaternalRecord.patient_id` 这类，与 P1-35/P1-48 同族，
  也正是横向越权闸门分母漏掉的那一类）；**D 50 个表里没有任何归属维度**——
  只有这一类是真正开放的问题：要人回答「这是全域配置，还是归属从未定义」。
  **把一个混合数拆成四个形状，是让人能拍板的前提**——拿「97 处无收口」去找人裁，谁也裁不了。
  - ⚠️ **这份分类自己被改对过一次，教训比分类本身值钱**：第一版判据是「没有 `org_id`
    也没有 `patient_id` → 配置目录」，跑出 62 条"配置目录"，里面赫然是薪酬、劳动合同、
    消毒批次——归属列**不叫** `org_id` 而已（`employee_id` 一跳到人，或 `center_org_id`）。
    **判据比缺陷窄，于是给出一个看着很确定的错答案。** 这是本轮第四次同一形状
    （越权扫描漏 `require_admin`、棘轮把 docstring 当代码、收口统计漏
    `assert_patient_visible`、这次漏别名归属列）。**数字要写进文档给人拍板之前，
    先问一句「这个判据认得全吗」。**

- ◐ **P2-8 第四批 4b（2026-09-07）：切完 8 个有收口或按设计全域可见的端点，基线 126 → 118**。
  与 4a（修四处「统计吃截断样本」）分开提交——一个修 bug、一个做治理，混在一起 review 分不开。
  - **判据自己被校准了一次**：审计最初把 `spd:consult_messages` 判成「无收口」，
    因为扫描只认 `scope_org_list`/`scope_patient_list`/`visible_org_ids`/`stats_org_ids`
    四个名字；它实际用的是 `assert_patient_visible(db, user, consult.patient_id, ...)`
    ——**是收口，只是名字不在表里**。同一处偏差让上一条记的仓库级统计报小了「有收口」
    的那一半：**21/104 实为 26/97**，已在 TECH_DEBT 里更正。
    **「判据比缺陷窄」这条，这一轮在我自己的度量上又发生了一次**（前两次分别在越权扫描
    与棘轮的 docstring 盲区上）。数字写进文档之前得先问一句「这个判据认得全吗」。
  - **三张配置目录表没有收口是对的**（干预模板 / 问卷 / 报表模板）：它们是按设计全域可见的
    配置字典，与量表目录同类，不是 P1-49 那种缺口。审计一度给三张结构相同的表判了
    两种结论，由对抗复核纠回——**这正说明「有没有收口」和「该不该有收口」是两个问题**，
    而 P1-49 要裁定的是后者。
  - `vaccine_supply:list_batches` 的 `usable_only` 谓词下推 SQL 之后才切：留在
    `paginate` 外面会让总数头（含不可用的）与响应体（只剩可用的）对不上。
    变异验证：放回去之后头报 4、体只有 3。
  - 排序全序闸门仍是零基线零豁免：**120/120**。既有 81 条相关用例一条没改就全绿；新增 10 条。

- ◐ **P2-8 第四批（2026-09-07）：这一批基本没在「切分页」，在修同一个病的第三到第六例**。
  基线 128 → 126。20 个候选逐个核完，只有 6 个是干净可切的，**10 个卡在没有机构收口**，
  4 个是真缺陷——而那 4 个都是同一句话：**取回来的那批行不能代表全体**。
  - **`spd:assessment_stats`（上限 5000）与 `spd:edu_stats`（上限 20000）**：人数、人次、
    风险分布、逐题分布、覆盖人数、阅读完成率，全都算在被截断的样本上。与刚修的
    `quality:record_qc_summary` 是同一个病的第三、第四例。`edu_stats` 还多一层——
    它**连 `order_by` 都没有**，超过 20000 之后「取哪 20000 条」由数据库决定，
    同一份数据两次请求可能给出不同的完成率，**不是少算，是不可复现**。
    改法分两种：`edu_stats` 六个数全部 SQL 表达得了，整套下推；`assessment_stats` 的
    逐题分布要读每行 `answers` JSON，SQL 表达不了，改成 `yield_per` 流式扫全量
    ——**内存不随行数涨**（人数是患者 id 的集合、分布是小字典，都由输出规模决定），
    循环体一字未动，所以键序这类响应字节也原样保住。
  - **`vaccine_supply:expiring_batches`**：与第三批修的 `pharmacy:expiring_drug_batches`
    **一模一样的形状**——只有上界没有下界 + 按到期日升序 + 取完 500 行再用 Python 筛掉发完的，
    于是 `.limit(500)` 砍掉的恰好是「即将到期」那端。实测原实现返回 **0 条**。
    ⚠️ **对抗复核在这里拦下一个错误修法**：有报告建议顺带加 `expire_date >= today` 下界，
    那会把已过期批次从预警里删掉——而这个端点的口径明写「过期的也一并列出并标注，
    提示按报废流程处理，别让它躺在冰箱里被误用」。**加下界是把截断缺陷换成口径缺陷**，
    已按原口径只下推余量谓词，并补用例钉住「已过期但仍有余量的必须还在列」。
  - **`vaccine_supply:batch_recipients`**：`"total": len(rows)` 而 rows 被 `.limit(1000)` 截断
    ——一个批次发给 3000 人时，**召回追溯页显示「总计 1000」**，与真的只有 1000 人无法区分。
    这是召回时反查受种者的那个查询，少报意味着少通知的那些人不会有任何提醒。
    改成 SQL `count`；列表本身仍受 1000 行上限（放开要先解决它没有机构收口，见 P1-49）,
    但至少现在会**如实说出还有多少人没列出来**。
  - **P1-49 扩大，并第一次量出规模**：仍会静默截断的 125 个端点里，**104 个函数体内
    没有任何收口调用**——但这是**上限不是精确值**，其中一部分是量表/问卷/模板这类
    **按设计全域可见的配置目录**，没有收口是对的。把这两类分开数，是让这条债
    可裁定的第一步（否则「104 处越权」既吓人又不准）。同一批审计里也确实抓到了
    分类不一致：三张结构相同的配置字典表被判了两种结论，由对抗复核纠回。
  - 四处修复各自变异验证过：5000→5104、20000→20103、临期预警 0 条→3 条、召回 1000→1200。
    **其中一次变异是我自己造错的**（给 `edu_stats` 注入 `limit` 之后 SQLAlchemy 直接拒绝
    `group_by`，测试红得不是地方）——重造了忠实变异才算数。**变异造错不算证据**，
    这条与第一批那次「变异造了个规则本就不该管的形状」是同一个教训。

- ◐ **P2-8 第三批（2026-09-07）：只切「有机构收口」的 10 个，基线 139 → 129**；
  同四个模块里另外 11 个**故意没切**，理由不是漏了而是不该由我决定。
  - **停手的那条线**：`admin_mgmt` 的 docs/rosters/qc/staff-contracts/payroll、
    `inpatient` 的 beds/orders/order-executions、`quality` 的 adverse-events/record-qc、
    `pharmacy` 的 batch_dispense_trace，函数体内**都没有 `scope_org_list`/`scope_patient_list`**
    （AST 实扫）。今天硬编码的 `.limit(200)` 事实上把任何登录用户能捞到的行数压在 200；
    切了分页，可枚举面就从「最多 200 行」变成「整表可翻」，还附送一个精确的**全域总数头**。
    **那是扩大暴露面的决定，不是分页决定**——已登记 **P1-49**，与 P1-35 同族，可一并裁定。
    棘轮里钉成「恰好等于」，并写明**少掉一条比多出来一条严重**（少掉说明有人在没裁定的
    情况下把面放开了）。
  - **修掉一个真缺陷：近效期预警其实早就不预警了。** `/api/pharmacy/batches/expiring`
    原实现是「先取 500 行、再用 Python 筛掉发完的」，而过滤**只有上界没有下界**
    （docstring 明写含已过期）、排序又按到期日**升序**——`.limit(500)` 砍掉的恰好是
    「即将到期」那一端，留下的是「早就过期、也早就发完」那一端。发完的批次不删行，
    真实库里这 500 行大半是零余量，于是**最该预警的批次一条都出不来**，
    页面上和「没有近效期批次」一模一样。把余量条件下推 SQL 之后预警恢复有效，
    且 `X-Total-Count` 与响应体数的才是同一批行。变异验证：造 520 条已过期已发完 +
    3 条即将到期有余量，原实现返回 **0 行**。
  - **判据在这一批又误报了一次，形状是新的**：把「取完行之后的 Python 过滤」
    当成了逐行映射。它比嵌套子查询那种更坏——留在 `paginate` 外面会让**头与体数的
    不是同一批行**，调用方按「`len(page) < limit` 即最后一页」翻页会在第一页就早停，
    **等于把静默截断换成静默早停**。全仓现存 paginate 站点里 0 处这么写，范式是另起一条查询。
  - `list_batches` 补了 `id` 尾键（`expire_date` 只有 index，表级唯一约束不含它）。
    排序全序闸门仍是零基线零豁免：112/112。
- ✅ **P2-8 续：重写 `quality:record_qc_summary` 的聚合口径（2026-09-07），基线 129 → 128**。
  它不是列表端点而是**聚合统计**，两个缺陷叠着、都不报错：
  ① `.limit(5000)` 截断的是**统计口径的输入行**——库里 6000 份病历、最新 5000 份恰好都是
  甲级时，接口报「零丙级病历」，那 1000 份丙级一条不剩地消失；
  ② **月份过滤发生在截断之后**（Python `strftime` 比对），查较早的月份会返回 `total: 0`，
  **与「那个月确实没病历」完全无法区分**——而这个端点用 `require_month` 正是为了防
  「一份全零的质控统计」，结果同一个函数里又从另一头造了一份。
  改法：月份下推 SQL（`month_bounds` 的左闭右开区间比 `created_at`，该列有索引）、
  Python 分桶换 `GROUP BY`、`.limit(5000)` 删掉——统计接口本就不该有「只算前 N 条」。
  - **先补特征化网再动手**（`test_qc_summary_characterization.py` 6 条），钉住两处
    只存在于实现细节里的响应字节：**并列组的次序**（原来是「Python 稳定排序 + 按 id DESC
    迭代」，等价于并列时 max(id) 大的在前，`GROUP BY` 版要写成 `(-total, -max_id)` 才等价）；
    **意料之外的等级是「多长一个键」而不是被忽略**（于是 `grade_a+b+c < total` 是现状允许的形状）。
  - **变异验证分得很干净**：把取数改回原样，两条缺陷回归当场红（`total` 报 5000 而非 5300、
    `period=2026-03` 报 0 而非 200），而特征化网**仍全绿**——正好证明前者钉的是「修了什么」、
    后者钉的是「没改什么」。只绿一份的组合都是错的。
  - **顺手修掉棘轮判据自己的一个盲区**：它原先把 **docstring 里的示例**也当代码数
    ——重写时为讲清缺陷在 docstring 里引了一句 `.limit(5000)`，一个已经改好的端点
    就此永远留在计数里。**两个方向都会错**：散文里提一句 `paginate(` 会让真有缺陷的端点
    被静默排除，那是假阴性。已改成扫描前剥掉 docstring，并加自证用例钉住。
    （ADR-0009 那台外壳棘轮踩过同一个坑，那次是块注释。）

- ◐ **P2-8 第二批（2026-09-07）：居民端两个 portal 模块 23 个端点切完，基线 162 → 139**。
  这一批**不是照抄第一批就能过**——26 个候选逐个人工核过之后，只有 21 个是干净的列表端点，
  另外 5 个各有各的形状，四件事值得记下来：
  - **判据误报了 3 处，误报率是量出来的（26 里 3）**：`portal_my_contract` 的 `.limit(20)`
    在"每份签约附最近 20 条履约"的**子查询**上（外层合同列表根本没有 limit）；
    `spd/archive` 的 30 次就诊 + 20 条随访合并后 `[:50]`，30+20 恰好等于切片长度、
    实际永不真截断；`spd/journey` 是"每个 enrollment 附最近 30 条任务 / 10 条转诊"。
    **照着规则去"迁"会改错语义。** 没给它们建豁免名单（那条路的终点是"守卫全绿但没人信"），
    改成写进棘轮 docstring：**这个数是上限不是精确值**，迁之前先核一遍是不是这三种形状。
  - **四处排序不是全序，补尾键是切分页的前提而不是优化**（`/me/slots` 与 spd 的
    tasks / followups / revisits）。它们按 `String(10)` 日期串排，其中三处 `default=""`
    ——同日/空串并列是常态；`/me/slots` 更是被模型自己的六元组唯一索引证明可以大量并列。
    OFFSET/LIMIT 在并列行上会重复+漏行，**等于拿「静默少返回」换「静默重复+漏行」**，是往回走。
    顺手把业务侧同类的 4 处也补了（积分榜按余额排、体征 measured_at、复诊 plan_date、
    随访 planned_at），于是新闸门 `tests/test_pagination_sort_stability.py` 能做成
    **零基线零豁免**（102/102 站点全过）。判据是"order_by 末位键必须是主键或 unique 列"
    ——字典序比较下末位唯一即全序。**这条只能是静态判据**：SQLite 对并列行按 rowid
    稳定返回，能跑的那个环境恰好是照不出这个缺陷的环境（CLAUDE.md §6 那句话的又一个样本）。
  - **修掉一个真缺陷：住院费用清单的合计会少算**。`/me/admissions/{id}/bill` 原先把明细
    `.limit(500)` 之后，拿这批**被截断的行**去算 `total_amount` 与 `by_category`。
    一次长住院的收费明细（每次给药、每项检查各一行）过 500 条很平常，于是**给患者看的
    账单合计是错的**，而"合计 ¥500.00"和真的就是 500 长得一模一样。现在汇总对全部明细算、
    明细列表另走分页，取整链路一字未动。变异验证：造 520 条 1 元明细，修之前算出 **500.00**。
  - **两个免登录端点的单次上限压回原值**（`/health-articles` 与 `spd/scales`，`max_limit=50`）。
    `paginate` 默认 `max_limit=500`，照默认切等于把匿名调用者单次可取量抬高十倍
    ——**翻页给的是可达性，不是吞吐**；宣教文章的响应体还带全文。
  - 另外补了 `/me/appointments` 空名单早退路径的 `X-Total-Count: 0`（那条不经过 `paginate`，
    不补就成了"同一个端点有时带头、有时不带"），并在 docstring 记下留痕口径从
    "一次调阅一批"变成"一页一批"。既有 146 条 portal 用例一条没改全绿；新增 31 条（行为回归 27 + 排序闸门 3 + 棘轮 1）。
  - **下一批按处数排**：`admin_mgmt.py` 7，其后 `pharmacy.py`/`inpatient.py`/`maternal.py`/
    `quality.py`/`vaccine_supply.py`/`spd/care.py`/`spd/followup.py` 各 5。

- ◐ **P2-8 列表分页没铺满：162 个 GET 端点会随数据量静默截断**（第一批已切 `billing.py`）。
  这类端点长这样：`return [... for x in q.order_by(...).limit(500).all()]`。数据没到 500 条时
  一切正常；超过之后接口**静默少返回**——没有 `X-Total-Count`、没有 `offset`，
  调用方（前端表格）连「还有没有」都无从知道。**列表少一半，在页面上和「就这么多」长得一模一样**，
  没有任何一处会报错、变红或写日志。`docs/接口标准与治理.md` 早把「列表走 `deps.paginate`」
  写成了标准，只是一直没有守卫，于是这条标准只在新代码里被想起来。
  - **切法是兼容增强**：每个端点的 `limit` 默认值**取它原来那个硬编码值**，
    所以不带参数调用时第一页与切之前逐条相同；新增的只有两个可选查询参数与一个响应头。
    `billing.py` 既有 63 条用例一条没改就全绿，这是「没改行为」的第一重证据；
    新增 `test_billing_pagination.py` 10 条补的是那三件**切之前根本做不到**的事：
    总数看得见、翻得动页、筛选后的总数跟着筛。
  - **判据刻意收窄**（`tests/test_list_pagination_ratchet.py`，基线 170 → **162**）：
    只算「硬编码字面量 + 没有翻页参数」，`.limit(body.limit)` 与已带 `offset/page/cursor` 的不算。
    窄的代价是可能漏形状，好处是**报出来的每一条都是真的**——上一轮在越权那条线上刚学过
    反面教训（判据比缺陷宽 → 淹在假阳性里 → 棘轮被豁免加到失效）。
  - **变异验证顺带撞出一处盲区并如实记在 docstring 里**：只把返回那行改回 `.limit(200)`、
    签名里的 `offset` 留着，这条规则看不见（它按「有没有翻页参数」排除）。那种状态其实更糟——
    接口**声明了 `offset` 却忽略它**。把这种「没红」当成「规则不好使」去放宽判据，才是真会出事的那步。
  - 一个真踩到的坑：`list_bill_details` 改签名时漏加 `response: Response`，`paginate(..., response, ...)`
    直接 NameError，模块用例当场报红。切分页这件事机械但不能盲改。
  - **下一批按处数排**：`portal.py` 13 / `spd/portal.py` 13 / `admin_mgmt.py` 7，
    其后 `pharmacy.py`/`inpatient.py`/`maternal.py`/`quality.py`/`vaccine_supply.py`/
    `spd/care.py`/`spd/followup.py` 各 5。切之前要确认前端没依赖「一次返回全部」。

- ⚠️ **P1-42 横向越权：目标池分发不校验机构归属（已修，但按 CLAUDE.md §8 需人工复核后再合）**。
  `POST /api/spd/candidates/distribute` 按 body 里的 id 批量改目标池记录、**一次都没用签名里的 `user`**。
  实测：挂甲院的 `doctor` 一次请求把乙院的目标人群改挂到自己机构、改状态、指派给自己，返回 200。
  同文件的 `claim_candidate` 一直是对的——同一个文件里两套写法只有一套对。
  修法取"来源 + 去向各校验一次、先全量校验再落笔"（越权不做半成功）；`director`/`admin` 不受影响。
  决策见 **ADR-0019**（Proposed，待裁定"跨机构分发是否只允许 director/admin"）；
  回归 5 条，其中 3 条在修复前的代码上实测转红。
  **它此前被记成 P2-12「未用参数」——那不是清理项，是越权，优先级被系统性低估了。**
- ⚠️ **P1-47 第五处已证实的横向越权：批量处理任务不校验归属（已修，需人工复核）**。
  `POST /api/spd/tasks/batch` 按 body 的 task_ids 批量改 `SpdTask`。实测：甲院 doctor
  一次请求把乙院的待办任务取消掉（200）；别家的待办还能被接走、催办、升级、改派给任意人。
  **这已经是第二次「单条版对、批量版漏」**（第一次是 P1-42 的
  `claim_candidate` 对 / `distribute_candidates` 漏）——**批量接口是这套守卫的系统性盲区**。
  越权不进 `skipped`、整批 403：该接口刻意做逐条跳过，但那是给**业务**条件的，
  机构归属不是业务条件，静默跳过会让调用方拿着 200 以为整批都办了。回归 4 条，变异验证过。
- ◐ **P1-48 两张表根本没有 `org_id`，机构可见性体系够不到**（判定的产物，不是漏守卫）。
  `SpdPathInstance`（临床路径实例）与 `SpdServiceApply`（居民服务申请）都没有机构列，
  所以 `adjust_path_instance`（改路径实例并顺带取消其名下任务，且**连 user 参数都没有**）
  与 `handle_service_apply` **无法用 `assert_org_writable` 校验**。
  要推进得先裁定「一条路径实例属于哪家机构」（经 enrollment 推 / 经 patient 推 / 补列回填），
  **这是数据模型问题不是补一行守卫**。与 P1-35（23 张带 patient_id 却无机构列的表）同类，可一并裁定。
- ⚠️ **P1-46 第三处已证实的横向越权：病历质控写入不校验就诊机构（已修，需人工复核）**。
  `POST /api/quality/records` 用 `encounter.org_id` 落库却不校验能否写这家机构。实测：
  甲院 doctor 写乙院就诊的病历返回 201，病历挂乙院名下、书写人是甲院医生，
  且这条路径同步跑 `_apply_qc` 算缺陷扣分——**写进去的内容直接进乙院的质控成绩**。
  **这是三类里最没争议的一类**：`org_id` 不由调用方给、是从 encounter 推出来的，
  「以为自己在写本院」这种误用都不成立。ADR-0021；回归 3 条，变异验证过。
  ⚠️ **修完发现一条既有用例把越权当成了正常路径**：`test_qc_summary_by_org_and_doctor`
  原本让县医院的赵医生去写乡镇院就诊的病历。改的是**造数据的路径**（赵医生改建在第二家
  机构名下），不是断言——仍是两家机构、赵医生仍只有 1 份 C 级病历，覆盖面一个字没少。
  **这也说明补守卫会撞上「测试写着的现状」，得逐条看是用例错还是守卫错。**
- 🔸 **P1-45 第四处已证实的越权，但故意没修——它需要业务裁定**。
  `POST /api/contracts` 里，甲院 doctor 把乙院一份已解约的家庭医生签约**重新激活、
  并把家庭医生改成了自己**（201）；这个接口连 `user` 参数都没有。签约还喂着「家医履约」指标。
  不修的理由写在 ADR-0021 附录：家庭医生签约是「居民↔机构」的关系，现实里可能有
  「县医院经办代乡镇院登记」的合法用法，一刀切加校验会掐掉正在用的流程。
  要裁定：①跨机构代登记是否合法、走哪种授权；②**即便同机构，重新激活时静默覆盖
  `doctor_name` 是否可接受**（把别人签的约改到自己名下，至少该留痕）。
  **猜错方向的「修复」比暂不修更贵**——这条危害也低于病历与药品实物账。
- ⚠️ **P1-44 第二处已证实的横向越权：药品调拨不校验调出机构（已修，需人工复核）**。
  `POST /api/pharmacy/transfers` 按 body 的 `from_org_id` 扣库存却不校验能否写这家机构；
  角色是 operator/pharmacist，两个都不在 GLOBAL_ROLES 里。实测：挂甲院的 operator
  把**乙院**的药调给**丙院**，201，乙院被搬走 40、丙院凭空多 40——调用者与两端都没关系。
  **比 P1-42 更重：调拨搬的是实物账**，批次跟着一起搬，调出方少的那些在盘点前无人发现，
  而缺药预警看的正是这个汇总。
  修法**只校验调出方、不校验调入方**（ADR-0020）：减少谁的库存就要能写谁；
  收货不减少任何人的库存，而"甲把药调给乙"正是主用法，两端都要求可写等于只剩 admin/director
  能调拨。**与 P1-42 故意不同**（那里的去向是改归属，这里是收货）——这条差异写进了 ADR，
  否则下一个人会以为其中一处漏了。回归 3 条，变异验证过。待裁定：代发起调拨是否合法。
- ◐ **P1-43 横向越权棘轮的判据比缺陷窄两层**（修 P1-42 时按形状挖出来的）。
  `_byid_org_write_endpoints` ①只看路径带 `{}` 的端点、②只认 `db.get(Model, ...)`；
  而 P1-42 那个洞是"id 从 **body** 收 + `query(...).filter(...id.in_(...))`"，两条都绕过去了。
  放宽形状后扫出 **16 处候选**（登记在 TECH_DEBT）——**是候选不是已证实的越权**，
  多数可能是"新建行"而非"改别家的行"。下一步：逐条判定 → 该修的修、按设计跨机构的写豁免理由
  → 再把放宽后的判据接进棘轮。不先判定就放宽，只会让 CI 挂在一堆未核实的名字上。
- ✅ **ADR-0009 第二步收官（2026-09-06，第十七批）：pages-clinical.js 最后 25 处，四个页面文件清零**。
  门诊档案 / 写审计 / 调阅留痕 / 知情同意 / 院前急救 / 在线咨询 / 病理标本 / 行政项目 /
  角色权限 / 公卫应急 / 危急值 / 室内质控 / 绩效指标，91 个夹具页逐字符一致。
  - **总账**：手写外壳 ~295 → **8 处**（其中 1 处是文档注释里的示例，不是代码）；
    `panel()` 调用 **350 处**；比对夹具 **91 页**。
    剩下的 6 处代码外壳**不是欠账，是有据可查的例外**——3 处标题里嵌了 HTML
    （`panel()` 会把标题整段 esc()，迁过去按钮/标签变成转义文本），
    3 处在点击路径上（比对器覆盖不到，**没有字节证据就不迁**）；
    第 7 处是组件自己的模板。另有 10 处 `class="panel hidden"` 的点击容器，规则本来就数不到。
  - **白名单升级成棘轮**：`MIGRATED_PAGES` 只保证"迁过的别退回去"，拦不住**新写**一个
    手写外壳的页面。总量到底了才有资格钉死，于是加了 `test_手写面板外壳只减不增`——
    扫全部 static/*.js、把每处外壳归到所在函数、与登记表逐条比对，多一处就红并点名函数。
    变异验证过（在 renderEmergency 里手写一个，当场报 `{'renderEmergency': 1}`）。
  - **十七批下来真正起作用的**：①"换外壳是 no-op"这个前提本身就是最强的检查，
    它把标准定死成字节不许变，含糊处全变成可判定的；②取证工具要被验、而且不止一次
    （空表假绿、appendSection 不在比对范围——**绿灯是真的，只是没在看你以为它在看的东西**）；
    ③撞见一处就按形状全量扫；④夹具照分支造、把载荷塞在标题插值处；
    ⑤写夹具前先读行模板怎么用这个字段（名字与形状都能猜错）；⑥没有字节证据就不迁。
- ✅ **ADR-0009 第十六批（2026-09-06）：pages-public.js 整包清空，21 处；先补了取证工具的盲区**。
  - **这批第一件事不是迁移，是修比对器。** 这个文件有六个 `drawXxx()` 用 `appendSection()`
    把面板追加到**别的页面**尾部，而 `appendSection()` 走 `document.createElement`，
    比对器只登记经 `document.querySelector` 拿到的容器——**这些面板一个字节都没进过比对**
    （前几批写的"不在本页计数内"就是指这里）。让 createElement 也登记之后，
    tcm/education/maternal/performance/contracts/cssd 六页比对长度当场涨了一到两倍
    （tcm 1417 → 3930 字符），且全部 72 页仍逐字符相同——**新纳入的内容本来就是对的，
    只是从没被看过**。补完盲区才敢迁那六个 draw*。
    这是第二批"取证工具本身也要被验一遍"的第二次兑现：那次是空表假绿，这次是整块内容不在视野里，
    **两次都是"绿灯是真的，但它没看你以为它在看的东西"**。
  - **条件面板的两支都要有证据**：`renderInfectiousDir` 的迟报清单是"有迟报 / 无迟报"两个面板外壳，
    只造一份夹具的话另一支迁完零证据。造了两份（同一个 fn、不同数据），两支各比一次。
  剩余手写外壳 54 → **33**（core 5 / clinical 25 / mgmt 0 / public 0 / spd 3）。
  其中 8 处已有定论，**真正待迁 25 处，全部集中在 `pages-clinical.js` 一个文件里**。
- ✅ **ADR-0009 第十五批（2026-09-06）：pages-mgmt.js 整包清空，14 处**。
  统一规则 / 就诊凭据 / 机构分组 / 人员下沉 / 专病管理，72 个夹具页逐字符一致。
  这条规则数得到的手写外壳，`pages-mgmt.js` 至此清零（文件里仍有 3 个
  `class="panel hidden"` 的点击容器，带额外 class 与 id，`panel()` 出不来、规则也数不到）。
  两处标题原本自己写着 `esc()`（覆盖面板的 `group_type_name`、入组面板的 `current.name`），
  按老规矩去掉；夹具在这两个字段里都塞了载荷，**双重转义会立刻变成一处字节差异**——
  第十批那次是专门跑变异来验证这条守卫，这批起把它变成常规：
  让夹具在这些位置天然带载荷，就不必每次再补一次变异测试。
  剩余手写外壳 68 → **54**（core 5 / clinical 25 / mgmt 0 / public 21 / spd 3）。
  真正待迁 **46 处，只剩 clinical(25) 与 public(21) 两个文件**。
- ✅ **ADR-0009 第十四批（2026-09-06）：pages-spd.js 整包清空，17 处**。
  临床指导中枢 / 筛查建档纳管 / 路径与任务中心 / 转诊闭环 / 报告推送，67 个夹具页逐字符一致。
  这批按"整个文件"而不是"挑几页"来做，好处是**夹具能共用**（`/api/spd/catalog`、`/api/spd/meta`
  几乎每页都要），成本只落在各页自己的业务接口上。
  **第九批那条教训的第二种表现**：那次的结论是"别照接口猜字段名"，这次 `renderSpdReport`
  的 `sections` 名字猜对了、**形状**猜错了（是数组不是计数），渲染当场抛
  `(t.sections || []).map is not a function`。教训要写全：**先读行模板怎么用这个字段**。
  剩余手写外壳 85 → **68**（core 5 / clinical 25 / mgmt 14 / public 21 / spd 3）。
  其中 8 处已有定论（core 2 处非债 + core 3 处标题嵌 HTML + spd 3 处点击路径），
  **真正待迁 60 处，集中在 clinical / mgmt / public 三个文件**。
- ✅ **ADR-0009 第十三批（2026-09-06）：core.js 收尾九处，可迁外壳清零**。
  会诊 / 签约 / 字典 / 转诊 / 药房 / 慢病，62 个夹具页全量重跑逐字符一致。
  - **把一个个案扫成了形状**：药房页第二个面板的标题里嵌着 `<span>`（缺药预警条数），
    `panel()` 会把 title 整段 `esc()`，迁过去那个 span 就变成转义文本。按第二批立的规矩
    立刻按形状全量扫（含 `m/` 那套前端），**恰好 3 处**：药房、慢病、`openDrilldown`。
    三处都不迁——把 span/button 挪出标题是**改 DOM**，违背"换外壳是 no-op"的前提；
    前两处登记进 `KNOWN_UNMIGRATED_SHELLS` 写明理由，扫描证明这就是全部。
  - **被自己的取证工具抓了一次现行**：给这两处写说明时第一版用了 `<!-- -->` 注释，
    而它写在模板字符串里就是**会渲染进页面的内容**。比对器当场报差异，改成模板外的
    `//` 注释才一致。在模板串里写的注释不是注释，是内容。
  剩余手写外壳 94 → **85**（core 5 / clinical 25 / mgmt 14 / public 21 / spd 20）。
  core 那 5 处已全部有定论（2 处非债 + 3 处上面那个形状），**真正待迁 80 处，全在另外四个文件里**。
- ✅ **ADR-0009 第十二批（2026-09-06）：core.js 五页 13 处外壳换 `panel()`**。
  绩效考核 / 消毒供应 / 医疗废物 / 成员机构 / 居民主索引，56 个夹具页全量重跑逐字符一致。
  这批是上一批组件分支的**第一次成批兑现**：五页各带一处无标题裸面板，全都迁掉了；
  在第十一批之前，这五页每页都会卡在最后一处上。
  新踩到的夹具坑：**取数带 `.catch(() => null)` 的分支，夹具缺了不报错、只是整块不渲染**，
  比对照样报"逐字符相同"（两侧比的是同一段空）。绩效页的监测指标表就是这个形状。
  这比"缺接口整页抛错"更难发现——抛错至少会红。读渲染函数时要专门找 `.catch(` 与 `try {`
  包着的取数，静默降级的分支必须显式喂上。
  剩余手写外壳 107 → **94**（core 14 / clinical 25 / mgmt 14 / public 21 / spd 20；
  core 那 14 处里仍是 2 处不是债、1 处迁不了，真正待迁 11 处）。
- ✅ **ADR-0009 第十一批（2026-09-06）：五页 21 处外壳换 `panel()`，这批先改了组件本身**。
  账号与参数 / 老年健康 / 证明与体检 / 门急诊文书 / 医保基金池，51 个夹具页全量重跑逐字符一致。
  - **`panel()` 加了 `title === ""` 不出 `<h3>` 的分支**。清点剩余外壳时发现有 16 处**裸面板**
    （壳里直接就是一个 `table(…)`，表格自带表头，再加标题是重复）；组件原样一律吐 `<h3></h3>`，
    迁它们等于**往 DOM 里加一个空元素**——而这一步的前提正是"换外壳是 no-op"。
    判据取**严格等于空串**不取假值：`esc()` 把 undefined 变成空串，`panel(undefined, …)`
    今天渲染成看得见的空 `<h3>`，放宽成假值判断它会连 `<h3>` 一起消失，
    "本来就没标题"与"标题算成了 undefined"就再也分不开。
  - **改组件先证明对既有调用无害**：改完先跑全量比对（51 页、229 处调用零差异，
    现有调用没有一处标题是空串），再用新能力迁那两处裸面板。顺序不能反。
  - **确认一处迁不了并写下理由**：`openDrilldown` 的下钻外壳标题里嵌着「关闭」按钮，
    `panel()` 会把标题整段 `esc()`，迁过去按钮变成转义文本。顺手把它里面同名遮蔽的
    局部变量 `panel` 改成 `drill`（与第十批驾驶舱那处同一个坑）；这条路径要点击才走到、
    比对器覆盖不到，另加了一个取证脚本 `scripts/drilldown_equiv.js`：HEAD 版与工作区版
    各跑一次，offset 取 0/20/40 覆盖分页三支，逐字符相同。
  剩余手写外壳 128 → **107**（core 27 / clinical 25 / mgmt 14 / public 21 / spd 20；
  core 那 27 处里 2 处不是债、1 处是上面那个迁不了的，真正待迁 24 处）。
- ✅ **ADR-0009 第十批（2026-09-06）：五页 18 处外壳换 `panel()`，含首页驾驶舱**。
  中医药服务 / 疫苗批次冷链 / 检查结果互认目录 / 妇幼保健 / 驾驶舱，46 个夹具页全量重跑逐字符一致。
  这批的价值在**驾驶舱是首页**，且它四个面板里三个是条件渲染的（风险预警带 accent、慢病分级、
  机构绩效——绩效那块还另拉一个接口且失败时整块不渲染），条件一律留在调用点，`panel()` 只换外壳。
  一条陷阱当场验过：绩效面板标题原本自己写了 `esc(perf.period)`，`panel()` 会再转一遍，
  把 `&` 变成 `&amp;amp;`。夹具把周期也塞成 XSS 载荷，故意留着 `esc()` 跑一次比对，
  比对器立刻报出差异（第 6238 字符起）——守卫 `test_已迁移的页面标题不得重复转义` 早写着这条，
  这次是**证明它真拦得住**，不是靠记性。
  顺手（童子军）：驾驶舱点击回调里的局部变量 `const panel = $("#drill-panel")` 正好遮住同名组件，
  同一函数里既 `panel()` 又 `panel.innerHTML` 是给后来人挖坑，就近改名 `drill`。
  剩余手写外壳 146 → **128**（core 27 / clinical 34 / mgmt 22 / public 25 / spd 20）。
- ✅ **ADR-0009 第九批（2026-09-06）：五页 21 处外壳换 `panel()`，第一次迁 `core.js`**。
  共享中心检查 / 处方与合理用药 / 预约导诊 / 教育培训 / 住院管理，41 个夹具页全量重跑逐字符一致。
  两处夹具坑记进 ADR：**照接口猜字段名会让页面直接抛错**（处方点评实际读 `grade`/`issues`/`comment`/`at`），
  **渲染函数末尾追加的内容也要喂**（教育页 `drawEduGaps()` 还要两个接口）——这类坑第六、七、九批
  各踩一次，结论是先把渲染函数从头读到尾比试错快。
  剩余手写外壳 167 → **146**（core 31 / clinical 48 / mgmt 22 / public 25 / spd 20）。
- ✅ **ADR-0009 第八批（2026-09-06）：pages-mgmt.js 五页 25 处外壳换 `panel()`，顺带修掉一个真缺陷**。
  住院文书 / 手术麻醉 / 流程引擎 / 运营分析四页是纯迁移，全量比对逐字符一致。第五页会计核算是
  **先修后迁**：备夹具时发现它的合并报表面板嵌在凭证表格的行回调里，一条凭证渲染一份（夹具两条
  → 出现两次，且嵌在 `<tr>` 内，生产上 30 条凭证就是 30 份）。修复与迁移改的是同几行，写在同一个
  提交里，但证据分开给：剥掉该面板后页面其余部分逐字符未变、面板本身 2 次 → 1 次、其余 34 个
  夹具页零差异、回归用例在修复前的源码上实测转红（TECH_DEBT **P1-41** 已销）。
  结论记进 ADR：**为迁移造的夹具本身就是读页面的一种方式**，比通读模板字符串更容易看出结构性错误。
  剩余手写外壳 192 → **167**（core 44 / clinical 56 / mgmt 22 / public 25 / spd 20）。
- ✅ **ADR-0009 第七批（2026-09-06）：三个文件五页 28 处外壳换 `panel()`，逐字符一致**。
  知识库 / 人力财务物资 / 医疗质量 / 慢专病考核 / 慢专病随访，各自先造 `render_fixtures.json`
  条目再迁；迁前比一次（2486～7057 字符，证明表格与条件面板真渲染出来了），迁完再比一次，
  最后全部 **31 个夹具页**一并重跑无一差异。两处**故意不迁**并写明理由：`renderHrFinance` 的
  两个 `class="panel hidden" id=…` 容器（`panel()` 出不了额外 class 与 id，且计数规则本来
  就看不见它们）、`renderSpdAssess` 点击后才渲染的下钻明细（比对器覆盖不到点击路径，
  没有证据就不换）——后者登记进新的 `KNOWN_UNMIGRATED_SHELLS`。顺手把本来就是 `panel()`
  写、却从没进白名单的 `renderSpdMember`/`renderSpdManager` 补登（没登记就没有防退回的网）。
  剩余手写外壳 219 → **192**（core 44 / clinical 56 / mgmt 47 / public 25 / spd 20）。
- ✅ **P1-30 第二步（2026-09-05 完成）：审出来的「业务上确实唯一」下沉到库，抢输的一路拿与顺序请求一样的 409**。
  13 条（部分）唯一索引分两条链落地（平台 `b9c8d7e6f5a4` 七条、spd `f4e3d2c1b0a9` 六条），
  14 个写入点改走 `insert_or_conflict`/`insert_if_absent`；另有 6 处「唯一性长在父行状态跃迁上」
  的改成条件 UPDATE（调价、采购验收、转诊推进、随访办结、出院、服务包绑定），登记进
  `GUARDED_BY_PARENT_UPDATE`。**取证不靠推理**：在只加索引、还没改接口的中间状态上跑了
  全量单测 3109 条，没有一条因为撞新索引而红——全仓没有任何测试/种子/脚本会在这 13 个键上
  合法地造出第二行；真 PG 结构漂移 0（259 表 / 2508 列 / 834 索引 / 433 外键全量比对）；
  每张表带行为面 + 防拆卸静态钉 + **真 PG 八路并发**三层网。三处「竞态档在全新库上直接红」
  的坑是自己在**新建库**上逐档单跑才发现的（各档只在别人先跑过迁移的库上绿），已改成
  「缺表/缺索引就自跑一次幂等 upgrade heads」。度量本身也修了一处反直觉缺陷：
  把 `db.add` 改成 `insert_or_conflict` 会让那处从分子分母一起消失、覆盖数**下降**——
  一条会因为修复而报警的度量早晚被调松，现在插入助手也计入写入点（226→284 个），
  覆盖 59（26.1%）→ 131（46.1%），形状识别 100%。
  一条审出来为真却**故意不建**的索引：`spd_lifecycle_events` 没有「撤回待确认迁出」的通道，
  建了就把一条良性多余行换成一份卡死的档案（TECH_DEBT P1-40）。
  收尾（2026-09-05 `d4577f2`）：最后一格 `spd_interventions` 改由纳管档案行的临界区串行
  （表级唯一被证伪——手工按同一模板批量/周期开具是设计功能；条件 UPDATE 也不行——复评仍高危
  而上次干预已办结时必须重开）。待决 10 → 8，**既未覆盖也未审计 26 → 0**：284 个写入点
  每一个都有去向。P1-30 账本已销。
- ✅ **P1-30 第一步（2026-09-03）：闸门看不到的 72.5% 逐表审计入库，三份清单 + 两条新棘轮**。
  并发防复发闸门只看"带唯一约束的表"，167 个写入点它完全不看；打印出缺口不等于知道那里
  有没有问题。136 张表逐张审（分类员 → 怀疑者复审 → 业务/代码/数据三视角分别试图推翻），
  结论入库：`AUDITED_MULTI_ROW_TABLES` 112 张（多行是设计本意或由别处守住，条条附理由）、
  `AUDITED_UNDECIDED_TABLES` 5 张（键在表上表达不出来或要产品决定，只减不增）、
  `GUARDED_BY_PARENT_UPDATE`（父行状态跃迁守住的表，先立空清单）。审计判为"业务上确实
  唯一"的 20 张表不进清单，去向是唯一索引下沉 + `insert_or_conflict` 或父行条件 UPDATE
  （同一工程包后续提交）；为此新增棘轮"既未覆盖也未审计"26 → 只减、"待决"8 → 只减，
  新表进路由前必须先回答"多行合法吗"。`test_已审计清单不得腐烂` 守清单本身。顺手让扫描器
  认出 `for x in xs: db.add(x)`（对账差异明细），未识别写入点 1 → 0、形状识别 100%。
- ✅ **11 处被顶下去的 docstring 换回首行（2026-09-02，P2-4 的 docstring 半边）**。
  形状全部一样：补横向越权校验那轮把 `assert_org_writable(...)` 插到了 docstring 上面，
  Python 不报错但 `__doc__` 为 None，接口文档页上这 11 个端点是空白。守卫
  `test_docstring_position.py`：源码形状（docstring 不得是第二条语句）+ 生成物闭环
  （有 docstring 的端点 OpenAPI 必有 description，分母从源路由模块推导——`app.routes`
  上挂的是 `_IncludedRouter` 包装，APIRoute 只剩个位数，拿它当分母会什么也守不住）。
- ✅ **月度期间口径收敛到单一真源（2026-09-02）：P1-34 五处 `2026-13` 放行清零**。
  财务记账、薪酬、基金周期预结的 `period` 与月报导出、质控统计的查询参数原先各自写着
  "四位-两位"的正则——只管形状不管日历，`2026-13` 入了库就是一条永远对不上任何月份的
  记账，进了过滤就是一份全空却不报错的报表（与 D-3 假日期同一个坑的月度版）。`datetypes`
  新增 `PeriodStr`（形状 + 真实日历），查询参数配 `deps.require_month`（原 422 文案不变、
  月份不存在给新文案），`period_bounds` 的形状改为引用真源对象。守卫与 DateStr 那条同构：
  扫 `app/` 全部字符串字面量，别处再写月度正则即红；两处变异转红。
- ✅ **PII 出口脱敏守卫落地（2026-09-02）：P1-33 从"靠人记得"变成棘轮，首个洞已堵**。
  `privacy.py` 写着"新增返回身份证号/电话的接口必须复用本模块"，此前没有任何检查在证明它。
  新守卫 `test_privacy_egress_guard.py` 的分母从路由对象推导（946 个端点的响应模型递归
  展开找 `id_card`/`phone` 字段），判定沿调用链找 `desensitize`/`mask_*`（正确写法多半在
  `_out()` 帮手里，只看函数体会满屏假红），`/api/portal/` 按口径视为居民本人侧，其余必须
  逐条书面登记且不得腐烂；裸 `dict/Any` 契约这个唯一盲区逐个复核登记而非给总数。盘出的
  第一个洞：`POST /api/patients` 幂等命中既有档案时把别人录入的那份原样返回——一个只知道
  证件号的账号借"建档"就能套出电话，现改走 `desensitize`。登记的 24 处明文出口
  （慢专病工作台行内电话 17、120 呼救人回拨 4、村医通讯录 3）是**待产品裁定**（P1-39）
  而非放行：电话随访默认人工拨号、120 靠回拨，一刀切掩码会把业务做没。三处变异各自转红。
- ✅ **读-改-写真追加清零（2026-09-02）：P1-28 八处逐处落地，P1-31 陈账销账**。
  `obj.col = f(obj.col, 新值)` 把旧值读进 Python、算完整体写回，并发下后写的把先写
  的盖掉，而两笔的日志看上去都成功了。按列的类型三种修法：**字符串追加**（孕产妇风险
  因素 ×2、高危儿风险备注）改 `concurrency.append_text`——拼接下沉到 SQL 由行锁排队，
  空则不带分隔符；**处方药师审核**改成一条带 `status='pending_review'` 条件的 UPDATE，
  状态迁移与意见追加同条 SQL，顺带关掉"两位药师同时审、后者覆盖前者结论"的双审竞态
  （真 PG 八路并发恰一路审到，其余七路拿 409）；**JSON 列整体覆写**（复诊日志、外呼
  结果+证据、召回联系记录）没有可移植的原子追加，进 `serialized_on` 行锁临界区、
  先 `db.refresh` 再追加——该闸门原是 billing 私有的，上提到 `app/concurrency.py`。
  防复发规则同步升级：豁免只认"临界区内**且 refresh 之后**"（顺序写反照样红，合成
  代码五形状自证）；欠账清单从"函数名"改成"**函数 × 条数**"并加"条数必须相等"的
  防腐烂用例——只登函数名时，`record_call_result` 名下两条真追加与两条幂等回填混在
  一起，修掉真追加后再塞一条新的照样报绿；这条新用例顺手揪出三条早已修好却还挂着
  的陈账（`refund_payment` / `_finish_task` / `dispatch_edu_push`，后者即 P1-31）。
  回归 11 条（顺序语义逐字不变 + 静态钉 + 不得回清单）+ 真 PG 三条八路并发直测，
  三处变异各自转红。
- ✅ **静默双写清零（2026-09-01）：P1-29 五条逐条落地**。三条真不变式由迁移
  `b8e3d5f70a91` 下沉为**部分唯一索引**（在院唯一 / 号源唯一 / 首次病程唯一），
  接口改走 `insert_or_conflict`——此前它们都是"先查再建"的 check-then-act，
  并发下不报错、**静默写出两条**：同一个患者被登记进两张床、同一时段放出两份号、
  同一次住院两份法定文书。号源那条特意拆成两条部分索引：SQL 里 NULL != NULL，
  不挂医师的检查/检验号源在单一复合唯一索引下等于不设防。
  另两条如实处置而非硬修：`create_settlement` 是**陈账**（早已由 e5b7c9d1f3a4
  修好，清单没跟上）；`bill_details` 的**判据被推翻**——床位费/护理费按天逐条记
  同一收费项是正常业务，加唯一约束会拒掉第二天的计费，已移出清单并用特征化网
  钉住现状（含"谁按旧判据加约束就变红"）。回归 12 条（含**绕开接口层直插证明库里
  真拦得住**——索引"在不在"与"拦不拦得住"是两回事），三处变异转红。
- ✅ **迁移-模型结构漂移清零（2026-08-31）：P1-27 两个方言档 39/41 → 0**。迁移
  `e7c4b19d02fa` 一次收口三类"模型说了、库没做"的差异：补 **14 处外键**（此前生产
  PG 根本不做参照完整性校验，孤儿行静默落库）、收紧 **25 列 NOT NULL**（迁移建成
  可空、模型 NOT NULL，非 ORM 写入能把 NULL 塞进 created_at 与 JSON 列）、删掉
  **2 处**与模型唯一索引重复的无名唯一约束（删前先确认同列另有唯一索引在守，
  不会出现"删完没人守唯一"）。存量冲突按 CLAUDE.md §4 走不阻塞路径：**逐项**探测、
  探到孤儿行/NULL 只跳过该项并打**指名主键**的 ERROR 日志，人工处置 SQL 写进迁移
  docstring；纯 DDL，不 UPDATE/DELETE 任何存量业务数据。跳过分支由
  `test_migration_conflict_skip.py` 六条**行为**回归咬住（三处变异转红——其中"日志
  指名"那条最初假绿：冲突行 id=1 时退化文案"共 1 条"里也有个 1，改用可辨识主键后
  才真的咬住）。
- 🔧 **本机集成档不再赊给 CI**：新增 `server/scripts/dev_services.sh`（start/env/stop），
  一行拉起本机 PG+Redis 并导出两个 `*_TEST_URL`。开发容器里这两个服务往往**已经装好
  只是没跑**，而缺服务时 integration 档会**整档 skip 且退出码为 0**（"没跑"与"全对"
  的绿灯长得一样）。落地即验：`make test-integration` 本地 **19 passed / 0 skipped**
  （真 PG 12 + 真 Redis 7），本轮 PG 档漂移清零正是这样实测的，而不是只跑 SQLite
  就宣布结论。

- ✅ **契约治理收官（2026-08-31）：收官两批 40 → 0，946 端点覆盖率 100%，欠账账户销户**。
  收官批②八散点（patients/vaccination/eldercare/exams/publichealth/consultations/
  appointments/emergency，21 端点，40→19）+ 收官批①（chronic/insurance/integration/
  medication，19 端点，19→0），**四十六模块全数 FULLY_GOVERNED，基线归 0 从此只许为 0**。
  取证同前法（capture_plugin 套件级逐字节、噪声底先归零：831/91 键 0 差异；变异 5+5 转红；
  patients 授权四端点带脱敏双视角与 AccessLog 留痕逐字节钉）。六轮并行批合计 450→0。
  收官轮同日另收六件：**created_at 棘轮 2→0 收官**（blood_stocks 顺路补齐；admissions
  冻结表先过 ADR-0018——历史行回填 1970 哨兵而非抄 admitted_at，取舍见 ADR）；
  **P2-31 渲染器监听窗口全仓根修收官**（acorn AST 清点 8 文件 22 条挂载移到 await 前，
  守卫升级 ROOT_FIXED_RENDERERS 清单式 12 渲染器 + 5 处登记例外）；**P1-18/P1-19
  事务边界专档**（8 条资金/库存/状态机链路「失败无半写 + 重来恰一次」，真 PG 档扩至
  11 条）；顺带实证并随即修复 **spd 并发认领洞**（claim/批量/办结三处条件 UPDATE 收口，
  回归 7 条 + 防拆卸静态钉）；**P1-20 测试 fixture 去重**（136 文件收敛 conftest 共享
  三件套，净 −1914 行，两轮受控全量探针验证零回退；顺带根修两处 CWD 相对路径假红）；
  **P2-26④ 生产锁剔除测试工具**（运行时依赖 13→11、lock 重生成，镜像不再含 pytest，
  ADR-0017 后记留档）。
- ✅ **多 agent 并行批第三、四轮（2026-08-31）：二十四模块 178 端点，基线 218 → 40，覆盖率 95.8%**。
  七个并行会话分两波（billing+inpatient 29 / admin_mgmt+maternal 32 / 四小簇 32 //
  药事三簇 25 / 临床五小簇 30 / 运维八小簇 30），主会话分六次收基线
  218→189→157→125→100→70→40，每批隔离 worktree 验证后推送。取证升级点：Money 的
  int/float 一律 `type(x) is int` 显式钉（dict 相等对 15==15.0 是盲的）；cost 汇总同一行
  两族并存（0.0 桶起加恒 float vs .get(id,0) 兜底 int|float）——判据是产地不是字段名；
  todos 分节真多态照 drilldown 先例宽字典+type 自描述；两个真下载端点（audit/export
  NDJSON、附件字节流）照 CsvResponse 先例走媒体类型声明并入册豁免清单钉（+2）。
  四轮合计 **410 端点（450→40）、三十四模块进 FULLY_GOVERNED**；剩余 40 处零散分布在
  exams(3)/insurance(5)/integration(5)/chronic(5)/consultations(2)/medication(4)/
  patients(4)/publichealth(3)/vaccination(2)/eldercare(2)/emergency(1)/appointments(1)
  等部分治理模块，可按同法一批收完。
  同日另收：**P1-9 启动种子化加固**（逐块隔离 + PG advisory lock 专用连接串行化 +
  撞键按已种好继续，见 TECH_DEBT 销账行与 ADR 先例注释）；**CI 两条 e2e flake 根修**
  （原生表单提交竞态——监听先于 await + shared.js document 层兜底，P2-31 登记剩余面；
  瞬态断言改稳定终态）；手写 SQL 棘轮 15→17（advisory lock 为 ORM 无法表达的合法增量）。
- ✅ **多 agent 并行批第二轮（2026-08-28）：六模块 118 端点，基线 336 → 218，覆盖率首破 77%**。
  同一套编队与纪律（三会话各领一簇、互不碰文件、主会话分三次收基线 336→302→259→218、
  每批隔离 worktree 验证后推送）：①esb 13 + education 21（payload/steps 外来 JSON 宽 dict
  透传且用自定义键钉"不许滤键"、Float 列 score→float 与 Money 相反）；②spd/followup 29 +
  spd/referral 14（auto-match 两分支二选一联合、详情独有 steps 继承拆两模型防注入 null、
  分级审核语义零改动）；③spd/population 28 + vaccine_supply 13（患者 brief 条件键双向钉、
  closed 宽字典、360 档案与纳管详情的 paths 两个形状分建）。三批取证 234/712/838 个组合
  目标范围全部 0 差异，合计 15 处变异验证转红，零跳过。累计十三模块进 FULLY_GOVERNED，
  欠账 218/946——首次降到总端点四分之一以下。
  同日另收：**e2e 去 flaky**（居民自查用例改断稳定终态"待受理"——"风险等级"是瞬态文本，
  断言它是在跟自家重画抢时间，CI 慢半拍就输；含冷启动时序连跑全绿）。
- ✅ **多 agent 并行批（2026-08-28）：七模块 114 端点，基线 450 → 336**。三个并行会话各领一簇、
  互不碰文件，主会话统一收基线与 `FULLY_GOVERNED`（分三次下调 450→418→375→336，每批一个
  自洽提交、隔离 worktree 验证后推送）：①质控三簇 quality 20 + dataquality 6 + rules 6
  （条件键 exclude_unset、qc-summary 分组键 int|str、rules sample 三型并存 bool 不得变 0/1）；
  ②spd/assess 24 + spd/tasks 19（scores-analysis 两分支键序不同→二选一联合、动作回执 27 键
  与清单行 29 键拆两模型）；③spd/care 31 + spd/workbench 8（Float 列→float 与 Money 相反、
  workbench/team 三角色条件键三种键序各钉一遍、4 个重名模型改名防 OpenAPI 长限定名改写）。
  取证均为 capture_plugin 套件级前后逐字节比对（60/105/691 个组合各 0 差异），三批合计
  12 处变异验证转红。零跳过——没有一个端点靠"跳过"绕行。
  同批并行完成：**P1-1 居民端调阅留痕**（28 读端点接 AccessLog，见 TECH_DEBT 销账行）、
  **E2E 复活接入 CI**（11/11 零 flaky 阻断 + 自证闸门，两条假绿修真，揪出 m.js loadSpd
  并发渲染竞态真 bug——已建任务卡未夹带）、**技术债账本 14 条实证销账 + README 真数**。
- ✅ **接口契约棘轮已清零（2026-09-11 实测复核）**：欠账 **0**、已治理端点 **946**、覆盖率 **100.0%**、零欠账模块 **87**。下面那些 ◐ 批次行是当时的施工日志，**不是还没做的活**——本轮按路线找活时正是被「portal 余 48」误导了一次，而那句话在契约清零之后就再没人回头核。各棘轮的当下数字改由
  **`docs/闸门现状.md`**（自动生成 + 新鲜度闸门）统一给出，别再在路线里手写数字。
- ◐ 接口契约棘轮：按 `docs/接口标准与治理.md` 逐块迁移。已治理 12 模块，基线 **757→741**。本轮做掉 encounters 的 `/archive/360`——全平台聚合度最高的接口（一次吐出一个人的就诊/检查/慢病/处方/结算/体检），嵌套九段**逐段建模**而不是 `dict[str, Any]`（写成 Any 等于没声明契约，而这个接口恰恰最需要）。先补特征化网 `test_archive_360_contract.py` 十三条钉住键集合与类型、再加契约、加完网照样绿（响应字节不变）；网里六段都造了数据——空列表什么都钉不住。两处变异（契约少一个字段 / 字段名写错）各自转红。本轮再做掉 performance 的 `/orgs` 机构计分卡，基线 **741 → 740**，`performance` 三个端点全部有契约、已进 `FULLY_GOVERNED`。两处麻烦点：`weights` 的键来自指标表是**动态的**（只能 `dict[str, float]`）；`detail` 五段**混形状**（三段分子/分母、两段裸计数），逐段建模而非 `dict[str, Any]`。`score` 特意验过恒为 float——`_normalized_weights` 表空时退回非空默认，求和恒在浮点上做；若可能是 int，声明 float 就会把 `0` 变 `0.0`，那是改字节不是治理。特征化网 `test_performance_orgs_contract.py` 十条（造了五个维度的真实数据，全零响应什么都钉不住），并逐字钉住折算值（42.5 / 32.5 / 67.5）；三组参数下**响应字节逐字节比对一致**；两处变异各自转红。✅ **`metrics` 与 `analytics` 两簇已做完**，基线 **724 → 719 → 709**，两模块均进 `FULLY_GOVERNED`。刻意**只做契约不动口径**（这两簇正是 ADR-0007 口径合并的交集区，混着改就分不清「这是治理」还是「这是改数」）。本批最要紧的建模判断：`round()` 与 Money 列派生的数值一律 `int | float` 而非 float——实测同一个 `total_amount` 字段一行返回 `1234.5`、另一行返回 `100`（int），`Money` 是 `Numeric(14,2, asdecimal=False)`，整数值读回来就是 int；写成 float 会把 `100` 变 `100.0`。变异验证直接抓到了这一字节差，不是理论推演。两处**多态** items（`drilldown` 八种行形状、`performance-report` 失败项多一个 `error` 键）用宽字典而非逐字段——后者会给成功项注入 `"error": null`，同样改字节；多态不等于无契约：`drilldown` 同一响应里的 `fields` 就是行的字段清单，有用例钉住两者相等。metrics 46 个请求 + analytics 17 个请求，加契约前后逐字节一致。✅ **`reports` 三端点也做完，基线 709 → 706**，并顺带**把棘轮的判据修对了**：两个 CSV 导出直接返回 `StreamingResponse`，`response_model` 对它们没有意义（函数不返回可序列化对象，FastAPI 也会跳过模型）——把这类端点永远算作欠账，等于往棘轮里掺一笔**永远还不掉的账**，数字就不再表示「还有多少接口没契约」。判据放宽为「有 `response_model` **或**显式声明了非 JSON 媒体类型」，且**从路由对象推导、不是手工豁免清单**。放宽**没有白送任何端点**（先查过：全仓库只有 `printing` 的 12 个端点声明过 `response_class`，而它们本来就都有 `response_model`），并有用例钉住「靠媒体类型脱账」的清单，拿空 `responses` 刷低欠账会让它变长。写法见 `docs/接口标准与治理.md` 新增的对照表——`printing` 的 `response_model=str` 在 CSV 上不适用，改用自带 `media_type` 的 `CsvResponse`，**声明与实际返回是同一个类**。
- ◐ **`portal` 开工：先做 auth 组 + 两个公开列表（10 个），基线 706 → 696**，portal 余 48。**刻意没做完整个 portal**：`my-archive`(2) 与 `surveys`(1) 三个遗留端点与 `me/archive` 共用 `_build_archive`，拆开做会把同一个形状建模两次，故留给 `me`(19) 那批一起做；`spd`(26) 再一批。本批关键是两个**条件键**——`auth/sms/code` 的 `debug_code` 与 `auth/wechat/authorize` 的 `mock_code`：声明成带默认值的可选字段会给**每一个**响应注入 `null`，既改字节，又等于在生产响应与 OpenAPI 里公告该字段存在（`debug_code` 是登录验证码的回显口子，P0 整改专门收紧过它——契约不该把刚收紧的东西又摆回台面）。两个端点用 `response_model_exclude_unset=True`，**两条分支都逐字节比对过**：回显开时有该键、关时整个键不出现。另有 `price-list.price` 的 Money 陷阱（整数价仍是 int，否则公示页「50 元」变「50.0 元」）。
- ✅ **`portal/me` 组 + 三个遗留端点做完（22 个），基线 696 → 674**，`app/routers/portal.py` 契约欠账清零、已进 `FULLY_GOVERNED`。三处判断：①`me/family` 的**条件键** `member_id`——本人那一行没有它（本人不是一条代管关系），声明成可选字段会注入 `"member_id": null`，客户端照着 null 去调 `DELETE /me/family/None` 就是平白多出来的错误路径，故用 `response_model_exclude_unset=True`；②Money 陷阱这一批出现了**四处**（账单三项、费用明细单价与金额、分类汇总的**值**、押金余额），整数金额声明成 float 会把「200 元」变「200.0 元」；③`_build_archive` 被三个端点共用（`me/archive` 与两个已废弃的 `my-archive`），只建一个 `ArchiveOut`，并有用例钉住三者形状相等——否则日后改一处漏两处。消息两端点**复用** notifications 已有的 `NotificationOut`/`UnreadCountOut`，不另建同形模型。29 个请求加契约前后逐字节一致，四处变异各自转红（见 `test_portal_me_contract.py`）。
- ✅ **顺带把棘轮的模块 key 按包限定**（spd 的加 `spd/` 前缀）。此前 `app/routers/portal.py` 与 `app/spd/routers/portal.py` 被合并成一个 key `portal`——后果是前者治理干净了也**进不了** `FULLY_GOVERNED`（合并后的 key 还带着后者的 26 项欠账），于是它被改回裸 dict 时不会单独变红，只剩总基线兜底，而总基线是可以被别处的治理抵消的。这正是该文件开头声称已关掉的那种「静默回退」，只是换了个入口，本批治理完 portal 才让它现形。补了反空转守卫 `test_两个包里的同名模块不被合并成一个key`：去掉前缀当场转红。
- ✅ **顺手修**：`/me/deposits` 的 `amount`/`balance` 声明成了 `float`（a911f61 引入），而 `Deposit.amount` 是 Money 列、`deposit_balance()` 也返回 int——1000 元的押金以 `1000.0` 出账。与本批同一类陷阱，就近修掉并补回归。该端点目前无前端消费方，改回 int 不影响任何页面。
- ✅ **`spd/portal` 26 个端点做完，基线 674 → 648**，两侧居民端契约就此都清零、都进 `FULLY_GOVERNED`。最要紧的是 `/screenings` 的**三种形状**——草稿+量表（带 `answered`/`total_items`）、草稿无量表（只有四个键）、落库（带 `id`/`result`/`can_apply`），逐字段建模会把三者的字段互相注入 null，故 `response_model_exclude_unset=True`，三条分支各钉一遍。`score` 是 `int | float`：有量表时 `round(total, 2)` 是 float，无量表时兜底字面量 `0` 是 int。与平台侧的 Money 陷阱**方向相反**，spd 这边多是 Float 列（`measurement.value`、`assessment.score`），整数值读回来就是 `140.0`，声明 float 才是原样。两处我自己犯的错都被机制当场抓到并写进了模型 docstring：转诊详情模型继承列表模型、凭空要求了一个详情不返回的 `created_at`（响应校验拦下）；`SpdScreeningOut` 字段顺序把 `advice` 排在 `result` 前面（序列化按声明顺序走，逐字节比对拦下）。41 个请求加契约前后逐字节一致，五处变异各自转红（见 `test_spd_portal_contract.py`）。
- ✅ **`spd/config` 58 个端点做完，基线 648 → 588**，已进 `FULLY_GOVERNED`。config 是个包（ADR-0008 拆的），6 个子模块，**分三批三个提交**做——一次比 58 个端点，逐字节比对出了问题不好定位，粒度本身就是这套办法的价值。①catalog 9 + centers 4（648→635）：`ProgramDetailOut` 继承 `ProgramOut` 是对的（详情是列表的**严格超集**——与 spd/portal 那批的转诊详情正相反，那个不是超集，同样写继承就错了）；`target_low`/`target_high` 可空 Float；`org-tree` 自引用递归。②paths 7 + devices 9（635→619→614）：`_template_out` 三形状用 `exclude_unset` 且 `nodes` 须在 `node_count` 之前；**判据第二次放宽**——204 无响应体也算声明了契约，这次**确实白送了 5 个端点**（上次放宽媒体类型是 0 个），故账要拆开看：真治理 16 个 + 放宽降 5。③scales 15 + teams 12（614→588）：服务包 `price` 是 Money 列；标签新建与列表**不同形**（列表没有 active）；两个二维码改用 `_base.SvgResponse`。三批合计 102 个请求逐字节一致，十五处变异各自转红。
- ✅ **顺手修**：村医绑定二维码编的是 `/m/doctor.html`，而那个地址**没有路由**（`main.py` 只挂了 `/static`，医生端入口是显式的 `/m/doctor`）——印出去的码扫开是一张 404，村医绑不上账号还看不出是码的问题。回归用例不比对字符串（写错成另一个不存在的路径照样绿），改成把 path 抠出来真请求一次。
- **两条「不由字节决定」的记录**（免得日后被当成守卫失效）：`last_sync_at` 从 `str` 改 `str | None` 零处转红（handler 永远给值）；去掉 `response_class=SvgResponse` 也不改响应字节（返回的类自带 media_type），变的是 OpenAPI 会回落成 `application/json`——规格书写了假话。这类判断的理由是「契约要诚实」，不是「字节会变」，不咬人的变异不能当证据。
- ✅ **一次十二个模块，基线 588 → 470**（118 个端点）：medwaste 11 / clinical_docs 9 / materials 9 / surgery 10 / accounting 9 / disease_programs 9 / rbac 9 / surveillance 9 / tcm_heritage 9 / workflows 9 / outpatient_docs 13 / fund 12，十二个模块全部清零。覆盖率首次过半（**50.26%**），已治理模块 24 → 36 个。
- 🔧 **取证方式换了**，这是本轮真正的产出：不再每个模块手写一份捕获脚本，改成给 app 装一个中间件，把**整个测试套件**跑出来的每个响应按 `(方法, 路由模板, 状态码)` 记下字节，加契约前后各跑一次逐项比对——一次覆盖 **1847 个组合**，治理十个模块和治理一个模块的取证成本一样。工具 `tests/capture_plugin.py`，用法与**三条注意事项**写进了 `docs/接口标准与治理.md`：①用之前必须先量噪声底（同一份代码跑两次差异必须为 0，实测第一次有 2 处随机项）；②它覆盖不到测试没跑过的端点，那是**沉默的缺口**不是保证；③改了路由代码就要重跑（pytest 启动时导入 app，之后的编辑不影响正在跑的那轮——踩过一次）。
- 本轮的建模判断：Money 列一律 `int | float`（accounting 全模块、fund 全模块、materials 采购与耗材单价——fund 那个直接决定各机构分到多少钱，不是显示问题）；条件键用 `exclude_unset`（accounting 凭证的 entries、rbac 内置角色的 note、fund 超额预付的 warning、**tcm_heritage 决策点的 answer/explain**——最后这个是嵌套条件键，学员拉题目时答案整个键不出现，声明成可选字段等于把答案的存在公告出去）；「新建回执」与「列表行」键集合不同的一律两个模型，不硬套继承。
- ✅ **ADR-0006 收官批的 20 个端点已补契约，基线 470 → 450**（覆盖率 52.38%）。`cssd` 加回 `FULLY_GOVERNED`（搬家那个提交里因搬入 3 个无契约端点短暂移出——**总欠账当时一点没变**，470→470 只是换了名下，只有总基线的话那次回退会完全静默）；`surveys`/`triage` 生而全契约。两处会改字节的判断：`ExamResource.price` 是 Money 列（捕获里实测到 `"price":240`，声明 float 就变 `240.0`）；`survey_stats` 的**字段顺序**照 handler 实际出键排——它 `pop("count")` 后又重新赋值，`count` 因此被挪到 `distribution` 与 `negative` 之后。套件级捕获 1856 个组合前后比对，**落在这 20 个端点内的差异 0 处**；捕获盖不到的两处另补了用例（`GET /api/surveys` 一次没被跑过、`GET /api/cssd/requests` 只跑到过空列表——空集钉不住字段），四处变异各自转红。
- 剩余候选：`spd/care`(31) / `spd/followup`(29) / `spd/population`(28) / `spd/assess`(24) / `education`(21) / `quality`(20) / `admin_mgmt`(20) / `spd/tasks`(19)…（共 450，模块数 50）。
- ✅ **搬家带来的 8 个端点契约已补齐**，基线 **740 → 732**，`cssd` 与 `performance` 双双回归 `FULLY_GOVERNED`。最容易踩的是 `cssd` 的 `total_cost`：它是 `round(totals.get(id, 0), 2)`，**没有成本项时返回 int `0` 而不是 `0.0`**，声明成 `float` 就会把 `0` 变成 `0.0`（改字节）。契约写 `int | float` 原样透传，变异验证：改成 `float` 立刻转红。八个端点响应字节逐字节比对一致（含空集与有数据两条分支）。
- ✅ `created_at` 欠账迁移**收官**：52 → **2**（16 个迁移批次，平台链+spd 链，全部常量默认回填→batch 撤默认范式，全程响应字节不变）。仅剩有意留置 2 张：`blood_stocks`（小型 upsert 表，价值低降级）、`admissions`（核心表，改列需先 ADR）。此后新表一律带 created_at（棘轮基线=2 顶住）。
- ✅ 测试隔离修复：**七条**用例在 `pytest -k` 子集下会红（整模块跑得过，纯靠执行顺序）。根因不是共享库本身，是**跨用例借数据**——`GET …?keyword=张三` 取上一条用例建的人、`enrollments[0]` 取上一条用例建的档案、DRG 统计里指望上一条用例的病例已在。改法：把前置数据交给**幂等的模块级 fixture** 负责交付（已有就返回、没有才建），用例声明依赖即可。`path_template` fixture 顺带把模板发布掉——「可用的模板」本就该由建它的 fixture 交付，而不是靠 `test_publish_*` 排在前面顺带做掉。现每条单选跑均绿。
- ✅ **测试隔离全量普查（2026-08-22）**：112 个测试模块**逐个单跑**，零失败——"单跑就红"这一类已经干净。
  再换一个方向查"顺序依赖"：全量套件按模块名**逆序**跑一遍（不引入 pytest-randomly，第 12 条不为这个加依赖），
  抓到**一条**真的顺序依赖，而且是条鉴权守卫——`test_authz_matrix.py::test_未守卫写接口清单必须是审阅过的白名单`。
  根因：pytest 在跑任何用例前会**导入全部测试模块**，`tests/test_p0_fixes.py` 导入时就往共享的 `app` 上挂了
  `POST /api/_test/boom`（`include_in_schema=False`，用来触发 500）。正序时 test_authz… 先导入、看不到它；
  逆序时看得到，于是这个测试脚手架被当成"没配守卫的写接口"。
  改法不是无条件跳过 `include_in_schema=False`——那等于给隐藏写接口开免检门，而这是**鉴权**守卫，静默跳过即漏洞。
  改成"跳过 + 清账"：跳过的每一条都必须在 `TEST_ONLY_ROUTES` 里，新增任何隐藏写接口当场转红（已变异验证）。
  另加 `test_矩阵不受测试脚手架路由影响_与模块导入顺序无关`：强制先导入那个模块再重跑收集，
  让这个回归在**正序**下也能复现——否则它只在"逆序跑全量"时才看得见，而没人会天天那么跑。
- ◐ 三套并行子域（ADR-0003 已 **Accepted**）：转诊读侧聚合的**接口侧**已落地——`GET /api/portal/me/referrals/all` 把平台 `referrals` 与 `spd_referral_cases` 并成一份（带 `source` + 分源中文标签 + 完整时间戳排序）；两个老接口响应字节不动（两条特征化用例各钉一个）；注册制登记源，子系统关掉自动降级为平台单源。实施中确认**无需去重**（两批单子真正不相交，spd 从不写 `referrals`），真正要处理的是**同名不同义**的状态码（平台 accepted=已接收 vs spd accepted=县级医院已接收），故每条带 `source` + 分源标签，措辞与 `m.js` 既有文案逐字对齐。
- ✅ **居民端已切到聚合接口**：`static/m/m.js` 两个页面均取自 `/me/referrals/all`，慢专病页用 **`?source=spd` 服务端收窄**（不能客户端 filter——条数上限是合并后才截的，平台转诊一多就会把慢专病的单子整段挤出窗口，页面显示「暂无」而其实有在办的；回归用例已钉住）。**用户可感知的转诊孤岛就此消除**。顺带删掉前端两张 `REFERRAL_STATUS`/`SPD_REF_TEXT` 标签表——同一份映射不该有两个副本，状态文案权威统一到后端 `status_label`；两个页面的卡片渲染合并为一份 `referralCard()`；详情链接直接用后端 `detail_path`（已带 patient_id，代管家属才点得开）。静态守卫 `test_portal_referral_frontend.py` 七条防复开，四处变异验证。
- ✅ 业务端转诊文案收归后端：`ReferralOut` 新增 `status_label`（三个端点都带上），`core.js` 只留配色。**刻意保留两套措辞**——居民端「待接收」面向患者、业务端「待接诊」面向医师，同一个状态、两个读者、两套词是对的；不对的是同一套词在前后端各存一份。回归 `test_referral_status_label.py` 四条，含「两套映射覆盖的状态码必须一致」与「后端映射覆盖全部可达状态」。
- ◐ 三套并行子域的其余概念——**逐个核过是不是真孤岛**，不机械造接口：
  - ✅ **患者入组**：确是居民端可感知的孤岛（`/me/archive` 的 `chronic_care` 读 `chronic_patients`，`/spd/archive` 的 `profiles` 读 `spd_enrollments`，看到哪份取决于点了哪个入口）。已加 `GET /api/portal/me/enrollments/all`，做法与转诊完全一致：注册制登记源、服务端 `source` 收窄、老接口字节不动。**刻意不统一分级词汇**：平台 `level` 是 1/2/3（控制良好/需干预/高危）、spd 是 low/mid/high/very_high（并发症风险分层），两把尺子量的不是同一件事，硬映射就是编一个不存在的等价关系——故各留原始码 + 各自中文标签。特征化网各钉一个老接口，九条用例，两处变异（只用平台源 / 硬映射分级）各自转红。
  - ☐ **随访**：平台侧**没有**面向居民的随访列表（`/me/archive` 只给 `next_followup_due` 一个日期），只有 spd 有 `/spd/followups`。所以这不是"两份打架"，而是平台侧缺一块——聚合等于给居民**新增**平台慢病随访视图，属加功能而非消孤岛，需先定要不要给。
  - ☐ **病种目录**：`chronic_disease_types` / `disease_programs` / `spd_programs` 三张表都是**运营配置**，只在管理端出现，居民端根本看不到，不存在"居民看到两份"的问题。真正的重复在配置口径（同一个 `hypertension` 码三处各带各的阈值），那是 ADR-0003 方案 C 的范围，不是读侧聚合能解的。
- ✅ `docs/API_MAP.md` 已更新：写侧仍两套（方案 C 待立项），读侧已聚合。

## Later（C 类重构：**四项均已出 ADR，待批准后逐块动手**）

> 这四项都是对**可运行模块**的结构性改动，按 CLAUDE.md §1.2/§9 必须先有 ADR。
> ADR 已写齐（0006-0009，七段式，含实测数据与分批建议），等的是批准，不是分析。

- ✅ **拆倾倒场收官（ADR-0006 Done）**：`gapfill.py`(1125 行/34 端点/6 前缀) 与 `service_extras.py`(521 行/20 端点) 两个倾倒场**均已删除**，54 个端点全部回到业务前缀。分批做的，每批前先跑「端点 URL 集合零漂移」守卫拿基线。
  - ✅ 前置守卫已就位：`test_refactor_drift_guards.py` 快照 885 个端点，搬漏/改名立刻红；另有 `test_遍历本身没瞎` 防守卫自身失效（写这个守卫时先踩过一次：遍历写错、快照只存下 1 个端点、用例照样绿）。
  - ✅ **第一批 `/api/performance` 已搬回**：5 个 `improvements*` 端点从 `gapfill.py` 移进 `routers/performance.py`（gapfill 1125 → 945 行）。零漂移守卫实测 885 个端点纹丝不动。
  - ⚠️ **搬出来一个真问题**：同一个 `/api/performance` 前缀上挂着**两套鉴权**——原 `performance.py` 的 router 是 `require_roles("director")`，gapfill 那个是 `get_current_user`（登录即可）。这正是 ADR-0006 problem 点名的「鉴权分裂」。搬家**刻意没有合并两个路由**：并成一个会把这 5 个端点从「登录可见」收紧到「仅 director」，那是行为变更不是搬家。收益是此前这个分裂散在两个文件里根本看不见，现在并排躺在同一文件里。已逐端点实测鉴权与搬前一致。
  - ✅ **鉴权口径已定**：逐端点核过之后**不做统一收紧**——5 个里 4 个本就妥当（POST 三个各有 `require_roles` + `assert_org_writable`；`GET /improvements` 有 `scope_org_list` 只给本机构明细）。真正漏的只有 **`GET /improvement-stats`**：它连 `user` 参数都没有，任何登录账号拿到的都是**全县**汇总。修法是加 `scope_org_list(..., stats=True)`（统计走医共体范围、明细走本机构，这个区分 `visibility` 早就建好了），而不是锁成 director-only——`pages-public.js` 把它和明细列表放在同一个 `Promise.all` 里取，锁角色会让还能看列表的人整页报错。全域角色响应与整改前一致。回归四条，含「汇总与同屏列表口径一致」。
  - ✅ **剩余五个前缀一次搬完，`gapfill.py` 已删除**——倾倒场从 1125 行归零。`tcm`/`cssd`/`education`/`maternal` 并入同名模块（四者鉴权与目标 router **完全一致**，可直接合成一个 router，不像 performance 那样存在鉴权分裂）；`homevisits` 新建模块。零漂移守卫实测 885 端点不变。
  - 搬家顺带撞出一处**同名遮蔽**：`maternal.py` 里出现两个 `ScreeningCreate`（儿童筛查 vs 产前筛查）与两个 `list_screenings`。当前行为是对的（各自的使用点都在自己的定义之后、重定义之前），但一个写在 417 行之后、想用儿童筛查 schema 的新端点会**静默拿到产前筛查的校验规则**。已把搬来的那套改名 `PrenatalScreeningCreate`/`list_prenatal_screenings`，并实测两套 422 校验各自正确。
  - `cssd` 与 `performance` 暂时移出 `FULLY_GOVERNED`：搬进来的端点本就无契约，总欠账仍 740（只是换了名下），补完即加回。
  - ✅ **`service_extras.py` 一次拆完并删除（2026-08-24）**：20 个端点 → `exams`(6，报告模板/报告修订/检查资源要素档案) · `cssd`(3，物品申领) · `appointments`(3，服务黑名单) · `consultations`(2，会诊专家) · `education`(2，宣教文章)；`surveys`(3) 与 `triage`(1) **新建模块**——满意度评价的对象横跨签约/就诊/会诊三类，导诊是到院前环节，挂进任何一个既有域都会让别的看起来像附属品，理由写进了各自 docstring。零漂移守卫实测 885 个端点纹丝不动。
  - 搬之前逐项核过三件事，**都清**，所以这批比第一批 `/api/performance` 简单得多：六个目标模块的 router 鉴权与倾倒场**完全一致**（都是 `dependencies=[Depends(get_current_user)]`，不存在鉴权分裂）；**无同名遮蔽**（gapfill 那次撞到过两个 `ScreeningCreate`）；**无路径冲突**。
  - 差点漏一个：`_CENTERS` 常量定义在倾倒场 router 那行的**下面**、不在任何分节里，按分节切块时没带上——ruff 的 F821 当场拦下。分节切块这个手法对"节外的模块级常量"是有盲区的。
- ◐ 统计簇 `analytics/metrics/reports/performance` 合并口径 → **[ADR-0007](docs/adr/0007-统计簇口径合并.md)（Accepted）**。**第一步已交付**：`docs/统计口径对照表.md` —— 逐条比对四个模块的实际代码，摸出 5 处口径分歧 + 1 处更严重的问题：
  - 🔴 **有两个平行的机构评分体系**：`performance/orgs`（`performance_indicators` 表 + 硬编码五维度）与 `analytics/performance-report`（`performance_formulas` 表 + 可编辑公式）。两个接口都叫「机构绩效」、两个分数不可比。
  - ⚠️ **随访覆盖率不带时间窗**：「有过任意一次随访」就永久计入覆盖，数字只涨不跌、随管理年限趋近 100%，基本没有考核价值。更像缺陷而非分歧，但修它会让所有机构该项得分明显下降。
  - 另 4 处分子/分母口径：转诊结案率的分母（`from_org_id` vs 全部）、互认算不算远程诊断服务量、`auto_passed` 算不算处方合格、诊疗人次是否只算基层。
  - ✅ **第 0 条已裁定**：两套评分引擎**都留、分工写死**——`performance/orgs` 做**考核基准**（口径固定才谈得上全县横向可比；且开箱即用、前端三处在用、有契约与特征化网），`analytics/performance-report` 做**自定义分析**（公式可编辑正是探索场景该有的样子，但空表开箱 0 分，做不了基准）。✅ 已落地：两个页面互相点名对方并各自标明口径（考核页还显示评分周期），顺带修掉「运营月报CSV（累计）」这个只对一半的按钮标签；`tests/test_performance_caliber_labels.py` 盯住，含一条"文案别被挪进注释"的防呆用例。
  - ✅ **第 3 条已裁定并落地**：`performance/orgs` 改**周期口径**，`period` 支持 `YYYY`（缺省当年）与 `YYYY-MM`。慢病一项的时间语义**不对称**：分母是在管存量（不按期，去年入组今年仍在管的照样考核），分子是本期随访到的人。响应新增 `period` 字段，前端两处都标出评分周期。回归六条，两处变异验证。
  - ✅ 第 1、2、4 条已裁定（2026-08-22），**三条都不动分数**（分数经 `fund.distribute` 切基金池，无卫健口径文件不动真金白银）：
    - **第 1 条**转诊结案率维持按转出机构。裁定前先修了更要紧的：`PATCH /api/referrals/{id}/status` 当时只有角色守卫、**没有机构归属校验**，任何机构的医师都能把别人的单子接诊/结案（实测 200）——而 `completed` 正是本指标的分子。已补"仅接收方机构可推进"（全域角色放行），归属校验排在状态机之前以免泄露单据状态。`tests/test_referral_status_authority.py` 6 条，三处变异验证。
    - **第 2 条**互认照计。关键是这一项按 `from_org_id`（**申请方/基层**）计，衡量的是平台使用而非诊断工作量，互认正是想要的结果。**随之暴露一条新待裁定**：真正出报告的共享诊断中心在该维度拿 0 分，`claimed_org_id` 已让"按诊断方计分"可行，但属新增计分口径、会改基金分配，需卫健口径支持。已用例断言住"中心拿 0 分"这个事实，改动时会被看见。
    - **第 4 条** `auto_passed` 维持计入，但补 `detail.rx_pass.rule_covered`（本期至少一味药对得上**生效**规则的处方张数，按处方去重）。`auto_passed` 的真实含义是"没有规则被触发"，而 `drug_rules` 全县共用一张表——规则库越稀疏合格率越接近 100% 且越无区分度。照 `ddd` 未维护的既有先例"明说没维护"，而不是按缺省值硬算。追加字段，不改既有字节。
  - ✅ **已修（2026-08-22）**：共享诊断中心的工作量现已计分——`remote_exam` 维度改为两侧都计（申请方按 `from_org_id` 数 reported+recognized，中心按 `claimed_org_id` 只数 reported）。**这会改分数**（中心侧上升）并影响此后的基金分配；已分配的池子是冻结快照不受影响。已知偏差：`claimed_org_id` 是 2026-08 加的列，更早周期的中心侧数字偏低，跨该时点不可同比。
  - ✅ **已改名**：`远程诊断服务量` → `共享诊断协同量`（新库自动生效）。**存量库只报告不代改**——起初写的 `UPDATE` 被平台的迁移数据安全闸门判为 A 档拦下，拦得对：这是管理端可编辑的现场配置，可能已被写进各县考核文件与报表标题。改成升级时打 WARNING 指名该行、处置 SQL 写在 docstring 里，改不改由现场定；不改不影响计分。⚠️ `reports.py` 的同名监测指标是 2024 版国家指标体系的法定名称、全县一个总数，**不改**——同名不同物。
  - ☑ `performance/orgs` 的 **N+1** 已消除：8 条 `GROUP BY org_id` 替代「每机构 8 条 count」，SQL 条数与机构数解耦。18 组参数逐字节比对老实现同值；`test_查询条数不随机构数增长` 用「两次不同机构数的差值」防复发（写死上限会被无关改动误伤）。顺带补上 `test_慢病随访按人去重` —— 原用例每人恰好一次随访，`distinct` 丢了也测不出来。
  - ✅ 周期解析器合并完毕，而且**是三份不是两份**——`cost._period_bounds` 与 `analytics._period_bounds` 逐字节相同（16 组输入实测同值）。合成 `deps.month_bounds` 一份，五个既有端点行为零漂移（含原实现宽松的 `2026-1` 也照收、422 文案一字不改）。
  - ✅ 顺带修掉三份副本共有的一个 **500**：右端点的计算写在 `try` 外面，`9999-12` 的次月是 10000 年，溢出没人接 → `?period=9999-12` 在 5 个端点上返回 500 而不是 422。**`deps.period_bounds` 自己也中招**（本轮周期口径改动里只圈进了年度形式 `9999`，月度形式漏在外面），一并修。`tests/test_period_parsing.py` 27 条守住，三处变异验证。
  - ✅ **已裁定（2026-08-22）：月度解析器不接受 `YYYY`，两个解析器分工保持**。合并会把 5 个既有端点当前的 422 变成 200——那不是去重，是给未受影响的接口加功能；而且「年度成本」「年度运行效率」这些口径本身没定义过（`efficiency` 的日均担负按天数摊，年度窗口下含义完全不同）。想要年度粒度应当是一次带口径设计的新需求，不是解析器合并的副产物。已有 `test_两个解析器分工不同_月度那个不认年度` 防「顺手统一」。
- ✅ **God 文件分域拆包已完成（ADR-0008 Done）**：`spd/routers/config.py`(1549 行) 与 `models.py`(3989 行/187 类) 都已拆成包，**调用方 import 路径一行没改**。下面的子条目记着两次拆包的实测数据与踩到的坑。（顶行此前一直挂着 ◐，是标记没跟上——子条目早就写着 ✅。）
  - ✅ 前置守卫已就位：同一份用例快照 246 个 ORM 类名——拆包漏了重导出会让类不再注册进 `Base.metadata`，**建表静默少一张**而不是报错，这条把「静默」变成「报错」。
  - ✅ **演练完成：`spd/routers/config.py` 1549 行 → 包（8 个文件，最大 331 行）**。按业务分节拆成 catalog / paths / scales / teams / devices / centers 六个子模块 + `_base`（路由对象与跨节工具）。**导入路径一行没改**（`from .routers import config` 照旧），零漂移守卫实测 885 端点 + 246 模型纹丝不动。
  - 拆之前先用 AST 扫了一遍**跨节引用**：`_bump_version`（专病档案↔标准路径）、`_qr_svg`（评估量表↔村医档案）、`_target_out`（管理目标↔专病档案）三个共用件——靠肉眼读 1549 行是找不全的，找漏一个就是 NameError。前两个跨了分组边界，收进 `_base`。
  - 另加两条守卫：**导入路径不变**、**子模块注册顺序与原分节一致**。后者不是洁癖——路由靠 import 时装饰器注册，顺序决定 FastAPI 匹配优先级，乱序**不报错**只会让某些路径悄悄匹配到别的处理函数。变异验证：打乱顺序 → 转红；漏掉一个子模块 → 端点守卫报「消失 4 个」。
  - ✅ **`models.py` 3989 行 / 187 类 → 分域包（16 个文件，最大 505 行）**：core / clinical / emergency / pharmacy / inpatient / chronic / publichealth / contracts / finance / assets / hr / quality / platform / portal + `_base`（Money、utcnow）。**导入路径一行没改**（`from ..models import X` 照旧），零漂移守卫实测 885 端点 + 246 模型不变，空库迁移仍建出 247 张表，`SCHEMA.md` 无 diff。
  - 拆之前先用 AST 查了两件事：① 187 个类是**完整划分**（无遗漏无重复，脚本断言）；② 14 处 `Mapped[SomeClass]` 的**硬顺序依赖**——本仓库没开 `from __future__ import annotations`，这类注解在建类时就要求被引用的类已定义。14 对全部落在同一域内，故各域内保持原文件顺序即可满足。
  - 顺带收紧了 spd 边界用例：`PLATFORM_TOUCHPOINTS` 从**按文件名**匹配改成**按相对路径**。拆包后触点落在 `models/__init__.py`，按文件名放行 `__init__.py` 等于给每个包都开口子。
  - 实测确认："漏掉一个域"**不会静默**——187 个类全都被某处按名 import，漏一个立刻 ImportError。快照守卫仍有价值（挡的是将来新增却没人按名引用的类），已用加一个探针类验证它能报出单个类的增减。
- ◐ 前端组件抽取与工具函数合并（[ADR-0009](docs/adr/0009-前端组件抽取与工具函数合并.md) **Accepted**）。**第一步已完成**：`$` 与 `esc` 三份逐字相同的实现合并进 `static/shared.js`，三个 HTML 入口都把它排在第一个 script；守卫 `test_frontend_shared_utils.py` 十三条（含转义表逐字符、加载顺序、消费方不得再自定义），三处变异各自转红。动机不是整洁而是安全：`esc()` 是 §8 红线，近百处手写插值，一份实现才有一处审查点。**`api()` 刻意未合并**——三套认证语义不同（localStorage / sessionStorage / 不带令牌），连 401 的处理时机与文案都不一样，合并需要把令牌来源与 401 回调参数化，那是行为重构不是去重，留作后续单独一步。
- ◐ ADR-0009 第二步已启动：`panel()` 已抽出（放 core.js 与 `table()` 并列，**不放 shared.js**——`.panel` 是管理端的标记约定，居民端/医师端另有一套）；第一页 `renderServiceRequests` 迁完，Node 比对确认 `panel()` 本身是 no-op。**人工过这一页找出了全量扫描的盲区**：一个解构出来的局部变量（`UNIFIED_STATUS` 查不到时 `text` 回落成后端原始状态码）一直裸插进 innerHTML，扫描只认 `x.y` 属性访问所以漏了。已修并回归。后续逐页迁，不设期限。
- ◐ **ADR-0009 第二批（2026-08-26）：又迁六页**——随访 / 定时任务 / 满意度 / 站内消息 / 质量指标 / 运行监控，`pages-mgmt.js` 里 16 处手写外壳换成 `panel()`，六页输出逐字符一致。三件事值得记：
  - 🔴 **顺着"人工过一页"把上一轮那个盲区按形状扫了一遍，又挖出 33 处同样的未转义**（core 6 / pages-clinical 9 / pages-mgmt 5 / pages-public 11 / m 2）。全是 `const [text, color] = MAP[x.status] || [x.status, ""]` 之后 `${text}` 裸插——映射查不到时 `text` 就是**后端原始状态码**。上一轮撞见这个形状时只修了眼前那一处，**个案修复漏掉了 97%**。已全部修掉并加形状守卫（`test_frontend_escape_guard.py` 新增一条，含防空转与"兜底是字面量不得误报"）；实测该守卫在修复前的代码上正报 33 处、修复后 0 处。教训：撞见一处未转义要立刻扫**形状**，别只修个案。
  - **取证工具做成了可复用的**：`scripts/render_diff.js` + `scripts/fixtures/render_fixtures.json`——在 Node 里按夹具真渲染页面，拿迁移前后的 innerHTML 逐字符比。上一轮那个是一次性脚本；剩下还有 ~300 处外壳要迁，每页重写一遍取证脚本不合算。夹具刻意**塞 XSS 载荷**，这样比对的不只是"标签没挪位"而是"转义行为没变"；三处变异（吃掉 esc / 外壳掉 class / 标题重复转义）各自转红。
  - **标题里的 `esc()` 必须去掉**：`panel()` 自己转义 title，留着就是转两遍（`&` → `&amp;amp;`），是改字节。`renderMonitor` 上实测到 `本实例进程内&lt;b&gt;` → `本实例进程内&amp;lt;b&amp;gt;`，已加守卫。
  - 剩余手写外壳约 300 处（core 43 / pages-clinical 106 / pages-mgmt 56 / pages-spd 52 / pages-public 40），继续逐页迁，不设期限。
  - 🔁 **自审补课（同轮 `/code-review`）**：上面三条新守卫**判据都比缺陷窄**，合成用例逐条试穿全部绕过——限定了"映射名首字母大写"（可 `spdTag(map, key)` 就是小写形参）、"兜底第二元恰好是 `""`"（带默认配色就绕过）、"`panel(` 后紧跟反引号"（标题是单个动态值时最自然的写法没有反引号）。这些差异与缺陷无关，纯是拼写；判据窄于缺陷 = 留了个**看不见的**后门（守卫还是绿的）。已放宽到同宽并把绕过写法钉成参数化回归；放宽前后在修复前代码上都正报 33 处（没有靠放宽把账做小）。**取证工具自己也被验了一遍**，查出五处"看着绿其实没验到"，最要紧的是空表假绿——夹具"列表必须非空"只写在注释里没真检，而空列表下 `table()` 照样吐几百字符的"暂无数据"表格，于是报"逐字符相同"而行模板一次都没求值。已改为显式检出并报红。
  - 登记一条不在本轮修的（`docs/TECH_DEBT.md`）：**P2-25** 裸 `${MAP[key]}` 查不到时显示字面量 `undefined`（同族，但只是显示缺陷、非崩溃非 XSS，刻意不并进那两条守卫——一条守卫混两种严重度迟早因噪声被加豁免；且需逐处定文案）。
- ◐ **ADR-0009 第六批（2026-09-02）：再迁四页**——慢专病运行中枢 / 症候群病原监测 /
  通用资源 / 医保，17 处手写外壳换成 `panel()`，四页 `render_diff` 逐字符一致，26 个夹具页
  全部重跑无差异。夹具里两个判断：运行中枢页在赋值 innerHTML 之后还要拉 `/api/spd/meta`
  建规则编辑器，夹具不给就是"渲染失败"而非"字节不同"，比对器两种都会报；医保页特病申报
  的状态字面量在现状里是裸插的后端枚举，夹具照给载荷——比对钉的是"迁移前后一样"，
  它安不安全归 `test_frontend_escape_guard` 那条线。剩余手写外壳 **219** 处
  （core 44 / pages-clinical 70 / pages-mgmt 47 / pages-public 28 / pages-spd 30）。
- ◐ **ADR-0009 第五批（2026-09-02）：再迁四页**——接种 / 名老中医医案 / DRGs / 科室成本，
  17 处手写外壳换成 `panel()`，四页 `render_diff` 逐字符一致。夹具里的三个判断：接种页渲染时
  不调接口，夹具 `api` 为空是如实的（钉的是外壳与表单字节）；医案页两个接口共前缀，靠比对器
  的最长前缀匹配分流；成本页把 localStorage 里的期间/机构固定住，让条件面板渲染出来。
  成本页标题里的 `esc(period)` 去掉（组件转义标题）。剩余手写外壳 **236** 处
  （core 44 / pages-clinical 82 / pages-mgmt 47 / pages-public 28 / pages-spd 35）。
- ◐ **ADR-0009 第四批（2026-09-02）：再迁五页，这次先补夹具**——团队端 / 卫健委端 /
  公文排班质控 / 传染病 / 血库，24 处手写外壳换成 `panel()`。夹具成本兑现在这一批：五页各造
  一份 `render_fixtures.json` 条目（列表非空、文本字段塞 XSS 载荷、条件面板的条件推成真、
  role 类条件走夹具 `localStorage`），迁移前先比对一次证明夹具真把面板渲染出来了
  （1061～5026 字符），迁完再比一次仍逐字符相同。剩余手写外壳 **253** 处
  （core 44 / pages-clinical 90 / pages-mgmt 52 / pages-public 32 / pages-spd 35）。
  一条边界：`renderEldercare` 有一个**没有标题**的外壳（`<div class="panel">${table(…)}</div>`），
  `panel()` 必带 `<h3>`，迁了就是改字节——这类外壳不迁，也不该为它给组件加"无标题"分支。
- ◐ **ADR-0009 第三批（2026-09-02）：又迁五页**——物资 / 用药 / 计费结算 / ESB / 慢专病中心端，
  22 处手写外壳换成 `panel()`，五页 `render_diff` 逐字符一致。三件事：
  - **挑页的依据是"夹具已经有"**：比对器 13 个页面键里还剩这五页带手写外壳，取证零成本；
    也是第一次迁出 `pages-mgmt.js`（clinical 2 / public 1 / spd 1），守卫从"只读 mgmt"
    改成按函数名找文件。
  - **比对器抓到一处不该改的字节**：把 `innerHTML = \`\n    <div class="panel">…\`` 改成
    `innerHTML = panel(…) + panel(…)` 会丢掉模板字面量开头那个换行+缩进文本节点——
    肉眼看不出、DOM 里就是少了一个文本节点。三页当场报差异，改回 `\`\n    ${panel(…)}\``
    的模板骨架后一致。结论：迁移只换外壳，**模板骨架不动**。
  - 剩余手写外壳 **277** 处（core 44 / pages-clinical 97 / pages-mgmt 52 / pages-public 36 /
    pages-spd 48）。下一批要先给页面补夹具（`scripts/fixtures/render_fixtures.json`），
    每页造非空数据 + XSS 载荷，成本主要在这里。
- ✅ **P2-26 收敛：34 处状态标签收进 `statusTag()` 组件**（同日续做）。那 33 处未转义**全是在手抄同一段三行代码**，而同一个仓库里 `spdTag()` 早就写对了——只是没铺满。这正是 ADR-0009 的论点（"转义收进组件就漏不掉"）第二个、也是有血的样本。三处判断：
  - **放 `shared.js` 而不是 `core.js`**（`panel()` 放的是 core.js）。判据是"三端是不是真的都在用"：`.panel`/`.card` 是管理端独有的，而 `.tag` 三套前端都在用且标记契约逐字相同（`style.css:60` 与 `m/m.css:141` 各自定义 `.tag` 与 `.tag.red/.green/.orange`，配色不同、类名约定一致）。
  - **合并实现 ≠ 统一行为**：慢专病历来把空状态显示成 `—`、管理端显示空白，两套都保住（`spdTag` 委托时自己传 `key || "—"`）。顺手统一那是改字节，不是去重。
  - **等价性是证出来的不是推出来的**：`scripts/statustag_equiv.js` 把两种写法在输入矩阵上逐字符比（命中/未命中/空串/null/undefined/XSS 载荷/**数字状态码**）。数字那条当场抓到真问题——初版组件用 `key || ""` 兜底，会把慢病分级的 `0` 吞成空白，改用 `key ?? ""`。慢专病侧的委托还有个**前提**（映射表不能用假值当键），脚本里真的扫了 5 张表来验，没有默认它成立。
  - 取证：10 个页面渲染字节比对一致（含两处**手工**改的 `PAY_STATUS`/`ESB_MSG_STATUS` —— 脚本因为把 CSS 的 `color:` 误判成"第二处用途"而跳过了它们，跳过是对的，机械替换那两处会出错）。比对器顺带补上一个盲区：原先只捕 `#page-body`，`renderEsb` 这类由内层函数写进子容器的表格根本没进比对，现改为捕获全部容器。⚠️ 居民端/医生端的 4 处只有等价性证明与人工复核，**没有字节比对**——比对器只加载管理端那套文件。
- ✅ **顺手修（同一次形状扫描带出来的第三种失败方式）**：`pages-clinical.js` 的缺药登记 `const [t, col] = SS[s.status]` **没有兜底**，而后端会把状态写成 `collected`/`no_show`/`cancelled`（`medication.py` 的 `shortage.status = body.result`）——`SS` 里没有这三个，解构 undefined 抛 TypeError，且这一句在 `table()` 的行渲染回调里，所以炸的是**整页**：只要有一条缺药登记结了案，这一页就再也打不开。已用比对器复现（旧版抛 `undefined is not iterable`，新版正常）并按仓库既有写法修掉（`|| [s.status, ""]` + `esc`）。全仓库扫过只此一处，已加守卫。

## 待决策（先 ADR，后动手）

- ✅ ADR-0002 生产迁移停用 create_all（Accepted，已实施）。
- ✅ ADR-0003 三套并行子域收敛（**Accepted**：先 B 读侧聚合、中期再评估 C、否决 D）。转诊那步已落地（接口+居民端），其余概念待续。
- ✅ ADR-0006 倾倒场路由回归业务前缀（**Accepted**）。第一步「端点 URL 集合零漂移」守卫已就位。
- ✅ ADR-0007 统计簇口径合并（**Accepted**）。⚠️ 但第一步仍**不是写代码**——需先出「同名指标在几处各算什么」的对照表交产品裁定。
- ✅ ADR-0008 God 文件分域拆包（**Accepted**）。第一步「模型名字集合零漂移」守卫已就位。
- ✅ ADR-0009 前端组件抽取与工具函数合并（**Accepted**）。第一步（合并 `$`/`esc`）已完成。

---

## Done（已完成，倒序）

- ✅ 童子军法则写入 CLAUDE.md §12。
- ✅ ADR 体系建立（`docs/adr/`：README + 模板 + 0001/0002/0003）。
- ✅ 核心数据不可变定义 + 守卫（`test_core_data_invariants.py`）。
- ✅ 数据模型治理：SCHEMA 输出、核心表冻结、created_at 棘轮。
- ✅ 接口标准治理棘轮 + 样板迁移（checkups）。
- ✅ 特征化测试指引 + spd/rules 样板网。
- ✅ 模块分级 A/B/C/D。
- ✅ 六项能力（build/lint/typecheck/unit/integration/smoke）+ Makefile + CI 接入。
- ✅ AS-IS 架构审计 + 六张地图。
