# 接口标准与治理：混乱代码 → 标准接口

```
   混乱代码（裸 dict / 无契约 / 分页各异）  ──逐块──▶  标准接口（声明契约 / 统一分页 / 向后兼容）
                                          每块先补特征化网，再动；棘轮锁住只进不退
```

881 个端点里历史上仅 ~14% 声明了响应契约（`response_model`）。本文件定义**标准接口**、
**逐块治理配方**，以及一台**棘轮**机器（`tests/test_api_contract_governance.py`）让治理
可度量、可强制、只进不退——不搞一次性大改（那违反"改动限定在任务本身"且会破坏向后兼容）。

---

## 一、标准接口（目标形态）

| 维度 | 标准 | 现状 |
|---|---|---|
| **响应契约** | 每个端点声明 `response_model`；单对象 `XxxOut`，列表 `list[XxxOut]` | 14% → 逐块补 |
| **分页** | 列表走 `deps.paginate`（`limit`/`offset` + `X-Total-Count` 头） | 32/89 文件；其余散用 `.limit()` |
| **错误** | `raise HTTPException(status_code, detail=...)`，响应体 `{"detail": ...}` | 已是事实标准，保持 |
| **命名** | 路径 kebab-case、集合用复数、路径参数下划线 | ✅ 已良好，勿退化 |
| **动作响应** | 新端点用规范结果体；**存量 ad-hoc 键保留**（改键名破坏客户端，属破坏性变更，需 §9 ADR） | 各自发明键名 |

> **硬约束（CLAUDE.md 第7条）**：治理**不得改变响应字节**。`response_model` 的字段必须与
> 当前输出一一对应——少声明会删字段、多声明会加字段，两者都是破坏性变更。**用特征化测试守住。**
>
> 多声明还有一重代价，是第三批才发现的：`test_pii_output_masking_guard` **按
> `response_model` 判"这个端点会不会吐 PII"**。给一条永远不返回电话的端点声明
> 一个 `phone`，它就会去找一条不存在的脱敏调用并报违规。契约是给别的闸门读的
> 声明，不只是给人读的文档。

---

## 二、逐块治理配方（每个模块照做）

1. **拿到字节基线**：优先用套件级字节捕获（见下节「取证方式」），一次覆盖所有模块；
   捕获覆盖不到的端点再手写特征化用例，钉住**当前** JSON 的键集合与取值
   （范式见 `tests/test_checkups_characterization.py`：断言"键恰好是这几个"）。
2. **加标准接口**：给缺契约的端点加 `response_model`，模型字段**精确镜像**当前输出。
   列表端点若还没分页，评估是否切 `paginate`（会加 `X-Total-Count` 头与 `limit/offset`，
   属**兼容增强**，但要确认前端不依赖"一次返回全部"）。
3. **验证字节不变**：重跑特征化网 + 该模块既有测试，全绿=行为未变。
   两类陷阱在本仓库反复出现，加契约前先照着查一遍（见下节）。
4. **下调棘轮**：把 `test_api_contract_governance.py` 里的 `BASELINE_WITHOUT_RESPONSE_MODEL`
   改小到新值，并把模块名加进 `FULLY_GOVERNED`（若已 100%）。
5. **提交**：一个模块一个提交，信息说明"混乱→标准：<模块>"。

---

## 三、棘轮如何工作（`tests/test_api_contract_governance.py`）

- `test_响应契约欠账不许变大`：断言"无 `response_model` 的端点数 ≤ 基线"。
  **新端点漏契约 → 欠账变大 → 变红**，逼着补。
- `test_已治理模块不许回退`：`FULLY_GOVERNED` 里的模块若冒出裸 dict 端点 → 变红。
- 治理使欠账变小 → 测试提示你下调基线，保持"咬得紧"。

与仓库既有"欠账不许变长"用例（`test_stage15_horizontal` 的机构归属欠账）同一手法。

---

## 四、进度台账

台账不手工维护——下面这三个数由棘轮自己算出来并打印在
`test_契约覆盖率台账` 的输出里，改完代码跑一次就有。

| 指标 | 值（2026-09-21） |
|---|---|
| 端点总数 | 948 |
| 已声明契约 | 894（94.3%） |
| 欠账基线 | **54**（轨迹：757 → … → 470 → 449 → 368 → 327 → 273 → 244 → 216 → 183 → 104 → 54） |
| 已全治理模块 | 72 个（见 `FULLY_GOVERNED`） |

模块 key **按包限定**：`app/spd/routers/x.py` 的 key 是 `spd/x`。不加前缀时两个包里的
同名模块会被合并成一个 key，被合并的那一方即便治理干净也进不了 `FULLY_GOVERNED`
（合并后的 key 还带着另一方的欠账），于是它日后被改回裸 dict 时不会单独变红。
现实里踩到过一对：`portal` 与 `spd/portal`——两者现在都零欠账、都登记在册，合并成一个 key 的话，其中一方回退时另一方还撑着这个 key，回退便不再变红。

### 下一批建议目标（无契约端点数，取自实测）

表头是「**无契约**/总」——别读反了（写这段时就把 `education` 的 `21/23`
读成了"只差两个"，实际是缺 21 个）。

| 模块 | 无契约/总 | 备注 |
|---|---|---|
| ~~`spd/care` · `spd/followup` · `spd/population`~~ | ~~31 / 29 / 28~~ | 已清 |
| ~~`spd/assess` · `education` · `quality` · `admin_mgmt`~~ | ~~24 / 21 / 20 / 20~~ | 已清 |
| ~~`spd/tasks` · `billing` · `spd/referral` · `inpatient`~~ | ~~19 / 15 / 14 / 14~~ | 已清 |
| ~~`maternal` · `pharmacy` · `tcm`~~ | ~~12 / 9 / 9~~ | 已清 |
| ~~`spd/workbench` · `drgs` · `followups` · `homevisits`~~ | ~~8 / 6 / 6 / 6~~ | 已清 |
| ~~`cost` · `staffing` · `jobs` · `access_logs` · `attachments` · `monitor` · `todos`~~ | ~~5 / 5 / 4 / 3 / 3 / 3 / 1~~ | 已清 |
| `prescriptions` · `users` | 7 / 6 | 下一批 |
| `chronic` · `insurance` · `integration` · `medication` · `patients` · `vaccination` | 5~4 各一 | |

**"整模块清零"到此为止**：剩下的 54 笔散在 15 个模块里，全是**局部缺口**
（模块里多数端点已有契约，缺的是一两条动作型端点），单模块最多 7 笔。
再往下按端点数排，不再按模块凑。

2026-09-21 一轮做掉十二个模块（122 个端点）：`org_groups` `blood` `dataquality`
`projects` `resources` `pathology` `rules` `credentials` `esb` `vaccine_supply`
`education` `quality`——`vaccine_supply` `esb` `education` `quality` 从上表里划掉了。

这一轮**实测到四处键被契约吃掉、两处键序被改写**（全部修正，见「陷阱三」「陷阱四」）：

| 端点 | 被吃掉/改掉的 | 谁发现的 |
|---|---|---|
| `POST /api/credentials` | `superseded` | 定向测试 |
| `POST /api/vaccine-supply/cold-chain` | `hint`（超温才有） | 定向测试 |
| `GET /api/quality/clinical-indicators` | `uncollected`（只有一项指标有） | 定向测试 |
| `GET /api/dataquality/summary` | `by_rule[].rule_type` | **只有字节比对** |
| `GET /api/resources/match/or-rooms` | 键序 | **只有字节比对** |
| `GET /api/credentials/resolve` | 键序 | **只有字节比对** |

后三处是这套取证方式存在的理由：**没有任何用例断言过它们**，定向测试全绿。

### 2026-09-21 第二批（`spd/care` 31、`spd/assess` 24）：契约**太严**也会出事

前一批踩的都是"契约太松/漏键"。这一批踩到的是相反方向——**契约比现实严，
会把能工作的响应变成 500**（`ResponseValidationError`）：

| 端点 | 问题 | 根因 |
|---|---|---|
| `GET /api/spd/case-reports` | `task_id` 声明成 `int` | 该列 **nullable**（不挂任务的个案上报） |
| `GET /api/spd/point-accounts/me` | `account_id` 声明成必填 | 还没有积分账户时**没有这个键** |
| `GET /api/spd/assessments` | `score` 声明成 `int` | 该列是 **FLOAT**，`12.0` 会被输出成 `12` |
| `GET /api/spd/measurements/trend` | 漏了 `total` 与 `latest` | 读 handler 时把 return 读截断了 |
| `POST /api/education/courses/{id}/exam` 等 3 条 | `score` 声明成 `int` | `training_records.score` / `training_assessments.score` 都是 **FLOAT**，`55.0` 被输出成 `55` |

> **"分数"这个词在本仓库里三次都是 FLOAT 列**（量表得分、课程考核、实训考核）。
> 凡是遇到 score / rate / amount 这类"看起来像整数"的字段，一律先看列类型。

前三条的共同点：**光读 handler 看不出来，必须去看 ORM 的列定义**。
第三条尤其容易想当然——"量表得分"听起来像整数，列类型却是 FLOAT。

> 建模前先把这两件事做了：
> 1. 列出该模块所有模型的 `nullable` 列，逐个对照自己声明的可选性；
> 2. 把声明成 `int` 的字段名与模型里的 `Float`/`Numeric` 列做一次交叉比对。
> 两条都能用十行脚本跑出来，比事后从 500 里倒推快得多。

### 陷阱五：**继承**会把父类字段排到前面，键序就变了

`{"record_id": ..., **result}` 这种"多一个字段、且在最前"的返回，用继承建模
（`class XxxOut(ResultOut): record_id: int`）会输出成 `{score, ..., record_id}`
——父类字段先排，子类新增字段在后。**键序变了就是改字节。**

`GET /api/quality/records/{id}/qc` 就是这么栽的：继承看着省事，实测把
`record_id` 从第一个挪到了最后一个。改法是逐字段重列、顺序照 handler，
不用继承。继承只在"多出来的字段本来就在末尾"时才安全。

### 有一种端点**加不了契约**，不是没做，是做不了

`GET /api/spd/scores-analysis` 出两种形状，而两种的**键序互不相容**：

| | 键序 |
|---|---|
| 无数据 | `total, distribution, top_deductions, average` |
| 有数据 | `total, average, distribution, top_deductions, ranking` |

`average` 在前者是最后一个键、在后者是第二个。`response_model_exclude_unset`
按**声明顺序**输出，一份声明只能给出一种顺序——两边不可能同时对上。

要收掉这笔账，得先让 handler 两个分支的键序一致，**那是改响应字节的行为变更，
需要单独立项**，不能夹带在"补契约"里。这一条刻意留在欠账里，handler 上写明
了原因。这也是这套棘轮的正确用法：**说不清怎么还的账，就留着并写清楚为什么**，
不要为了把数字做小而动响应。

### 2026-09-21 第三批（十个模块 171 个端点）：**契约声明多了，也会出事**

前两批的教训是"太松会吃键"和"太严会 500"。这一批新加一条：**声明一个
handler 根本不会输出的字段，本身就是一句不真的话，而且有闸门在听。**

`spd/population` 的 `_screening_out` / `_candidate_out` / `_enroll_out` 都接一个
可选的患者摘要（`brief`），有 brief 时在末尾追加姓名、电话等键，没有时这些键
整个不出现。图省事合成**一个**带可选摘要字段的模型之后，建档 / 认领 / 改状态
那几条"不带 brief"的端点就凭空声明了一个 `phone`——

`test_pii_output_masking_guard` 正是**按 `response_model` 判"这个端点会不会吐
PII"**的。于是它去找一条根本不存在的脱敏调用，报了 7 条假泄漏。

做法：拆成父类（无摘要）+ `*RowOut` 子类（有摘要），端点各取所需。
`spd/tasks` 的 `_task_out` 同一形状，同一拆法。

> 推论：契约字段与 handler 输出要**双向**一一对应。少声明会吃键、多声明会误导
> 别的闸门——两个方向都不是"无害的宽松"。

这一批被**测试**（不是静态检查）抓出来的建模错，按类归一下：

| 类 | 实例 |
|---|---|
| 名字像什么、列不是什么 | `evidence` 是附件 id 清单不是 URL 串；`swept` 是分类计数字典不是总数；`points` 是天数偏移量不是对象；`rule_version` 是 `"v1"` 不是整数 |
| 分支形状 | 失访随访没有 `action`；门诊结算没有押金冲抵三键；同步支付没有 `pay_url`/`qr_code` |
| 数值列类型 | 金额列一律 `Money`（`int \| float`）；量表得分 / 监测值 / DRG 权重 / 绩效系数是真 Float |

`InstanceAdvancedOut`（推进路径）值得单独看一眼：两种形状共有的 `status` 必须
排在各自第三个键之前，`exclude_unset` 才发得出与 handler 一致的键序。按"读起来
顺眼"排就是改字节。

字节验证：`spd/followup` + `spd/population` 两个模块在套件捕获里覆盖到 74 个
`(方法, 路径, 状态)` 组合，改前改后**逐字节零差异**。


大包（如已做完的 `spd/config`，58 个端点分 6 个子模块）**按子模块分批、分提交**：
一次比 58 个端点，逐字节比对出了问题不好定位——粒度本身就是这套办法的价值。

> 文件下载/CSV/SVG 等**非 JSON 端点**与 **204 无响应体**的端点不再算欠账——判据已改为
> 「有 `response_model` **或**显式声明了非 JSON 媒体类型 **或** status_code=204」，
> 详见下一节。把永远还不掉的账留在基线里，会让这个数字不再表示「还有多少接口没契约」。

---

## 取证方式：套件级字节捕获（推荐）

早期每治理一个模块就手写一份捕获脚本：种数据、逐个端点发请求、把响应存下来、
加契约后再跑一遍比对。做到第十几个模块时这件事本身成了瓶颈——脚本写得比契约还久，
而且每份脚本只覆盖它自己那个模块。

现在改成**给 app 装一个中间件**，把整个测试套件跑出来的每个响应按
`(方法, 路由模板, 状态码)` 记下字节，加契约前后各跑一次逐项比对。工具见
`tests/capture_plugin.py`（治理用的临时件，不进 CI）：

```bash
MEDPLAT_CAPTURE=/tmp/base.json  PYTHONPATH=tests pytest tests/ -q -p capture_plugin
# ...加契约...
MEDPLAT_CAPTURE=/tmp/after.json PYTHONPATH=tests pytest tests/ -q -p capture_plugin
# 逐 key 比对两个 json
```

一次跑覆盖全部模块（实测 1847 个「方法×路径×状态」组合），治理十个模块和治理
一个模块的取证成本一样。

**用它之前必须先量噪声底**：同一份代码跑两次，差异必须是 0。第一次实测有 2 处
（短信验证码回显、二维码随机令牌），归一化后才归零。噪声底不为 0 就开始用，
等于把自己的随机数当成「契约改坏了」，或者反过来——真差异淹没在噪声里看不见。

**它覆盖不到的**：测试套件没跑过的端点，前后都没有记录，比对自然是「一致」。
这是**沉默的缺口**，不是保证。治理时要对照模块的路由清单看哪些 key 没出现；
对没被覆盖的端点，仍需手写用例（本仓库覆盖率约 87%，缺口主要在错误分支）。

**还有一种更隐蔽的缺口：key 在捕获里，但那次调用是空的。** 列表端点返回 `[]`
时，任何契约都能通过——字段一个都没被验到，而 key 明明在捕获里，对照路由清单
是查不出来的。ADR-0006 收官批就撞到一次：`GET /api/cssd/requests` 被跑到了，
但那一次恰好没有数据。所以自查要分两步：

1. 模块的每个端点在捕获里**有没有 key**（对照路由清单）；
2. 有 key 的，那几条记录**是不是空集**（`[]` / `{}` / 只有分页壳）。

第 2 步现在还是手工看的；写用例造数据是唯一的补法——空集钉不住字段这件事，
在特征化网那边早就写过（「网里六段都造了数据——空列表什么都钉不住」），
换成套件级捕获之后它以另一种形式回来了。

**改了路由代码就要重跑**：pytest 在启动时导入 app，之后的编辑不影响正在跑的
那一次。踩过一次——加完一个模块才发现它不在刚启动的那轮捕获里，只能重跑。

---

## 三条「这个判断由什么决定」

治理时会遇到两类判断，别混：

- **由字节决定的**：改了声明，响应体就变——Money 的 int/float、条件键、字段顺序。
  这类必须有变异验证（改错了要能当场转红）。
- **不由字节决定的**：改了声明，响应一个字节都不变，变的是 OpenAPI 与可读性。
  实测踩到过两处：`last_sync_at` 从 `str` 改成 `str | None` 零处转红（handler 永远
  给值）；去掉 `response_class=SvgResponse` 后 content-type 照样是 `image/svg+xml`
  （返回的类自带 media_type），变的是规格书会回落成 `application/json`。
  这类的理由是「契约要诚实」，**不能拿不咬人的变异当证据**。
- **随机项不能进比对**：`token_urlsafe` 生成的令牌、时间戳，每次运行都不同。
  逐字节比对前先归一化，并做**对照实验**（同一份代码跑两次，看这一项是否也变）
  ——否则会把自己的随机数当成"契约改坏了"。

---

## 两个反复踩到的建模陷阱

### 陷阱一：Money 列的整数值是 `int`，声明成 `float` 就改了字节

`Money = Numeric(14, 2, asdecimal=False)`（`models/_base.py`）。整数金额读回来是
**`int`**，不是 `float`；`round(x, 2)` 作用在整数上同样返回 `int`。把这类字段声明成
`float`，Pydantic 会把 `200` 序列化成 `200.0`——账单页上的「200 元」变成「200.0 元」。

**写法**：`total_amount: int | float`（Pydantic smart union 原样透传，不做转换）。
适用于所有 `Money` 列、以及由它们 `sum()`/`round()` 派生的数值，**包括 dict 的值**
（`by_category: dict[str, int | float]`）。

**反过来也要看清**：`Float` 列（如 `spd_measurements.value`、`spd_assessments.score`）
整数值读回来就是 `140.0`，那里声明 `float` 才是原样，写 `int | float` 反而没意义。
两种列长得像，判断依据是**列类型**，不是字段名里有没有"金额"。

**怎么验**：种子数据里同时造整数与小数两种取值，逐字节比对；再把声明改回 `float`
看用例是否转红——不转红说明数据里根本没有整数值，那条用例什么也没守住。

已踩记录：`cssd.total_cost`（`0` → `0.0`）、`analytics` 多处、`portal/price-list.price`、
`portal/me/bills` 与 `/me/admissions/{id}/bill` 四处、`portal/me/deposits`（这一处是
**已经出账的 bug**，1000 元押金以 `1000.0` 返回，直到治理同一批时才被发现）。

### 陷阱二：条件键——只在某些分支出现的字段

有的键只在特定条件下出现。声明成「可选字段 + 默认值」会给**每一个**响应注入
`"key": null`：既改字节，又等于在响应体与 OpenAPI 里公告该字段存在。

**写法**：字段声明为 `X | None = None`，端点上加 `response_model_exclude_unset=True`
——handler 没放这个键，序列化时它就整个不出现（不是 `null`）。

**怎么验**：两条分支都要有用例——「出现时键在」和「不出现时键**整个不在**」。
只钉前者等于没钉，去掉 `exclude_unset` 照样绿。

已踩记录：`portal/auth/sms/code` 的 `debug_code`（登录验证码回显口子，P0 整改专门
收紧过它——契约不该把刚收紧的东西又摆回台面）、`auth/wechat/authorize` 的 `mock_code`、
`portal/me/family` 的 `member_id`（本人那一行没有它；注入 `null` 会让客户端照着调
`DELETE /me/family/None`）。

> **多态 ≠ 条件键**。同一列表里行的形状真的不同（如 `metrics/drilldown` 的八种行），
> 逐字段建模只会把各分支的字段互相注入 `null`，那时用 `list[dict[str, Any]]` 才对——
> 但要有别的东西自描述形状（`drilldown` 同响应里的 `fields` 就是行的字段清单，
> 有用例钉住两者相等），否则就是拿宽字典逃避契约。

### 陷阱三：`_out()` 被多个端点复用，而有的端点在它之外**又加了键**

`_credential_out()` / `_cold_out()` 这类辅助函数是共用的，但调用方常常在它返回的
dict 上再塞一个键：

```python
result = _credential_out(credential)
result["superseded"] = [...]      # 只有"发放"这条有
return result
```

给这几个端点挂同一个 `XxxOut` 就会把多出来的键**吃掉**。**契约漏一个键不会报错，
只会让它从响应里消失**——这正是加契约最容易造成的破坏形状：没有异常、没有 500，
调用方拿到的响应少一段，两边都不知道。

**写法**：继承出一个子模型（`CredentialIssuedOut(CredentialOut)`），只给那一条端点用。
不要把它合回基模型再加默认值——那会给清单注入一个 `"superseded": null`，
既改字节，也让"这里没有这个东西"看起来像"这个东西是空的"。

已踩记录（2026-09-21 那轮，三处都是字节比对报出来的）：
`POST /api/credentials` 的 `superseded`（总是有，被清单模型吃掉）、
`POST /api/vaccine-supply/cold-chain` 的 `hint`（超温才有，属陷阱二）、
`GET /api/dataquality/summary` 里 `by_rule[]` 的 `rule_type`。

**第三处值得单说**：它是嵌套行里漏的一个字段，**没有任何用例断言过它**——
定向测试全绿，只有套件级字节比对看得见。嵌套行的字段最容易漏，因为读 handler 时
眼睛会跳过；照着 handler 的 dict 字面量**逐键抄**，不要凭印象。

### 陷阱四：`exclude_unset` 按**声明顺序**输出，多形状模型要把顺序排对

JSON 键序也是字节。用了 `response_model_exclude_unset=True` 之后，输出顺序跟的是
**模型字段的声明顺序**，不是 handler 里 dict 的插入顺序。

`GET /api/resources/match/or-rooms` 出两种形状：

| | handler 的键序 |
|---|---|
| 该机构没有启用中的手术间 | `scheduled_date, rooms, hint` |
| 正常 | `scheduled_date, window, rooms, caliber` |

一个模型要同时满足两种，声明顺序必须是二者的**交错并集**：
`scheduled_date, window, rooms, hint, caliber`。按第一种顺序声明（`scheduled_date,
rooms, hint, window, caliber`），正常那条的 `window` 就被挪到了 `rooms` 后面——
键序变化同样是改字节，套件级比对把它报了出来。

---

## 非 JSON 端点（文件下载 / HTML 单据）怎么算契约

棘轮的判据是「**有 `response_model` 或显式声明了非 JSON 媒体类型**」，两者都算已声明。

**为什么不是只认 `response_model`。** 直接返回 `StreamingResponse` / `FileResponse` 的端点，
`response_model` 对它没有意义——函数不返回可序列化对象，FastAPI 也会跳过模型。
把这类端点永远算作欠账，等于往棘轮里掺一笔**永远还不掉的账**，
数字就不再表示「还有多少接口没契约」。它们的契约是「我返回 text/csv 字节流」。

**两种写法，别用错：**

| 端点实际返回 | 正确写法 | 例子 |
|---|---|---|
| 一个**字符串**，由 `response_class` 渲染 | `response_class=HTMLResponse, response_model=str` | `printing` 的 12 个 HTML 单据 |
| 一个 **Response 对象**（流式/文件） | 自带 `media_type` 的 Response 子类当 `response_class` | `reports.CsvResponse` |

第二种**不要写 `response_model=str`**：函数根本不返回 str，那是在说假话。
也**不要只写** `responses={200: {"content": {"text/csv": {}}}}`：FastAPI 会保留默认的
`application/json` 条目，OpenAPI 于是声称"这个端点可能返回 JSON"——实测如此。
让 `response_class` 自带 `media_type` 才能把默认条目去掉，
而且**声明与实际返回是同一个类**，不会两处漂开。

**这不是给自己开口子。** 判据从路由对象推导，不是手工豁免清单；
`test_放宽媒体类型口径没有白送任何端点` 钉住了"靠媒体类型脱账"的端点清单——
拿空 `responses` 刷低欠账，会让那份清单变长而当场变红。

## 五、与其它治理线的关系

- **分级**（`docs/模块分级_*.md`）：C 类模块拆分时**顺带**补契约（大模块别单独为契约动它）；
  A/B 类小模块可独立走本配方。
- **特征化测试**（`docs/特征化测试指引.md`）：本配方第 1 步就是它——契约迁移是特征化网的
  典型受益场景。
- **命名**：新测试用 `test_<模块>_characterization.py`，勿用 `test_stageN_*`。
