# 闸门现状（自动生成，勿手改）

> 由 `server/scripts/dump_gate_status.py` 生成，
> 由 `server/tests/test_gate_status_freshness.py` 钉住——改了闸门基线却没重跑本脚本，CI 会红。
>
> **数字是各闸门源码里的登记基线。**「基线是否仍等于实测」由各闸门自己那条用例断言；
> 这里不重跑扫描——再写一遍测量逻辑就是第二份实现，两份实现迟早会飘。

**零基线**（值为 0 的那几行）比「只减不增」强一档：新出现一条就红，不需要有人记得去减。

| 分组 | 指标 | 当前登记值 | 判据出处 |
|---|---|---:|---|
| 横向越权（写侧） | 按 id 写接口机构归属欠账 | 0 | `tests/test_stage15_horizontal.py` |
| 横向越权（写侧） | 归属隔一跳的无守卫写端点 | 0 | `tests/test_stage15_horizontal.py` |
| 横向越权（写侧） | 挂在患者上的表（含隔一跳）× 按 id 写无守卫（待裁定） | 13 | `tests/test_stage15_horizontal.py` |
| 横向越权（写侧） | 挂在患者上的表（含隔一跳）× 按设计不阻断（逐条写明理由） | 1 | `tests/test_stage15_horizontal.py` |
| 横向越权（写侧） | 新建挂在患者上、无机构列的行 × 按设计不判（逐条写明理由） | 6 | `tests/test_stage15_horizontal.py` |
| 横向越权（写侧） | 新建挂在患者上、无机构列的行 × 口径待裁定 | 4 | `tests/test_stage15_horizontal.py` |
| 横向越权（写侧） | 按 id 写别名机构列（center_org_id 等）的表 × 按设计不判（逐条写明理由） | 5 | `tests/test_stage15_horizontal.py` |
| 横向越权（写侧） | 按设计跨机构的豁免（逐条写明理由） | 8 | `tests/test_stage15_horizontal.py` |
| 横向越权（写侧） | 已登记的领域守卫 | 4 | `tests/test_stage15_horizontal.py` |
| 横向越权（读侧） | 按 id 读患者资源的豁免 | 3 | `tests/test_stage15_horizontal.py` |
| 横向越权（读侧） | 跟进 helper 后新看见的读侧欠账 | 4 | `tests/test_stage15_horizontal.py` |
| 横向越权（读侧） | 无调用方身份的患者读接口（待裁定） | 35 | `tests/test_unscopable_patient_reads.py` |
| 横向越权（读侧） | 仅聚合无身份（信息项，非欠账） | 10 | `tests/test_unscopable_patient_reads.py` |
| 横向越权（读侧） | 无调用方身份 × 隔一跳/两跳的患者维度表（待逐条判定） | 19 | `tests/test_unscopable_patient_reads.py` |
| 接口契约 | 缺 response_model 的端点 | 0 | `tests/test_api_contract_governance.py` |
| 接口契约 | 已完全治理的模块 | 87 | `tests/test_api_contract_governance.py` |
| 列表分页 | 仍会静默截断的 GET 端点 | 92 | `tests/test_list_pagination_ratchet.py` |
| 列表分页 | 已切 paginate 的端点（反方向棘轮） | 142 | `tests/test_list_pagination_ratchet.py` |
| 列表分页 | 等机构/患者收口裁定后才能切 | 9 | `tests/test_list_pagination_ratchet.py` |
| 列表分页 | 筛选用的 id 名单先截断再用（搜索名单静默少一截） | 0 | `tests/test_filter_ids_not_truncated.py` |
| 并发冲突 | 写唯一约束表却未处理冲突 | 0 | `tests/test_stage14_concurrency.py` |
| 并发冲突 | 已审计的写入点 | 131 | `tests/test_stage14_concurrency.py` |
| 并发冲突 | 未审计的写入点 | 0 | `tests/test_stage14_concurrency.py` |
| 并发冲突 | 待裁定的写入点 | 8 | `tests/test_stage14_concurrency.py` |
| 并发冲突 | 读-改-写欠账 | 8 | `tests/test_stage14_concurrency.py` |
| 时间口径 | app/ 里绕过 clock.today() 的 date.today() | 0 | `tests/test_clock.py` |
| 时间口径 | app/ 顶层时间快照的豁免 | 1 | `tests/test_clock.py` |
| 横向越权（写侧） | body 收 id 的无守卫写端点（候选，未逐条判定） | 1 | `tests/test_body_id_org_write_guard.py` |
| 横向越权（写侧） | 按设计无调用方身份的豁免（body 收 id 那一族） | 1 | `tests/test_body_id_org_write_guard.py` |
| 横向越权（写侧） | 角色门只允许全域角色（非可越权入口） | 4 | `tests/test_body_id_org_write_guard.py` |
| 横向越权（写侧） | 请求声明机构、却无归属判定的写端点（候选，只减不增） | 0 | `tests/test_body_declared_org_write_guard.py` |
| 横向越权（写侧） | 请求里的机构按设计就是别家（逐条写明理由） | 4 | `tests/test_body_declared_org_write_guard.py` |
| 横向越权（写侧） | 请求声明机构 × 早已登记待业务裁定、故意未修 | 1 | `tests/test_body_declared_org_write_guard.py` |
| 横向越权（写侧） | 请求体捎带的机构归属对象未判 × 按设计（逐条写明理由） | 3 | `tests/test_secondary_body_id_guard.py` |
| 横向越权（写侧） | 请求体捎带的机构归属对象未判 × 口径待裁定 | 1 | `tests/test_secondary_body_id_guard.py` |
| 横向越权（读侧） | 查询参数收机构号、只拿 resolve_org_scope 当范围 × 按设计（逐条写明理由） | 15 | `tests/test_org_param_read_guard.py` |
| 横向越权（读侧） | 查询参数收机构号、只拿 resolve_org_scope 当范围 × 口径待裁定 | 7 | `tests/test_org_param_read_guard.py` |
| 横向越权（读侧） | 按 id 读机构归属表、无读侧判定 × 按设计（逐条写明理由） | 6 | `tests/test_byid_org_read_guard.py` |
| 横向越权（读侧） | 按 id 读机构归属表、无读侧判定 × 患者数据已在别的欠账名单 | 4 | `tests/test_byid_org_read_guard.py` |
| 横向越权（读侧） | 按 id 读机构归属表、无读侧判定 × 口径待裁定 | 3 | `tests/test_byid_org_read_guard.py` |
| 横向越权（读侧） | 按路径参数读、不经机构 / 患者表、无读侧判定（盲区）× 按设计 | 15 | `tests/test_byid_org_read_guard.py` |
| 横向越权（读侧） | 按路径参数读、不经机构 / 患者表、无读侧判定（盲区）× 患者数据已在别的欠账名单 | 7 | `tests/test_byid_org_read_guard.py` |
| 横向越权（读侧） | 按路径参数读、不经机构 / 患者表、无读侧判定（盲区）× 口径待裁定 | 4 | `tests/test_byid_org_read_guard.py` |
| 日期入参 | 未经 require_date / resolve_business_date 的日期查询参数 | 0 | `tests/test_date_query_params.py` |
| 日期入参 | 请求体里注解为裸 str 的日期字段 | 0 | `tests/test_datestr_single_source.py` |
| 日期入参 | 未经校验的月度期间查询参数 | 0 | `tests/test_periodstr_single_source.py` |
| 日期入参 | 名字像期间、按设计不是 YYYY-MM 的豁免 | 4 | `tests/test_periodstr_single_source.py` |
| 日期入参 | 带校验器的类型别名写成 `= Query(...)`（FastAPI 丢掉校验器） | 0 | `tests/test_query_param_validators.py` |
| 日期入参 | `pattern=` 里用 `\d`（认全角 / 阿拉伯-印度数字） | 0 | `tests/test_ascii_digit_patterns.py` |
| 功能完善 | 该有界面却没有的端点（孤儿） | 4 | `tests/test_orphan_endpoints.py` |
| 功能完善 | 按设计不需要界面的豁免路径 | 7 | `tests/test_orphan_endpoints.py` |
| 功能完善 | 前端含 prompt( 的行（录入不靠弹窗） | 0 | `tests/test_frontend_prompt_guard.py` |
| 功能完善 | 免确认的破坏性操作（可逆，逐条写明怎么恢复） | 6 | `tests/test_frontend_destructive_confirm_guard.py` |
